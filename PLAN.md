# Plan

## Operating Rules

- [x] `.cswinrt/` is the implementation baseline for observable WinRT behavior, ABI ownership, metadata interpretation, generated surface shape, authoring contracts, packaging evidence, and validation strategy.
- [x] Keep dependency order: `winrt-runtime` -> `winrt-metadata` -> `winrt-generator` / `winrt-compiler-plugin` -> `winrt-projections` -> `winrt-authoring` -> `winrt-samples`.
- [x] Tests validate the reference-first design; do not derive runtime, generator, authoring, or packaging rules from sample failures.
- [x] Keep Gradle configuration cache and build cache enabled; cache failures are task modeling bugs to fix.
- [x] Work on Windows. Prefer `.\gradlew.bat`; when running from WSL, use `./.agent_scripts/run_windows_gradle.sh <gradle-args...>`.
- [x] Keep codebase slimming frozen until functional completeness is closed. Remove duplication only when it is necessary for an active parity slice.

## Reference Boundaries

- [x] CsWinRT is projection-behavior evidence, not a mandate to copy `.NET` target splits, MSBuild infrastructure, release tooling, `Perf`, `Benchmarks`, or `cswinmd`.
- [x] Kotlin reflection is not a substitute for CsWinRT source-generator discovery. Equivalent annotation, metadata, registration, and source-generation behavior belongs in `winrt-generator` and `winrt-compiler-plugin`; runtime discovery stays fail-closed.
- [x] WinMD ingestion targets native WinMD metadata. Do not add `cswinmd` compatibility unless a concrete Kotlin projection requirement appears.
- [x] Packaging targets Kotlin `appx` / `msix` applications, PRI/MRT/resource indexing, manifest processing, dependency payload staging, signing/test-install hooks, and runtime asset layout without cloning full MSBuild.
- [x] Use `io.github.composefluent.winrt` as the base package for runtime, metadata, generator support, generated projections, authoring, and samples unless a narrow external compatibility exception is documented.

## Current Focus Queue

- [x] Completed owner-scoped projection support initializer fix: generator-emitted compiler support manifests now carry an owner column, compiler-plugin support initializer class and lowered init function hashes include the owner identity, merged manifests preserve owner-scoped rows across application and dependency artifacts, and JVM-only app builds skip the Mingw application-entry task when no native main class is present.
- [x] Completed owner-scoped projection support anchor fix: generated projection modules now include an owner-scoped compiler-plugin anchor file, and projection support initializer IR functions are emitted into that owner-specific file facade instead of arbitrary generated support files such as `AuthoringMetadataTypeMappingHelperKt`.
- [x] Completed CI follow-up for owner-scoped compiler support manifests: updated compiler-plugin malformed-row coverage so the extra-column rejection test now treats the new five-column owner manifest as valid and uses a sixth column for fail-closed validation.
- [x] Completed issue #5 native authored XAML Grid override parity: fixed K/N direct-call struct ABI lowering so `Rect`/large structs are passed by pointer, added a `mingwX64` authored `Grid` + `SwapChainPanel` sample with `onCreateAutomationPeer`, `measureOverride`, and `arrangeOverride`, and validated it against `.cswinrt/src/cswinrt` overridable interface CCW generation plus `.cswinrt/src/WinRT.Runtime` composable/CCW lifetime responsibilities.
- [x] Completed issue #5 ABI follow-up audit: rechecked compiler-plugin descriptor lowering, `winrt-runtime` `ComAbiValueKind.Struct` generic vtable/callback paths, delegate struct marshaling, and JVM FFM struct layout descriptors; fixed TestKit generator-worker classpath drift and added explicit `Struct8_4` / `Struct16_4` intrinsic-lowering regression coverage so generator layout tokens stay visible through compiler lowering.
- [ ] 正在做 generated projection size reduction: reduce repeated generated `.class` output in prebuilt Windows SDK and Windows App SDK projection artifacts without changing Maven coordinates, projected API compatibility, runtime hot-path behavior, or multi-module classpath safety.
- [x] Closed KWINRT-030 KMP partial dependency checker early-resolution mutation: resolvable identity inputs now come from live source configuration file collections instead of mutating a resolved identity configuration, so partially-resolved dependency checks and sample classpath resolution no longer force late `kotlinWinRtIdentity` mutation.
- [x] Closed KWINRT-031 projection-unsafe authoring TypeDetails casts: generated authoring bridges now route WinRT runtime-class dispatch through `winrtAs(WinRtTypeHandle(...))` using metadata-owned default-interface or dispatch-interface IIDs instead of direct Kotlin runtime-class casts or projection-only `Metadata.TYPE_HANDLE` assumptions, while authored-class dispatch keeps the narrow local cast path.
- [x] Completed KWINRT-031 CI follow-up for common/mingw TypeDetails: authored WinUI base dispatch no longer emits runtime-class `Metadata.TYPE_HANDLE`, so generated common and `mingwX64` sample sources compile against the current projection metadata surface.
- [x] Closed KWINRT-032 composable/runtime-class inheritance shape mismatch: runtime-class shells now open when the WinMD shape supports composable construction instead of waiting for an in-surface derived type, keeping subclassable WinRT projections non-final.
- [x] Closed KWINRT-041 dependency-owned WinRT class-shape collision: dependency identity now suppresses downstream regeneration of dependency-owned runtime-class base chains so one WinRT identity does not get emitted with conflicting class shapes across jars.
- [x] Closed KWINRT-044 non-WinUI authored-candidate validation scope leak: authored-candidate validation now registers only for Windows JVM and `mingwX64` WinUI targets, so non-WinUI compilations do not consume WinUI authored scanner output.
- [x] Closed KWINRT-045 dependency-owned authored Application subclass regeneration: dependency identity now carries authored host-manifest activatable classes into downstream suppression, so upstream-authored runtime classes like `WinUIXamlApplication` are not regenerated by dependents.
- [x] Closed KWINRT-046 dependency-authored Application manifest registration: staged JVM and `mingwX64` authored host manifests now feed the final Win32 application manifest so dependency-owned activatable classes register under the actual host DLL instead of leaving empty `<asmv3:file>` entries.
- [x] Closed KWINRT-047 dependency-authored WinUI Application run-host activation: `runWinRtApplicationHost` now depends on the JVM authoring host build, generated JVM authoring host DLLs load jars from the run-host `lib` layout, and native `.dll` authored targets remain free of JVM runtimeconfig generation.
- [x] Closed KWINRT-048 cross-module runtime-class wrapper inheritance: generated non-sealed runtime-class `_inner` wrapper constructors now use Kotlin `protected` visibility, matching `.cswinrt/src/cswinrt/code_writers.h` `protected internal` object-reference constructors closely enough for dependency-owned bases such as `Panel` to be subclassed by downstream projections such as `Canvas` without exposing sealed wrapper constructors.
- [x] Closed KWINRT-049 cross-module overridable exclusive interface visibility: generated exclusive interfaces remain internal by default, but exclusive interfaces implemented as `OverridableAttribute` surfaces by non-sealed runtime classes, such as `Microsoft.UI.Xaml.IApplicationOverrides`, are now treated as cross-module authoring contracts and emitted public with exposed vtable metadata, matching `.cswinrt/src/cswinrt/code_writers.h` override-interface ABI exposure rules.
- [x] Closed KWINRT-050 setter-only interface property projection parity: interface properties are now projected when either getter or setter metadata exists, and setter-only NativeProjection getters follow `.cswinrt/src/cswinrt/code_writers.h` `find_property_interface` by searching base interfaces, exclusive-to peer interfaces, and static attributed interfaces before forwarding through `winrtAs`; this restores cross-module WinUI surfaces such as `ICompositionSupportsSystemBackdrop.systemBackdrop`.
- [x] Closed KWINRT-051 CsWinRT property-interface recursion parity: Kotlin `findNativeProjectionGetterInterface` now matches `.cswinrt/src/cswinrt/code_writers.h` `find_property_interface` by stopping at the first interface that declares the same property name and can project a property accessor, rather than skipping setter-only peer interfaces to reach a native getter in a base interface; regression coverage verifies the chained setter-only forwarding path.
- [x] Closed KWINRT-052 setter-only exclusive interface projection-surface closure: metadata filtering now mirrors `.cswinrt/src/cswinrt/code_writers.h` `find_property_interface` dependency reachability for setter-only properties, so interfaces reached only through overridable signatures, such as `ICompositionSupportsSystemBackdrop` from `SystemBackdrop.OnTargetConnected`, keep their exclusive-to peer runtime class and getter interface surface (`CompositionTarget`/`IBackdropSource`) instead of publishing an empty dependency-owned interface shell.
- [x] Closed KWINRT-053 projection shape ownership rule alignment: generator visibility now honors `.cswinrt/src/cswinrt/code_writers.h` `settings.public_exclusiveto` for exclusive interfaces, metadata surface closure routes setter-only forwarded properties through a shared member-forwarding helper while event metadata remains fail-closed on add/remove accessor parity, and Gradle dependency identity now carries `projectionShapeVersion` so only current-shape `projectedTypes` suppress downstream generation.
- [x] Closed KWINRT-054 KMP generated projection stale-layout cleanup: KMP projection generation now deletes the legacy `src/main/kotlin` projection and authoring output roots in addition to previous JVM and native-host roots before writing `commonMain` output, so old snapshot-era generated files with internal wrapper constructors or internal override interfaces cannot remain on the source path after `generateWinRtProjections`.
- [x] Closed KWINRT-055 WinAppSDK composition dependency-surface parity: aligned Kotlin with `.cswinrt/src/Projections/WinAppSDK/WinAppSDK.csproj` by treating `Windows.UI.Composition` references in Microsoft WinAppSDK metadata as Windows projection/dependency closure surface, not as an automatic projection exclude or `Microsoft.UI.Composition` type rewrite; kept only the narrow WinUI XAML interop redirects that correspond to the cswinrt WinAppSDK filter comments. Validation: `:winrt-generator:test :winrt-gradle-plugin:test` and local `winrt-generator` / `winrt-gradle-plugin` Maven publication pass; Skiko `:skiko-winui:generateWinRtProjections`, `:skiko-winui:compileKotlinWinuiJvm`, and `:skiko-winui:compileKotlinWinuiMingw` pass with the CI WinUI properties. The full Skiko snapshot local publish entry `:skiko-winui:publishSkikoWinuiToMavenLocal` reaches generated WinRT output but is blocked locally by `:skiko:compileCommonMainKotlinMetadata` in the core Skiko included build, not by the WinRT projection surface.
- [x] Closed KWINRT-056 published identity authored-host manifest portability: dependency identity now publishes structured `authoredHostManifestRecords` instead of requiring consumers to read producer build-machine `*.host.json` paths, downstream projection suppression and runtime asset staging consume only those records, and `buildWinRtAuthoringHost` builds JVM host DLLs only from records with jar targets and `hostExportsClass` so native authoring manifests cannot poison JVM host generation. Validation includes `:winrt-gradle-plugin:test` and local Skiko `:skiko-winui:generateWinRtProjections` plus `:skiko-winui:generateWinRtIdentity` with JDK 25, where the generated identity contains records and no `authoredHostManifests`.
- [x] Closed KWINRT-057 published identity authored-asset portability: dependency identity now publishes binary-safe `authoredMetadataRecords`, `authoredTargetArtifactRecords`, and grouped `compilerSupportFileRecords` instead of producer build-machine `authoredMetadata`, `authoredTargetArtifacts`, `authoringMetadataIndexes`, or `compilerSupportManifests` paths; projection generation materializes dependency-authored `.winmd` records for metadata loading, runtime asset staging writes dependency `.winmd` and target-artifact records into the consumer asset root, compiler-support merge materializes grouped support TSV records before merging, and authoring metadata discovery consumes only `authoringMetadataIndexRows`. Targeted validation covers identity generation, dependency runtime asset staging, malformed portable record names, and dependency compiler-support merge.
- [x] Closed KWINRT-058 published identity runtime-asset portability: library identity now publishes configured runtime assets as binary-safe `runtimeAssetRecords` instead of producer-machine `runtimeAssets` paths, application staging materializes dependency runtime asset records from identity files, and the Gradle plugin no longer wires dependency `runtimeAssets` strings into downstream file collections. Full identity audit confirms remaining dependency identity consumers read only portable scalars/specs (`nugetPackages`, projection surface lists, `authoringMetadataIndexRows`) or structured records (`runtimeAssetRecords`, authored metadata, host manifests, target artifacts, compiler support); published library identity also omits local metadata input paths while preserving portable metadata selectors such as `sdk+` and `nuget:<id>@<version>`. The same audit fixed the authored-candidate validation task graph so scanner-generated artifacts from `generateWinRtProjections` are explicit validation inputs, not implicit ordering assumptions. Application-local identity may still record local `runtimeAssets` and `dependencyIdentityFiles` because it is not a published library contract.
- [x] Closed KWINRT-059 authored WinMD external-interface parity: `winrt-metadata` authored WinMD output now keeps authored runtime classes as TypeDefs while referencing existing WinRT base and override interfaces as TypeRefs, matching `.cswinrt/src/Authoring/WinRT.SourceGenerator/WinRTTypeWriter.cs`; metadata source resolution preserves source priority so SDK/NuGet WinMD definitions win over dependency-authored compatibility records; and dependency-authored metadata records feed both scanner and compiler TypeDetails generation so authored interface IID resolution stays identical across paths. Validation includes `:winrt-metadata:test`, `:winrt-gradle-plugin:test`, local `kotlin-winrt` publish, local `skiko-winui` projection generation and publish, Compose `stageWinRtRuntimeAssets`, and the Compose WinUI Skiko sample host run with local artifacts.
- [x] Closed KWINRT-060 XAML `Style.TargetType` metadata self-initialization parity: compiler-generated projection support now owns generated runtime-class metadata registration rather than `winrt-runtime` reflecting over per-type companions or setter paths manually calling initialization; this keeps Kotlin aligned with `.cswinrt/src/cswinrt/strings/InitializeProjection.cs` and `.cswinrt/src/cswinrt/main.cpp` module-level projection initializer responsibilities.
- [x] Closed KWINRT-061 XAML `DependencyProperty` identifier nullability parity: `winrt-metadata` now treats Microsoft and Windows XAML `DependencyProperty` identifier properties as non-null projection values while preserving nullable projection for ordinary runtime-class properties, so generated static DP surfaces match CsWinRT-style direct use such as `Control.backgroundProperty` instead of nullable `Metadata` call-site guards.
- [x] Closed KWINRT-062 XAML collection property nullability parity: `winrt-metadata` now keeps XAML collection runtime-class properties such as `Style.Setters` non-null while preserving explicitly nullable XAML object properties such as `Style.BasedOn`, `Window.SystemBackdrop`, and `UIElement.Clip`; this lets style construction use `style.setters.add(...)` without generator or sample call-site null guards.
- [x] Closed KWINRT-063 WinUI resource container access parity: sample resource setup now uses direct projected access for `Application.resources` and `ResourceDictionary.mergedDictionaries` instead of redundant `checkNotNull` guards, so XAML resource installation follows the same non-null collection surface used by style and controls resource setup.
- [x] Closed KMP application-entry source-set visibility gap: generated `mingwX64` application entry sources now attach to the `mingwX64` main source set instead of `commonMain`, so entry wrappers can import app `main` functions declared in custom intermediate source sets such as `winuiMain`; compiler-plugin support output also tolerates empty JVM compilation units that still receive merged WinRT support manifests during full sample assembly.
- [x] Closed KWINRT-040 target-artifact mismatch for KMP authoring manifests: `winrt-gradle-plugin` now derives the JVM authoring target artifact from the configured `jar` archive file name, and compiler-plugin host manifests reuse the same target artifact naming path.
- [x] Closed KWINRT-038 public setter visibility gap for projected runtime-class forwarders: expect/actual runtime-class generation now preserves writable property shape when the public interface exposes the setter, including projected enum/value-class properties such as `KeyboardAccelerator.modifiers`.
- [x] Closed KWINRT-042 authored interface override wrapping: compiler-generated TypeDetails now wrap interface override parameters with `IUnknownReference` while preserving `IInspectableReference` wrapping for runtime-class parameters.
- [x] Closed KWINRT-043 nullable struct setter boxing: generated `IReference<T>` setter IID expressions now use full struct field signatures, and generated struct `Metadata.register()` emits value-boxing registration from WinMD struct metadata so nullable WinRT struct values expose the expected `IReference<T>` / `IReferenceArray<T>` interfaces without runtime-owned struct enumeration.
- [x] Completed size-reduction baseline: metadata/accessor helper reduction, owner-qualified event/generic support facades, module-local fixed-shape ABI helpers, projected reference helper regrouping by `IInspectableReference` / `IUnknownReference`, and helper frequency thresholding are in place.
- [x] Completed authored CCW reachable ABI parity audit: aligned generator support with `.cswinrt/src/WinRT.Runtime/Projections/KeyValuePair.cs`, `.cswinrt/src/WinRT.Runtime/Projections/Nullable.cs`, `.cswinrt/src/WinRT.Runtime/ComWrappersSupport.cs`, and `.cswinrt/src/cswinrt/code_writers.h` for the generator-visible authored ABI surface and runtime CCW suffix behavior. Overridable authored interfaces now marshal supported `IKeyValuePair<TKey,TValue>`, generic collections, bindable collection parameters and returns, async references, `IPropertyValue`, runtime-owned custom objects such as `Windows.Foundation.Uri`, and `IReference<T>` / `IReferenceArray<T>` through shared runtime projection helpers, default CCWs append `ICustomPropertyProvider` after `IStringable` when enabled, delegate arguments use the same full delegate CCW surface as retained delegate handles, and unsupported reachable shapes fail before support rendering.
- [x] Completed custom `IMarshal` CCW suffix parity: matched `.cswinrt/src/WinRT.Runtime/ComWrappersSupport.cs` by preserving an authored or generated `IMarshal` interface in its original exposed interface-table position while only suppressing the default free-threaded-marshaler suffix entry. `GetIids` no longer moves a user-provided `IMarshal` behind `IWeakReferenceSource`.
- [x] Completed authored CCW receive-array fail-closed parity: removed the support-render fallback path for unsupported `ReceiveArray` ABI combinations. Single projected receive-array return slots still render, while multiple receive arrays or receive-array parameters that cannot become the projected Kotlin return now fail during generator validation with the owning runtime class, interface, slot, and parameter name before `WinRTAuthoringCcwFactories.kt` is emitted.
- [x] Completed authored CCW support-output fallback cleanup: removed stale authored ABI class array unsupported helpers, required server activation factory bindings to fail before handler rendering when their ABI shape is unsupported, and extended generated-output audits so authored CCW support files cannot contain hidden `Unsupported authored ABI`, `Authored ReceiveArray parameter`, or `unsupportedAuthoringAbiArrayOperation` fallback tokens.
- [x] Completed WinUI sample merge gate: `:winrt-samples:runWinRtApplicationHost` passes with Windows App SDK `2.1.3`, `runWinUiSmoke=true`, `autoExitWinUi=true`, `skipSettingsCard=true`, and `skipShimmer=true`.
- [x] Completed WinUI Controls sample DPI manifest parity: generated Win32 application manifests now align with `.cswinrt/src/Samples/AuthoringDemo/WinUI3CppApp/app.manifest` by declaring `true/PM` and `PerMonitorV2, PerMonitor`, so JVM and `mingwX64` WinUI hosts do not run under system-DPI virtualization.
- [x] Completed XAML application host lifetime fix: `runWinRtApplicationHost` and the `mingwX64` native application entry now create a runtime-owned STA application-host scope before user `main`, generated hosts pass the packaged/unpackaged deployment mode into that scope, custom launchers can use `WinRtWindowsAppSdkBootstrap.initializeApplicationHost(...)`, and sample smoke paths no longer wrap XAML startup in raw `RuntimeScope.initializeSingleThreaded()`.

## Post-Baseline Completion Queue

The remaining completion work runs in this order after the completed baseline: `winrt-runtime` hardening -> `winrt-authoring` parity -> `winrt-projections` size/output pass -> `winrt-samples` expansion -> final `winrt-projections` release pass. This queue is for post-baseline hardening and validation; it must not move runtime behavior into samples or derive generator/runtime contracts from sample failures.

### Runtime Hardening First

- [x] Runtime-01: validate `.cswinrt/src/WinRT.Runtime` COM lifetime parity in `winrt-runtime` for RCW/CCW identity caches, `QueryInterface`, `AddRef`/`Release`, explicit disposal, finalization, repeated casts, and cross-thread release behavior.
- [x] Runtime-02: close weak-reference, agile-reference, and reference-tracker parity gaps against `.cswinrt/src/WinRT.Runtime/Interop/IReferenceTracker.cs`, `IWeakReferenceSource*`, and `IAgileReference*`, tracking WinRT semantics rather than `.NET` target-framework branches.
- [x] Runtime-03: harden restricted-error-info and exception bridging for Kotlin exceptions crossing activation factories, authored CCWs, delegates, callbacks, and async completions, with HRESULT and restricted-error-info round-trip coverage.
- [x] Runtime-04: validate collection, bindable, async, nullable, `IReference<T>`, `IReferenceArray<T>`, `IKeyValuePair<TKey,TValue>`, and `IMapView` composition through shared runtime projection helpers instead of new per-shape branch tables.
- [x] Runtime-05: stabilize WinUI/XAML runtime hooks for application-host scope, Windows App SDK deployment, XAML metadata providers, resource manager wiring, runtime assets, and packaged/unpackaged startup on JVM and `mingwX64`.
- [x] Runtime gate: before leaving this queue slice, run targeted Windows validation with `.\gradlew.bat :winrt-runtime:jvmTest :winrt-runtime:mingwX64Test validateWinRtMingwParity`.

### Authoring Second

- [x] Authoring-01: continue replacing source-scanner-derived authoring inputs with compiler-symbol or IR-visible authoring metadata in `winrt-compiler-plugin`, while keeping scanner artifacts only as narrow transition inputs.
- [x] Authoring-02: expand `.cswinrt/src/Authoring/WinRT.SourceGenerator` parity for complex runtime-class inheritance, default interfaces, overridable interfaces, composable factories, protected overrides, and WinUI control-derived classes.
- [x] Authoring-03: extend authored CCW marshaling validation for arrays, nullable structs, generic collections, delegates, events, async references, custom runtime objects, `IPropertyValue`, and mixed object/interface/runtime-class parameters and returns.
- [x] Authoring-04: harden native authoring host fixtures for multiple activatable classes, dependency factory chaining, unload behavior, repeated activation, interface query coverage, and native consumer validation.
- [x] Authoring-05: improve generator and compiler-plugin diagnostics for unsupported authoring members, invalid projected types, bad visibility, invalid constructors or factory shapes, and unsafe runtime-class casts; unsupported shapes must fail before handler rendering.
- [x] Authoring gate: before leaving this queue slice, run `.\gradlew.bat :winrt-authoring:native-component-fixture:verifyNativeAuthoringComponentFixture :winrt-authoring:native-consumer-fixture:verifyNativeAuthoringConsumerFixture`.

### Projections Third

- [x] Projections-01: finish the current generated projection size-reduction slice for prebuilt Windows SDK and Windows App SDK artifacts without changing Maven coordinates, projected API compatibility, runtime hot-path behavior, or multi-module classpath safety.
- [x] Projections-02: keep owner-scoped support output deterministic across compiler-support TSVs, generic ABI support, event helpers, projection registrars, TypeDetails, fixed-shape ABI helpers, and module-local support facades.
- [x] Projections-03: strengthen generated-output audits for duplicate FQNs, duplicate helper emission, repeated type/category branch tables, JVM-only leakage into common or native output, stale support registries, forbidden fallback bodies, and unexpected source or class-size growth.
- [x] Projections-04: keep Windows SDK and Windows App SDK prebuilt projection compile gates fixed for JVM and `mingwX64`, including `:winrt-projections:windows-sdk:*` and `:winrt-projections:windows-app-sdk:*` compile tasks.
- [x] Projections-05: validate third-party WinMD and NuGet projection inputs with dependency identity, preprojected dependency suppression, owner-scoped support manifests, and local-generation opt-in behavior.
- [x] Projection gate: before leaving this queue slice, run `.\gradlew.bat validateWinRtProjectionCompile`.

### Samples Fourth

- [x] Samples-01: keep `winrt-samples` validation-only; do not add sample-local runtime, generator, authoring, packaging, or projection workarounds.
- [x] Samples-02: expand JVM WinUI sample coverage for window lifetime, resource loading, XAML metadata provider aggregation, Windows App SDK deployment, common control events, collection binding, and application shutdown.
- [x] Samples-03: expand `mingwX64` WinUI sample coverage for native application entry, authored control override, DPI manifest behavior, packaged and unpackaged modes, resource staging, and runtime asset layout.
- [x] Samples-04: add an authoring end-to-end sample where a Kotlin-authored WinRT component is consumed by another Kotlin app, covering activation factories, interface query, events, collections, async, and exception propagation.
- [x] Samples-05: add a packaging validation sample for appx/msix layout, PRI/MRT resource indexing, dependency payload staging, signing or test-install hooks, and Windows App SDK payload resolution after the owning upstream contracts are stable.
- [x] Sample gate: before leaving this queue slice, run `.\gradlew.bat validateWinRtSampleSmoke validateWinRtQueue16`.

### Projections Final Pass

- [x] Projection-Final-01: review sample expansion fallout for projected API-shape issues and move any required fixes back to metadata, generator, runtime, or authoring instead of sample glue.
- [x] Projection-Final-02: remeasure projection artifact size, class duplication, generated support ownership, binary/source compatibility, and Maven coordinate stability after samples pass.
- [x] Projection-Final-03: run the release-facing projection validation gates again before treating Windows SDK and Windows App SDK projection artifacts as publish-ready.

## Module Alignment

- [x] `.cswinrt/src/WinRT.Runtime` maps to `winrt-runtime`, which owns ABI primitives, HRESULT/error-info, HSTRING ownership, COM/WinRT initialization, platform calls, object identity, activation, marshaling, delegates/events, collections, async, weak/agile/reference tracking, custom projections, and WinUI runtime hooks.
- [x] `.cswinrt/src/cswinrt` maps to `winrt-metadata`, `winrt-generator`, `winrt-compiler-plugin`, and `winrt-gradle-plugin`, split by metadata normalization, declaration/member planning, generated support emission, compiler-visible lowering, and Gradle orchestration.
- [x] `.cswinrt/src/Projections` maps to `winrt-projections`; checked-in projection output stays narrow while broad Windows SDK and Windows App SDK output is generated deterministically by the plugin.
- [x] `.cswinrt/src/Authoring` maps to `winrt-authoring`, which owns authored type candidates, metadata indexes, authored WinMD/descriptor/host manifest generation, TypeDetails rendering, activation/hosting contracts, and runtime-facing registration helpers.
- [x] `.cswinrt/src/Samples` maps to `winrt-samples`; samples are validation surfaces only and must not contain runtime, generator, authoring, or packaging workarounds.
- [x] `.cswinrt/src/Tests` maps to owning Kotlin module tests instead of a separate top-level test module.

## Completed Baseline

- [x] Runtime JVM baseline: ABI primitives, object references, activation, CCW/RCW ownership, delegate/event/collection/object marshaling, async bridges, WinUI bootstrap, vtable calls, restricted error info, weak/agile references, default `ICustomPropertyProvider` CCW suffix, custom `IMarshal` position preservation, full delegate argument CCW parity, and custom projections are implemented and validated for the current surface.
- [x] Native `mingwX64` runtime/compiler/projection baseline: promoted common runtime tests pass through real native actuals; generated projection compilation covers the full Windows SDK root KMP surface; fallback audits reject reflection, JVM-only imports, `ComVtableInvoker`, and `invokeGenericArgs` leakage.
- [x] Metadata baseline: native WinMD ingestion, SDK/file/directory/NuGet input expansion, deterministic normalization, signatures, default/static/factory metadata, implemented interfaces, custom attributes, mapped types, and diagnostics are implemented.
- [x] Generator/compiler-plugin baseline: declaration planning, member/property/event/method emission, activation/static/factory/composable surfaces, ABI fail-closed diagnostics, compiler-support handoff, descriptor lowering, authored candidate artifacts, and large projection registrar chunking are implemented.
- [x] Projection baseline: Windows SDK and Windows App SDK projections are generated through deterministic plugin output; prebuilt projection publication, versioning, CI workflow, multi-SDK support, and README baseline badges are in place.
- [x] Authoring baseline: generated TypeDetails, authored WinMD/descriptor/host manifests, CCW factories, activation-factory exports, dependency factory chaining, inherited/overridable interfaces, diagnostics, JVM host bridge, and native exported-DLL host validation are implemented.
- [x] Packaging/resource baseline: Kotlin-owned appx/msix staging, manifests, dependency payloads, PRI/MRT/resource processing, signing/test-install hooks, runtime asset layout, Windows App SDK deployment, XAML metadata provider aggregation, and third-party component resource wiring are implemented.
- [x] Validation baseline: representative Windows gates exist for runtime, metadata, generator, compiler-plugin, projections, authoring, packaging, samples, native runtime, native projection, native authoring, and the broader `validateWinRtQueue16` queue.

## Phase 1 Runtime And ABI

- [x] ABI primitives first: `Guid`, `HResult`, HSTRING/string ownership, `PlatformAbi`, `ComVtableInvoker`, runtime initialization, platform API calls, and low-level COM pointer helpers are centralized in `winrt-runtime`.
- [x] Initialization and platform boundary: COM/WinRT initialization, Windows API loading, HRESULT translation, restricted error info, dynamic library loading, and deployment bootstrap are runtime-owned.
- [x] Object reference and identity: `IUnknown`/`IInspectable` wrappers, CCW/RCW identity caches, query/cast support, composable object lifetime, weak/agile/reference-tracker support, and runtime class name lookup are runtime-owned.
- [x] Activation: `RoGetActivationFactory`, manifest-free fallback, activation factory caching, dependency activation-factory chaining, and runtime-class activation helpers are implemented.
- [x] Parameterized IID/signature support: `WinRtTypeSignature`, `ParameterizedInterfaceId`, collection/async/delegate generic IDs, and generated generic interface acquisition are runtime-owned.
- [x] Delegates, events, collections, async, custom projections, WinUI deployment/resource/XAML hooks, and callback-vtable reuse are implemented after the foundational runtime slices.

## Phase 2 Metadata

- [x] WinMD ingestion, namespace/type discovery, SDK/reference/NuGet input expansion, include/exclude filtering, response files, and cache-stable inputs are implemented.
- [x] Symbol fidelity covers generic parameters, default interfaces, implemented interfaces, activatable/static/factory metadata, method/property/event signatures, accessor identity, parameter semantics, and custom attributes.
- [x] Projection-shape inputs are centralized for primitive, inspectable/interface, collection, bindable, async, mapped type, runtime-class-name, and attribute classification to avoid duplicated branch tables.
- [x] Unsupported or malformed metadata fails closed before generator output.

## Phase 3 Generator And Compiler Plugin

- [x] Declaration planning and shells are stable for namespaces, enums, structs, delegates, interfaces, runtime classes, companions, and metadata surfaces.
- [x] Member/property/event/method emission, activation/static/factory surfaces, collections, async, custom mappings, WinUI source additions, namespace/type excludes, and unsupported-shape diagnostics are implemented.
- [x] Generated support files remain owner-qualified and deterministic across projection modules, including compiler-support TSVs, projection registrars, generic ABI support, event helpers, TypeDetails, and module-local ABI helpers.
- [x] Compiler-plugin lowering owns descriptor-backed calls, native direct calls, compiler-visible authored candidate artifacts, support initialization, projection registrar initialization, and Kotlin 2.4-compatible IR lookups.
- [x] Large generated projections avoid JVM class limits through registrar/support chunking without adding runtime reflection, classpath scanning, vararg dispatch, or global registries.
- [x] Authored CCW pre-render contracts cover generator-known inherited/overridable ABI surfaces: supported `IKeyValuePair<TKey,TValue>`, generic collections, bindable collection parameters and returns, async references, `IPropertyValue`, runtime-owned custom objects, and `IReference<T>` / `IReferenceArray<T>` decode/write through shared runtime projections, while unsupported reachable parameter or return shapes fail closed before `WinRTAuthoringCcwFactories` or server activation factory support can emit unreachable handler bodies.

## Phase 4 Projections

- [x] Broad Windows SDK and Windows App SDK projection output is generated only after upstream runtime, metadata, and generator contracts exist.
- [x] Dependency identity files define cross-module projection ownership so application modules do not duplicate dependency-owned projected FQNs.
- [x] NuGet projection defaults distinguish preprojected baselines from third-party packages, with opt-in local generation for Windows SDK / Windows App SDK packages when needed.
- [x] Generated-output audits scan for forbidden fallback paths, JVM-only leakage, stale support registries, duplicate FQNs, duplicate type/category tables, and unexpected source-size growth.
- [x] Prebuilt projection artifacts preserve metadata-baseline versioning, snapshot suffixing, publication workflows, SDK installation reliability, and package-coordinate stability.

## Phase 5 Authoring

- [x] Authoring contracts live in `winrt-authoring`; Gradle, generator, compiler-plugin, and runtime consume those contracts instead of defining parallel models.
- [x] Compiler-symbol authored metadata replaces source-scanner-derived behavior where parity requires K2/IR-visible symbols, while scanner artifacts remain parity inputs until fully retired.
- [x] Authored ABI boundary covers factories, static members, composable factories, TypeDetails, metadata type mapping, object lifetime/identity, CCW/RCW ownership, and host manifests.
- [x] Authoring marshaling covers arrays, structs, delegates/events, object/interface/runtime-class parameters and returns, collections, async, nullable/reference shapes, HRESULT/error propagation, and fail-closed diagnostics.
- [x] Inherited authored interface CCW coverage includes map-view/key-value-pair, generic/bindable collections, async references, `IPropertyValue`, runtime-owned custom objects, and nullable/reference parity: `IMapView`-style `GetValueByIndex` returns are marshaled through the runtime key-value-pair collection host, supported collection/async/property-value/custom-object/reference parameters and bindable collection returns are decoded or written before invoking authored Kotlin members, and unreachable unsupported shapes are diagnosed by the generator instead of downstream CCW scanning.
- [x] Native authoring host covers `DllGetActivationFactory`, `DllCanUnloadNow`, export-table validation, activation lookup/query, dependency factory chaining, native component/consumer fixtures, and common-owned KMP generated support.

## Phase 6 Packaging And Validation

- [x] Kotlin appx/msix packaging covers manifest generation/validation, appx/msix layout, dependency payload resolution, resources/PRI/MRT, signing/test-install hooks, packaged/unpackaged modes, and disabled-generation output preservation.
- [x] Gradle identity propagation publishes and consumes `kotlin-winrt-identity` metadata variants for reusable WinRT libraries and final application modules.
- [x] Gradle plugin publication uses the shared Maven Central publishing convention, signing policy, snapshot/release versioning, and root/included-build publication flows.
- [x] Configuration-cache and build-cache compatibility are required for touched generation, packaging, runtime asset, authoring, projection, and sample tasks.
- [x] WinUI sample validation covers generated app hosts, Windows App SDK deployment, XAML metadata providers, WinUIEssential component resources, `SettingsCard`, stabilized `Shimmer` loading, and `MenuFlyout.ShowAt(Point)` lifetime behavior.

## Frozen Until Prerequisites Close

- [x] `winrt-samples`: keep validation-only; expand only for completed runtime/generator/authoring/packaging slices with no sample-local runtime workarounds.
- [x] `winrt-projections`: avoid broad checked-in generated growth; prefer plugin-generated output gated by completed upstream contracts.
- [x] `mingwX64`: runtime/compiler/projection parity is open only for regression fixes unless a new native parity slice is explicitly planned; sample expansion remains gated by owner-module contracts.
- [x] Codebase slimming and standalone source-count reduction: frozen until runtime, metadata, generator/compiler-plugin, projections, authoring, and packaging are functionally complete.

## Validation Gates

- [x] Runtime gate: targeted Windows `:winrt-runtime:jvmTest`, native runtime compile/test gates, and common-test promotion for platform-neutral contracts.
- [x] Metadata gate: Windows `:winrt-metadata:test` for native WinMD ingestion, normalized symbols, signatures, accessors, custom attributes, and diagnostics.
- [x] Generator/compiler-plugin gate: Windows `:winrt-generator:test`, `:winrt-compiler-plugin:test`, generated projection compile, support-file validation, and stale artifact cleanup.
- [x] Projection gate: generated-output audit plus JVM and `mingwX64` compile gates, including lightweight and full Windows SDK root KMP validation.
- [x] Authoring gate: generated TypeDetails, authored WinMD, CCW factories, activation/hosting, native exported-DLL fixtures, receive-array handling, and WinUI composable boundaries.
- [x] Packaging gate: PRI/MRT/resource/package staging, appx/msix layout, runtime assets, signing/test-install hooks, and Windows App SDK deployment validation.
- [x] Sample gate: use `winrt-samples` and KMP WinUI runs only as representative end-to-end validation after owning contracts are implemented.
