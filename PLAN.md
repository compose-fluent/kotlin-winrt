# Plan

## Rules

- [ ] `.cswinrt/` is the implementation baseline for runtime, metadata, generator, authoring, projections, and samples.
- [ ] Keep dependency order: `winrt-runtime` -> `winrt-metadata` -> `winrt-generator` -> `winrt-projections` -> `winrt-authoring` -> `winrt-samples`.
- [ ] Tests validate `.cswinrt` parity; do not derive runtime/generator rules from sample failures.
- [ ] Keep `sample-jvm-winui3` legacy-only unless explicitly requested.

## Current Focus

- [ ] CCW/object marshaling parity 正在做: close remaining cswinrt `GetInterfaceTableEntries` differences for boxed values/delegates and keep WinUI smoke as validation only.
- [ ] Clipboard/WinRT async consumption hardening 正在做: keep generated clipboard APIs on the normal projection path while closing runtime async completion lifetime first; transient clipboard-open retry policy remains a later runtime/projection-owned slice.
- [ ] Generator audit closure 正在做: keep non-authoring gaps explicit instead of claiming full parity; authoring writer output is closed and remaining active authoring work is host packaging.
- [x] WinUI object collection insertion: populated ComboBox/ListView `ItemCollection<Any?>` through generated `items.add(string)`, validating the cswinrt `MarshalInspectable<object>`/`IBindableVector`-style object input path instead of keeping samples empty.
- [x] Generated interface artifact object marshaling: `WinRtGeneratedInterfaceProjectionRuntime` now routes `System.Object` ABI parameters and returns through `WinRtObjectMarshaller` instead of duplicating local CCW/RCW branches, matching cswinrt `MarshalInspectable<object>` ownership for generated interface-projection artifacts.
- [x] Delegate object marshaling convergence: `WinRtDelegateAbiMarshaller` now routes delegate `System.Object` ABI arguments and return values through `WinRtObjectMarshaller` while keeping `IUnknown`/`IInspectable` reference parameters on the reference path, and delegate CCWs expose cswinrt's `IPropertyValue`/`IReference<TDelegate>` boxed delegate surface instead of a delegate-local CCW/RCW branch.
- [x] Inspectable-any marshaler convergence: `Marshaler.inspectableAny()` now delegates create/from-ABI/from-managed object ownership to `WinRtObjectMarshaller`, so generic `System.Object` marshaler use sites get the same delegate-as-object and managed-object identity behavior as generated interfaces, events, collections, and delegate ABI paths.
- [x] Bindable object marshaler convergence: `WinRtBindableObjectMarshaller` now uses `WinRtObjectMarshaller` for outbound `IBindable*` object values while preserving bindable-specific inbound `IPropertyValue` projection, so bindable vector/list updates no longer bypass the shared `MarshalInspectable<object>` delegate/object path.
- [x] Generic-parameter object marshaler convergence: `MarshalGenericParameter` and `WinRtGenericParameterProjection` now route object ABI ownership through `WinRtObjectMarshaller`, keeping generic `T` values aligned with the same cswinrt-style `MarshalInspectable<object>` behavior used by generated object paths.
- [x] Property-value CCW suffix parity: pure `IPropertyValue` hosts now pass through the same cswinrt-style `InteropRuntimeHooks` augmentation as `IReference<T>` and `IReferenceArray<T>` hosts, exposing `IStringable`, `IWeakReferenceSource`, `IMarshal`, `IAgileObject`, `IInspectable`, `IUnknown`, and hidden `IReferenceTrackerTarget` instead of a property-value-only COM surface.
- [x] AsyncInfo host CCW suffix parity: task-to-async `IAsyncAction`/`IAsyncOperation` hosts now use the same `InteropRuntimeHooks` augmentation as cswinrt managed CCWs, keeping `IStringable`, `IWeakReferenceSource`, `IMarshal`, `IAgileObject`, `IInspectable`, `IUnknown`, and hidden `IReferenceTrackerTarget` available on runtime-created async operation objects.
- [x] Task-to-async completed-handler assignment parity: runtime-created async action/operation hosts now mirror cswinrt `TaskToAsyncInfoAdapter.Completed` by allowing only one non-null completed handler assignment and returning `E_ILLEGAL_DELEGATE_ASSIGNMENT` on later writes.
- [x] Task-to-async close-state parity: runtime-created async action/operation hosts now mirror cswinrt `IAsyncInfo.Close` state rules by rejecting close while started with `E_ILLEGAL_STATE_CHANGE`, making repeated close idempotent, and returning `E_ILLEGAL_METHOD_CALL` from post-close accessors.
- [x] Bindable collection CCW suffix parity: runtime-created `IBindableIterable`/`IBindableIterator`/`IBindableVectorView`/`IBindableVector` hosts now use the same cswinrt-style CCW augmentation path, so WinUI bindable collection consumers can QI standard suffix interfaces and hidden `IReferenceTrackerTarget`.
- [x] Generic collection CCW suffix parity: runtime-created `IIterable<T>`/`IIterator<T>`/`IVectorView<T>`/`IVector<T>`/`IMapView<K,V>`/`IMap<K,V>`/`IKeyValuePair<K,V>` hosts now share the same cswinrt-style CCW augmentation path as bindable collections.
- [ ] Compiler-plugin reflection replacement 正在做: move cswinrt runtime-reflection surfaces into compiler-generated indexes and generated registration code.
- [x] Interface native projection IR lowering: replaced the process-wide `WinRTInterfaceProjectionRegistry`/classloader discovery path with a generated marker call that the compiler plugin lowers directly to `WinRtGeneratedInterfaceProjectionRuntime`, using `interface-native-projections.tsv` only as compile-time input. Targeted compiler-plugin, generator, and runtime tests pass; broader WinUI KMP compile validation is blocked locally by JVM CodeCache/native-memory exhaustion, not a Kotlin source diagnostic.
- [ ] Expect/actual projection ABI migration 正在做: generator emits common `expect` WinRT APIs and JVM `actual` ABI-owning projection implementations for a narrow non-generic interface/member slice before broad projection rollout.
- [ ] Generated support compile-surface reduction 正在做: keep public projection source complete, reduce generated Kotlin parse/diagnostic hot spots first, and only move support tables to compiler artifacts when the same slice removes the equivalent Kotlin source.
- [x] Direct runtime-class event add/remove cswinrt parity: generated instance event functions such as `addSizeChanged(handler)` now forward to the generated `WinRtEvent` property (`sizeChanged.add/remove`) instead of issuing a separate delegate ABI call, so the `EventSource`/`EventSourceState` owner keeps the delegate handle alive until remove/close like `.cswinrt/src/cswinrt/code_writers.h` `Subscribe` / `Unsubscribe`.
- [x] Event-source cswinrt parity correction: generated event properties now call owner-scoped support helper methods that directly construct typed `EventSource` instances near the projected owner surface, matching `.cswinrt/src/cswinrt/code_writers.h` `Get_EventName` / `ConditionalWeakTable` ownership instead of a process-wide lookup table. New generator output no longer emits `event-sources.tsv` or `event-source` compiler-support manifest rows for this path.
- [ ] WinUI interface NativeProjection artifact migration 正在做: keep complete public generated projection APIs, emit deterministic interface ABI plan TSV for eligible non-generic interface methods/properties, move their RCW implementation bodies out of generated Kotlin source through compiler/runtime-generated JVM artifacts, and leave unsupported interfaces on the existing Kotlin fallback.
- [x] Skiko WinUI upstream issue triage fix: event-source owner helpers now require a real generated event source path for imported helpers and reuse the same `Metadata.wrap` capability check as projected interfaces/runtime classes instead of maintaining event-specific type-name whitelists; this covers generic `IObservableMap.MapChanged` metadata wrapping and `ICoreDispatcher` typed-event owner helpers so generated event properties never import a missing `WinRTEventProjectionHelper_*`. Interface native projection artifact descriptors also map projected runtime-class/interface ABI values to the shared `System.Object` marshaler path, matching cswinrt `MarshalInspectable<object>` for generated `IWindow.Content`-style members instead of leaving them as unsupported artifact rows.
- [x] Compiler support registry aggregation superseded for interface native projections: library WinRT identities still publish merged compiler-support manifests for compile-time inputs, but interface native projection no longer uses a fixed-FQN `WinRTInterfaceProjectionRegistry` runtime registrar.
- [x] Compiler-generated interface registry classpath-shadow fix superseded: the content-addressed registry workaround is replaced by compiler-plugin IR lowering from the generated marker call to direct runtime proxy creation, so dependency/app classpath ordering no longer participates in interface factory lookup.
- [x] Compiler-generated interface registry direct-call path superseded: generated projection bytecode now carries the projected type marker and Kotlin interface class literal; the compiler plugin supplies the member descriptor payload directly to `WinRtGeneratedInterfaceProjectionRuntime` instead of passing a registry class token to `ComWrappersSupport`.
- [ ] MSBuild PRI pipeline parity 正在做: current KWINRT-008 fix implements the app-level `resources.pri` / `resources.language-*.pri` generation needed to aggregate staged component PRI files for WinUI resource lookup, and now exposes `ProjectPriIndexName`, XML manifest-derived index-name/default-language/qualifier inputs, explicit application PRI resource files/directories, `Page` / `ApplicationDefinition` layout inputs, default project `.resw` / `.xaml` / image content inputs, `.xbf` layout filtering, content/image and embed-file package payload staging, `TargetPath`, `ExcludedFromBuild`, and an `AppxPriInitialPath`-style input subpath for app-owned resources. Remaining full parity is the larger Windows App SDK `GeneratePriConfigurationFiles` resfile/priconfig split plus the non-default `WinAppSdkExpandPriContent` path.
- [ ] KMP WinRT graph baseline 正在做: treat KMP `library -> library -> application` graphs as a mandatory validation surface, not a later sample expansion. Source-set ownership, compiler-plugin injection, projection identity, generated registries, runtime assets, and resource staging must work through customized KMP source-set names and transitive project dependencies; single-module JVM samples are smoke coverage only.
- [x] KWINRT-019 focus validation: the dual-module WinUI sample now waits for a post-layout XAML callback before calling generated `UIElement.Focus(FocusState.Programmatic)`, sets controls as tab stops, checks the returned `Boolean`, records `focusState`, observes generated focus events, and exits cleanly. Local evidence shows the generated projection path can focus live WinUI controls after layout; compose-winui's remaining failure should be treated as a call-timing/layout precondition until a new projection/runtime fault is reproduced.
- [x] Interface artifact fallback gate: interface TSV/registry entries stay limited to descriptors whose member ABI value kinds are fully supported; projected-object, mapped-collection, unsupported, and floating-point members remain on the Kotlin/IR fallback instead of emitting unsupported artifact rows.
- [x] WinRT reference property nullability: mirrored `.cswinrt/src/cswinrt/code_writers.h` `FromAbi(IntPtr.Zero)` behavior by treating projected WinRT property reference types as nullable from metadata type shape instead of maintaining XAML owner/property descriptor tables. Getter/setter planning and renderer paths now carry `typesByQualifiedName` into the property type decision, static projected-object getters choose nullable intrinsics, and custom object ABI parameters such as `WinRtUri?` marshal null as an ABI null pointer.
- [x] JVM FFM downcall handle caching: compiler-plugin JVM FFM lowering now obtains unbound `MethodHandle`s from a runtime ABI-shape cache while the plugin still owns vtable lookup, carrier conversion, HRESULT handling, and result readback; this avoids K2 field-initializer ICEs, generated source/classes, runtime invoker layers, and `ComVtableInvoker` fallback.
- [x] ComVtableInvoker projection fallback shrink: removed the remaining single-argument `Byte`/`Float`/`Double` typed `invokeArgs` overloads and JVM fixed descriptors that existed only for generated projection fallback shapes now covered by compiler-plugin JVM FFM lowering; retained runtime-owned ABI primitives for IUnknown/IInspectable, agile/weak/marshal, standard delegate tokens, async, collections, callbacks, value boxing, and XAML bootstrap.
- [x] No-argument Unit descriptor marker routing: generated no-arg `Unit` projection calls now route through the existing descriptor `WinRtProjectionIntrinsic.callUnit(reference, slot, "")` marker and compiler-plugin JVM FFM lowering, without reviving the old `invokeUnit` marker or adding a runtime invoker layer.
- [x] Closable fallback source marker routing: generated fallback `AutoCloseable.close()` bodies for mapped `Windows.Foundation.IClosable` native projections now route through the existing no-argument descriptor `WinRtProjectionIntrinsic.callUnit(nativeObject, 6, "")` marker in intrinsic/support-file mode, keeping non-intrinsic output on the existing inline HRESULT path.
- [x] Derived composable factory descriptor marker routing: eligible generated `createInstanceForSubclass` raw-pointer factory calls now route through the existing descriptor `WinRtProjectionIntrinsic.callUnit` marker; no-exception/event remove paths remain on their current fallback until their HRESULT semantics are represented explicitly.
- [x] KMP JVM compiler-plugin wiring: kotlin-winrt Gradle plugin integration now configures `KotlinJvmCompile` tasks so multiplatform JVM targets such as compose-winui's `compileKotlinWinuiJvm` receive the same projection intrinsic lowering options as ordinary `org.jetbrains.kotlin.jvm` projects, while generated projections remain attached to KMP `commonMain`.
- [x] KMP JVM intrinsic lowering guard: compiler-plugin IR lowering now fails JVM projection compilation when `WinRtProjectionIntrinsic` calls are present but the compile JDK does not expose `java.lang.foreign`, preventing JDK 21 KMP builds from silently emitting runtime fallback intrinsics such as `Application.Metadata.start(...)`; TestKit also checks a KMP `commonMain` intrinsic compiles to `WinRtJvmFfmDowncallHandles` bytecode.
- [x] Compiler-plugin daemon bytecode compatibility: `winrt-compiler-plugin` is emitted as Java 21 bytecode while still built with the JDK 22 toolchain, so compose-style builds whose Kotlin daemon runs on JDK 21 can load the registrar and reach the explicit `java.lang.foreign` lowering guard instead of leaving stale intrinsic fallback classes behind.
- [x] WinRT JVM compile target enforcement: the Gradle plugin now raises Kotlin/JVM WinRT compile tasks to JVM target 22 and normalizes `-Xjdk-release=22`, matching the runtime's Java FFM dependency; the compiler plugin also fails when recognized projection intrinsics cannot see JVM FFM symbols or otherwise remain unlowered, instead of producing bytecode that calls `WinRtProjectionIntrinsic` at runtime.
- [x] JVM FFM MethodHandle signature-polymorphic lowering: compiler-plugin direct FFM calls now instantiate `MethodHandle.invoke` with expanded carrier parameters and primitive HRESULT return type, matching Kotlin JVM backend polymorphic-signature lowering instead of emitting the invalid `invoke(Object[])Object` vararg shape; KMP TestKit validates the generated class descriptor.
- [x] Projection intrinsic owner matching: compiler-plugin intrinsic detection now requires calls to be owned by `WinRtProjectionIntrinsic`, so ordinary generated WinRT members named like intrinsic helpers such as `IJsonValue.getString()` are not misclassified and rejected before lowering.
- [x] Transitive projection identity graph: library `kotlinWinRtIdentityElements` variants now publish their own WinRT identity project dependencies, so `liba -> libb -> application` resolves the full projection-owner graph instead of only suppressing direct application dependencies.
- [x] KMP multi-layer projection ABI visibility: the WinUI KMP validation now uses a real `base library -> WinUI library -> application` graph. The base library owns non-UI projections such as `Windows.System.Launcher`; the middle WinUI library consumes that projection; the app deliberately redeclares both base-owned and direct-library-owned types and verifies they are suppressed from app generation. Generated projection ABI metadata used across module boundaries, including slot constants, enum ABI values/converters, and struct ABI copy/read helpers, is public so downstream generated projection code can consume dependency-owned projection assemblies instead of regenerating duplicate FQNs.
- [x] Object marshaller boxed-value readback: `WinRtObjectMarshaller.fromAbi` now reuses the shared inspectable value projection before falling back to generic RCW creation, so native-returned `IPropertyValue`/`IReference<T>` boxes such as XAML `ContentControl.Content` strings round-trip as Kotlin values instead of opaque wrappers.
- [x] XAML object value lifetime and concrete collection RCW recovery: `System.Object` CCWs created for non-WinRT Kotlin values are retained across WinRT calls so XAML dependency properties can return the same boxed value pointer after setter scope exit, and runtime-class collection adapters now route element readback through `ComWrappersSupport.createRcwForComObject` before static fallback. The dual-module WinUI KMP sample validates `Button.content` string round-trip, `Canvas.children[0] is Button`, and detached `Canvas.setLeft/setTop` positioning before window attachment, covering KWINRT-018, the concrete-wrapper path behind KWINRT-009, and the currently reproduced Canvas attached-positioning path behind KWINRT-007; `winrt-runtime:compileKotlinMingwX64` also passes after keeping event shutdown unregister logic compatible with Kotlin/Native maps.
- [x] Event-source registry ABI parity: compiler-generated `WinRTEventProjectionRegistry` now calls `WinRtGeneratedEventSourceRuntime` through the Kotlin `object` instance ABI (`INSTANCE` + `invokevirtual`) and the runtime no longer exposes that factory as a static-only method, matching ordinary Kotlin call sites and avoiding mixed static/instance `IncompatibleClassChangeError` failures in generated event accessors.
- [x] KWINRT-013 WinUI event-source registration: generated support now emits a `WinRTEventProjectionRegistry` that installs all supported event-source descriptors into the runtime before projected event access, matching cswinrt's generated support-table ownership instead of relying on sample-local setup. Closed generic event delegates now carry their parameterized IID into `event-sources.tsv`, delegate `System.Object` callback arguments use the runtime-decoded value directly, and the dual-module KMP WinUI library > application sample registers window/control/textbox events through the library-owned projection surface.
- [x] Event-source shutdown removal: generated/runtime `EventSource` subscriptions now register active native event tokens with a process-shutdown registry and the Windows App SDK bootstrap close path drains that registry before `MddBootstrapShutdown`, so remaining WinUI event handlers are removed before JVM FFM upcall stubs can be called by native shutdown callbacks, addressing KWINRT-013's late-callback crash class.
- [x] Event-source shutdown failure cleanup: the JVM shutdown registry now always closes the Kotlin `EventSourceState` even if native event removal throws while the source is already tearing down, keeping the cswinrt EventSource/EventSourceState ownership split while preventing stale handler state from surviving into late WinUI callbacks.
- [x] Shared `EventHandler<T>` / multi-module event descriptor parity: compiler-artifact event-source descriptors marked `usesSharedEventHandlerSource` now instantiate the runtime `EventHandlerEventSource<T>` path instead of the generic reflection/Proxy event source, matching `.cswinrt/src/WinRT.Runtime/Interop/EventHandlerEventSource{T}.cs` for XAML events such as `FrameworkElement.LayoutUpdated`. The native WinUI failfast was traced through WER as `E_FAIL (0x80004005)` in `Microsoft.UI.Xaml.dll`, and the Kotlin-side cause was an application-module descriptor with empty IID/parameter columns blocking the dependency library's complete descriptor. Generator support now still emits `EventHandler<T>` / `TypedEventHandler<TSender,TArgs>` ABI parameter kinds when the delegate declaration is owned by another module, and runtime registration/load order upgrades incomplete duplicate descriptors from classpath resources instead of letting a packaging-only application module shadow the library-owned projection.
- [x] KMP WinRT library-to-application validation: added a multi-module KMP WinRT library > KMP WinRT application validation path so dependency identity, common generated projections, runtime asset staging, and JVM lowering are exercised across module boundaries instead of only in single-module samples; KMP authored `Application` type-details are now generated into the source set that owns the scanned source root instead of common generated sources.
- [x] KMP authored Application source-set ownership: the dual-module WinUI sample now validates projection consumption with an authored `Application` subclass in the KMP source set that owns the scanned source root, instead of relying on common generated authoring details or sample-local `App.xaml` glue.
- [x] Application packaging-only projection boundary: application modules that only declare `application {}` plus NuGet packages for runtime staging no longer expand those NuGet packages into a full projection surface; app modules consume dependency identity and stage assets while library modules own the generated WinUI projection surface.
- [x] Empty projection input boundary: non-application Gradle projects with no explicit `winRt` metadata sources, namespaces, or types now generate an empty projection identity instead of falling back to the entire Windows SDK, so KMP library/application samples only project SDK metadata when a real projection filter is declared.
- [x] KWINRT-011 duplicate projection suppression validation: the dual-module KMP WinUI application now deliberately redeclares library-owned WinUI projection types (`AutomationProperties`, `Button`, `TextBox`) while depending on the WinUI library; `:winrt-samples:winui-kmp-app:compileKotlinWinuiJvm` passes and the application generated-source directory contains no duplicate `microsoft.ui.xaml` projection files, matching cswinrt's single projection-assembly ownership model.
- [x] Compose-winui-shaped KMP compiler plugin classpath: the dual-module WinUI validation now mirrors compose-ui's `commonMain -> winuiMain -> winuiJvmMain` source-set layering, SDK version/extension settings, projection type list, and `sourceRoots` override; the Gradle plugin also injects the compiler plugin into late-created KMP compiler-plugin classpath configurations instead of assuming only `kotlinCompilerPluginClasspath` exists.
- [x] WinUI controls activation regression: `buildWinRtAuthoringHost` remains a mandatory `winrt-samples:run` prerequisite; no-arg `Unit` projection methods now stay on the generator inline vtable path after `WinRtProjectionIntrinsic.invokeUnit` regressed `Microsoft.UI.Xaml.Window.Activate`.
- [x] Static overload ABI slot binding: runtime-class static method shells now bind through the matched declaring static interface method row when computing generated slot constants, so overloaded statics such as `Windows.System.Launcher.launchUriAsync(uri)` no longer miss existing `ILauncherStatics` ABI bindings; async operation result readback also covers `System.Guid` so newly exposed static/interface paths can project `IAsyncOperation<Guid>` results.
- [x] KWINRT-003/005 sample validation: the dual-module KMP WinUI library sample now verifies `ContentControl.Content` null clear/readback on `Button.content`, and keeps a compile-only call to the natural `Launcher.launchUriAsync(WinRtUri)` one-argument overload so nullable XAML object properties and the launcher overload do not regress silently.
- [x] KWINRT-008 WinUI resources bootstrap: runtime asset staging now mirrors the MSBuild application PRI path instead of copying the WindowsAppSDK Runtime MSIX merged `resources.pri`: component PRI files remain copied payload, and application staging invokes `makepri` over a temporary PRI-only input root to generate the app-level `resources.pri` plus `resources.language-*.pri` satellites. This keeps the self-contained target behavior that deletes the Runtime MSIX merged PRI, avoids reindexing WinUI payload files as duplicate folder resources, and lets both the single-module WinUI controls sample and the dual-module KMP WinUI library > application sample construct/install `XamlControlsResources()` without the previous native failfast.
- [x] KWINRT-008 PRI default qualifier parity: application PRI generation now exposes application-level Gradle options for project PRI generation, `ProjectPriIndexName`, default language, default qualifiers, and Appx manifest inputs. Application package staging reads `<Resource Language="...">` from Appx manifests through shared secure XML DOM support and feeds the resolved default qualifiers into the MSBuild-style full-index priconfig writer before invoking `makepri new`; the older runtime-assets compatibility task still carries the legacy `makepri createconfig/new` path until that surface is fully retired.
- [x] KWINRT-008 manifest identity PRI index-name parity: when `projectPriIndexName` is not explicitly configured, application PRI generation now reads the Appx manifest `<Identity Name="...">` through the same secure XML DOM path and uses it as the `makepri new /in` index name before falling back to the Gradle project name, matching the MSBuild `ProjectPriIndexName` default.
- [x] KWINRT-008 explicit PRI resource inputs: application PRI staging now accepts app-owned PRI resource files/directories and copies them into the temporary makepri project root under a validated relative initial path, matching the `PRIResource` / `AppxPriInitialPath` responsibility split without broad sample-local resource probing.
- [x] KWINRT-008 explicit PRIResource target path parity: single-file `projectPriResource(...)` inputs under the Gradle project root now preserve their project-relative path beneath `AppxPriInitialPath`, matching MSBuild `PRIResource` target-path behavior instead of flattening explicit resources to the PRI input root.
- [x] KWINRT-008 explicit directory target path parity: explicit `projectPriResource(...)`, `projectPriPage(...)` / `projectPriApplicationDefinition(...)`, and `projectPriContent(...)` / `projectPriImage(...)` directories that live under the Gradle project root now stage files by project-relative target path beneath `AppxPriInitialPath`, matching MSBuild packaging output target paths while preserving directory-relative behavior for external resource roots.
- [x] KWINRT-008 PRI input target de-duplication: application PRI staging now tracks normalized target paths across both dependency component PRI payload and explicit project PRI resources before invoking `makepri`, so project resource inputs cannot silently overwrite an already staged component PRI at the same project-root path. Full MSBuild PRI item classification is still tracked under the active `MSBuild PRI pipeline parity` item.
- [x] KWINRT-008 default `.resw` PRI item parity: application projects now mirror `Microsoft.Windows.SDK.BuildTools.MSIX.Common.props` default PRI item behavior by staging project-local `**/*.resw` files when default project PRI resources are enabled, preserving their project-relative paths under `AppxPriInitialPath` and letting explicit `projectPriResource(...)` inputs take precedence over the broad default scan.
- [x] KWINRT-008 Page/ApplicationDefinition PRI layout input parity: application PRI staging now treats project-local `**/*.xaml` plus explicit `projectPriPage(...)` / `projectPriApplicationDefinition(...)` inputs as layout resources, preserving project-relative paths under `AppxPriInitialPath` before `makepri new`, matching the MSBuild `Page` / `ApplicationDefinition` responsibility without adding sample-local App.xaml runtime hooks.
- [x] KWINRT-008 compiled XAML layout filtering parity: PRI layout staging now accepts `.xbf` layout payloads and applies the MSBuild `MrtCore.PriGen.targets` rule that removes a `.xaml` layout input when the matching `.xbf` is present at the same target path, so makepri sees the compiled XAML resource shape instead of duplicate source/compiled layout entries.
- [x] KWINRT-008 Content/Image PRI input parity: application PRI staging now treats explicit `projectPriContent(...)` / `projectPriImage(...)` inputs and default project image globs (`.png`, `.bmp`, `.jpg`, `.dds`, `.tif`, `.tga`, `.gif`) as project layout resources under `AppxPriInitialPath`, matching the MrtCore `Content`/`Image` path that feeds `ContentFilesProjectOutputGroup` into PRI generation.
- [x] KWINRT-008 application package staging boundary: Gradle wiring now separates NuGet/runtime/authoring asset copying from Appx-style application resource packaging. `stageWinRtRuntimeAssets` produces the raw runtime asset layout, `stageWinRtApplicationPackage` copies that layout into an application package output and owns app-level PRI generation/resource inputs, and Java resources/distributions consume the application package output. The old runtime-task PRI entry point remains only as a compatibility path for existing task-level coverage until the PRI tests are migrated fully to the package task.
- [x] KWINRT-008 application package item model seed: `StageWinRtApplicationPackageTask` now routes component PRI, explicit/default `PRIResource`, layout, and content resources through a typed `ApplicationPackageItem` model with target-path-based de-duplication before copying into the PRI input root, giving the PRI pipeline a single place to add future Appx-style metadata such as `TargetPath`, `ExcludedFromBuild`, and embed/resource group ownership.
- [x] KWINRT-008 explicit TargetPath metadata seed: application PRI packaging now accepts explicit target-path mappings for single-file `projectPriResource(...)`, `projectPriPage(...)` / `projectPriApplicationDefinition(...)`, and `projectPriContent(...)` / `projectPriImage(...)` inputs, so Gradle DSL users can model MSBuild `TargetPath` without relying on project-relative source layout.
- [x] KWINRT-008 ExcludedFromBuild metadata seed: application PRI packaging now accepts explicit source-path exclusions for project PRI resources, layout files, and content/image files, and applies those exclusions before both explicit item staging and default project resource scans enter the `ApplicationPackageItem` model.
- [x] KWINRT-008 EmbedOutputGroupForPackaging input seed: application PRI packaging now accepts explicit embed-file inputs with the same `TargetPath` and `ExcludedFromBuild` metadata behavior as other project PRI items, staging them through the `ApplicationPackageItem` model so the later full `GeneratePriConfigurationFiles` resfile path has a distinct embed item source.
- [x] KWINRT-008 application package payload parity seed: application PRI packaging now copies staged layout, content/image, and embed-file items into the application package output at their resolved target paths while keeping `.resw` `PRIResource` source files as makepri-only inputs, matching the MSBuild split between PRI indexing inputs and package layout payload.
- [x] KWINRT-008 directory TargetPath metadata parity: explicit directory inputs for `PRIResource`, `Page` / `ApplicationDefinition`, content/image, and embed files now treat configured `TargetPath` as the root target path for their child files, instead of only honoring `TargetPath` for single-file inputs.
- [x] KWINRT-008 package payload/makepri boundary validation: application package staging now has coverage that layout/content payloads are copied into the package output even when `makepri.exe` is unavailable, while `resources.pri` generation remains skipped, keeping package layout staging independent from PRI tool discovery.
- [x] KWINRT-008 GeneratePriConfigurationFiles input inventory seed: application PRI packaging now writes deterministic `filtered.layout.resfiles`, `resources.resfiles`, `pri.resfiles`, and `embed/embed.resfiles` inventories under the task-owned `project-pri-config` directory before invoking `makepri`, matching the MSBuild item classification boundary that feeds `WinAppSdkGeneratePriConfigurationFiles`; final full-index XML is generated by `ProjectPriConfigXmlWriter` rather than `makepri createconfig`.
- [x] KWINRT-008 excluded layout resfile inventory: application PRI packaging now records `.xaml` layout files filtered out by matching `.xbf` payloads into `excluded.layout.resfiles`, matching the MSBuild `ExcludedLayoutResfilesPath` classification without copying the excluded source layout into the PRI input root.
- [x] KWINRT-008 unfiltered layout resfile inventory: application PRI packaging now writes `unfiltered.layout.resfiles` from the pre-filter layout/content classification, preserving the MSBuild `UnfilteredLayoutResfilesPath` boundary separately from filtered layout and `.xbf`-excluded XAML entries.
- [x] KWINRT-008 embed-file staging parity: application PRI packaging now stages explicit embed files and compiled `.xbf` layout outputs under the task-owned `project-pri/embed` root, writes `embed/embed.resfiles` relative to that root, and stops copying embed-only inputs into the package layout payload, matching the MSBuild `_EmbedFileCopy` plus `EMBEDFILES` indexer boundary.
- [x] KWINRT-008 full-index priconfig seed: application PRI packaging now writes its own MSBuild-style full-index `priconfig.xml` with separate filtered layout, RESW/RESJSON/resource, PRI, and EMBEDFILES index sections instead of shelling out to `makepri createconfig`, leaving `makepri new` as the only PRI tool invocation for the staged project root.
- [x] KWINRT-008 priconfig writer ownership: the MSBuild-style full-index XML writer now lives in a task-independent `ProjectPriConfigXmlWriter`, keeping PRI config generation as reusable Appx packaging infrastructure instead of private application-task glue.
- [x] KWINRT-008 manifest PRI metadata ownership: Appx manifest language/index-name parsing and MSBuild default-qualifier expansion now live in shared `ProjectPriManifestSupport`, so application package staging and the legacy runtime-assets compatibility path no longer carry separate XML parsing and qualifier rendering rules.
- [x] KWINRT-022 flags enum projection: generator enum declaration planning now mirrors `.cswinrt/src/cswinrt/code_writers.h` `is_flags_enum` handling by treating `System.FlagsAttribute` enums as unsigned bitmask projections instead of closed Kotlin `enum class` values. Flags enums emit an `@JvmInline value class` over `UInt`, public named constants, ABI converters that accept combined bits, and `contains`/`hasFlag`/`or`/`and` helpers; `contains` uses a block body so KotlinPoet cannot wrap the comparison into an invalid expression-body `UInt` return during WinUI KMP projection generation.
- [x] KWINRT-014 local attached dependency-property validation: the dual-module KMP WinUI library sample now reads generated `AutomationProperties.accessibilityViewProperty` and passes the returned `DependencyProperty` wrapper to `button.clearValue(...)` while the sample application also consumes the library projection. This covers the module-mangled static attached-property getter path in the current repository shape; compose-winui should retest its disabled accessibility toggle before removing its workaround.
- [x] KWINRT-015 local AutomationProperties validation: the dual-module KMP WinUI library sample now declares `Microsoft.UI.Xaml.Automation.AutomationProperties` and `AccessibilityView`, then calls generated `AutomationProperties.setAccessibilityView(button, AccessibilityView.Raw)` while the `Button` is still detached from `Window.content`. The run passes through the generated static attached-property setter before window attachment, so the currently reproduced sample path does not show a kotlin-winrt ABI/projection crash; compose-winui should still retest its broader offscreen host path before closing the original issue.
- [x] KWINRT-020 default-interface call-shape validation: generated-interface artifact invocations now route no-argument `Unit` methods through the existing fixed HRESULT vtable primitive instead of the dynamic generic FFM call path; runtime coverage exercises the generated proxy path against a fake WinRT interface, and the dual-module KMP WinUI library keeps `DisplayRequest.requestActive()` / `requestRelease()` on the compile surface for compose-winui retesting without putting DisplayRequest's native activation preconditions into the default sample run.
- [x] KWINRT-021 protected WinUI member projection validation: generator coverage now locks the cswinrt protected-interface shape where the protected interface projection remains internal but the runtime class exposes the member as a protected Kotlin property for subclasses. This matches `Microsoft.UI.Xaml.UIElement.ProtectedCursor`: generated `UIElement` has `protected var protectedCursor: InputCursor`, while consumers should expose any public cursor API from their own XAML subclass rather than calling `IUIElementProtected` ABI slots directly.
- [x] Closed generic event delegate IID parity: generated event-source subclasses now pass the descriptor's closed delegate IID into `WinRtDelegateBridge.createDelegate(...)` instead of the generic delegate definition IID, matching `.cswinrt` parameterized-interface identity for events such as `TypedEventHandler<System.Object, WindowActivatedEventArgs>` and avoiding WinUI add-handler paths receiving a delegate CCW under the wrong interface identity.
- [x] Multi-module event-source registry parity: event-source support now follows the same Gradle/compiler-artifact aggregation model as interface native projections. Libraries publish merged compiler-support manifests through WinRT identity, downstream KMP/JVM compile tasks compile one fixed `WinRTEventProjectionRegistry.class` containing local plus transitive dependency event-source rows, and the runtime no longer parses `kotlin-winrt/event-sources.tsv` resources.
- [x] Compiler-generated event callback argument parity: generic JVM event sources registered by the compiler-generated registry wrap declared projected event argument types through the generated `Metadata.wrap(...)` companion before invoking Kotlin handlers, so WinUI callbacks such as `Window.Activated`, `Window.VisibilityChanged`, and `TextBox.TextChanging` receive the same typed arguments as generated event-source subclasses without runtime resource scanning.
- [x] WinUI KMP sample callback run validation: the dual-module KMP WinUI sample now exercises window, button, and text-box event subscriptions through the library projection and queues auto-exit through `DispatcherQueueHandler`, allowing `:winrt-samples:winui-kmp-app:runWinuiKmpSample` to finish after real WinUI callbacks instead of failfasting during shutdown.
- [x] WinUI KMP multi-layer run validation: `:winrt-samples:winui-kmp-app:runWinuiKmpSample` passes with the sample shaped as `winui-kmp-base-library -> winui-kmp-library -> winui-kmp-app`, covering transitive projection identity suppression, cross-module projection ABI metadata access, app resource staging, WindowsAppSDK bootstrap, XAML resources installation, and event callbacks in the same run.
- [x] Required iterator projected runtime-class element binding: runtime-class required `IIterator<T>` forwarders now resolve generic element bindings against the owning projection model, so map-entry iterator elements such as `Map.Entry<String, NamedResource>` keep `NamedResource` as a projected runtime class instead of degrading to `Unsupported`.

## Completed Baseline

- [x] Modules exist with `.cswinrt` ownership mapping: `winrt-runtime`, `winrt-metadata`, `winrt-generator`, `winrt-projections`, `winrt-authoring`, `winrt-samples`.
- [x] Runtime baseline covers ABI primitives, activation, object identity, marshaling, delegates/events, collections, async, XAML helpers, configuration, and vtable fast paths.
- [x] Metadata baseline covers WinMD ingestion, deterministic model construction, type refs, ABI descriptors, semantic helpers, source/cache handling, diagnostics, and writer-handoff descriptors.
- [x] Generator app-consumption baseline covers declaration/member emission, activation/static/factory surfaces, mapped collections, async/reference/custom mappings, generic ABI inventory, event helpers, ABI invoke planning, support handoff APIs, and method-generic inputs.
- [x] Plugin baseline covers `winRt {}` DSL, SDK/NuGet inputs, Microsoft NuGet CLI restore/cache semantics, NuGet dependency closure, incremental generated-source wiring, identity JSON, and application resource/runtime staging.
- [x] WindowsAppSDK consumption uses direct `nugetPackage("Microsoft.WindowsAppSDK", version)` plus generic NuGet dependency closure, root version-info staging, package-local lifted WinRT registrations, and content-driven framework/runtime assets.
- [x] Runtime asset staging cleans its task-owned output directory, copies package-declared `runtimes-framework/<rid>/native` resources and lifted registrations for every resolved NuGet package, deliberately does not stage the merged `resources.pri` from the WindowsAppSDK Runtime MSIX because MSBuild self-contained targets delete it and copy component PRI files separately, generates the application-level PRI from staged component PRI inputs, and keeps WindowsAppSDK root include/version files root-owned.
- [x] Project package root is migrated to `io.github.composefluent.winrt`; generated WinRT API projections now use direct WinRT namespace packages such as `windows.data.json` and `microsoft.ui.xaml`, while generator/runtime/support internals stay under `io.github.composefluent.winrt`.

## Recent Runtime Findings

- [x] Runtime-class parameters now follow cswinrt `AsValue(iid)` intent across inline ABI and descriptor intrinsic paths: generated ABI marshaling QIs projected runtime-class arguments to the default-interface IID via `winRtProjectionMarshaler(...)` before passing a `RawAddress`, covering attached-property setter shapes such as Canvas/Automation instead of passing the wrapper's current native pointer.
- [x] Delegate CCW public suffix matches cswinrt `GetInterfaceTableEntries`: delegate IID, `IPropertyValue`, `IReference<TDelegate>`, `IStringable`, `IWeakReferenceSource`, `IMarshal`, `IAgileObject`, `IInspectable`, `IUnknown`.
- [x] Managed CCWs now expose hidden `IReferenceTrackerTarget` QI support for WinUI tracker probes without adding it to `IInspectable.GetIids`.
- [x] Delegate-as-object marshaling now exposes `IReference<TDelegate>` using cswinrt parameterized IID rules; `AddHandler(Object handler)` no longer fails on `DEA1E123-12EA-5CB3-B923-ABE74E426D9E`.
- [x] Boxed reference hosts now expose cswinrt-style `IPropertyValue` plus standard CCW suffix interfaces and hidden `IReferenceTrackerTarget`.
- [x] Delegate callback marshaling now aligns generated callback argument value kinds with cswinrt delegate signatures so runtime-class event args decode as `IInspectableReference` instead of `IUnknownReference`.
- [x] CCW interface-table augmentation now preserves pre-existing hidden interfaces while appending hidden `IReferenceTrackerTarget`, keeping authored/synthetic custom QI surfaces compatible with cswinrt-style public `GetIids` suffix ordering.
- [x] JVM WinUI bootstrap now stages WindowsAppSDK assets, creates an activation context from lifted registrations when available, and falls back to `MddBootstrapInitialize2` when needed.
- [x] `System.Object` projections now surface as Kotlin `Any?` with runtime object/delegate CCW marshaling instead of leaking `IInspectableReference` into public APIs.
- [x] WinUI app smoke sample uses generated projections for click/tapped handlers and WinAppSDK startup instead of sample-local delegate/runtime glue.
- [x] WinUI controls sample composes common WinUI 3 controls through generated projections, installs `XamlControlsResources`, and sets the window `MicaBackdrop` without sample-local runtime glue.
- [x] WinUI runtime-class wrappers now acquire default interfaces with explicit interface lookup instead of assuming sealed class activation returns a default-interface pointer; this matches cswinrt ObjectReference interface ownership and fixes WinUI string setters such as `TextBlock.text`.
- [x] Generated string input parameters now use reference HSTRING marshaling, matching cswinrt `MarshalString` input semantics.
- [x] `IVector<System.Object>` input marshaling now routes through the shared `WinRtObjectMarshaller` path, matching cswinrt `MarshalInspectable<object>` behavior for null/object/delegate/boxed-value inputs instead of using a collection-local non-null `IInspectable` shortcut.
- [x] Collection CCW output marshaling now routes `System.Object` elements through `WinRtObjectMarshaller` as well, so managed `Iterable/List<Any?>` null elements are returned as null `IInspectable*` instead of being treated as missing elements or rejected by a collection-local adapter.
- [x] Observable collection RCW slot-interface cache: generated native projections for derived mapped collection interfaces now QI/cache the actual slot interface such as `IVector<T>`/`IMap<TKey,TValue>` before routing through `WinRtListProjection`/`WinRtDictionaryProjection`, matching cswinrt `IWinRTObject` query-interface cache ownership instead of passing an `IObservableVector<T>`/`IObservableMap<TKey,TValue>` pointer to collection helpers.
- [x] JVM manifest-free activation now resolves candidate DLL names from plugin-staged `kotlin-winrt-runtime-assets` before falling back to the process working directory, matching `.cswinrt/src/WinRT.Runtime/ActivationFactory.cs` namespace-DLL probing while adapting the module base directory to Gradle runtime asset staging; `SimpleMathComponent.SimpleMath` component smoke activation passes.
- [x] Generated WinRT methods now project as Kotlin lower-camel members (`start`, `activate`, `addHandler`, `onLaunched`) while mapped collection ABI members stay hidden behind Kotlin collection APIs.
- [x] No-arg `Unit` projection calls are back on generated inline vtable calls, matching the pre-IR-intrinsic path used by WinUI `Window.activate()` and avoiding the native WinUI activation crash introduced by the `invokeUnit` marker slice.
- [x] Composable factory base-inspectable/reference-tracker parity: JVM composable construction now aligns with `.cswinrt/src/cswinrt/code_writers.h`/`ComWrappersHelper.Init` by passing an `IInspectable` base interface for aggregation, registering native instance/inner RCW identity aliases, and initializing reference trackers for non-aggregation factory-created WinUI objects without extra tracker-source ownership.
- [x] Interface slot ordering now accounts for properties/events/methods by metadata row order before method-only wrappers, matching cswinrt ABI slot layout.
- [x] Event-source helper generation now keeps owner mappings separate from reusable source classes, so shared delegate helpers such as `RoutedEventHandler` remain available for `IButtonBase.Click`.
- [x] Generic delegate event-source generation now substitutes delegate type arguments into callback marshaling and emits reusable source classes only when projected callback arguments have generated `Metadata.wrap` support.
- [x] WinRT async await completion cleanup: `await()` now mirrors the cswinrt `AsyncInfoToTaskBridge` one-shot completion intent by guarding terminal completion/cancellation, translating `GetResults()` failures into coroutine exceptions, and closing the WinRT `Completed` delegate handle after callback, cancellation, synchronous post-registration completion, or result-read failure; this reduces KWINRT-012/013 callback lifetime risk without claiming clipboard retry policy is closed.
- [x] Task-to-async CCW interface parity: runtime-created `AsyncInfo` action/operation hosts now pass through `InteropRuntimeHooks.augmentInspectableDefinition`, so async objects exposed back to WinRT have the same standard CCW suffix and hidden tracker QI support as cswinrt managed async adapters.
- [x] Task-to-async completed handler setter parity: `WinRtTaskToAsyncInfoAdapter` now follows `.cswinrt/src/cswinrt/strings/additions/Windows.Foundation/TaskToAsyncInfoAdapter.cs` by rejecting a second completed-handler assignment with `E_ILLEGAL_DELEGATE_ASSIGNMENT` instead of silently replacing the native callback reference.
- [x] Task-to-async close-state parity: `WinRtTaskToAsyncInfoAdapter` now tracks closed state like cswinrt, rejects `Close` before terminal completion/cancel/error with `E_ILLEGAL_STATE_CHANGE`, clears callback references only after a legal close, and maps post-close `Status`/`ErrorCode`/handler/result access to `E_ILLEGAL_METHOD_CALL`.
- [x] Event-source cached delegate liveness: Kotlin `EventSource` now mirrors `.cswinrt/src/WinRT.Runtime/Interop/EventSource{TDelegate}.cs` by checking whether the cached event-invoke delegate still has native COM/reference-tracker references before reusing cached state; stale cached states are closed and re-registered instead of skipping native event registration after the previous delegate was released.
- [x] JVM raw callback upcall guard: `ComVtableInvoker` now catches carrier conversion and managed callback failures inside the FFM upcall bridge, sets WinRT error info, and returns `platformHResultFromThrowable(...)` just like cswinrt generated ABI stubs return `ExceptionHelpers.GetHRForException(...)` instead of letting exceptions escape across the native callback boundary; closed callback handles still return `E_POINTER`.
- [x] JVM raw callback close idempotence: `NativeCallbackHandle.close()` now removes the FFM upcall registry entry only once, matching the idempotent close contract used by `WinRtDelegateHandle` and making callback teardown robust when explicit cleanup and owner rollback/finalization race.
- [x] Reference-tracker target count parity: hidden `IReferenceTrackerTarget` CCWs now track `AddRefFromReferenceTracker` / `ReleaseFromReferenceTracker` counts instead of returning a fixed value, and event-source liveness treats any non-zero tracker count as a native reference like cswinrt `EventSourceState<TDelegate>.HasComReferences`.
- [x] Delegate ABI disposal parity: `MarshalDelegate.disposeAbi` now mirrors `.cswinrt/src/WinRT.Runtime/Marshalers.cs` `MarshalInterfaceHelper<T>.DisposeAbi` by releasing the ABI COM pointer directly instead of routing local delegate pointers through a Kotlin-only strong registry; temporary event/delegate references therefore do not leave stale runtime mappings after normal COM release.
- [x] Generated event-source object argument projection: JVM generated event sources now project `System.Object` callback arguments through `WinRtObjectMarshaller.fromAbi`, matching cswinrt `MarshalInspectable<object>.FromAbi`, and close temporary decoded `IInspectable`/`IUnknown` references after wrapping.
- [x] Delegate object argument/return projection: `WinRtDelegateAbiMarshaller` now uses the shared `WinRtObjectMarshaller` for delegate `System.Object` callback arguments, outbound invocation arguments, and callback return values, matching `.cswinrt/src/WinRT.Runtime/Marshalers.cs` `MarshalInspectable<object>` instead of keeping object-specific logic in the delegate ABI marshaller.
- [x] Generic inspectable-any marshaler object projection: `Marshaler.inspectableAny()` now reuses `WinRtObjectMarshaller` for create/from-ABI/from-managed operations, removing the remaining generic object marshaler CCW/RCW branch table and preserving cswinrt-style delegate-as-object `IReference<TDelegate>` support.
- [x] Bindable object outbound projection: `WinRtBindableObjectMarshaller` now routes create/from-managed object values through `WinRtObjectMarshaller`, keeping bindable-specific inbound value projection separate while ensuring `IBindableVector`/`IBindableIterable` element updates preserve the same object/delegate COM surface as the generic object marshaller.
- [x] Bindable collection host CCW parity: managed `IBindable*` collection hosts now pass through `InteropRuntimeHooks.augmentInspectableDefinition`, aligning WinUI collection CCWs with cswinrt's standard `IStringable`/weak-reference/marshal/agile/inspectable/unknown suffix and hidden tracker support.
- [x] Generic collection host CCW parity: managed generic collection and key/value-pair hosts now pass through `InteropRuntimeHooks.augmentInspectableDefinition`, so collection CCWs no longer diverge from cswinrt's standard suffix and hidden tracker QI behavior.
- [x] Generic parameter object projection: generic parameter marshaling now reuses `WinRtObjectMarshaller` for create/from-ABI/from-managed operations, so `T` values that flow as inspectable/object parameters preserve managed identity and delegate-as-object support instead of taking a generic-parameter-only CCW/RCW branch.
- [x] Property-value host CCW parity: `WinRtPropertyValueProjection` now creates pure `IPropertyValue` hosts through the same augmented CCW definition path as boxed `IReference<T>` and `IReferenceArray<T>`, so property-value-only marshaling keeps cswinrt's standard suffix interfaces and hidden reference-tracker QI support.
- [x] WinUI resource-manager registration rollback: generated `Microsoft.UI.Xaml.Application` wrappers no longer install a runtime-owned `ResourceManagerRequested` handler; `.cswinrt` WinUI samples load `XamlControlsResources` through `App.xaml` and generated `InitializeComponent`, so resource bootstrap must align with that XAML/MRT path instead of a projection-constructor side effect.
- [x] WindowsAppSDK self-contained staging parity correction: runtime asset staging no longer extracts the merged `resources.pri` from `Microsoft.WindowsAppSDK.Runtime` MSIX as a root-level shortcut; `.cswinrt`/MSBuild `Microsoft.WindowsAppSDK.SelfContained.targets` deletes that file after MSIX extraction and stages component PRI files through `ReferenceCopyLocalPaths`, so Kotlin staging keeps package `runtimes-framework/<rid>/native` PRI files as independent payloads.
- [x] Windows SDK tool discovery consolidation: Gradle tasks now share one Windows SDK layout resolver that honors `KOTLIN_WINRT_WINDOWS_SDK_ROOT`, the configured `winRt.windowsSdk(version)`, and the current runtime architecture. Application PRI generation resolves `makepri.exe` through that SDK layout instead of independently scanning Program Files or PATH.
- [x] KMP authoring type-details source-set ownership: generated authoring type-details and their registrar now go to a separate generated source directory and are wired back to KMP source sets whose Kotlin source roots were scanned, so a `winuiMain` `Application` subclass no longer produces common generated sources that cannot resolve the target-source-set class.
- [x] WinUI KMP sample generation inputs: the dedicated two-module WinUI KMP sample now defaults its own WindowsAppSDK version because its WinUI source sets are always compiled; this prevents no-property validation runs from producing `plans=0`, deleting WinUI projection sources, and then failing with unresolved `microsoft.*` imports. Generator stale cleanup also removes obsolete `kotlin-winrt-support` TSV files so old registrar/identity data cannot survive after a narrowed or empty projection run.
- [x] Generated interface callback wrappers: interface factory/wrap artifacts are no longer limited to interfaces whose every member can be fully projected. The JVM generated-interface runtime already carries `Unsupported` member descriptors, so generator-owned callback paths such as `TypedEventHandler<IContentSiteAutomation, ...>` can compile and wrap the sender while unsupported members still fail at the actual member call boundary.
- [x] Generated interface wrapper fallback: when an interface is needed as an ABI callback argument but cannot yet receive a full generated native-projection factory, its `Metadata.wrap` now creates a JVM generated-interface proxy with an empty descriptor set. This preserves the object identity/`IWinRTObject` surface and fails at unsupported member invocation instead of producing uncompilable callback glue.
- [x] Composable reference-tracker init parity: `ComWrappersSupport.initializeComposableReference(...)` now mirrors `.cswinrt/src/cswinrt/code_writers.h` calls to `ComWrappersHelper.Init(_inner, false)` by detecting `IReferenceTracker` on factory-created composable instances without issuing an extra `AddRefFromTrackerSource`; disposal therefore does not emit a matching `ReleaseFromTrackerSource` for this CLR-owned reference-tracker path.
- [x] Projection registrar classpath fallback: JVM startup still loads the compiler-generated fixed `WinRTProjectionRegistrar` first, but no longer treats it as terminal; it also enumerates `kotlin-winrt/type-index.tsv` resources so dependency module type indexes remain visible when only one fixed registrar class can be loaded by the classloader.
- [x] KWINRT-008 app project PRI/MRT indexing: application runtime staging now generates root `resources.pri` and language satellite PRI files from component PRI inputs using Windows SDK `makepri.exe`, matching MSBuild's application `GenerateProjectPriFile` ownership for dependency component PRI aggregation without reintroducing sample-local `ResourceManagerRequested` or App.xaml workarounds. This is not yet a full clone of MSBuild's PRI item pipeline (`Page`, `ApplicationDefinition`, explicit `PRIResource`, manifest default-language parsing, and all resfiles filtering remain future work); it is the minimal app-level MRT index needed by the current WinUI resource path. Validation covered `:winrt-samples:run` and `:winrt-samples:winui-kmp-app:runWinuiKmpSample` with WindowsAppSDK 1.8.260416003; both now pass through `XamlControlsResources()` installation.
- [x] WinUI desktop sample entry now mirrors `.cswinrt/src/Samples/WinUIDesktopSample` and real WinUI template code: initialize the runtime, call `Application.start { WinUiDesktopApp() }`, and let XAML own the dispatcher loop instead of running a sample-local Win32 message loop.
- [x] WinUI desktop sample Application class is public so the authoring scanner treats it as a valid cswinrt-style component type instead of a private projection-only helper.
- [x] Authoring activation factory runtime contract: Kotlin now has a `WinRtActivationFactory` CCW surface for cswinrt-style server factories that implement `IActivationFactory.ActivateInstance`.
- [x] WinUI unpackaged resource-manager hook rejected: local dual-module validation and a CsWinRT/.NET pure-code probe both showed that constructing `XamlControlsResources` without XAML-generated app resource initialization fails before a window is created; the runtime `ResourceManagerRequested` helper was deleted instead of being expanded into a Kotlin-only replacement for WinUI resource generation.
- [x] WinUI resource bootstrap ownership clarified: KWINRT-008 is now tracked as missing XAML/MRT resource initialization support, not as an `Application.current` registration problem, so future work must start from `.cswinrt` `App.xaml`/`InitializeComponent` and Windows App SDK PRI-generation responsibilities.
- [x] WinUI shutdown ordering stays event-source scoped: Windows App SDK bootstrap still drains kotlin-winrt generated event-source registrations before shutdown for KWINRT-013, but no longer owns a separate WinUI resource-manager registration table.
- [x] KWINRT-019 post-layout focus probe: generated `DispatcherQueue.tryEnqueue` and generated `UIElement.Focus(FocusState.Programmatic)` both work in the dual-module WinUI sample once the controls have reached a post-layout callback; earlier immediate post-activation `false` results are now recorded as a WinUI timing/layout precondition, not a kotlin-winrt projection helper gap.
- [x] KWINRT-020 compiler-support aggregation validation: targeted Gradle plugin coverage confirms `mergeWinRtCompilerSupport` combines local and dependency support rows while excluding Kotlin source-only mapping artifacts, `:winrt-samples:winui-kmp-app:compileKotlinWinuiJvm` passes with WindowsAppSDK `1.8.260416003`, and the app merged `interface-native-projections.tsv` contains dependency-owned `Windows.System.Display.IDisplayRequest`. The former `WinRTInterfaceProjectionRegistry.class` validation is obsolete because interface native projections now lower through the compiler plugin.
- [x] Event-source compiler-support aggregation validation: the compiler plugin no longer writes `kotlin-winrt/event-sources.tsv` to JVM class output, runtime event-source startup only invokes the compiler-generated fixed registry, and the existing merged compiler-support task keeps `event-source` rows in the downstream manifest so KMP library-owned WinUI event descriptors are compiled into the consuming application's registry artifact.

## Generator Gaps

- [x] Generic signature/IID parity: metadata writer descriptors now mirror `.cswinrt` `write_guid_signature` fragments with IID-based interface/delegate signatures, recursive runtime-class default-interface signatures, struct field tokens, and flags enum `u4` handling; existing generator/runtime paths cover `IReference<T>`, async, collections, and generic delegate parameterized IID creation.
- [x] Mapped type declaration parity: planner now enters through metadata `getMappedType(ns,name)` and skips mapped declarations such as `IReference<T>`/`IPropertyValue`; Kotlin keeps only support declarations needed for mapped collection/custom-member slot metadata, while KMP value structs such as `Point`/`Vector3` remain generated because they are not .NET aliases.
- [x] Generic event-source parity: confirmed against `.cswinrt/src/cswinrt/code_writers.h` that only `Windows.Foundation/System.EventHandler<T>` share `EventHandlerEventSource`; other generic delegates keep their own source subclasses.
- [x] Support-file defaults: direct `KotlinProjectionGenerator` callers that provide a real projection context must set `emitSupportFiles=true`, so CLI/plugin-style generation cannot silently omit required support files.
- [x] Direct WinRT namespace projection parity: generator package planning no longer prepends the Kotlin project package to WinRT API types, authored source classes are suppressed from ordinary projection output without suppressing dependency projections, and generated imports rely on KotlinPoet keyword escaping for namespaces such as `windows.data`.
- [x] Kotlin keyword property forwarding: runtime-class interface forwarders now mirror `.cswinrt/src/cswinrt/code_writers.h` identifier escaping intent by emitting KotlinPoet name references for lower-camel property accesses, so WinRT members such as `Package` can project as a backtick-escaped Kotlin `package` property without invalid generated member access.

## Authoring Plan

- [x] JVM authoring runtime foundation: composable CCW path, authoring activation registry, metadata type lookup, generated type-details lookup, compiler-plugin scanner split, and K2/IR extension entrypoint exist.
- [x] Generator authoring handoff plans exist for wrapper class, ABI class, custom QI, activation factory, module activation factory, exposed type details, and metadata type mapping.
- [x] Authoring source discovery now mirrors `.cswinrt/src/Authoring/WinRT.SourceGenerator/Generator.cs`: light-tree scanning only collects effective-public candidates, K2/IR diagnostics skip non-public WinRT-derived helpers, validate non-generic/non-inner/final class shape for authored candidates, exclude generated projection sources, and Gradle wires the shared metadata index into the compiler plugin.
- [x] Authoring WinMD runtime-class shell emission: authored descriptors are owned by `winrt-metadata`, merge into the projection model before source generation, emit deterministic descriptor output, write loadable TypeDef/base TypeRef/InterfaceImpl rows plus Default/Overridable/Activatable/Version attributes, and Gradle now writes the project-named `.winmd` artifact.
- [x] Authoring writer output: generated support now covers metadata mapping, wrapper/ABI facades, custom QI, server factories, module registration, generated host exports, CCW method tables, arrays, and generic-parameter marshaling, matching the `.cswinrt/src/Authoring/WinRT.SourceGenerator` writer responsibilities that are viable on JVM.
- [x] Compiler-plugin type-index foundation: K2/IR plugin writes `kotlin-winrt/type-index.tsv` for projection classes, Gradle routes the output into the Kotlin classpath root, and JVM runtime loads the index into `WinRtTypeRegistry`/`TypeNameSupport` as the first replacement for cswinrt assembly/type reflection.
- [x] Generated runtime-class RCW factory registration: projection `Metadata.register()` now installs `TYPE_NAME -> Metadata.wrap(IInspectableReference)` factories and the compiler-generated type-index loader invokes those generated registration hooks, replacing cswinrt `ProjectedRuntimeClassAttribute`/runtime-class wrapper reflection for generated projections.
- [x] Generated runtime-class default-interface registration: projection `Metadata.register()` now installs non-generic `RuntimeClass -> DefaultInterface` mappings, replacing cswinrt `ProjectedRuntimeClassAttribute.DefaultInterface` reflection where Kotlin `KClass` can represent the interface directly.
- [x] Generated generic default-interface signature registration: projection `Metadata.register()` now also installs `runtimeClassName -> defaultInterfaceName/defaultInterfaceSignature` mappings, so generic default interfaces such as `IFoo<T>` follow a metadata/signature path instead of impossible Kotlin class literals.
- [x] Compiler-generated projection type index now carries runtime-class base type names from the metadata index and registers them through `TypeNameSupport.registerProjectionTypeBaseTypeMapping`, matching cswinrt `RegisterProjectionTypeBaseTypeMapping` fallback for derived RCW resolution without runtime assembly reflection.
- [x] Generated runtime-class `Metadata.register()` now installs cswinrt-style custom ABI/helper mappings for the wrapper class itself, so `TypeExtensions.findHelperType` and runtime-class helper lookup no longer depend on reflection attributes for generated projections.
- [x] Generated runtime-class `Metadata` companions now self-register during class initialization, so the compiler-generated type-index loader no longer reflects over `Metadata.register()` and only needs to load indexed projection classes.
- [x] Library authoring runtime-assets packaging: Gradle library jars now include generated authored `.winmd` and `.host.json` under `kotlin-winrt-runtime-assets/`, matching the runtime manifest loader path used for cswinrt-style activation fallback discovery.
- [x] Application authoring target-artifact staging: library identity now records the real Gradle jar artifact and application runtime-assets staging copies dependency target jars beside authored `.host.json`, matching `.cswinrt/src/Authoring/WinRT.Host` target-assembly lookup semantics.
- [x] Authoring host runtime-config staging: application runtime-assets now writes `<assembly>.runtimeconfig.json` files with cswinrt-style `activatableClasses` target mappings derived from authored host manifests, so native host DLL export work has the same class-to-target lookup contract as `.cswinrt/src/Authoring/WinRT.Host`.
- [x] Server/host activation: JVM host bridge mirrors `.cswinrt/src/Authoring` `DllGetActivationFactory`/`DllCanUnloadNow` HRESULT semantics, generated support exposes stable host-shim address entrypoints for both exports, library identity/staging handles authored WinMD and cswinrt-style activatable-class target host manifests, staged/plugin-runtime-assets directory or jar manifests install as activation-factory fallbacks, host loading uses manifest target artifacts like cswinrt shim target-assembly loading, target artifact names follow the real Gradle jar archive, and `buildWinRtAuthoringHost` now generates/builds per-assembly native DLLs exporting `DllGetActivationFactory`/`DllCanUnloadNow` through the JVM host bridge.
- [x] Authored type-details fixed registrar wiring: Gradle emits a deterministic generated registrar for scanned `WinRT_*_TypeDetails` objects and the JVM runtime loader invokes that fixed registrar beside the projection registrar, matching cswinrt source-generator handoff without per-type probing or sample-side manual registration.
- [ ] `mingwX64` authoring frozen: keep shared contracts native-viable, but defer native CCW/host implementation until JVM authoring stabilizes.

## Compiler Plugin Reflection Replacement Plan

- [x] K2/IR compiler-plugin foundation exists as `winrt-compiler-plugin`, separate from the Gradle plugin and generator modules, so authoring/source analysis can move out of runtime reflection and into compilation.
- [x] Compiler-plugin type-index foundation: K2/IR plugin writes `kotlin-winrt/type-index.tsv` for projection classes, Gradle routes the output into the Kotlin classpath root, and JVM runtime loads the index into `WinRtTypeRegistry`/`TypeNameSupport` as the first replacement for cswinrt assembly/type reflection.
- [x] Generated runtime-class metadata self-registration: runtime-class `Metadata` companions self-register during class initialization, so the type-index loader no longer reflects over `Metadata.register()`.
- [x] Generated projection registrar: generator support output now emits `WinRTProjectionRegistrar.register()` with direct projection class literals, runtime-class `Metadata.register()` calls, and structured type-index handoff, while excluding authored/suppressed source types that are not generated projection classes; JVM runtime loads that fixed registrar first and keeps the TSV path only as compatibility fallback.
- [x] Generated interface registry retry: `ComWrappersSupport.wrapGeneratedInterfaceProjection(...)` now retries compiler-generated projection registry loading on a missing interface factory, matching cswinrt's lazy projection helper discovery more closely and preventing secondary interfaces from failing permanently after an early bootstrap with the wrong context classloader.
- [x] Generated authoring type-details registrar: JVM runtime now consumes explicitly registered authored type-details factories instead of probing `WinRT_<Type>_TypeDetails` by `Class.forName`; Gradle-generated type-details objects emit `register()` entrypoints that hand factories to `ComWrappersSupport`, matching cswinrt source-generator handoff without runtime class-name probing.
- [ ] K2 symbol model extraction: move effective-public authored type discovery, WinRT base/interface discovery, member signature capture, and diagnostics onto K2/IR symbols instead of light-tree/string parsing where semantic data is required.
- [ ] Generated runtime metadata tables: have the compiler/plugin or generator emit deterministic tables for runtime-class name, default-interface name/signature, base type, helper/ABI mapping, delegate/struct/enum/value-boxing metadata, and projection aliases so `TypeNameSupport` and `TypeExtensions` no longer need reflection-style fallbacks for generated projections.
- [x] Generated support plan/audit source removal: generator no longer emits non-runtime `WinRTAbiImplementationPlan.kt` or `WinRTTypeShapeWriterPlan.kt` into app/sample Kotlin sources; custom mapped/member metadata remains on the generated public/helper surfaces that consume it.
- [x] Projection registrar method-size reduction: `WinRTProjectionRegistrar.register()` keeps the fixed runtime entrypoint but delegates deterministic registration batches to private chunk methods, removing the JVM 64KB method-size blocker while preserving direct generated registration behavior.
- [x] Event support compile-surface reduction: `WinRTEventProjectionHelpers` now suppresses expected generated-code cast/check diagnostics and initializes event-source rows through deterministic chunk methods, removing the sample compile warning flood while preserving event-source registration behavior.
- [x] Generated event-source registry ABI call shape: compiler-plugin event registry artifacts now invoke `WinRtGeneratedEventSourceRuntime.createEventSourceFactory(...)` as a JVM static method, matching the runtime `@JvmStatic` ABI and avoiding `IncompatibleClassChangeError` when generated event properties install sources.
- [x] Compiler support manifest handoff: generator emits `kotlin-winrt-support/compiler-support.tsv` with deterministic registrar/event/generic support table ownership and entry counts, Gradle passes it to the compiler plugin, and the compiler plugin parses it as the input contract for later class artifact generation.
- [x] Compiler support class artifact foundation: compiler plugin writes `WinRTCompilerSupportManifest.class` into the Kotlin main class output from `compiler-support.tsv`, proving the generated class artifact path before moving behavior-bearing registrar/event/generic tables out of Kotlin source.
- [x] Support table artifact migration: registrar, event-source, and generic-instantiation support tables now move through compiler artifacts only in slices that also remove the migrated non-public Kotlin support table from normal source, so compiler artifacts do not duplicate data.
- [x] Projection registrar artifact migration: generator now emits `projection-registrar.tsv` as compiler input, no longer emits `WinRTProjectionRegistrar.kt`, and the compiler plugin writes the fixed `WinRTProjectionRegistrar.class` artifact that triggers generated `Metadata` self-registration plus type-index registration.
- [x] Event source table artifact migration rollback note: the earlier compiler-artifact registry path exposed the wrong ownership model for generated event properties. The active correction is to keep any artifact/resource registry only as compatibility surface while generated event properties call owner-scoped helper methods that construct typed EventSource instances directly.
- [x] Event source Kotlin helper source reduction rollback note: source-size reduction must not remove the cswinrt-aligned owner/event source helper shape. Generated Kotlin may still reduce table bulk, but typed `EventSource` subclasses and direct owner helper methods belong in the projection support source until an equivalent IR/source-generated owner-local replacement exists.
- [x] Generic instantiation table artifact migration: generator now emits `generic-instantiations.tsv` as compiler input, removes `ENTRIES`/source maps from `WinRTGenericTypeInstantiations.kt`, and the compiler plugin writes `WinRTGenericTypeInstantiationRegistry.class` while generated Kotlin keeps runtime binding functions.
- [x] Projection source diagnostic suppression: ordinary projection files and opt-in expect/actual source-set files now emit shared generated-code file suppressions, reducing Kotlin diagnostic work over complete projection source without adding runtime resources or filtering referenced types.
- [x] Compiler-plugin generated-source skip: IR authoring validation now derives the generated projection root from `metadata-index.tsv` and skips generated projection files before collecting authored class contexts, matching the Gradle scanner boundary and keeping the compiler plugin focused on authored source facts instead of re-walking WinUI projection classes.
- [x] Runtime loader boundary: runtime now exposes a structured compiler-generated projection type-index registration helper and keeps the JVM resource loader limited to reading generated index rows and resolving the already-named class token; mapping installation is centralized in common runtime, with no source/package scanning or sample inference.
- [x] Fixed registrar plus resource index loading: the JVM loader invokes compiler-generated fixed registrar artifacts and then continues through all `kotlin-winrt/type-index.tsv` resources, preserving dependency projection indexes across modules without package scanning or manually inferred WinMD/projection names.
- [x] Interface projection artifact foundation: runtime exposes the generated-interface proxy runtime, and the compiler plugin reads `interface-native-projections.tsv` to lower generated marker calls directly to runtime proxy creation for interfaces whose member bodies have already been migrated; the obsolete runtime registry/classloader writer has been removed from the active path.
- [x] Interface projection TSV/wrap generator foundation: with support files enabled, the generator emits deterministic `interface-native-projections.tsv` rows and routes eligible non-generic interface `Metadata.wrap(IUnknownReference)` through `ComWrappersSupport.wrapGeneratedInterfaceProjectionFromCompilerPlugin(TYPE_HANDLE, instance, projectedTypeName, Interface::class)` instead of emitting `private class NativeProjection`; direct generator use without compiler support keeps the Kotlin `NativeProjection` fallback.
- [x] KMP JVM compiler-support output ownership: each `KotlinJvmCompile` task now writes compiler-generated projection registries and type indexes into that task's own class output, matching cswinrt's projection-assembly registration boundary and fixing KMP library default-interface wrappers such as `Windows.System.Display.IDisplayRequest` at runtime.
- [x] Validation path: after the registrar slice, targeted compiler-plugin, generator registration, and runtime registry tests run through Windows Gradle on the required JDK 22 JVM target; narrow `winrt-samples:compileKotlin` exposed and drove the authored-type registrar fix, but the representative validation surface is now the WinUI-enabled `winrt-samples` configuration because it exercises the Windows App SDK projection closure.

## Expect/Actual Projection ABI Migration Plan

- [x] Generator layout foundation: added an opt-in generation layout that writes common `expect` projection declarations and JVM `actual` projection implementations without changing the default single-source-set output.
- [x] Expect/actual support layout: support files generated with the opt-in `ExpectActualJvm` layout are written under `commonMain/kotlin` so projection support registries stay in the shared source set instead of leaking into an unowned third source root.
- [x] Narrow first slice: migrated non-generic interface/runtime-class method and property projection shape first; current implementation emits ordinary common interfaces plus JVM-side projection support objects instead of `expect`/`actual` interfaces, and emits simple non-generic runtime-class `expect`/JVM `actual` wrappers with matching RCW constructors that forward through the full public interface inheritance closure; interface/runtime-class forwarding covers inherited interface member forwarding with duplicate redeclaration suppression guarded by conflict checks, runtime-class member rows covered by inherited non-conflicting public interfaces with normalized raw interface names, matching member return/property types, method parameter names/types, no-exception HRESULT behavior, and property getter/setter shape; eligibility reuses the computed interface proxy closure for each decision point and permits member public ABI kinds inside the scalar/string/GUID/object/projected-interface/runtime-class boundary; unsupported ABI call plans, non-FFM ABI shapes, static-member types, setter-only-property projection types, async surfaces, and collection/custom mapped surfaces stay on the common fallback path instead of failing or silently dropping members during actual generation, while delegates, collections, async, structs, enums, activation, and generic closures remain queued for later target-specific call coverage.
- [x] JVM ABI call ownership: JVM actual non-generic interface member bodies now route supported pointer/scalar ABI shapes through generated Java FFM private `JvmAbi.invoke_*` helpers with per-function-address downcall caches, including scalar combinations that were never practical as fixed `ComVtableInvoker.invokeArgs` overloads; projected-interface parameters pass their `IWinRTObject` pointer and projected-interface returns wrap through the target interface JVM projection support object; simple runtime-class actual wrappers delegate member calls to those interface projections, and generated JVM actual output no longer contains a hidden `ComVtableInvoker` fallback for selected migrated members.
- [x] Metadata table ownership: runtime-class factory, default-interface name/signature/class, helper/ABI, base-type, and type-index registration for generated projections now live in `WinRTProjectionRegistrar` support output; compiler plugin remains limited to authored source facts and registrar handoff, while delegate/enum/struct/boxing metadata stay on existing runtime/generator ownership until their dedicated slices move.
- [x] Runtime fallback shrink: removed generated-projection scalar/mixed-shape `ComVtableInvoker.invokeArgs` overloads now covered by JVM actual FFM calls; retained runtime primitive shapes for `IUnknown`/`IInspectable`, events, collections, async, callbacks, value boxing, and temporary migration fallback.
- [x] Event expect/actual slice: non-static interface/runtime-class events with concrete add/remove slots stay on the expect/actual JVM path, using generated event-source helpers for subscription while static events/factories/activation remain on fallback.
- [x] Authoring scanner process memory: the forked `KotlinWinRtAuthoringScannerCli` helper now uses a small fixed JVM footprint because it only scans authored source roots and metadata-index rows; this removes the JDK 22 default ergonomics path that reserved roughly 12GB heap before projection generation reached the real WinUI surface.
- [ ] WinUI projection compile-surface reduction 正在做: generated public APIs remain complete and WindowsAppSDK reference closure is not filtered. Current direction is generator-emitted descriptor marker calls plus compiler-plugin IR lowering from shared ABI plans, not Java sources, reflection, generated JVM class artifacts, per-parameter-combination intrinsics, or a new runtime `ComAbiInvoke` layer. Runtime ownership stays limited to stable primitives such as HSTRING ownership, memory slots, HRESULT translation, COM vtable primitives, object identity, and wrappers. No-arg `Unit` descriptor routing is validated by WinUI compile and controls smoke; next compile-surface work should continue shrinking remaining inline/generated fallback bodies without changing the representative WinUI validation surface.
- [x] Inherited interface native projection artifacts: generator now emits compiler-plugin JVM projection descriptors for eligible non-generic interface inheritance closures, using base-before-child ABI slot ordering and rejecting only JVM `name + arity` descriptor collisions so unsupported/conflicting closures keep the Kotlin fallback.
- [x] Runtime collection projection helper returns: generator now routes collection returns/delegate initializers through `WinRtIterableProjection`, `WinRtListProjection`, `WinRtReadOnlyListProjection`, `WinRtDictionaryProjection`, and `WinRtReadOnlyDictionaryProjection` when key/value/element adapters are available, preserving Kotlin fallbacks for unsupported primitive-map shapes.
- [x] Delegate mapped collection/array parameter parity: generator now treats delegate `IIterable<T>`/`IVector<T>`/`IVectorView<T>`/`IMap<K,V>`/`IMapView<K,V>` parameters as normal WinRT interface-pointer ABI values and connects them to the existing runtime collection projection/marshaler helpers; runtime delegate ABI descriptors also expand `Array<UByte>` parameters into cswinrt-style `length + data` ABI arguments for PDU-style callbacks instead of leaving those delegates unsupported, with focused generator/runtime tests and WinUI KMP library projection generation passing.
- [x] Delegate generic-parameter ABI parity: bare delegate generic parameters such as `GenericHandler<T>.Invoke(T): T` now stay on the cswinrt-style object/generic-parameter ABI path instead of being rejected or reclassified as the instantiated concrete ABI kind; generated callback surfaces still cast to the instantiated Kotlin type such as `String`.
- [x] Runtime reference property helper calls: generator routes simple runtime-class nullable `IReference<T>` property getters/setters through `WinRtReferenceProjectionInterop`, keeping `.cswinrt` nullable/reference ownership in `winrt-runtime` and removing repeated `PlatformAbi`/`ComVtableInvoker`/`WinRtReferenceProjection` blocks from generated Kotlin for supported shapes.
- [x] Runtime async operation helper calls: generator routes async action-with-progress and async operation result-readback construction through `WinRtAsyncProjectionInterop`, keeping `GetResults` scope/allocation/HRESULT mechanics in `winrt-runtime` and leaving generated projection code to provide only signatures plus typed result readback lambdas.
- [x] Mapped closable interface artifacts: generator now treats mapped `IClosable`/`AutoCloseable` inheritance as an artifact-supported `close` ABI method descriptor at slot 6, so direct closable interfaces whose remaining members are fully artifact-supported, such as deferral/reference surfaces, can use compiler-generated JVM RCWs instead of Kotlin `private class NativeProjection` fallback bodies; closable interfaces with unsupported members keep the Kotlin fallback.
- [x] Runtime static scalar helper calls: generator routes supported static `Int32`/`UInt32`/`Int64`/`UInt64`/`Float`/`Double` getter and direct method ABI bodies through `WinRtStaticProjectionInterop`, keeping scalar out-slot allocation/HRESULT/readback in `winrt-runtime` instead of repeating `PlatformAbi`/`ComVtableInvoker` blocks in generated Kotlin.
- [x] Runtime static setter helper calls: generator routes direct static property setters with non-scoped ABI arguments through the existing `WinRtStaticProjectionInterop.callUnit` helper, matching `.cswinrt` typed property setter call-shape ownership without adding a new generic invoker layer.
- [x] Runtime instance scalar getter helper calls: generator routes supported runtime-class and fallback interface `Boolean`/`Int32`/`UInt32`/`Int64`/`UInt64`/`Float`/`Double` property getter ABI bodies through `WinRtInstanceProjectionInterop`, keeping simple scalar out-slot allocation/HRESULT/readback in `winrt-runtime` and preserving enum/struct/object/array fallbacks for later slices.
- [x] Runtime instance projected object getter helper calls: generator routes supported runtime-class and fallback interface projected runtime-class/interface property getter ABI bodies through `WinRtInstanceProjectionInterop`, keeping pointer out-slot allocation/HRESULT/null handling in `winrt-runtime` while generated Kotlin supplies the typed `Metadata.wrap` function.
- [x] Projection intrinsic lowering foundation: generator emits `WinRtProjectionIntrinsic` marker calls for eligible instance scalar getters when compiler support files are enabled, and the compiler plugin lowers those marker calls in IR to the existing narrow JVM scalar getter runtime primitives. The marker is a compile-time intrinsic fallback that throws if not lowered; it is not a runtime ABI invoker and does not introduce Java source/class artifacts.
- [x] Single-argument primitive intrinsic lowering: generator emits `WinRtProjectionIntrinsic` marker calls for eligible instance and fallback-interface no-arg primitive/String calls plus single-argument primitive/String Unit calls, and the compiler plugin lowers those calls to narrow runtime primitive helpers. This keeps setter-style ABI allocation/marshaling out of generated Kotlin without adding vararg dispatch or a replacement `ComAbiInvoke` abstraction.
- [x] Float/String Unit intrinsic lowering: real WinUI composition-style `Float, String -> Unit` interface/runtime-class methods route through a narrow `WinRtProjectionIntrinsic.callUnitWithFloatAndString` marker so generated Kotlin source does not inline the ABI body.
- [x] Float/String Unit runtime-helper removal: the `Float, String -> Unit` marker now lowers directly in the compiler plugin through the shared two-argument HSTRING IR path; the old `WinRtInstanceProjectionInterop.callUnitWithFloatAndString` runtime helper is removed, leaving only the marker fallback that throws if lowering is unavailable.
- [x] Float/String/projected-object Unit intrinsic lowering: real WinUI composition-style `Float, String, projected object -> Unit` methods route through a narrow `WinRtProjectionIntrinsic.callUnitWithFloatStringAndProjectedObject` marker so generated Kotlin source does not inline the ABI body.
- [x] Float/String/projected-object Unit runtime-helper removal: the `Float, String, projected object -> Unit` marker now lowers directly in the compiler plugin through the shared HSTRING/projected-object argument IR path; the old `WinRtInstanceProjectionInterop.callUnitWithFloatStringAndProjectedObject` runtime helper is removed, leaving only the marker fallback that throws if lowering is unavailable.
- [x] String/Float Unit intrinsic lowering: real WinUI composition-style `String, Float -> Unit` methods route through a narrow `WinRtProjectionIntrinsic.callUnitWithStringAndFloat` marker so generated Kotlin source does not inline the ABI body.
- [x] String/Float Unit runtime-helper removal: the `String, Float -> Unit` marker now lowers directly in the compiler plugin to the member-specific IR sequence for HSTRING reference ownership, vtable invocation, and HRESULT checking; the old `WinRtInstanceProjectionInterop.callUnitWithStringAndFloat` runtime helper is removed, leaving only the marker fallback that throws if lowering is unavailable.
- [x] String/projected-object Unit intrinsic lowering: real WinUI composition-style `String, projected object -> Unit` methods route through a narrow `WinRtProjectionIntrinsic.callUnitWithStringAndProjectedObject` marker so generated Kotlin source does not inline the ABI body.
- [x] String/projected-object Unit runtime-helper removal: the `String, projected object -> Unit` marker now lowers directly in the compiler plugin through the shared HSTRING IR path plus projected-object pointer extraction; the old `WinRtInstanceProjectionInterop.callUnitWithStringAndProjectedObject` runtime helper is removed, leaving only the marker fallback that throws if lowering is unavailable.
- [x] Descriptor-driven Unit intrinsic marker: the generator now emits one `WinRtProjectionIntrinsic.callUnit(reference, slot, "Float,String,Object", vararg args)` marker for migrated multi-argument `Unit` ABI bodies, and the compiler plugin parses the generated descriptor before lowering HSTRING/projected-object argument IR. The old `callUnitWith...` parameter-combination marker names are removed so adding a new argument order does not require a new intrinsic function.
- [x] Single-argument Unit runtime-helper removal: `setString` and primitive one-argument `Unit` projection markers now lower directly in the compiler plugin to typed `ComVtableInvoker.invokeArgs` overloads plus HRESULT checking; the matching `WinRtInstanceProjectionInterop.setString/setBoolean/setInt32/setUInt32/setInt64/setUInt64/setFloat/setDouble` helpers are removed, and setter markers no longer route through `invokeGenericArgs`/runtime argument-shape inference.
- [x] Descriptor Unit typed vtable lowering: known multi-argument `callUnit` descriptors used by WinUI composition surfaces (`String,Object`, `String,Float`, `Float,String`, `Float,String,Object`) now lower in the compiler plugin to typed `ComVtableInvoker.invokeArgs` overloads with generated HSTRING/projected-object ABI operands. The descriptor path no longer has a plugin-side `invokeGenericArgs` fallback.
- [x] Boolean Unit descriptor ABI lowering: the same descriptor intrinsic path now covers WinUI boolean Unit shapes (`Float,Boolean` keyframe insertion and `String,Boolean` property-set insertion), with Boolean marshaled to ABI `Byte` in IR and no generated Kotlin `invokeGenericArgs` body for those members.
- [x] Descriptor Unit token-driven lowering lookup: compiler-plugin `callUnit` lowering now resolves the target ABI primitive from the descriptor token stream (`Float`, `Boolean`, `String`, `Object`) instead of enumerating complete argument combinations. This is an intermediate cleanup on the active IR migration path; the next slice replaces the resolved JVM call target with direct Java FFM IR emission so projection calls do not depend on expanding `ComVtableInvoker.invokeArgs` overloads.
- [x] Descriptor Unit JVM FFM direct lowering: compiler-plugin `callUnit` lowering now emits JVM Java FFM IR directly for descriptor-token ABI calls, including token-driven `FunctionDescriptor` construction, vtable entry lookup, per-token carrier conversion, HRESULT checking, and HSTRING lifetime cleanup. This path no longer calls `ComVtableInvoker.invokeArgs`/`invokeGenericArgs` for WinUI descriptor Unit shapes; the next performance slice is IR-owned downcall-handle caching by ABI shape so direct FFM does not allocate a handle on every call.
- [x] Descriptor Unit runtime fallback shrink: compiler-plugin descriptor `callUnit` no longer falls back to typed `ComVtableInvoker.invokeArgs`, and runtime/mingw stubs removed the mixed scalar overloads that existed only for migrated WinUI descriptor Unit shapes (`RawAddress,Float`, `RawAddress,Byte`, `Float,RawAddress`, `Float,Byte`, `Float,RawAddress,RawAddress`). Runtime keeps primitive pointer/scalar shapes still used by IUnknown/IInspectable, events, collections, async, callbacks, and one-argument projection intrinsics.
- [x] Descriptor Unit JVM FFM handle caching: compiler-plugin per-file static `MethodHandle` field injection remains blocked because K2 backend lowering hit a `FunctionReferenceLowering` ICE on generated WinUI projection sources; the resolved design uses a narrow `winrt-runtime` ABI primitive that caches unbound descriptor-shaped FFM downcall handles by ABI shape, while compiler-plugin IR still emits direct vtable lookup, argument preparation, `MethodHandle.invoke`, HRESULT checking, and result readback.
- [x] Single-argument Unit JVM FFM direct lowering: existing `setString`/`setBoolean`/`setInt32`/`setUInt32`/`setInt64`/`setUInt64`/`setFloat`/`setDouble` markers now lower through the same token-driven JVM FFM call builder used by descriptor `callUnit`, so setter-style generated projection members no longer call `WinRtProjectionIntrinsic` markers or `ComVtableInvoker.invokeArgs`; WinUI sample bytecode checks covered `IWindow.setTitle` and `ICompositionPropertySet.insertScalar`.
- [x] No-argument getter JVM FFM direct lowering: `getString`/`getBoolean`/`getInt32`/`getUInt32`/`getInt64`/`getUInt64`/`getFloat`/`getDouble` markers now lower from the compiler plugin to JVM FFM IR instead of `WinRtInstanceProjectionInterop` fallback helpers. The lowering uses one return-kind model plus the existing token-driven vtable/downcall builder, with scoped out-slot allocation/readback kept at the ABI primitive boundary; WinUI bytecode checks covered `IWindow.getTitle`, `IContentIsland.getId`, `IContentIsland.isConnected`, and `IContentIsland.getRasterizationScale`.
- [x] Struct-result JVM FFM direct lowering: `getStruct` markers now lower from `WinRtInstanceProjectionInterop.getStruct` to compiler-plugin JVM FFM IR. The adapter remains the owner of struct layout/read/dispose semantics while the plugin owns scoped out-buffer vtable downcall emission, avoiding `invokeGenericArgs` without duplicating struct field marshaling in IR; WinUI bytecode checks covered `IWindow.getBounds` and `IContentIsland.getActualSize`.
- [x] Struct-parameter JVM FFM direct lowering: `setStruct` markers now lower from `WinRtInstanceProjectionInterop.setStruct` to compiler-plugin JVM FFM IR. The adapter remains the owner of struct layout/write/dispose semantics while the plugin owns scoped ABI buffer allocation, direct vtable downcall emission, HRESULT handling, and ABI cleanup; WinUI bytecode checks covered `ICompositionEllipseGeometry.setCenter` and `ICompositionVisualSurface.setSourceOffset`.
- [x] Array-result JVM FFM direct lowering: instance `getArray` markers now lower from `WinRtInstanceProjectionInterop.getArray` to compiler-plugin JVM FFM IR. `Marshaler` remains the owner of element array decode/dispose semantics while the plugin owns length/data out-slot allocation, direct vtable downcall emission, null-to-empty result normalization, and ABI cleanup; WinUI bytecode checks covered `ResourceCandidate.getValueAsBytes`.
- [x] Projected-object getter JVM FFM direct lowering: `getProjectedRuntimeClass`/`getProjectedInterface` helper calls from `WinRtInstanceProjectionInterop` now lower to compiler-plugin JVM FFM IR. Generated metadata `wrap` functions remain the owner of RCW construction while the plugin owns pointer out-slot allocation, direct vtable downcall emission, null handling, and `IUnknownReference`/`IInspectableReference` argument preparation; WinUI bytecode checks covered `IWindow.getCompositor` and `IContentIsland.getCustomProperties`.
- [x] No-argument Unit JVM FFM direct lowering: generated `ComVtableInvoker.invoke(instance, slot)` calls now lower in the compiler plugin to direct JVM Java FFM IR without reintroducing `WinRtProjectionIntrinsic.invokeUnit`. WinUI validation covered `:winrt-samples:compileKotlin`, `IWindow.NativeProjection.activate/close` bytecode with direct `FunctionDescriptor`/`downcallHandle`/`MethodHandle.invoke`, and WinUI controls smoke through native window activation.
- [x] Typed `invokeArgs` JVM FFM direct lowering: generated typed `ComVtableInvoker.invokeArgs(instance, slot, ...)` overload calls now lower by deriving the ABI shape from the resolved overload parameter types and feeding that shape into the existing JVM FFM builder. WinUI validation covered `:winrt-samples:compileKotlin`, `IWindow.NativeProjection.content/setTitleBar` and `IDispatcherQueueTimer.NativeProjection.interval/repeating` bytecode with direct FFM descriptors, and WinUI controls smoke through native window activation.
- [x] Vararg `invokeGenericArgs` JVM FFM direct lowering: generated `ComVtableInvoker.invokeGenericArgs(instance, slot, vararg args)` calls now lower by deriving ABI shape from each vararg element's static IR type and feeding that shape into the existing JVM FFM builder. WinUI validation covered `:winrt-samples:compileKotlin`, `DependencyObject.unregisterPropertyChangedCallback` bytecode with direct `RawAddress,Long` FFM descriptor, zero `ComVtableInvoker` references in generated sample classes, and WinUI controls smoke through native window activation.
- [x] Dead `invokeUnit` projection helper removal: removed the unused `WinRtInstanceProjectionInterop.invokeUnit` runtime helper, the matching `WinRtProjectionIntrinsic.invokeUnit` marker, and compiler-plugin helper mapping now that generated no-argument Unit calls lower directly from the resolved `ComVtableInvoker.invoke(instance, slot)` shape. Validation covered runtime/plugin compile, WinUI-enabled `:winrt-samples:compileKotlin`, WinUI controls smoke, and Application Error/WER crash check.
- [x] Projected-object getter helper removal: generated projected runtime-class/interface getter bodies now route through `WinRtProjectionIntrinsic.getProjected*` marker calls, compiler-plugin JVM FFM lowering is the sole generated projection path, and the matching `WinRtInstanceProjectionInterop.getProjected*` runtime helper/fallback mapping is removed. Validation covered narrow generator tests, runtime/plugin compile, WinUI compile, generated source/bytecode checks, WinUI controls smoke, and crash check.
- [x] No-argument scalar/string getter helper removal: removed the `WinRtInstanceProjectionInterop.getString/getBoolean/getInt32/getUInt32/getInt64/getUInt64/getFloat/getDouble` runtime fallback helpers and compiler-plugin helper mappings now that generated getter markers lower directly to JVM FFM. Validation covered narrow generator tests, runtime/plugin compile, WinUI compile, generated source/bytecode checks, WinUI controls smoke, and crash check.
- [x] Struct/array instance helper removal: removed the remaining `WinRtInstanceProjectionInterop.getStruct/getArray/setStruct` runtime fallback helpers and compiler-plugin helper mappings now that generated struct/array markers lower directly to JVM FFM. Validation covered narrow generator tests, runtime/plugin compile, WinUI compile, generated source/bytecode checks, WinUI controls smoke, and crash check.
- [x] Static array/String-object intrinsic helper removal: static array return markers and static `String -> projected object` markers now lower directly in the compiler plugin to JVM FFM, preserving marshaler-owned array decode and generated `Metadata.wrap` ownership while removing the matching `WinRtStaticProjectionInterop.staticGetArray/staticGetArrayWithProjectedObject/staticCallProjected*WithString` fallback helpers, plugin helper mappings, and dead intrinsic-to-helper rewrite path.
- [x] Static property intrinsic routing: generated static property getters and setters in support-file projection mode now route eligible scalar/String/projected-object shapes through existing `WinRtProjectionIntrinsic.get*/set*/getProjected*` markers, reusing compiler-plugin JVM FFM lowering instead of emitting `WinRtStaticProjectionInterop.call*/getProjected*` calls for those property bodies. Legacy non-intrinsic generator output remains on the existing static helper fallback until static method bodies migrate.
- [x] Static Unit method descriptor intrinsic routing: generated static `Unit` methods in support-file projection mode now route eligible primitive/String/projected-object parameter shapes through descriptor `WinRtProjectionIntrinsic.callUnit`, reusing compiler-plugin token-driven JVM FFM lowering instead of emitting `WinRtStaticProjectionInterop.callUnit` for those method bodies. Static methods with non-Unit returns remain on existing helper fallback until return-value static lowering is migrated.
- [x] Static Boolean method descriptor intrinsic routing: generated static `Boolean` methods in support-file projection mode now route eligible primitive/String/projected-object parameter shapes through descriptor `WinRtProjectionIntrinsic.callBoolean`, and the compiler plugin lowers that marker directly to JVM FFM with an ABI Boolean out slot instead of emitting `WinRtStaticProjectionInterop.callBoolean` for migrated method bodies.
- [x] Static Boolean helper closure: static no-argument `Boolean` methods now reuse the existing `getBoolean` marker path, and descriptor static calls accept Int32/UInt32 enum parameters by emitting the enum ABI value into the shared shape-driven `callBoolean` lowering. This removes the remaining WinUI-generated `WinRtStaticProjectionInterop.callBoolean` helper calls without adding parameter-combination intrinsics or a replacement runtime invoker.
- [x] Static projected-object descriptor intrinsic routing: generated static methods returning projected runtime classes/interfaces now route eligible non-String descriptor argument shapes through `WinRtProjectionIntrinsic.callProjected*`, and the compiler plugin lowers those markers directly to JVM FFM with a pointer out slot and generated `Metadata.wrap` ownership. Existing String-only projected-object static calls remain on their dedicated lowered marker, and Uri/custom-marshaled shapes stay on fallback.
- [x] Static scalar descriptor intrinsic routing: generated static methods returning Int32/UInt32/Int64/UInt64/Float/Double route eligible descriptor argument shapes through token-driven `WinRtProjectionIntrinsic.call*` markers, and the compiler plugin lowers those markers directly to JVM FFM with typed out slots instead of using `WinRtStaticProjectionInterop.call*` runtime helpers.
- [x] Static scalar marker surface contraction: replaced the per-return static scalar marker functions with one descriptor `WinRtProjectionIntrinsic.callScalar(returnShape, abiShape, ...)` marker so the compiler plugin composes JVM FFM lowering from shape tokens instead of growing the marker surface by return type.
- [x] Static scalar runtime fallback removal: removed `WinRtStaticProjectionInterop.callInt32/UInt32/Int64/UInt64/Float/Double` and stopped generating those helpers from static direct fallback paths; legacy unsupported static scalar members fall back to existing inline ABI generation instead of a runtime `invokeGenericArgs` scalar helper.
- [x] Static runtime fallback surface removal: removed the remaining `WinRtStaticProjectionInterop` Unit/Boolean/projected-object helpers and stopped routing generator legacy static direct paths through a runtime `invokeGenericArgs` helper; static fallback now uses existing inline ABI generation while migrated support-file paths continue through descriptor markers lowered by the compiler plugin.
- [x] Static runtime-class forwarding surface: static WinRT runtime-class shells now merge members from their static interfaces into the public `Metadata` companion, matching `.cswinrt/src/cswinrt/code_writers.h` static class projection shape so consumers can call surfaces such as `Clipboard.getContent()`, `Clipboard.setContent(...)`, `Clipboard.clear()`, and `StandardDataFormats.text` without reaching into `StaticInterfaces`.
- [x] Public generated rewrap entrypoint: generated runtime-class and interface `Metadata.wrap(...)` helpers are public instead of Kotlin-module-internal, providing a non-mangled projection rewrap path for collection/event/static-property returned `IInspectable`/`IUnknown` values while the deeper duplicate-FQN classpath isolation problem remains tracked separately.
- [x] Dependency projection identity boundary: application projection generation now consumes dependency `kotlin-winrt` identity files and suppresses only projected type names actually emitted by dependency libraries through the identity `projectedTypes` table, matching cswinrt's registered projection-assembly ownership model in Kotlin/Gradle form while avoiding namespace-claim over-suppression; this closes the duplicate generated FQN path behind KWINRT-011/014 without hiding app-owned SDK projections such as `Windows.Data.Json`.
- [x] Instance scalar descriptor marker routing: eligible instance methods returning Int32/UInt32/Int64/UInt64/Float/Double with descriptor-supported arguments now route through the shared `WinRtProjectionIntrinsic.callScalar(returnShape, abiShape, ...)` marker so generated support-file projections do not emit `invokeGenericArgs` for scalar-return method bodies such as `Double,Double -> Double`; current WinUI closure had no new support-file hits for this shape, so the generated WinUI `invokeGenericArgs` source count remains tracked separately.
- [x] Descriptor Unit numeric token routing: extended existing descriptor `callUnit(reference, slot, abiShape, vararg args)` marker emission to Double/Int32/UInt32/Int64/UInt64/enum arguments through the shared descriptor shape helper, reducing WinUI-generated `invokeGenericArgs` source for numeric Unit callbacks such as `IRangeBaseOverrides.onValueChanged(Double, Double)` without adding parameter-combination intrinsics.
- [x] Instance projected-object descriptor marker routing: eligible instance methods returning projected runtime classes/interfaces with descriptor-supported non-String arguments now route through `WinRtProjectionIntrinsic.callProjected*`, reducing WinUI-generated `invokeGenericArgs` source without adding parameter-combination intrinsics or runtime helpers.
- [x] Instance Boolean descriptor marker routing: eligible instance methods returning Boolean with descriptor-supported arguments now route through `WinRtProjectionIntrinsic.callBoolean`, reusing the existing token-driven JVM FFM lowering instead of generated `invokeGenericArgs` source.
- [x] Instance enum descriptor marker routing: eligible instance methods returning Int32/UInt32-backed enums with descriptor-supported arguments now route through `WinRtProjectionIntrinsic.callScalar` plus generated enum `Metadata.fromAbi`, reusing token-driven JVM FFM lowering instead of generated `invokeGenericArgs` source.
- [x] No-argument Unit descriptor marker validation: WinUI sample generation with WindowsAppSDK `1.8.260416003` produced `1752` generated Kotlin files and `76896` lines, with `392` empty descriptor `callUnit` markers, `0` generated `invokeGenericArgs` references, and `0` compiled generated-class string hits for `WinRtProjectionIntrinsic` or `ComVtableInvoker`; `:winrt-samples:compileKotlin` and WinUI controls smoke both passed, including `window activated` and no matching Application Error/Windows Error Reporting crash events.
- [x] Closable fallback source marker validation: mapped `IClosable.close()` fallback bodies now use the existing no-argument descriptor marker, reducing generated WinUI `ComVtableInvoker.invoke(instance = ...)` source references from `10` to `0` while keeping `invokeGenericArgs` source references at `0`; WinUI compile and controls smoke passed.
- [x] Mapped mutable-vector Unit descriptor routing: eligible generated collection `Unit` calls such as `IVector<T>.SetAt/InsertAt/Append/RemoveAt/Clear` now route through `WinRtProjectionIntrinsic.callUnit`, reusing existing descriptor tokens instead of collection-generated `invokeGenericArgs` source.
- [x] Descriptor Unit struct-argument routing: eligible generated `Unit` calls with `NativeStructAdapter`-backed struct parameters now route through the shared descriptor `callUnit` marker using a `Struct` token that the compiler plugin lowers to struct ABI buffer write/dispose plus direct JVM FFM.
- [x] Projected-object struct-argument descriptor routing: eligible generated projected runtime-class/interface return calls with `NativeStructAdapter`-backed struct parameters now route through `callProjected*` descriptor markers, using the same `Struct` token lowering instead of inline `invokeGenericArgs` ABI bodies.
- [x] Descriptor Unit multi-object routing: eligible generated `Unit` calls with multiple projected-object parameters now route through the existing descriptor `callUnit` marker by composing repeated `Object` tokens in compiler-plugin IR instead of keeping inline `invokeGenericArgs` ABI bodies.
- [x] Async descriptor pointer-return routing: eligible generated async operation/action members with descriptor-supported input parameters now route through the existing pointer-return `WinRtProjectionIntrinsic.callProjectedInterface` marker, keeping async result wrapping in `WinRtAsyncProjectionInterop` and avoiding new runtime invokers or parameter-combination intrinsics.
- [x] Inline Unit raw-ABI descriptor routing: eligible generated `Unit` inline ABI calls whose marshalers already produce RawAddress/scalar ABI arguments now route through the existing descriptor `WinRtProjectionIntrinsic.callUnit` marker, extending token parsing to raw ABI operands without adding runtime invokers or parameter-combination intrinsics.
- [x] Projected-object descriptor marker routing: extended the existing `callProjectedRuntimeClass`/`callProjectedInterface` marker lowering to prepare/dispose HSTRING ABI arguments from descriptor tokens and let activation-factory `IInspectableReference` creates use the existing projected-interface marker with `use { asInspectable() }` ownership, so generated projected-object returns stop falling back to inline `invokeGenericArgs` source without adding per-combination intrinsics or runtime helpers.
- [x] Struct-result descriptor marker routing: struct-return projection markers now cover descriptor-shaped scalar/String/Object/Struct input tokens, keeping `NativeStructAdapter` ownership for read/write/dispose while compiler-plugin IR composes the JVM FFM call directly.
- [x] Inline projected-object raw-ABI descriptor routing: inline ABI calls that already marshal parameters to scalar/pointer ABI operands and return projected runtime classes/interfaces now route through the existing `callProjected*` descriptor markers, preserving parameter marshaler scopes/finalizers without adding runtime helpers or per-argument-combination intrinsics.
- [x] Object-return raw-ABI descriptor marker routing: inline ABI calls returning `System.Object`/`Any?` now route through a single `callObject` descriptor marker lowered directly to JVM FFM plus `WinRtObjectMarshaller.fromAbi`, preserving existing parameter marshaler scopes without adding runtime invokers or parameter-combination intrinsics.
- [x] Inspectable factory raw-ABI descriptor routing: activation/composable factory methods that return `IInspectableReference` now route through the existing descriptor `callProjectedInterface` marker, including cswinrt-style base/inner composable out-parameter handling, without adding runtime invokers or parameter-combination intrinsics.
- [x] Mapped collection return descriptor routing: mapped collection returns now route through the existing descriptor `callProjectedInterface` marker and keep collection projection/readback in generator-owned lambdas, avoiding compiler-plugin collection semantics or new runtime invokers.
- [x] Inline result raw-ABI descriptor routing: inline ABI calls with generator-owned result slots/readback, including struct-array in/out methods, now reuse the existing descriptor `callUnit` marker for the HRESULT vtable call instead of emitting `ComVtableInvoker.invokeGenericArgs`, without adding array-specific runtime helpers or per-combination intrinsics.
- [x] Struct-result intrinsic lowering: generator emits `WinRtProjectionIntrinsic.getStruct` marker calls for eligible no-argument instance and fallback-interface methods/properties returning generated structs, and the compiler plugin now lowers those calls directly to JVM FFM while preserving adapter-owned struct read/dispose semantics.
- [x] Fallback-interface String getter intrinsic lowering: generator now routes fallback interface property getters with String/no-argument ABI shape through the existing `WinRtProjectionIntrinsic.getString` marker before falling back to inline HSTRING out-buffer code, closing the property path gap while preserving projected-object/scalar runtime helper priority.
- [x] Enum getter/setter intrinsic lowering: generator routes eligible Int32/UInt32 enum no-argument getters through `getInt32`/`getUInt32` intrinsic plus generated `Enum.Metadata.fromAbi`, and routes one-argument enum setters through `setInt32`/`setUInt32` intrinsic using the generated enum `abiValue`; narrower enum underlying types remain on fallback until runtime primitives exist.
- [x] Struct parameter intrinsic lowering: generator emits `WinRtProjectionIntrinsic.setStruct` for eligible generated-struct one-argument Unit methods/properties, and the compiler plugin now lowers it directly to JVM FFM while preserving adapter-owned struct write/dispose semantics.
- [x] Array return intrinsic foundation: generator emits `WinRtProjectionIntrinsic.getArray` for eligible no-argument instance/fallback-interface array returns when an element `Marshaler` exists, and the compiler plugin now lowers it directly to JVM FFM while preserving marshaler-owned array decode/dispose semantics. Static and parameterized array calls remain on fallback for later slices.
- [ ] Interface native projection IR migration 正在做: `.cswinrt/src/cswinrt` remains the projection-shape baseline; Kotlin ownership is `winrt-runtime` for narrow typed ABI primitives, `winrt-generator` for deterministic ABI call-shape selection and marker emission, and `winrt-compiler-plugin` for IR lowering to those primitives. Floating-point interface members currently stay on the IR-lowered Kotlin fallback instead of the older generated interface artifact path.
- [ ] `mingwX64` actual generation: after JVM projection actuals stabilize, emit `mingwX64` actual projection implementations from the same metadata call-shape model instead of designing a separate public API.

## Frozen

- [ ] `winrt-samples`: only validate completed runtime/generator/authoring slices; no sample-local runtime workarounds.
- [ ] `winrt-projections`: avoid broad checked-in projection growth; prefer plugin-generated output.
- [ ] `mingwX64`: full native parity starts after JVM authoring and WinUI app-consumption validation are stable.

## Validation

### Current Validation

- [x] Expect/actual event slice validation: targeted generator coverage passes for JVM event-source emission, runtime-class forwarding, and static-event fallback.
- [x] WinUI projection compile observation: representative validation is the WinUI-enabled `winrt-samples` configuration with WindowsAppSDK `1.8.260416003`; after JVM FFM handle caching, `:winrt-samples:generateWinRtProjections :winrt-samples:compileKotlin` passes, generated WinUI `invokeGenericArgs` source references stay at 0, generated class outputs contain zero `WinRtProjectionIntrinsic` or `ComVtableInvoker` references, and 848 generated classes reference `WinRtJvmFfmDowncallHandles`.
- [x] WinUI controls smoke: `:winrt-samples:buildWinRtAuthoringHost :winrt-samples:run` with `runWinUiSmoke=true` reaches `winui-controls: window activated`, exits with `SMOKE_EXIT=0`, and reports `NO_MATCHING_CRASH_EVENTS`.
- [x] ComVtableInvoker projection fallback shrink validation: targeted `:winrt-runtime:jvmTest`, `:winrt-runtime:compileKotlinMingwX64`, and WinUI-enabled `:winrt-samples:generateWinRtProjections :winrt-samples:compileKotlin` pass after removing single-argument `Byte`/`Float`/`Double` typed fallback overloads; `Long` stays because standard delegate event removal uses the runtime-owned token ABI path.
- [x] Component smoke activation regression: `:winrt-samples:buildWinRtAuthoringHost :winrt-samples:run` with `runComponentSmoke=true` now reaches `simple-math=5.5 + 6.5 = 12.0`; the fix is runtime manifest-free DLL resolution against plugin-staged runtime assets, separate from WinUI controls validation.
- [x] WinRT reference property nullability validation: targeted Windows Gradle coverage passes for generated nullable `ContentControl.content`, `Window.systemBackdrop`, `IWindow2.systemBackdrop`, and `UIElement.clip` getter/setter bindings, plus the affected expect/actual projected-interface/runtime-class renderer paths. `:winrt-samples:compileKotlin` also passes with broad projected-reference property nullability after fixing static projected-object getter and nullable custom-object parameter marshaling. The KMP WinUI library compile surface still exposes a separate unresolved `microsoft.*` projection ownership issue and remains tracked under `KMP WinRT graph baseline`.
- [x] Kotlin keyword property forwarding validation: targeted generator coverage passes for the single-source runtime-class forwarder that projects WinRT `Package` properties as backtick-escaped Kotlin `package` members and emits escaped member access; expect/actual coverage confirms the same projected-object property shape is safely handled through JVM interface delegation.
- [x] Async await completion cleanup validation: targeted `WinRtAsyncInteropTest` coverage confirms async action, action-with-progress, operation, and operation-with-progress `await()` close completion delegate handles after callback, cancellation, and `GetResults()` failure; `:winrt-runtime:compileKotlinMingwX64` also passes for the common runtime change.
- [x] Event-source cached delegate liveness validation: targeted `EventRuntimeInfrastructureTest` confirms event sources re-register when a cached delegate loses native references; `:winrt-runtime:compileKotlinMingwX64` passes for the common runtime change.
- [x] Runtime-class parameter marshaling validation: targeted generator coverage passes for descriptor Unit/Boolean/Scalar/ProjectedObject/Struct/raw-ABI/nullable-XAML setter paths that now emit `winRtProjectionMarshaler(..., defaultInterfaceIID)` plus `RawAddress`; `:winrt-runtime:jvmTest`, `:winrt-runtime:compileKotlinMingwX64`, dependency identity plugin tests, and non-WinUI `:winrt-samples:compileKotlin` pass.
- [x] WinUI resource-manager lifecycle rollback validation: generator coverage now confirms generated `Microsoft.UI.Xaml.Application` constructors do not call the removed `WinRtWinUiResourceManagerBootstrap`; KWINRT-008 remains open behind the dual-module sample because the aligned fix must provide XAML/MRT resource initialization equivalent to WinUI `App.xaml`.
- [x] KWINRT-013 dual-module WinUI event validation: Windows Gradle run of `:winrt-samples:winui-kmp-app:runWinuiKmpSample` with WindowsAppSDK `1.8.260416003` passes after the generated event-source registry fix; the sample reaches `Application.start`, creates `Application`/`Window`/`Canvas`/`Button`/`TextBox`, registers `Window` and XAML control events from the library module, observes native callbacks, and exits cleanly.
- [x] XAML object and collection validation: the same dual-module WinUI KMP sample now checks `Button.content` getter readback, concrete runtime-class recovery from `Canvas.children`, and `Canvas.Left/Top` attached positioning before the root is attached to the window, keeping KWINRT-018/009/007 covered by a real generated WinUI run instead of a single-module or offscreen-only assumption.
- [x] Automation attached-property getter validation: the same dual-module WinUI KMP sample now reads generated `AutomationProperties.accessibilityViewProperty` and clears that dependency property on the detached `Button`; the run reaches `winui-kmp-app: finished`, covering the KWINRT-014 static getter plus generated `DependencyObject.clearValue(...)` path.
- [x] Automation attached-property validation: the same dual-module WinUI KMP sample now calls generated `AutomationProperties.setAccessibilityView(button, AccessibilityView.Raw)` before assigning the panel to `Window.content`; the run reaches `winui-kmp-app: finished`, covering the generated setter path behind KWINRT-015 without adding runtime guards or compose-specific helper behavior.
- [x] Duplicate projection suppression validation: `:winrt-samples:winui-kmp-app:compileKotlinWinuiJvm` passes after the application module explicitly requests library-owned `AutomationProperties`, `Button`, and `TextBox`; direct generated-source inspection confirms those projection files are suppressed in the application module, covering the duplicate-FQN classpath path behind KWINRT-011.
- [x] Generated interface no-arg Unit validation: targeted runtime coverage confirms compiler-generated interface projections dispatch no-argument `Unit` methods through the fixed HRESULT vtable primitive; the WinUI KMP library sample also compiles natural `DisplayRequest.requestActive()` / `requestRelease()` calls so compose-winui can retest the real keep-screen-on path without the previous missing `IDisplayRequest` factory failure.
- [x] Generator regression sweep: full `KotlinProjectionGeneratorTest` passes on Windows/JDK 22 with generator-local test JVM memory settings after registrar ownership changes.
- [x] Narrow sample compile-surface blocker closed: after moving registrar, event-source, and generic-instantiation support tables out of normal Kotlin source, non-WinUI `winrt-samples:compileKotlin` passes on Windows/JDK 22 with the constrained JVM validation settings; this is no longer treated as representative of WinUI projection consumption.
- [ ] Compile-surface observation: JVM target validation requires JDK 22; treat OOM or native compiler pressure as evidence of excessive generated compile surface, duplicated data, or diagnostic/source hot spots before considering environment tuning.

### Validation History Summary

- [x] Runtime ABI/CCW baseline: targeted `winrt-runtime:jvmTest` coverage has validated delegate interface tables, boxed/reference value support, parameterized interface IDs, collection IIDs, activation factory support, and type-ID registration.
- [x] Metadata baseline: targeted `winrt-metadata:test` coverage has validated semantic helpers, authoring metadata, signature/IID descriptors, and cswinrt writer handoff inputs.
- [x] Generator baseline: targeted `winrt-generator:test` coverage has validated file output, Windows.Data.Json shapes, cswinrt writer support handoffs, mapped declaration skipping, collection/generic substitution, deterministic generation, and full `KotlinProjectionGeneratorTest` sweeps at major checkpoints.
- [x] Expect/actual generator slice through commit `b18eb649`: targeted generator tests have validated common support layout, ordinary common interfaces with JVM projection support, runtime-class expect/JVM actual wrappers, inherited member forwarding/deduplication, conflict/fallback gates, JVM FFM scalar/projected-interface descriptors, no hidden `ComVtableInvoker` fallback for migrated calls, and common fallback for async/collection/custom mapped/static/event/setter-only/unavailable ABI shapes.
- [x] Runtime fallback shrink through current expect/actual slice: targeted `winrt-runtime:jvmTest`, `winrt-runtime:compileKotlinMingwX64`, WinUI compile, and WinUI controls smoke validated removal of generated-projection scalar/mixed-shape `ComVtableInvoker.invokeArgs` overloads while preserving runtime primitive direct-call shapes.
- [x] Expect/actual narrow first slice closure: targeted `winrt-generator:test` coverage revalidated ordinary common interfaces, JVM projection support objects, runtime-class actual forwards, projected-interface ABI calls, fallback gates, and scalar FFM descriptors before marking the slice complete.
- [x] Generator regression sweep after registrar migration: full Windows/JDK 22 `KotlinProjectionGeneratorTest` passes; generator test JVM now overrides the root 128m test heap to avoid JDK 22 native OOM during the sweep.
- [x] Compiler/Gradle plugin registration path: targeted `winrt-compiler-plugin:test` and `winrt-gradle-plugin:test` coverage has validated authoring scanner/plugin wiring, generated-source wiring, projection identity JSON, local component runtime-assets staging, WindowsAppSDK framework/resource staging, and generated projection type-index loading.
- [x] Generated authoring type-details registrar: targeted runtime and Gradle-plugin tests validate explicit type-details factory registration and removal of JVM `Class.forName("WinRT_<Type>_TypeDetails")` probing.
- [x] Generated projection registrar: targeted generator and runtime registry tests validate generated class-literal registrar output, authored/suppressed type exclusion, and JVM fixed-entrypoint loading before TSV fallback; sample compile reached the registrar path and no longer reports unresolved authored `Metadata` calls after the suppression fix.
- [x] Metadata table ownership: targeted generator tests validate registrar-owned runtime-class metadata calls without `Metadata.register()` indirection, and JDK 22 `winrt-samples:generateWinRtProjections` confirms generated sample registrar output excludes authored app types and emits direct `ComWrappersSupport`/`Projections` tables.
- [x] Runtime loader boundary: targeted runtime registry test validates the common compiler-generated projection type-index registration helper installs type IDs, runtime-class names, aliases, and base-type fallback mappings from structured inputs.
- [x] Authoring path: targeted `winrt-authoring:test`, `winrt-metadata:test`, and `winrt-gradle-plugin:test` coverage has validated authored metadata emission, host manifest loading from runtime assets, native host export generation, target artifact staging, and cswinrt-style activation host contracts.
- [x] WinUI/sample validation: representative run is `:winrt-samples:buildWinRtAuthoringHost :winrt-samples:run` with WindowsAppSDK `1.8.260416003`; WinUI-only auto-exit run completes successfully, no-auto-exit run reaches `window activated` and remains stable until externally stopped, no matching `java.exe`/`Microsoft.UI.Xaml.dll` crash event is recorded, and targeted `WinUiDesktopSampleTest` projection-surface coverage passes.
