# Plan

## Notes

- [x] Establish the root implementation plan file and integrate it into the repository workflow
- [x] Rewrite AGENTS.md to make `.cswinrt` the primary reference and to define JVM-first execution, dual-target parity, commit discipline, and plan maintenance rules
- [x] Standardize the canonical plan file name to `PLAN.md`
- [x] Clarify in AGENTS.md that `.cswinrt` alignment is responsibility-by-responsibility, with an explicit mapping table rather than a misleading literal name match
- [x] Tighten AGENTS.md to require strict one-to-one module correspondence with `.cswinrt/src`, including matching top-level module names
- [x] Adjust AGENTS.md to use Kotlin-style top-level module names `winrt-runtime`, `winrt-metadata`, `winrt-authoring`, `winrt-projections`, and `winrt-samples`, with tests kept inside each module
- [x] Exclude legacy `sample-jvm-winui3` from the active refactor scope unless explicitly requested
- [x] Clarify in AGENTS.md that implementation is `.cswinrt`-reference-first and design-first rather than test-driven reverse engineering
- [x] Write a hard repository-level constraint that `.cswinrt` responsibility convergence must be preserved: repeated Kotlin enumeration of the same runtime type/category/shape decisions is not acceptable when `.cswinrt` already centralizes that logic
- [x] Collapse the duplicated JVM runtime primitive/object/string/guid classifiers into one shared `.cswinrt`-aligned owner so `GuidGenerator`, `TypeNameSupport`, and `Projections` stop carrying parallel branch tables
- [x] Collapse the duplicated collection/bindable projection marshaler container and borrowed-reference helpers into one shared runtime owner so those helper layers stop maintaining parallel wrappers for the same projection-hosting behavior
- [x] Collapse the duplicated generator-side mapped WinRT type and collection-shape tables into one `.cswinrt/src/cswinrt/helpers.h`-aligned owner so projected type mapping, collection binding discovery, and mapped ABI kind classification stop drifting across `SPECIAL_TYPE_MAPPINGS` and separate collection branch tables
- [x] Collapse the duplicated generator-side integral ABI layout/conversion/readback tables into one shared descriptor owner so enum codegen, ABI call planning, and literal emission stop maintaining parallel `WinRtIntegralType` switch ladders

## Direction

- [x] Define the repository-wide execution rule: inspect the matching `.cswinrt` implementation first, then establish the Kotlin module boundary and runtime/generator contract, then implement, and only then use tests for validation
- [ ] Drive current work in this order: `winrt-runtime` ABI/runtime foundation -> `winrt-metadata` WinMD loading and model construction -> generator pipeline inside the `.cswinrt/src/cswinrt` responsibility split -> deterministic checked-in output in `winrt-projections` -> `winrt-authoring` hosting/authoring support -> `winrt-samples` validation surfaces -> targeted tests that confirm parity instead of defining behavior
- [ ] For each active slice, keep the corresponding `.cswinrt` source area explicit in the task text or WIP note before expanding tests
- [ ] Reject test-driven backfilling when `.cswinrt` parity has not been established yet; if a test passes but the matching `.cswinrt` contract is still unknown, keep the slice open
- [ ] Reject duplicated Kotlin branch tables for the same runtime or generator semantic classification; if `.cswinrt` converges a decision into one helper/registry/marshaler/type-name layer, the Kotlin implementation must converge it too instead of repeating the enumeration in parallel files

## Current Focus Queue

- [ ] Focus KMP runtime split: align `winrt-runtime` with the `commonMain` / `jvmMain` / `mingwX64Main` ownership split that corresponds to `.cswinrt/src/WinRT.Runtime`, moving pure GUID/IID/signature/annotation/type-descriptor owners into common code and leaving native/platform/cache/finalization mechanics behind explicit target seams. 正在做: the module now has a Kotlin Multiplatform skeleton, common `Guid`/IID/signature/annotation owners, a pure-Kotlin SHA-1 path for parameterized IIDs, explicit cache/finalization `expect`/`actual` seams, initial JVM runtime adoption of those cache seams, descriptor-backed `WinRtTypeId` registration replacing `Class.forName` / annotation-scanning lookup in the `GuidGenerator` / `TypeExtensions` / `TypeNameSupport` / `Projections` owner set, descriptor-backed enum/value-array handling replacing the remaining `getDeclaredField("abiValue")` and `java.lang.reflect.Array` usage in `ValueBoxing.kt` and `Marshalers.kt`, a common `WinRtUri` value type replacing the temporary JVM `java.net.URI` projection in `SystemProjections.kt` and the runtime hooks/tests that consume it, and a new common pure-model batch for runtime identity/descriptor owners (`RuntimeClassId`, `IInspectable`, `WinRtTypeHandle`, `WinRtInspectableInfo`, the delegate descriptor/value-kind pair, plus the common `HResult` / `KnownHResults` core, `ActivationResult`, the WinRT exception/restricted-error-info model set, the shared async/bindable IID + slot constant owners, and the manifest-free activation candidate-name helper) so later reference/activation/commonization work stops depending on JVM-only definitions for those shapes. The `WeakReference<T>` owner from `.cswinrt/src/WinRT.Runtime/WeakReference.netstandard2.0.cs` is now also split the same way: the public weak-reference logic lives in `commonMain`, while JVM keeps the `IWeakReferenceSource` / `IWeakReference` ABI helpers and platform-owned weak-reference seams behind `PlatformManagedWeakReference`, `PlatformLock`, `NativeWeakReferenceHandle`, and `WeakReferenceInterop`. `mingwX64` currently has only the managed weak-reference half wired through Kotlin/Native weak refs; native weak-reference rehydration remains explicitly unimplemented there instead of being hidden in shared logic. The JVM platform boundary now starts converging on `jvmMain` with `WinRtPlatformApi` directly owning the former `WindowsRuntimePlatform` FFM calls plus a raw `NativePointer`/`NativeScope` seam that exposes the full platform API surface first while keeping existing JVM-only owners on direct `MemorySegment`/`Arena` usage. `WindowsRuntimeStrings` / `HString` / `ReferencedHString` are already in `commonMain`, the string marshaler core has now followed them there through `NativeStringMarshaller`, and the shared native-value seam has advanced one layer higher: `NativeInterop` now exposes slice/`float`/`char16` primitives, `NativeStructLayout`/`NativeStructAdapter` now live in `commonMain`, and the numerics/geometry value models from `.cswinrt/src/WinRT.Runtime/Numerics.cs` and `Geometry.cs` now also live in `commonMain` while JVM keeps only thin `MemoryLayout`/`MemorySegment` bridges in `GeometryJvmCompat.kt` / `NumericsJvmCompat.kt`. The activation lookup chain now consumes the raw seam (`ActivationFactory` calls `roGetActivationFactoryRaw`, `WinRtPlatformApi` now also exposes raw `IUnknown`/`DllGetActivationFactory` operations for the upcoming reference-layer commonization, `DllModule` plus the manifest-free activation fallback now live in `commonMain` against those raw seams, and the activation-factory cache/lookup core also lives in `commonMain` while the JVM owner only wraps the returned pointers as `IUnknownReference` / `ActivationFactoryReference`), and the initialization/MTA owner set now does the same (`RuntimeInitialization` uses raw init/uninit APIs and `WinRtModule` keeps the MTA cookie as a `NativePointer` from `coIncrementMtaUsageRaw`). `RuntimeScope` itself now also lives in `commonMain` behind a thin `PlatformRuntimeInitialization` expect/actual bridge, so the shared initialization-scope owner no longer depends on the JVM-only source set while `JvmComRuntime` / `JvmWinRtRuntime` stay as the JVM-facing wrappers over that bridge, and the old legacy `src/main` compatibility mount is now gone entirely: no production Kotlin sources remain under `winrt-runtime/src/main`, `winrt-runtime/build.gradle.kts` no longer mounts `src/main/kotlin` into `jvmMain`, and the remaining JVM-only owners now live explicitly under `src/jvmMain/kotlin` instead of a shared-path fallback. `WinRtModule` now also lives in `commonMain`; the shared MTA-cookie owner keeps its lazy state and `CoIncrementMTAUsage` flow in common code, while a narrow `PlatformProcessHooks` expect/actual seam owns only the process-shutdown cleanup registration on the target side. The agile/context owner set now now has its runtime core in `commonMain`: `AgileReference`, `AgileReferenceInterfaceReference`, `GlobalInterfaceTableReference`, `Context`, and `ContextCallbackReference` all consume raw seam acquisition paths for `RoGetAgileReference`, `CoCreateInstance(CLSID_StdGlobalInterfaceTable)`, `CoGetContextToken`, and `CoGetObjectContext` from shared code, while JVM keeps only the typed RCW adapter `AgileReferenceTyped` and the platform exception interop actual that bridges `setErrorInfo` / `Throwable -> HResult` back to `ExceptionHelpers` and `WinRtExceptionTranslator`. The WinUI runtime-hook slice now also converges further: Windows App SDK bootstrap loads, binds, and invokes `MddBootstrapInitialize2` / `MddBootstrapShutdown` through the same raw loader/proc seam instead of separate `SymbolLookup` or ad hoc `Linker` state, and the bootstrap discovery/version-header owner itself now also lives in `commonMain` behind a narrow `PlatformFileSystem` seam so it no longer remains as a JDK-only runtime owner in `jvmMain`; the XAML metadata provider owner itself now also lives in `commonMain`, while the pure metadata slot/runtime-class-name constants remain in the existing common model owner. The runtime error-info slice now also consumes the raw seam for `SetErrorInfo`, restricted-error-info borrow/report, `FormatMessage`, BSTR allocation, and BSTR readback/free, keeping the JVM fallback `ManagedErrorInfoComObject` aligned with `.cswinrt/src/WinRT.Runtime/ExceptionHelpers.cs` and `Interop/ExceptionErrorInfo*` without leaving another parallel platform helper path behind. The remaining broad HRESULT-success-check owners now also call `checkSucceededRaw` directly, which removes the last production `WindowsRuntimePlatform.checkSucceeded` dependency from the runtime owner set before the next `ComObjectReference`/reference-layer commonization slice. That next slice has now advanced into physical source-set relocation: `ComObjectReference`, `ActivationFactory`, `IWinRTObject`, and `InspectableRuntime` now live in `commonMain`, the shared object-reference chain is on `NativePointer`, and the remaining JVM-only `MemorySegment` / `FunctionDescriptor` entry points for that moved chain are isolated to `ComObjectReferenceJvmCompat.kt` instead of being left inside the common owners. The `.cswinrt/src/WinRT.Runtime/IInspectable.cs` owner split follows that relocation: `ActivationFactoryReference.activateInstance` and `InspectableReference.getRuntimeClassName` use a shared `commonMain` helper for pointer-slot allocation, HRESULT handling, HSTRING ownership, and tracker initialization, while `jvmMain` keeps only the slot-specific FFM downcall body. The raw ABI out-parameter support for pointer/int32/HSTRING/boolean/double returns is now also shared in `commonMain`, so `ComObjectReference` no longer owns those allocation/result-lifetime patterns locally and instead only supplies the slot-specific JVM FFM invocation lambda. The wrapper-kind selection for known COM reference wrappers now also lives in `commonMain`, so `ActivationFactory.wrapFactory` no longer duplicates the `ActivationFactoryReference` / `IInspectableReference` / `IUnknownReference` branch table and `WinRtProjectionInterop` has also now moved into `commonMain` on top of that shared selection logic. The borrowed/unwrapped reference decision based on `IWinRTObject.hasUnwrappableNativeObject` and interface availability is now also shared in `commonMain`, so `WinRtProjectionInterop.borrowedProjectionReference` and `ComWrappersSupport.tryUnwrapObject` no longer maintain separate copies of the same selection logic. The projection-marshaler lease that owns borrowed clone cleanup and hosted reference cleanup now also lives in `commonMain`, so `WinRtProjectionMarshaler` is down to a shared `NativePointer` ABI lease instead of a JVM-only `MemorySegment` carrier, and `ComWrappersSupport`/marshalers/value-boxing tests now consume that same `NativePointer` hot path; the only new boundary added for this relocation is the narrow `ManagedReferenceHost` interface that lets `WinRtProjectionInterop.hosted(...)` depend on shared host semantics instead of the JVM-only `WinRtInspectableComObject` type. The managed host lifecycle immediately above that seam now also converges in `commonMain`: `ManagedReferenceHostSupport` owns the shared `create lease` / `detach reference` / `wrap owned reference` patterns used by `WinRtProjectionMarshaler.hosted`, `ComWrappersSupport` CCW ownership, `WinRtInspectableComObject.detachReference`, and `ManagedErrorInfoComObject.detachReference`, with common tests locking the create/cleanup ordering and failure cleanup behavior. The `ComWrappersSupport` / `TypeNameSupport` registration side is now also moving off JVM `Class` tokens: CCW factory registration, projection-type registration, runtime-class-name lookup hooks, and the corresponding registry tests now use `KClass`-first APIs, while the remaining explicit Java-class bridge is isolated to a narrowly named compatibility function instead of being the primary owner surface. The raw object out-parameter carrier patterns immediately above the seam are now also being converged into `commonMain`: pointer-array allocation/readback has been added to `NativeInterop`, `RawObjectAbiSupport` now owns nullable object results, `IndexOf` boolean/index pairs, `GetMany` pointer-array unpacking, `ReplaceAll` pointer-array packing, and split-view pointer-pair results, and the JVM async/collection/bindable reference wrappers are now fully on `NativePointer` / `NativeFunctionDescriptor`, with the affected projection/tests updated to match, so repeated `Arena`/`MemorySegment` carrier code is no longer embedded in those wrapper families. The managed COM-host lifecycle one layer above that is now also being converged into `commonMain`: `ManagedComHostState` owns AddRef/Release, one-time cleanup, and QueryInterface success/failure bookkeeping for the JVM-side `WinRtInspectableComObject`, `WinRtDelegateComObject`, and `ManagedErrorInfoComObject`, leaving those owners with only their FFM stub tables, registry plumbing, and target-specific ABI payload methods. A new typed native callable seam is now in place as the immediate prerequisite for the full source-set reorg: `NativePlatformSeams` carries `NativeFunctionDescriptor`, `NativeCallbackHandle`, raw vtable/function invoke helpers, pointer-key helpers, and GUID read/write helpers; the JVM actual routes these directly to FFM without an extra wrapper layer, and `mingwX64` already matches the expect/actual shape. On top of that seam, the JVM runtime has now been cut over so `ComObjectReference`, `ActivationFactory`, `WinRtProjectionInterop`, `AgileReference`, `Context`, `WeakReference`, `WinUiXamlMetadataProvider`, and the collection/bindable/boxing/marshaler edges now use `NativePointer` on the hot path instead of carrying new direct `Linker`/`Arena` code of their own. Async has also now been pulled back to the original KMP plan boundary: `WinRtAsyncReferences` no longer owns `CompletableFuture` in the shared runtime surface, common code depends on `kotlinx.coroutines` and exposes coroutine-based `await`, blocking `join()`, and the shared `MonoSupport` no-op owner in `commonMain`, while JVM keeps a narrow `WinRtAsyncJvmCompat` facade for `toCompletableFuture()` only. That same relocation has now advanced one layer higher: `WinRtDelegateHandle`, `WinRtDelegateBridge`, `WinRtDelegateAbiMarshaller`, `WinRtDelegateComObject`, `WinRtInspectableComObject`, `WinRtCollectionProjections`, and `WinRtBindableProjections` now also live in `commonMain`, while JVM keeps only narrow compatibility bridges for the remaining legacy `FunctionDescriptor`/`MemorySegment` and boxed-value hooks. `WinRtBindableProjections` specifically now depends on a narrow `PlatformInspectableInterop` expect/actual seam for the still-legacy `ComWrappersSupport.createCCWForObject` and `WinRtValueBoxing.tryProjectBorrowedInspectable` entry points instead of retaining another JDK-only runtime owner in shared code. The `.cswinrt/src/WinRT.Runtime/Projections/Nullable.cs` entry owners for `IReference<T>`, `IReferenceArray<T>`, and `IPropertyValue` now also live in `commonMain` under `ValueProjectionInterop.kt`, while `PlatformValueProjectionInterop` keeps the remaining target-specific boxing/reference seams. The intrinsic scalar/object/string/guid classifier convergence point from `.cswinrt/src/WinRT.Runtime/TypeNameSupport.cs`, `GuidGenerator.cs`, and `Projections.cs` now also lives in `commonMain` as `WinRtTypeClassifier.kt`, while JVM keeps only the thin `Class<*>` bridge in `WinRtTypeClassifierJvmCompat.kt`. The next immediate blocker cluster is now the remaining value-boxing/marshaler/classifier owners that sit explicitly in `jvmMain` (`ValueBoxing`, `Marshalers`, `ComWrappersSupport`, and the type-name/classifier edge), and the goal remains to drain that remaining runtime-owner set out of `jvmMain` into `commonMain`/platform actual seams until only thin compatibility and actual/platform files remain, without falling back to another helper-only detour.
- [x] Focus 1: complete the remaining `.cswinrt/src/WinRT.Runtime` object-model parity in `winrt-runtime`, especially full `IWinRTObject` behavior, type-scoped QueryInterface caching, additional type-data caches, and `GetObjectReferenceForType`/`IsInterfaceImplemented` support before resuming any new generator slice. This is now closed by Runtime 1.10 plus Runtime 1.13: the JVM runtime has the formal `IWinRTObject` contract, typed/object-specific wrapper state, typed RCW factory registration, helper-type indirection, runtime-class-name-based wrapper lookup, unwrap helpers, CCW creation, and the Kotlin equivalent of `SingleInterfaceOptimizedObject`.
- [ ] Focus KMP runtime reference relocation: keep draining runtime owner files by whole runtime layers instead of helper-only cuts. 正在做: `WinRtCollectionReferences`, `WinRtBindableReferences`, `WinRtCollectionAdapters`, `WinRtAsyncReferences`, `WinRtDelegateHandle`, `WinRtDelegateBridge`, `WinRtDelegateAbiMarshaller`, `WinRtDelegateComObject`, `WinRtInspectableComObject`, `WinRtCollectionProjections`, and `WinRtBindableProjections` now all live in `commonMain`; the legacy physical `src/main` fallback is gone, and the next blocking JVM owner cluster is the remaining value-boxing/marshaler/classifier layer (`ComWrappersSupport`, `Marshalers`, `SystemProjections`, `ValueBoxing`, `TypeNameSupport`, `WinRtTypeClassifier`) now sitting explicitly in `jvmMain`.
- [x] Focus 2: complete the remaining `.cswinrt/src/WinRT.Runtime/Projections` generic collection runtime parity in `winrt-runtime`, especially `IEnumerable` / `IList` / `IReadOnlyList` / `IDictionary` / `IReadOnlyDictionary` RCW+CCW helpers, `CreateMarshaler` / `FromManaged` / `FromAbi` paths, and runtime-side `ToAbiHelper` / `FromAbiHelper` ownership. This is now closed by Runtime 1.11; the generic collection helper layer is in place, and the next active runtime focus has moved on to the later interop/value-projection/event/XAML queue in Runtime 1.16 through Runtime 1.19.
- [x] Focus 3: complete the remaining `.cswinrt/src/WinRT.Runtime/Projections/Bindable*` runtime parity in `winrt-runtime`, especially `IBindableIterable` / `IBindableVector` / `IBindableVectorView`, non-generic object marshalling, and bindable adapter behavior before restarting collection-parameter or bindable generator work. This is now closed by Runtime 1.12 plus Runtime 1.14: Bindable* helpers are present and the shared marshaler core has landed, and the projection/type-name registry slice from Runtime 1.15 is now also closed.
- [ ] Focus 4: after Focus 1 through Focus 3, continue through the remaining runtime-first parity queue `Runtime 1.18` through `Runtime 1.19` plus the residual interop/error-info inventory, especially event infrastructure and XAML/system projection helpers before reopening other module growth. 正在做: `Runtime 1.17` is now closed for the JVM baseline owner set (`Projections/Nullable.cs`, `IReferenceArray*`, `IPropertyValue*`, `IDisposable.net5.cs`, `IDisposable.netstandard2.0.cs`, `Type.cs`, `Type.ArrayMarshalling.cs`, `SystemTypes.cs`, `Uri.cs`, `Numerics.cs`, and `Geometry.cs`) with Windows-targeted compile/test coverage. The shared boxed-value owner now converges in `ValueBoxing.kt`, `SystemProjections.kt`, `ComWrappersSupport.kt`, `WinRtBindableProjections.kt`, and `TypeNameSupport.kt`, including boxed runtime-name lookup, enum/type/HResult nullable projection, `IReferenceArray<T>` handling, and `IClosable`/`AutoCloseable` projection behavior. The next active runtime blocker is `Runtime 1.18`; the remaining `Nullable.cs` delegate/event-handler specializations stay explicitly queued there with the event/delegate owner set instead of drifting back into the value-boxing slice. The JVM-specific narrow deviations that still remain explicit in the broader 1.16-1.19 queue are the CLR/Mono language-exception CCW path and automatic free-threaded-marshaler / `IMarshal` aggregation.
- [ ] Focus 5: keep the full `Runtime Remaining Parity Inventory` (`Inventory A` through `Inventory M`) as the runtime completion checklist; do not treat Focus 1 through Focus 4 as complete until every remaining `.cswinrt/src/WinRT.Runtime` responsibility is either implemented or explicitly narrowed as a residual Kotlin-specific exception.
- [ ] Freeze now: do not expand `winrt-metadata`, generator breadth, `winrt-projections`, `winrt-authoring`, or `winrt-samples` beyond maintenance needed to support Focus 1 through Focus 5.
- [ ] Freeze now: do not resume the unfrozen generator queue items `collection-parameter parity`, bindable collection ABI categories, interface-proxy ABI, or broader projection output growth until Focus 1 through Focus 5 are materially tighter.
- [ ] Exit condition: only after Focus 1 through Focus 5 are reduced to bounded residual gaps may the active queue move back to metadata/generator expansion.
- [ ] Convergence gate: during Focus 4 and Focus 5, do not accept new runtime or generator growth that re-enumerates already-known primitive kinds, projection mappings, collection/bindable shapes, or ABI categories in another file; first collapse that decision into the Kotlin abstraction that corresponds to the `.cswinrt` ownership point.

## Ordered Implementation Stages

- [ ] Stage 1: `winrt-runtime` first. Mirror the active `.cswinrt/src/WinRT.Runtime` slice, define the ABI/runtime contract, and do not move the primary effort to metadata or projections until the minimum contract for the current slice is in place.
- [ ] Stage 2: `winrt-metadata` second. Implement the WinMD loading and symbol/model layer required by the active projection slice only after the needed runtime contracts are known.
- [ ] Stage 3: generator pipeline third. Implement the matching `.cswinrt/src/cswinrt` generation rules only after Stage 1 and Stage 2 expose the runtime and metadata contracts those rules depend on.
- [ ] Stage 4: `winrt-projections` fourth. Expand deterministic checked-in projection output only after the corresponding generator rule exists; do not use handwritten projection growth as a substitute for missing generator work.
- [ ] Stage 5: `winrt-authoring` fifth. Start the authoring/hosting parity slice only after the runtime and metadata foundations required by `.cswinrt/src/Authoring` are already understood.
- [ ] Stage 6: `winrt-samples` and targeted tests last. Use samples and tests to validate the completed earlier stages, not to define missing runtime, metadata, or generator behavior.
- [ ] Stage gate: do not let `winrt-projections` become the starting point for a new slice. If projections seem blocked, return to the missing runtime, metadata, or generator prerequisite instead of expanding projection code directly.
- [ ] Stage gate: do not expand WinUI samples or broader test matrices until the corresponding runtime/generator slice has a documented `.cswinrt` mapping and an implemented contract.

## Current Ordered Slice Queue

- [ ] Queue 1: `winrt-runtime` ABI foundation from `.cswinrt/src/WinRT.Runtime` (GUID/IID/HRESULT primitives, HSTRING ownership, memory ownership boundaries, reusable vtable-call helpers, and initialization scope) must be completed before higher-level projection work expands.
- [ ] Queue 2: `winrt-runtime` object model and activation foundation from `.cswinrt/src/WinRT.Runtime` (object identity, `IUnknown`/`IInspectable` lifetime, activation factory lookup/caching, runtime-class activation, interface-cast helpers) must be stabilized before metadata-driven projection growth.
- [x] Queue 3: `winrt-runtime` projected object-model parity from `.cswinrt/src/WinRT.Runtime` (`IWinRTObject`, type-scoped objref caches, typed RCW factory registration, additional type data, and interface lookup fallback behavior) must be completed before collection/bindable generator growth resumes. This is now closed by Runtime 1.10 plus Runtime 1.13; the shared marshaler core from Runtime 1.14 and the projection/type-name registry from Runtime 1.15 are also in place, so the remaining frozen blockers for generator growth now move to the later value-projection, event, and XAML queue in Runtime 1.16 through Runtime 1.19.
- [ ] Queue 4: `winrt-runtime` projection-helper parity from `.cswinrt/src/WinRT.Runtime/Projections` (generic collection RCW/CCW helpers, bindable adapters, object marshalling, and `ToAbiHelper`/`FromAbiHelper` paths) must follow before collection-parameter or bindable generator slices expand.
- [ ] Queue 5: `winrt-metadata` real WinMD loading from the `.cswinrt/src/cswinrt` metadata responsibilities must replace placeholder modeling before projection planning or output breadth increases.
- [ ] Queue 6: `winrt-metadata` normalized symbol/model construction from `.cswinrt/src/cswinrt` (namespaces, types, generic instantiations, method/property/event shapes, signature normalization) must be completed before generator rules expand beyond the current skeleton.
- [ ] Queue 7: generator planning from `.cswinrt/src/cswinrt` must first cover declaration-shape planning for interfaces, runtime classes, enums, structs, and delegates before member emission breadth grows.
- [ ] Queue 8: generator emission from `.cswinrt/src/cswinrt` must then cover methods, properties, events, default interfaces, factories, custom mappings, collections, async, and WinUI-specific rules in dependency order rather than by whichever generated file currently fails.
- [ ] Queue 9: `winrt-projections` must prove that a representative standard WinRT API slice such as `Windows.Data.Json` can be regenerated through the generator path before any broader checked-in projection surface is added.
- [ ] Queue 10: `winrt-authoring` must follow the `.cswinrt/src/Authoring` hosting/lifetime/activation boundary requirements before broader source-generator or convenience work is expanded.
- [ ] Queue 11: `winrt-samples` must validate the active completed slice with the smallest possible sample first, then expand to WinUI bootstrap, resource, and message-loop validation only after the corresponding runtime/generator support exists.
- [ ] Queue 12: targeted tests must be added in the same dependency order: runtime unit tests -> metadata model tests -> generator regression tests -> projection/sample integration tests.

## Metadata And Generator Subslice Order

- [x] Metadata 2.1: replace placeholder modeling in `winrt-metadata` with real WinMD ingestion aligned with the metadata-reading responsibilities inside `.cswinrt/src/cswinrt`.
- [x] Metadata 2.2: stabilize deterministic namespace/type normalization in `winrt-metadata` so the same metadata input always produces the same ordered model before generator breadth expands.
- [x] Metadata 2.3: add core shape fidelity next: kind classification, GUID/default-interface data, base/implemented interface relationships, generic parameter shapes, and runtime-class activation/static/factory metadata.
- [x] Metadata 2.4: add member/signature fidelity next: method/property/event descriptions, parameter direction/pass-by semantics, and the normalized signature inputs needed by the generator.
- [ ] Metadata gate: do not grow projection breadth or handwritten generated API coverage to compensate for missing WinMD ingestion or incomplete model fidelity in `winrt-metadata`.
- [x] Generator 3.1: keep the Kotlin generator behind completed Metadata 2.1 through Metadata 2.4 prerequisites and derive its behavior from `.cswinrt/src/cswinrt` responsibilities rather than from existing handwritten projection files.
- [x] Generator 3.2: implement declaration planning first: determine the Kotlin declaration set for enums, structs, delegates, interfaces, runtime classes, and metadata companions before detailed member emission grows.
- [x] Generator 3.3: implement deterministic declaration-shell emission next: namespace layout, type declarations, interface/runtime-class scaffolding, companion metadata surfaces, and stable file/text ordering.
- [x] Generator 3.4: implement member emission next: methods, properties, events, interface inheritance handling, default-interface/runtime-class composition, and activation/static/factory surfaces.
- [x] Generator 3.5: only after Generator 3.1 through Generator 3.4 are coherent, add special-case rules for collections, async, custom type mappings, and WinUI-specific projection behavior.
- [ ] Generator gate: the current `KotlinProjectionGenerator` skeleton and `Windows.Data.Json` regression fixture are validation scaffolding only; do not treat them as permission to add handwritten standard WinRT projection files before the metadata and generator prerequisites are closed.

## Authoring And Sample Subslice Order

- [ ] Authoring 5.1: define the minimum `.cswinrt/src/Authoring`-aligned hosting boundary first: authored runtime object lifetime, ABI-facing identity/ownership, and activation/factory expectations.
- [ ] Authoring 5.2: define how authoring depends on the existing runtime and metadata/generator contracts next, including what generated metadata or helper code must exist before authored components can be surfaced safely.
- [ ] Authoring 5.3: only after Authoring 5.1 and 5.2 are explicit, expand `winrt-authoring` with source-generator or convenience surfaces.
- [ ] Authoring gate: the current `winrt-authoring` module is still a placeholder; do not let sample or test pressure invent its contract before the `.cswinrt/src/Authoring` boundary is mapped.
- [ ] Sample 6.1: keep `winrt-samples` on the smallest smoke scenario for the currently completed slice first; do not let samples depend on handwritten standard WinRT projection files.
- [ ] Sample 6.2: use samples to validate completed upstream work in dependency order: runtime smoke first, generated projection smoke next, authoring/hosting validation after that, and WinUI end-to-end scenarios last.
- [ ] Sample 6.3: only after runtime, metadata, generator, and authoring prerequisites are documented and implemented, expand to WinUI bootstrap, resources, window/message-loop, and broader desktop validation.
- [ ] Sample gate: if a sample requires new handwritten projection logic, authoring glue, or runtime workarounds, stop and move that missing behavior back into `winrt-runtime`, `winrt-metadata`, `winrt-projections`, or `winrt-authoring` instead of embedding it in `winrt-samples`.

## Runtime Subslice Order

- [x] Runtime 1.1: close `.cswinrt/src/WinRT.Runtime` ABI primitive gaps first: `Guid`, `HRESULT`, low-level memory layout helpers, raw vtable-call helpers, and WinRT string ownership/reference mechanics.
- [x] Runtime 1.2: close platform-boundary gaps next: COM/WinRT initialization lifetime, Windows API bindings, module loading, proc lookup, and HRESULT-to-exception/error translation.
- [x] Runtime 1.3: close object-reference gaps next: `IUnknown`/`IInspectable` wrappers, ownership/dispose semantics, interface query/cast helpers, runtime class name access, and object identity behavior.
- [x] Runtime 1.4: close activation gaps next: `RoGetActivationFactory`, manifest-free activation fallback, activation factory caching, and runtime-class activation helpers.
- [x] Runtime 1.5: close generic-signature prerequisites next: type-signature rendering and parameterized IID hashing needed by later collection/delegate/generic projection slices.
- [x] Runtime 1.6: only after Runtime 1.1 through Runtime 1.5 are coherent, expand delegate and callback marshaling support.
- [x] Runtime 1.7: only after Runtime 1.1 through Runtime 1.6 are coherent, expand collection adapters and projected collection interop breadth.
- [x] Runtime 1.8: only after Runtime 1.1 through Runtime 1.7 are coherent, expand async bridges and completion callback semantics.
- [x] Runtime 1.9: only after Runtime 1.1 through Runtime 1.8 are coherent, expand WinUI-specific runtime hooks, Xaml-specific mappings, and reference-tracking behavior.
- [x] Runtime 1.10: complete `.cswinrt/src/WinRT.Runtime/IWinRTObject*` parity next: query-interface caches, additional type-data caches, `GetObjectReferenceForType`, `IsInterfaceImplemented`, and the typed RCW lookup behavior that later runtime projections depend on. The JVM path now has a formal `IWinRTObject` contract, per-wrapper QI/additional-data state, object-reference lookup, and primary-interface self-lookup; later dedicated optimized-object/RCW-registration work remains in Runtime 1.13.
- [x] Runtime 1.11: complete `.cswinrt/src/WinRT.Runtime/Projections/IEnumerable*`, `IList*`, `IReadOnlyList*`, `IDictionary*`, and `IReadOnlyDictionary*` runtime parity next: RCW/CCW helpers, `CreateMarshaler` / `FromManaged` / `FromAbi`, and runtime-side `ToAbiHelper` / `FromAbiHelper` paths. The JVM runtime now has explicit iterable/list/map/view projection helpers, managed-hosted CCW stubs for these generic collection shapes, and projection-side smoke coverage; exact typed RCW registration and precise CCW backing allocation/reclamation are closed in Runtime 1.13 / Inventory B, while the next remaining blockers now move to the projection/type-name registry and later value-projection queue in Runtime 1.15 and Runtime 1.17.
- [x] Runtime 1.12: complete `.cswinrt/src/WinRT.Runtime/Projections/Bindable*` runtime parity next: `IBindableIterable` / `IBindableVector` / `IBindableVectorView`, non-generic object marshalling, and bindable adapter behavior before restarting bindable generator work. The JVM runtime now has explicit IBindable object-reference wrappers, non-generic iterable/vector/vector-view RCW+CCW helpers, bindable `CreateMarshaler` / `FromManaged` / `FromAbi` paths, bounded inspectable/object round-tripping for bindable collections, the shared marshaler core from Runtime 1.14 behind them, and the shared boxed-value projection owner from Runtime 1.17 feeding the non-generic object path. Remaining work now shifts to the later event/XAML/system-service slices rather than back to another round of `IPropertyValue` glue.
- [x] Runtime 1.13: complete `.cswinrt/src/WinRT.Runtime/ComWrappersSupport*`, `ObjectReference.cs`, `IInspectable*`, `SingleInterfaceOptimizedObject.net5.cs`, `CastExtensions.cs`, and `DerivedComposed.cs` parity next: typed RCW creation, CCW creation, runtime-class-name based wrapper lookup, unwrap helpers, and single-interface optimized object behavior. The JVM runtime now has `ComWrappersSupport`, helper-type and typed-RCW registration, runtime-class-name wrapper lookup, managed CCW factory registration, unwrap/cast helpers, `DerivedComposed`, `SingleInterfaceOptimizedObject`, and inspectable-info lookup for managed CCW hosts; the shared marshaler model is now also closed by Runtime 1.14, so the next runtime blocker is the projection/type-name registry in Runtime 1.15 rather than more object-wrapper glue.
- [x] Runtime 1.14: complete `.cswinrt/src/WinRT.Runtime/Marshalers.cs`, `FundamentalMarshalers.cs`, and projection marshaller parity next: generic `Marshaler<T>` behavior, `MarshalInterface`, `MarshalInspectable`, `MarshalBlittable`, object/value boxing rules, and array marshalling support instead of per-slice ad hoc generator logic. The JVM runtime now has a shared `Marshalers.kt` layer covering generic `Marshaler<T>`, blittable/scalar marshalling, HSTRING marshalling, interface/inspectable/object marshalling, delegate marshalling entry points, and typed array marshalling support, with runtime tests proving these paths independently of generator code.
- [x] Runtime 1.15: complete `.cswinrt/src/WinRT.Runtime/TypeNameSupport.cs`, `TypeExtensions.cs`, `GuidGenerator.cs`, and `Projections.cs` parity next: runtime-class-name lookup, projection assembly registration, custom type mapping registry, non-WinRT runtime-class lookup hooks, and helper-type discovery. The JVM runtime now has explicit `Projections.kt`, `TypeNameSupport.kt`, `TypeExtensions.kt`, `GuidGenerator.kt`, projection annotations for runtime GUID/helper/default-interface metadata, `ComWrappersSupport` registration hooks for projection assemblies and non-WinRT runtime-class-name lookups, targeted registry tests, and the ongoing KMP split now keeps this owner set on explicit `KClass`/descriptor registration instead of `Class.forName`, runtime annotation scanning, or `java.lang.reflect.Array`. The next remaining runtime blocker moves to the interop/error-info/platform slice in Runtime 1.16.
- [x] Runtime 1.16: complete `.cswinrt/src/WinRT.Runtime/Interop/*`, `AgileReference.cs`, `WeakReference.netstandard2.0.cs`, `Context.cs`, `MonoSupport.cs`, `ExceptionHelpers*`, and `Module.cs` / `Platform.cs` parity next: agile/weak/context/marshal interop, activation-factory interop, IID catalog coverage, richer exception/error-info flow, Mono-specific runtime hooks, and remaining platform helper surfaces. The JVM runtime now has the shared IID catalog converged on `.cswinrt/src/WinRT.Runtime/Interop/IID.tt`/`IID.g.cs`, `WinRtModule`-owned MTA usage for activation, dynamic WinRT error-entry loading plus system-message/BSTR/platform helpers, `ExceptionHelpers`-owned HRESULT classification and managed `IErrorInfo` fallback propagation, `AgileReference` with `RoGetAgileReference`/GIT fallback, `WeakReferenceSource`/`IWeakReference` consumer wrappers, `Context` token/callback helpers, and an explicit shared `MonoSupport` no-op owner, all covered by targeted Windows compile/test runs. During the KMP split, this platform/interop owner set is also converging physically under `jvmMain`: `WinRtPlatformApi` now carries the former `WindowsRuntimePlatform` FFM entrypoints directly instead of routing them through an extra abstraction layer, and the adjacent `AgileReference`, `Context`, `WeakReference`, and HSTRING owner `WindowsRuntimeStrings` have also been moved out of the legacy shared source tree. The follow-up quick migration now also lifts the raw `IWeakReferenceSource` / `IWeakReference` wrappers and the thin `WinRtExceptionTranslator` facade into `commonMain`, leaving only platform HRESULT/exception actuals and managed weak-reference storage behind target seams. This keeps the eventual common runtime seam close to cswinrt's `Platform.cs` / interop responsibility split without adding wrapper overhead on the JVM hot path. Narrow residual deviations are explicit rather than implicit drift: Kotlin/JVM does not host CLR/Mono exception CCWs or automatic COM aggregation, so `.cswinrt`'s language-exception propagation path and free-threaded-marshaler-backed `IMarshal` aggregation remain isolated as later platform-adaptation residuals instead of being re-spread across runtime call sites.
- [x] Runtime 1.17: complete `.cswinrt/src/WinRT.Runtime/Projections/Nullable.cs`, `IReferenceArray*`, `IPropertyValue*`, `IDisposable.net5.cs`, `IDisposable.netstandard2.0.cs`, `Type.cs`, `Type.ArrayMarshalling.cs`, `SystemTypes.cs`, `Uri.cs`, `Numerics.cs`, and `Geometry.cs` parity next: nullable/value boxing, `IReference<T>` / `IReferenceArray<T>`, property-value projections, `IClosable`/`IDisposable` projection behavior, WinRT type projections, and system/numerics/geometry custom projections. The JVM runtime now has `.cswinrt`-aligned boxed-value ownership in `ValueBoxing.kt`, including boxed runtime-class-name generation, enum/type/HResult nullable support, descriptor-backed enum ABI value registration instead of reflective `abiValue` field lookup, and shared RCW unboxing through `ComWrappersSupport` plus bindable object marshalling. The projection-facing `IReference<T>` / `IReferenceArray<T>` / `IPropertyValue` entry owners from `.cswinrt/src/WinRT.Runtime/Projections/Nullable.cs` now live in shared `commonMain` code under `ValueProjectionInterop.kt`, while `PlatformValueProjectionInterop` keeps the remaining platform boxing/reference seams target-specific for JVM and future `mingwX64` parity. `SystemProjections.kt` owns the matching `DateTime` / `TimeSpan` / `Type` / `Uri` / `IClosable` / numerics / geometry projections, but the Kotlin public/common contract now narrows the former JVM-only `java.net.URI` mapping to a runtime-owned `WinRtUri` value type so this slice stays viable for `mingwX64`. The JVM registry bridge now also treats `@WindowsRuntimeType` / `@WinRtGuid` / helper/default-interface annotations as a real metadata source, so the built-in numerics/geometry struct registration no longer duplicates WinRT type-name and signature literals in `SystemProjections.kt`. The primitive scalar and primitive-array classifier/boxing table now also converges under `WinRtTypeClassifier`, so `TypeNameSupport.kt` and `ValueBoxing.kt` no longer maintain parallel byte/char/int/unsigned array lookup tables, and `TypeProjection.fromManaged` no longer keeps a second `Any`/`String`/`Guid`/`Class` metadata-special-case branch outside `Projections.isTypeWindowsRuntimeType(...)`. Boxed `IReference<T>` / `IReferenceArray<T>` runtime-name construction and parsing now also converge under shared `WinRtReferenceTypeNames`, so `TypeNameSupport.kt`, `ValueBoxing.kt`, and `SystemProjections.kt` no longer hand-maintain parallel boxed-name string builders. `TypeNameSupport.kt` resolves boxed runtime names such as `Windows.Foundation.IReference`1<T>` and `Windows.Foundation.IReferenceArray`1<T>` back to the projected Kotlin type. The remaining delegate/event-handler-specific `Nullable.cs` branches are not left behind as drift; they stay explicitly queued with Runtime 1.18's event/delegate owner set.
- [ ] Runtime 1.18: complete `.cswinrt/src/WinRT.Runtime/Projections/EventHandler.cs`, `GenericDelegateHelper.net5.cs`, `DelegateExtensions.cs`, `EventRegistrationToken*`, `Interop/StandardDelegates.cs`, and `Interop/EventSource*` parity next: event source caches, registration-token tables, helper delegates, standard delegate ABI helpers, and projected event plumbing beyond the current minimal delegate bridge.
- [ ] Runtime 1.19: complete `.cswinrt/src/WinRT.Runtime/Projections/ICommand*`, `ICustomPropertyProvider*`, `INotifyPropertyChanged*`, `INotifyCollectionChanged*`, `INotifyDataErrorInfo*`, `PropertyChangedEventHandler.cs`, `PropertyChangedEventArgs.cs`, `NotifyCollectionChangedEventHandler.cs`, `NotifyCollectionChangedEventArgs.cs`, `DataErrorsChangedEventArgs.cs`, `IServiceProvider*`, and `IStringable.cs` parity next: XAML binding/runtime projections, non-generic event projections, property/data-error change event args, and remaining system-service projection helpers.
- [ ] Runtime gate: the existing delegate and generic collection helpers already present in `winrt-runtime` are still only partial runtime parity; do not use their presence as justification to skip unfinished Runtime 1.12 through Runtime 1.19 work or the broader Runtime Remaining Parity Inventory below.
- [ ] Runtime gate: while closing Runtime 1.16 through Runtime 1.19, do not introduce fresh duplicated primitive/category/shape classifiers in `Marshalers`, `GuidGenerator`, `TypeNameSupport`, `Projections`, collection helpers, bindable helpers, or generator call planners. If a new slice needs one of those decisions, refactor it into the shared `.cswinrt`-aligned owner first.

## Runtime Remaining Parity Inventory

- [x] Inventory A: close the remaining object-wrapper contract from `.cswinrt/src/WinRT.Runtime/IInspectable.cs`, `IInspectable.net5.cs`, `IWinRTObject.net5.cs`, and `SingleInterfaceOptimizedObject.net5.cs` by adding the missing dynamic interface-cast behavior, query-interface caching, additional type data, and wrapper-identity rules to `winrt-runtime`. The JVM runtime now has the formal `IWinRTObject` state model from Runtime 1.10 plus the `SingleInterfaceOptimizedObject` / cast / wrapper-identity layer from Runtime 1.13.
- [x] Inventory B: close the remaining RCW/CCW infrastructure from `.cswinrt/src/WinRT.Runtime/ComWrappersSupport.cs`, `ComWrappersSupport.net5.cs`, `ComWrappersSupport.netstandard2.0.cs`, `ObjectReference.cs`, `CastExtensions.cs`, and `DerivedComposed.cs` by implementing typed RCW registration, CCW creation, unwrap support, runtime-class-name-based wrapper lookup, and helper-type registration instead of leaving those responsibilities scattered across generated code. This is now closed by the JVM-side `ComWrappersSupport`, helper-type and typed-RCW factory registries, managed CCW creation, unwrap helpers, cast helpers, and `DerivedComposed` marker landed in Runtime 1.13.
- [x] Inventory C: close the remaining marshaler infrastructure from `.cswinrt/src/WinRT.Runtime/Marshalers.cs` and `FundamentalMarshalers.cs` by providing a single runtime marshaling model for interfaces, inspectable/object values, blittable/value types, arrays, and generic projections rather than relying on generator-specific inline special cases. This is now closed by `Marshalers.kt` plus the targeted `MarshalersTest`; the remaining `GuidGenerator.cs` / `TypeExtensions.cs` responsibilities are tracked with the type-name and projection-registry slice in Runtime 1.15 / Inventory D rather than with the universal marshaler core itself.
- [x] Inventory D: close the remaining projection registry and type-mapping infrastructure from `.cswinrt/src/WinRT.Runtime/Projections.cs`, `Projections.CustomTypeMappings.g.cs`, `Projections.CustomTypeMappings.EventHandler.cs`, and `TypeNameSupport.cs`, including projection assembly registration, runtime-class-name lookup, custom mapped ABI/helper types, and non-WinRT runtime class name lookup hooks. This is now closed by the JVM-side `Projections.kt`, `TypeNameSupport.kt`, `TypeExtensions.kt`, `GuidGenerator.kt`, runtime annotation metadata, and `ProjectionRegistryTest`; later system/value/event/XAML projections stay queued under Inventory G through Inventory K instead of this registry slice.
- [x] Inventory E: close the remaining generic collection projection layer from `.cswinrt/src/WinRT.Runtime/Projections/IEnumerable*`, `IList*`, `IReadOnlyList*`, `IDictionary*`, `IReadOnlyDictionary*`, `CollectionHybrid.net5.cs`, and `KeyValuePair.cs`, including RCW/CCW helper registration, managed-to-ABI collection adapters, and collection helper factories that generator slices can depend on without inventing new runtime contracts. The collection helper layer itself is now present in `winrt-runtime`; any remaining typed registration / wrapper-factory gaps are tracked under Inventory B, and any remaining universal value/interface marshaling gaps are tracked under Inventory C.
- [x] Inventory F: close the remaining bindable/non-generic collection projection layer from `.cswinrt/src/WinRT.Runtime/Projections/Bindable*`, including `IBindableIterator`, `IBindableIterable`, `IBindableVector`, `IBindableVectorView`, object marshalling, non-generic enumerable/list adapters, and bindable helper factories. The remaining higher-level object/value projection work is now explicitly the broader `Marshalers.cs` / `IPropertyValue` queue in Inventory C and Inventory G, not Bindable* helper ownership.
- [x] Inventory G: close the remaining value-boxing and system projection layer from `.cswinrt/src/WinRT.Runtime/Projections/Nullable.cs`, `IReferenceArray*`, `IPropertyValue*`, `Type.cs`, `Type.ArrayMarshalling.cs`, `SystemTypes.cs`, `Uri.cs`, `Numerics.cs`, and `Geometry.cs`, including `IReference<T>`, `IReferenceArray<T>`, property-value semantics, WinRT type projections, and system/numerics custom projections. This is now closed by the shared JVM owner split across `ValueBoxing.kt`, `SystemProjections.kt`, `ComWrappersSupport.kt`, `WinRtBindableProjections.kt`, and `TypeNameSupport.kt`, with targeted Windows coverage for boxed scalar/array/system values plus boxed enum/type/HResult round-tripping.
- [ ] Inventory H: close the remaining interop surface from `.cswinrt/src/WinRT.Runtime/Interop/*`, `AgileReference.cs`, `WeakReference.netstandard2.0.cs`, `Context.cs`, `MonoSupport.cs`, and `Module.cs` / `Platform.cs`, including agile references, weak references, context callbacks, marshal interfaces, activation-factory interop, IID coverage, standard delegate ABI helpers, and remaining platform helpers. Runtime 1.16 closes the JVM baseline owner convergence for IID coverage, activation-factory MTA usage, agile/GIT fallback, weak-reference consumers, context callbacks, Mono no-op ownership, and the low-level platform entry points; the remaining open work here is the deeper COM-aggregation/`IMarshal` story plus later standard-delegate/event helpers, not another round of duplicated IID/platform helper logic.
- [ ] Inventory I: close the remaining exception and restricted-error-info surface from `.cswinrt/src/WinRT.Runtime/ExceptionHelpers*`, `Interop/ExceptionErrorInfo*`, and related plumbing so WinRT-originated exceptions, managed exception wrapping, and restricted error info follow the same ownership/reporting model as `.cswinrt`. Runtime 1.16 closes the JVM baseline for HRESULT classification, restricted-error-info reads, managed `IErrorInfo` fallback hosts, and CCW stub `SetErrorInfo` propagation; the remaining open work is the CLR-specific language-exception / propagation-context path that cannot be mirrored directly on Kotlin/JVM without a separate platform adaptation layer.
- [ ] Inventory J: close the remaining delegate/event runtime infrastructure from `.cswinrt/src/WinRT.Runtime/DelegateExtensions.cs`, `EventRegistrationToken.cs`, `EventRegistrationTokenTable{T}.cs`, `Interop/EventSource*`, `Interop/StandardDelegates.cs`, and `Projections/EventHandler.cs`, including token tables, event-source caches, helper delegates, standard delegate ABI helpers, and event-source state management beyond the current minimal delegate ABI bridge.
- [ ] Inventory K: close the remaining XAML/system projection layer from `.cswinrt/src/WinRT.Runtime/Projections/ICommand*`, `ICustomPropertyProvider*`, `INotifyPropertyChanged*`, `INotifyCollectionChanged*`, `INotifyDataErrorInfo*`, `PropertyChangedEventHandler.cs`, `PropertyChangedEventArgs.cs`, `NotifyCollectionChangedEventHandler.cs`, `NotifyCollectionChangedEventArgs.cs`, `NotifyCollectionChangedAction.cs`, `DataErrorsChangedEventArgs.cs`, `IServiceProvider*`, and `IStringable.cs`.
- [x] Inventory L: close the remaining disposable/system projection layer from `.cswinrt/src/WinRT.Runtime/Projections/IDisposable.net5.cs`, `IDisposable.netstandard2.0.cs`, and related `IClosable`/`IDisposable` adapter behavior so system-disposable projection is owned by `winrt-runtime` rather than by ad hoc generator rules. This is now closed by the `AutoCloseable` <-> `Windows.Foundation.IClosable` custom mapping and the shared `WinRtClosableObject` / `ComWrappersSupport` / `SystemProjections` ownership in Runtime 1.17.
- [ ] Inventory M: close the remaining runtime attributes, feature switches, configuration, and runtime-message surfaces from `.cswinrt/src/WinRT.Runtime/Attributes.cs`, `AttributeMessages.net5.cs`, `Configuration/FeatureSwitches.cs`, `Interop/WuxMuxProjectedTypeAttribute.cs`, `WinRTRuntimeErrorStrings.Designer.cs`, `WinRTRuntimeErrorStrings.resx`, and the localized `ResX/*` resources so later runtime/generator/authoring slices inherit the same metadata/configuration/error-message hooks as `.cswinrt`.

## JVM Primary Path

- [x] Establish the JVM-first Gradle multi-module structure with the Kotlin top-level module names `winrt-runtime`, `winrt-metadata`, `winrt-authoring`, `winrt-projections`, and `winrt-samples`, and wire it into settings.gradle.kts
- [ ] Migrate the existing Kotlin package layout to `io.github.kitectlab.winrt` (正在做: new modules use the canonical package root; legacy `sample-jvm-winui3` is out of current scope unless explicitly requested)
- [ ] Create the `winrt-runtime` module and land the JVM runtime foundation corresponding directly to `.cswinrt/src/WinRT.Runtime` (正在做: Runtime 1.1 through Runtime 1.17 now cover the baseline ABI/object/activation path, the formal JVM `IWinRTObject` contract, the generic collection projection helper layer, the Bindable* runtime projection helper layer, the `.cswinrt`-equivalent RCW/CCW registration/object-wrapper layer, the shared marshaler core, the projection/type-name registry slice, the interop/error-info/platform owner convergence slice for IID catalog coverage plus agile/weak/context helpers, and the shared value-boxing/system-projection owner set. The active blocker now moves to the later event/XAML slices in Runtime 1.18 through Runtime 1.19 and the remaining runtime inventory, while the JVM-specific residual for CLR exception CCWs and automatic FTM/`IMarshal` aggregation stays explicitly documented instead of hidden in ad hoc helper drift.)
- [ ] Finish the earliest `winrt-runtime` prerequisite slices in queue order before taking on broader metadata or projection work (正在做: Runtime 1.1 through Runtime 1.17 are no longer treated as sufficient to unlock generator growth; Runtime 1.18 through Runtime 1.19 plus the remaining runtime inventory are the active prerequisite queue, and metadata/generator/projection growth stays frozen behind those remaining `.cswinrt/src/WinRT.Runtime` gaps.)
- [ ] Freeze expansion of later runtime-adjacent slices until the remaining runtime prerequisites are explicitly closed (正在做: metadata, generator, projections, authoring, and samples are limited to maintenance while Runtime 1.10 through Runtime 1.19 and the remaining runtime inventory stay open.)
- [ ] Create the `winrt-metadata` module and land the JVM metadata foundation aligned with the metadata-loading responsibilities inside `.cswinrt/src/cswinrt` (正在做: Metadata 2.1 through Metadata 2.4 are closed with a JVM-side PE/CLI metadata reader, canonical path deduplication, deterministic normalization, core type-shape fidelity, and member/signature modeling for methods, properties, and events; metadata is now in maintenance mode while runtime-first parity returns to `.cswinrt/src/WinRT.Runtime`.)
- [ ] Keep `winrt-metadata` work behind the active runtime prerequisite queue and focus it on real WinMD ingestion before richer projection-shape expansion
- [ ] Keep `winrt-metadata` in ingestion-first then fidelity-second order (正在做: ingestion, deterministic normalization, core type-shape fidelity, and member/signature completeness are now in place, and the active fidelity work is limited to ABI-facing semantics such as enum underlying types/members that unblock generator consumption rather than richer projection helpers)
- [ ] Create the generator surface aligned with `.cswinrt/src/cswinrt` while keeping Kotlin tests inside the owning modules (正在做: the metadata -> projection-plan -> KotlinPoet generator has reached a useful declaration/member baseline, but the previously next slice `collection-parameter parity` plus bindable collection ABI categories is now explicitly frozen. Current generator work is limited to maintenance and validation until runtime-side `.cswinrt/src/WinRT.Runtime` parity lands for `IWinRTObject`, typed RCW/CCW registration, generic collection projections, and bindable projections.)
- [ ] Keep generator work in declaration-first then member-emission order rather than expanding whichever projection file currently exists in `winrt-projections`
- [ ] Freeze handwritten projection growth until the existing generator skeleton can reproduce a representative standard WinRT API slice such as `Windows.Data.Json` through the planned metadata/generator path
- [ ] Create the `winrt-authoring` module and land the JVM authoring and hosting foundation corresponding directly to `.cswinrt/src/Authoring` (正在做: module exists, host/source-generator parity still missing; do not design this from sample pressure or failing tests)
- [ ] Keep `winrt-authoring` on boundary-first design (正在做: the module is still only a placeholder, so the next work must map hosting/lifetime/activation boundaries from `.cswinrt/src/Authoring` before any convenience API or sample glue appears)
- [ ] Create the `winrt-projections` module and wire deterministic generated output corresponding directly to `.cswinrt/src/Projections` (正在做: module is wired into the root Gradle build and `KotlinProjectionGeneratorTest` still carries a `Windows.Data.Json` generator regression fixture, but projection growth is frozen behind the remaining runtime-first parity queue. The current checked-in generator output is treated as validation scaffolding until `winrt-runtime` lands the remaining `.cswinrt` contracts for projection/type-name registry parity, value projections, and the later event/XAML/system runtime slices.)
- [ ] Limit `winrt-projections` work to generator-produced output for already-implemented slices; do not grow handwritten projection breadth
- [x] Delete handwritten standard WinRT API projection files such as `Windows.Data.Json` when they drift into `winrt-projections`; only generator output or the narrow `.cswinrt/src/Projections`-style handwritten glue slices may remain there
- [ ] Create the `winrt-samples` module and wire the JVM sample validation surface corresponding directly to `.cswinrt/src/Samples` (正在做: module is wired into the root Gradle build; sample work is intentionally held to bootstrap-level scaffolding until generator-produced standard API projections exist, so the module does not keep handwritten standard WinRT projection-driven smoke code.)
- [ ] Keep `winrt-samples` on smoke-first validation only until the upstream runtime/generator/authoring prerequisites are ready (正在做: the current module is intentionally minimal and should not expand into broader WinUI, hosting, or standard API smoke scenarios until the upstream generated projection contracts exist)
- [ ] Keep sample work to the smallest validation surface for the currently completed slice before any broader WinUI sample expansion
- [ ] Place tests in the relevant module source sets instead of creating a separate top-level tests module
- [ ] Rename or delete legacy top-level modules that do not match the strict `.cswinrt/src` names (正在做: legacy `sample-jvm-winui3` may remain on disk for reference, but it is not part of the active refactor target)
- [ ] Complete WinUI 3 startup, bootstrap, resource, and message-loop validation through the `winrt-samples` module and per-module test layouts

## .cswinrt Alignment Slices

- [ ] Align COM/WinRT marshaling, HRESULT translation, and lifetime management (正在做: Runtime 1.1 through Runtime 1.3 are closed for ABI primitives, COM/WinRT init lifecycle, Windows loader/proc bindings, semantic HRESULT translation, and baseline object-reference ownership/query/identity behavior; the remaining work in this parity slice now shifts to later activation, generic, delegate, and collection-dependent mechanics.)
- [ ] Align activation factory lookup and runtime-class activation semantics (正在做: Runtime 1.4 is closed for JVM-side `RoGetActivationFactory`, manifest-free fallback probing, activation factory caching, and direct runtime-class activation helpers; the current KMP split now also pushes the manifest-free fallback owner (`DllModule` plus the fallback probing path) and the activation-factory cache/lookup core into `commonMain` against raw loader/proc/`IUnknown` seams so the remaining activation/reference commonization can build on a shared lookup core. Runtime 1.9 also adds WinUI-specific activation consumers for Xaml metadata provider initialization, while broader authoring/reference-tracking parity still remains for later slices.)
- [ ] Align `IWinRTObject`, typed RCW/CCW registration, and runtime projection lookup behavior (正在做: Runtime 1.10 plus Runtime 1.13 now cover the JVM-side `IWinRTObject` contract, query-interface caches, additional type data, `GetObjectReferenceForType`, `IsInterfaceImplemented`, typed RCW registration, helper-type indirection, runtime-class-name wrapper lookup, managed CCW creation, unwrap/cast helpers, and the `SingleInterfaceOptimizedObject` model. The current KMP split now also lifts the `IWinRTObject` cache/additional-type-data/finalization support into `commonMain` through a generic owner so later reference-layer commonization no longer depends on a JVM-only state store. The shared marshaler core is closed by Runtime 1.14 and the projection/type-name registry slice is closed by Runtime 1.15; the remaining work in this broader runtime-projection-lookup story now moves to the later value/event/XAML/runtime-system slices rather than to more object-wrapper or registry glue.)
- [ ] Align generic interface projection, parameterized IID, and type-signature behavior (正在做: Runtime 1.5 is closed for primitive/enum/struct/delegate/runtime-class/parameterized signature rendering and parameterized IID hashing with cswinrt parity samples; remaining work now shifts to consuming those signatures in later delegate, collection, and generator slices rather than to the signature primitives themselves.)
- [ ] Align delegate bridges, delegate handles, and callback marshaling (正在做: Runtime 1.6 is closed for JVM-side delegate descriptors, parameter-kind decoding, real `IUnknown + Invoke` ABI stub generation, AddRef/Release/QueryInterface lifetime behavior, HRESULT-safe callback dispatch, explicit delegate return-kind descriptors, and native delegate reference invocation/readback for the currently supported scalar/string/reference return categories; remaining work now shifts to consuming these delegate primitives in later collection, async, and generator slices rather than to the delegate ABI boundary itself.)
- [ ] Align collection projection for iterable/list/map/view adapters (正在做: Runtime 1.11 closed the generic collection helper layer for `IEnumerable` / `IList` / `IReadOnlyList` / `IDictionary` / `IReadOnlyDictionary`, including JVM-side `CreateMarshaler` / `FromManaged` / `FromAbi` plus `ToAbiHelper` / `FromAbiHelper`. The non-generic bindable layer is also closed by Runtime 1.12, the shared marshaler core is closed by Runtime 1.14, and the higher-level boxed value/system projection owner is closed by Runtime 1.17; remaining work in the broader parity story now shifts to later event/XAML slices rather than to more collection or value-boxing helper ownership.)
- [ ] Align bindable collection projection and non-generic object marshalling (正在做: Runtime 1.12 has now landed the Bindable* runtime projection layer in `winrt-runtime`, including IBindable object references, non-generic iterable/list helpers, and bounded inspectable/object round-tripping for bindable collections. The shared universal object/value marshaler core is closed by Runtime 1.14, and the higher-level `IPropertyValue` / value-boxing / system projection owner is now closed by Runtime 1.17; the remaining gaps move to the later event/XAML/system-service slices rather than more Bindable* or boxed-value scaffolding.)
- [ ] Align async projection, task bridging, and completion-callback semantics (正在做: Runtime 1.8 is closed for `IAsyncInfo`/`IAsyncAction`/`IAsyncOperation<T>` wrapper coverage, async status/error/cancel/close semantics, completed-handler registration via delegate ABI stubs, parameterized async interface/handler IID helpers, and JVM-side `CompletableFuture` bridging; later work can consume these runtime primitives for generator-authored await/task surfaces and progress-enabled async variants.)
- [ ] Align authoring support, projected-object bridges, and hosting support
- [ ] Align WinUI-specific type mappings, Xaml metadata, and bootstrap behavior (正在做: Runtime 1.9 is closed for Windows App SDK bootstrap discovery/initialize/shutdown hooks, `Microsoft.UI.Xaml.XamlTypeInfo.XamlControlsXamlMetaDataProvider` activation/initialize helpers, `IXamlMetadataProvider` full-name lookup access, and baseline `IReferenceTracker` lifecycle integration on `ComObjectReference`; later work can consume these hooks from authoring/generator/sample layers rather than reimplementing them there.)

## Generation And Validation

- [ ] Establish deterministic generator-output validation and regression checks after the corresponding `.cswinrt`-derived generator contracts are explicit
- [ ] Establish a targeted JVM unit-test and integration-test matrix that validates agreed runtime/generator contracts instead of defining them
- [ ] Establish a Windows-first verification flow that prioritizes affected modules
- [ ] Keep validation breadth aligned with the ordered slice queue so that smoke, unit, generator-regression, authoring, and sample coverage expand only after their owning contracts are explicit

## mingwX64 Parity

- [ ] After the matching JVM slices are complete, define the runtime parity plan for `mingwX64` under the shared contracts
- [ ] Create `sample-mingw-winui3` or an equivalent native validation module
- [ ] Add validation coverage that proves `mingwX64` behavior matches JVM semantics
