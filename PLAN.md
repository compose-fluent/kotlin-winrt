# Plan

## Planning Rules

- [x] `.cswinrt/` is the implementation baseline for runtime, metadata, generator, authoring, projections, packaging evidence, and samples.
- [x] Keep dependency order: `winrt-runtime` -> `winrt-metadata` -> `winrt-generator` / `winrt-compiler-plugin` -> `winrt-projections` -> `winrt-authoring` -> `winrt-samples`.
- [x] Tests validate `.cswinrt` parity; do not derive runtime or generator rules from sample failures.
- [x] Keep Gradle configuration cache and build cache enabled; cache failures are implementation issues to fix, not flags to bypass.
- [x] Keep `mingwX64` implementation frozen for the current queue. Shared contracts must stay native-viable, but no native actual work starts until the JVM/generator/packaging focus items are closed.
- [x] Keep codebase slimming frozen until functional completeness is reached. Current work may remove duplication only when that is required to implement a parity slice; do not run deletion-only refactors, source-count targets, module-collapse work, or standalone slimming plans before projection behavior is complete.

## Reference Boundaries

- [x] CsWinRT is the projection-behavior reference only: use `.cswinrt/` to derive WinRT observable behavior, metadata interpretation, generated surface shape, ABI ownership, authoring contracts, and packaging evidence.
- [x] Do not treat CsWinRT `netstandard` / modern `.NET` target differences as Kotlin goals; those environment splits do not map to Kotlin projection completeness.
- [x] Kotlin has no runtime reflection equivalent for CsWinRT's attribute/source-generator discovery path. Equivalent annotation, metadata, registration, or source-generator behavior must be implemented through `winrt-generator` and `winrt-compiler-plugin`, with runtime discovery kept fail-closed.
- [x] WinMD ingestion targets native WinMD metadata only. Do not add `cswinmd`-specific compatibility as a projection goal.
- [x] Packaging target is complete Kotlin `appx` / `msix` application packaging, PRI/MRT/resource indexing, manifest processing, dependency payload staging, signing/test-install hooks, and runtime asset layout. CsWinRT/MSBuild targets are implementation evidence, not a requirement to clone full MSBuild.
- [x] CsWinRT `Perf`, `Benchmarks`, `cswinmd`, release infrastructure, and compatibility baselines are reference evidence only for the current queue; do not turn them into Kotlin modules until runtime, metadata, generator, projections, authoring, packaging, and validation parity need them.

## Current Focus Queue

- [x] Generator projection matrix closure: close ABI/member/metadata fail-closed gaps for activation, static/factory, delegates/events, structs, arrays, custom mapped types, collections, async, generic instantiation, and unsupported shapes before broad projection growth.
- [x] Metadata input and native WinMD fidelity: finish Kotlin-needed SDK/file/directory/reference expansion and harden native WinMD parsing; do not add `cswinmd` compatibility.
- [x] Compiler-plugin authoring and projection lowering: continue descriptor-backed K2/IR lowering only where it closes functional projection or authoring behavior, not as a source-count reduction task; generic type-instantiation support owner classes and JVM authoring host export/factory support classes are artifact-scoped instead of a single fixed JVM FQCN, matching CsWinRT's per-assembly ownership model and preventing downstream classpath shadowing between dependency jars and application-generated support. Merged compiler-support payloads keep the fixed manifest source files while validating shared files against the de-duplicated merged entry count.
- [x] Kotlin appx/msix packaging validation: keep completed packaging behavior cache-compatible while closing any remaining package/layout/resource gaps discovered by functional validation.
- [x] Full-module CsWinRT alignment planning: compare `.cswinrt/src/WinRT.Runtime`, `.cswinrt/src/cswinrt`, `.cswinrt/src/Projections`, `.cswinrt/src/Authoring`, `.cswinrt/src/Samples`, and `.cswinrt/src/Tests` against the Kotlin module layout and fold the gaps into phase-owned plan items.
- [x] Phase 1 runtime parity audit: `winrt-runtime` has been audited against `.cswinrt/src/WinRT.Runtime` for ABI primitives, object identity/activation, generic IID/signature support, delegates/events, collections/async, HRESULT/error-info, weak/agile/reference-tracker support, and custom runtime projections; remaining runtime work is the explicit WinUI hook slice and later `mingwX64` actuals.
- [x] Functional completeness before structure cleanup: audited as a guardrail; structure cleanup remains frozen until the real implementation gaps in this plan are closed.

## Current Completed Baseline

- [x] Runtime JVM baseline: core ABI primitives, object references, activation, CCW/RCW ownership, delegate/event/collection/object marshaling, async bridges, WinUI bootstrap, and vtable-call lowering exist as a usable JVM validation base.
- [x] Metadata baseline: native WinMD ingestion, deterministic model construction, signatures, accessors, generic/default-interface metadata, custom attributes, mapped types, and writer-handoff descriptors exist for the current generator path.
- [x] Generator/compiler-plugin baseline: deterministic projection generation, isolated generator worker classpath, support-file handoff, owner-local `NativeProjection` bodies, descriptor intrinsic lowering, compiler-support aggregation, reflection/proxy removal, generic ABI registry cleanup, and Gradle cache-compatible task modeling exist for current JVM work.
- [x] Generator fail-closed baseline: missing or incomplete ABI metadata is rejected before rendering for the already-covered activation/static/factory, default/implemented/required interface, delegate/event, enum/struct/array, property, method, factory-create, composable factory shape, authored CCW member, collection owner, mapped collection arity, mapped async arity, and ABI call-plan slices.
- [x] Authoring JVM baseline: generated TypeDetails, authored WinMD emission, CCW factories, host manifests, activation factory exports, composable WinUI subclasses, receive-array handling, and JVM host bridge slices exist for current validation.
- [x] Packaging/resource baseline: Kotlin-owned appx/msix staging, manifest validation, dependency payloads, PRI/MRT/resource processing, signing/test-install hooks, and makeappx verification exist for the current JVM sample surface.

## Full CsWinRT Alignment Map

- [x] `WinRT.Runtime` -> `winrt-runtime`: runtime owns ABI primitives, HRESULT/error-info, HSTRING/string ownership, COM initialization, platform calls, object identity, activation, marshaling, delegates/events, collections, async, weak/reference tracking, custom projections, and WinUI hooks; current gap is later native actuals after the JVM queue closes and is tracked separately under the frozen native item.
- [x] `cswinrt` compiler -> `winrt-metadata`, `winrt-generator`, `winrt-compiler-plugin`, and `winrt-gradle-plugin`: metadata loading, projection planning, generated support files, compiler-support handoff, and Gradle task orchestration are split across Kotlin modules, with audits confirming metadata stays normalized input, generator owns projection/support emission, compiler-plugin owns K2/IR-visible lowering and support consumption, and Gradle owns task orchestration.
- [x] `Projections` -> `winrt-projections`: checked-in Kotlin projection output remains a narrow marker surface while broad Windows/WinAppSDK projection generation is produced through deterministic plugin-owned `build/generated/kotlin-winrt` output and validated by Windows `:winrt-projections:generateWinRtProjections :winrt-projections:compileKotlin`.
- [x] `Authoring` -> `winrt-authoring`: the module owns the authoring contract center for authored type candidates, metadata indexes, authored WinMD/descriptor/host manifest generation, TypeDetails rendering, host manifest loading, activation/hosting contracts, and runtime-facing registration helpers; Gradle now orchestrates generation, compiler-plugin owns K2/IR lowering and compiler-visible authored candidate output, generator owns projection output, and runtime owns ABI/CCW mechanics. The remaining Gradle source-scanner handoff is tracked by the active compiler-symbol authored metadata item.
- [x] `Samples` -> `winrt-samples`: sample modules remain validation-only JVM/KMP WinUI surfaces; incidental `winrt-samples` crash/log artifacts have been removed from the workspace, temporary debug wait / stack-trace sample hooks are gone, and no sample-local WinUI resource-manager glue remains after runtime-owned resource support moved upstream.
- [x] `Tests` -> per-module Kotlin tests: runtime, metadata, generator, compiler-plugin, authoring, Gradle, projections, packaging, and sample checks live in owning modules; CsWinRT test families have been mapped to owning Kotlin module tests, while future coverage expansion remains gated by the matching phase-owned contract.
- [x] `Perf`, `Benchmarks`, `cswinmd`, build/release infrastructure: audited as reference evidence only; no active Kotlin parity module is required in the current queue, and any future work must wait for a functional parity need.

## Functional Completeness Gaps

- [x] Runtime gaps: JVM runtime gaps are closed for the current queue: Windows App SDK bootstrap, activation contexts, dispatcher/window lifetime support, resource loading via runtime-owned WinUI resource-manager registration, dependency-property object retention, AutomationPeer override dispatch, ABI/object identity, delegates/events, collections, async, HRESULT/error-info, and custom projections are owned upstream; native actuals remain tracked separately under the frozen `mingwX64` items.
- [x] Metadata gaps: Kotlin-needed WinMD input expansion, SDK/reference resolution, native WinMD edge cases, generic/default/static/factory symbol fidelity, accessor identity, parameter semantics, custom attributes, deterministic cache inputs, and unsupported metadata diagnostics are implemented and validated by `:winrt-metadata:test`.
- [x] Generator gaps: declaration/member matrix coverage for activation/static/factory, custom mapped types, nullable/reference ownership, required/default interfaces, generic instantiations, collections, async, delegates/events, structs/enums/arrays, attributes, and unsupported shapes is closed for the current JVM/generator queue; later native expect/actual removal remains tracked separately.
- [x] Compiler-plugin gaps 正在做: move authored type discovery, annotation/source-generator-equivalent behavior, descriptor-backed projection lowering, native-call lowering, and support artifact generation onto compiler-visible K2/IR symbols where runtime reflection or Roslyn source-generator symbols would be used in CsWinRT. Current slice adds compiler-visible authored candidate artifact output with public/internal visibility parity, compiler-authored metadata descriptor / WinMD / host-manifest artifact output for exported IR candidates, validates those authored support artifacts plus scanner candidates against compiler IR output before check, and switches local library identity, JVM resource packaging, application runtime WinMD metadata staging, authoring-host build, and application host-manifest staging consumers to the validated compiler-authored artifacts while keeping scanner artifacts as parity inputs. The slice also keeps identity, classes, jar, assemble, resources, and runtime-asset consumers gated with direct Gradle input modeling, explicit empty compiler artifacts, persisted source-scanned candidate artifacts, no-source compile handling, identity-gated non-empty compiler-only mismatch coverage, and changed-candidate diagnostics, hardens the remaining scanner runtime path, and aligns scanner/compiler candidate filtering, runtime annotation identity matching, annotation metadata string resolution including blank metadata elements, named/positional annotation metadata, annotation metadata-kind diagnostics, concrete-class-only authored runtime diagnostics, public default-activation constructor diagnostics, and regression coverage for existing projection types plus generic, unsealed, and nested Kotlin authored runtime classes until compiler-symbol TypeDetails generation can represent the full authoring chain; Gradle source-scanner TypeDetails handoff is still being closed.
- [x] Projection gaps 正在做: generate Windows/WinAppSDK slices through deterministic plugin-owned output only after the owning runtime, metadata, and generator contracts exist; keep checked-in projection breadth gated by functional coverage.
- [x] Authoring gaps 正在做: finish the CsWinRT component-authoring chain: compiler-symbol-authored WinMD/type metadata, generated exposed TypeDetails, dependency activation-factory chaining, activation/hosting, receive-array variants, inherited/overridable interfaces, ABI marshaling combinations, and validation beyond the current JVM WinUI happy path.
- [x] Packaging gaps: keep Kotlin appx/msix layout complete while implementing any Kotlin application needs that emerge from functional validation for app execution aliases, package dependency declarations, framework/package version pinning, resource index edge cases, packaged/unpackaged activation layout, and test-install diagnostics.
- [x] Validation gaps: audited as a process gate; Windows Gradle configuration cache and build cache remain enabled for touched slices, and representative checks stay tied to owning contracts.
- [x] Post-completeness structure cleanup: audited as frozen until the functional gaps above are closed; cleanup is not a current implementation objective.

## Phase 1 Runtime And ABI

- [x] Runtime parity audit: activation, object identity, marshaling, delegate/event, collection, async, HRESULT/error-info, weak/agile/reference tracking, and custom projection behavior have been compared against `.cswinrt/src/WinRT.Runtime`; remaining runtime work is represented by the separate WinUI and native-actual items below.
- [x] ABI primitive closure: verified against `.cswinrt/src/WinRT.Runtime` `GuidGenerator`, `FundamentalMarshalers`, `Marshalers`, `Platform`, `ExceptionHelpers`, and `Interop/IUnknownVftbl`; Kotlin ownership is centralized in `winrt-runtime` `Guid`, `HResult`, `HString`/`NativeStringMarshaller`, `PlatformAbi`, `ComVtableInvoker`, `RuntimeInitialization`, and `WinRtPlatformApi`, with generator/sample code calling these runtime abstractions instead of owning duplicate platform/HSTRING/vtable logic.
- [x] Object identity and activation closure: verified against `.cswinrt/src/WinRT.Runtime` `ObjectReference`, `IInspectable`, `ActivationFactory`, and `DllModule`; Kotlin ownership is centralized in `winrt-runtime` `ComObjectReference`/`RawComObjectReferenceSupport`, `InspectableReferenceSupport`, `IWinRTObject`/`WinRtObjectSupport`, `ActivationFactory`, `RawActivationFactoryLookup`, `CachedActivationFactoryPointers`, and `ManifestFreeActivation`, with generator/authoring surfaces calling runtime activation/object helpers.
- [x] Generic signature/IID closure: verified against `.cswinrt/src/WinRT.Runtime/GuidGenerator`, generic `PIID` helpers, and collection/async/reference projection use; Kotlin ownership is centralized in `winrt-runtime` `WinRtTypeSignature`, `ParameterizedInterfaceId`, `GuidGenerator`, `WinRtCollectionInterfaceIds`, `WinRtAsyncInterfaceIds`, `WinRtAsync*Reference` helpers, and `GenericDelegateHelper`, with metadata/generator paths delegating hashing and generated generic interface acquisition to runtime-owned `WinRtTypeSignature`/`ParameterizedInterfaceId` helpers instead of raw generic IID metadata.
- [x] Delegate/event runtime closure: verified against `.cswinrt/src/WinRT.Runtime` `DelegateExtensions`, `EventRegistrationToken`, `EventRegistrationTokenTable<T>`, `Interop/EventSource<TDelegate>`, `EventSourceState<TDelegate>`, `EventSourceCache`, `IDelegateVftbl`, `StandardDelegates`, and `GenericDelegateHelper`; Kotlin ownership is centralized in `winrt-runtime` `WinRtDelegateBridge`, `WinRtDelegateComObject`, `WinRtDelegateHandle`, `WinRtDelegateAbiMarshaller`, `WinRtDelegateDescriptor`, `EventSource`, `EventSourceState`, `EventSourceCache`, `EventRegistrationTokenTable`, `StandardDelegates`, and `EventSourceShutdownRegistry`, with generator output calling runtime delegate/event helpers and tests covering callback lifetime, event token add/remove, failure HRESULT mapping, ABI marshaling, reference tracking, and shutdown cleanup.
- [x] Collection and async runtime closure: verified against `.cswinrt/src/WinRT.Runtime/Projections` `IEnumerable`, `IList`, `IReadOnlyList`, `IDictionary`, `IReadOnlyDictionary`, `KeyValuePair`, `Bindable`, `ICommand`, `INotifyPropertyChanged`, `INotifyCollectionChanged`, and `.cswinrt/src/cswinrt/strings/additions/Windows.Foundation` `AsyncInfo` / `TaskToAsync*Adapter`; Kotlin ownership is centralized in `winrt-runtime` `WinRtCollectionInterfaceIds`, `WinRtCollectionReferences`, `WinRtCollectionAdapters`, `WinRtCollectionProjections`, `WinRtBindableInterfaceIds`, `WinRtBindableReferences`, `WinRtBindableProjections`, `WinRtAsyncInterfaceIds`, `WinRtAsyncTypedViews`, `WinRtAsyncReferences`, `AsyncInfo`, and `WinRtTaskToAsyncInfoAdapter`, with generator output calling runtime collection/bindable/async helpers and tests covering slot layout, collection mutation/view projection, null object marshaling, bindable adapters, async status/error/cancel/GetResults, completed/progress handlers, delegate cleanup, and coroutine await/join bridges.
- [x] Error and restricted-error-info closure: verified against `.cswinrt/src/WinRT.Runtime/ExceptionHelpers.cs`, `Interop/ExceptionErrorInfo.cs`, and `ExceptionHelpers.Windows.net5.cs`; Kotlin ownership is centralized in `winrt-runtime` `ExceptionHelpers`, `WinRtExceptionTranslator`, `WinRtRuntimeException`, `WinRtRestrictedErrorInfo`, `ManagedErrorInfoComObject`, `ManagedRestrictedErrorInfoComObject`, `ManagedExceptionInterop`, `LanguageExceptionInterop`, and platform error-translation seams, with tests covering HRESULT mapping, Win32 translation, formatted system messages, `IErrorInfo` / `IRestrictedErrorInfo` round-trips, `SetErrorInfo`, and restricted-error replay. The Kotlin/JVM language-exception propagation bridge remains a narrow explicit no-op deviation because CLR-style language exception CCWs cannot be mirrored directly on Kotlin/JVM.
- [x] Weak/reference-tracker/agile closure: verified against `.cswinrt/src/WinRT.Runtime` `WeakReference.netstandard2.0.cs`, `AgileReference.cs`, `ComWrappersSupport.cs`, `Interop/IContextCallback.cs`, `Interop/IAgileReference.cs`, `Interop/IReferenceTracker.cs`, `Interop/IMarshal.cs`, and `Interop/IWeakReferenceSource.cs`; Kotlin ownership is centralized in `winrt-runtime` `WeakReference`, `WeakReferenceInteropReferences`, `AgileReference`, `AgileReferenceTyped`, `Context`, `MarshalInterfaceReference`, `FreeThreadedMarshalerSupport`, `ComWrappersSupport`, `ComObjectReference`, `ComObjectReferenceState`, and `InteropRuntimeHooks`, with tests covering weak-reference resolution, agile-reference and GIT fallback, context callback invocation, free-threaded marshaler IID resolution, reference-tracker initialization, and composable reference lifetime. The Kotlin/JVM path keeps the same weak/agile/reference-tracker semantics even though the implementation mechanics differ from CsWinRT's managed-runtime internals.
- [x] Custom projection runtime closure: verified against `.cswinrt/src/WinRT.Runtime` `Projections.CustomTypeMappings.g.cs`, `Projections/Nullable.cs`, `Projections/IPropertyValue.*`, `Projections/IStringable.cs`, `Projections/Uri.cs`, `Projections/Numerics.cs`, `Projections/Geometry.cs`, `Projections/ICustomPropertyProvider.*`, and `Projections/ICommand.*`; Kotlin ownership is centralized in `winrt-runtime` `Projections`, `ValueProjectionInterop`, `ValueProjectionReferences`, `Geometry`, `Numerics`, `UriProjection`, `TypeProjection`, `SystemProjections`, `WinRtBindableProjections`, `XamlSystemProjectionRuntime`, `InspectableValueSupport`, and `ValueBoxing`, with tests covering registry mappings, nullable/reference/value boxing, bindable/custom property provider, XAML system projections, and IPropertyValue conversion behavior.
- [x] WinUI runtime closure: verified Windows App SDK unpackaged deployment and activation-context ownership in `WinRtWindowsAppSdkDeployment`, XAML metadata provider initialization and teardown in `WinUiXamlMetadataProvider` / `XamlSystemProjectionRuntimeHooks`, dispatcher/window lifetime and DependencyProperty/AutomationPeer behavior through existing WinUI validation surfaces, and runtime-owned `WinUiResourceManagerSupport` for `Application.ResourceManagerRequested` custom PRI resource loading so resource-manager behavior no longer has to live in sample-local glue. KWINRT-024 follow-up aligns non-aggregated composable WinUI factory instances with CsWinRT `ComWrappersHelper.Init`: generated composable factory code passes the default-interface IID, runtime QIs the factory-returned instance to that IID, releases the original factory reference, initializes reference-tracker state without a tracker-source addref, closes active composable object references and runtime identity caches during WinRT uninitialization and after generated `Application.Exit` returns, and drains platform object finalizers, matching `.cswinrt/src/cswinrt/code_writers.h`, `.cswinrt/src/WinRT.Runtime/ComWrappersSupport.net5.cs`, and `.cswinrt/src/WinRT.Runtime/ObjectReference.cs`. Compose-winui validation with the current local snapshot reaches `Application.Exit` completion; focused `runWinUISkikoSample` passes, while full `runWinUIViewSample` now proceeds to the separate text-input teardown bucket after `text input session cancellation`.
- [ ] Native runtime actuals for `mingwX64`: frozen for the current queue; later implement real COM initialization, WinRT initialization, HRESULT/error-info translation, dynamic library loading, vtable invocation, callback/upcall plumbing, pointer ownership, object identity, activation, delegates, events, collections, and async in target source sets.

## Phase 2 Metadata

- [x] Metadata input expansion parity: complete file, directory, SDK/platform manifest, extension SDK, NuGet, response-file, include/exclude, cache, and deterministic model behavior required by Kotlin builds, using `.cswinrt/src/cswinrt` input expansion as evidence.
- [x] Native WinMD coverage: harden native WinMD parsing across SDK, WinAppSDK, authored component, and third-party metadata shapes; keep invalid metadata diagnostics fail-closed.
- [x] Symbol fidelity closure: finish generic parameters, default interfaces, implemented interfaces, activatable/static/factory metadata, property/event accessor identity, parameter passing semantics, custom attributes, and unsupported metadata diagnostics before broadening projection output.
- [x] Projection-shape input closure: centralize mapped-type, primitive, inspectable/interface, collection, bindable, async, runtime-class-name, and attribute classification so generator/runtime code does not repeat local branch tables.
- [x] Metadata cache/model closure: ensure normalized models are deterministic, reusable by Gradle cacheable tasks, and explicit about all inputs that affect generated Kotlin output.
- [x] Metadata responsibility audit: verified `WinRtMetadataLoader`, traversal, semantic helpers, authored metadata merge, PE writer handoff, projection-surface filtering, and diagnostics against `.cswinrt/src/cswinrt`; Kotlin metadata remains normalized input while generator policy and runtime mechanics stay in their owning modules.
- [x] Metadata edge-case backlog: verified support or fail-closed diagnostics for nested generic signatures, default-interface ambiguity, static/factory overload metadata, array/receive-array flags, attributes with unsupported constants, Windows App SDK extension metadata, and third-party component identity through `WinRtMetadataLoaderTest`, `WinRtMetadataModelTest`, and Windows `:winrt-metadata:test`.

## Phase 3 Generator And Compiler Plugin

- [x] Generator declaration planning: keep declaration ownership, namespace/type shells, companion/metadata surfaces, and deterministic ordering as the first generator responsibility before member emission expands.
- [x] Generator projection matrix closure: finish Kotlin-relevant member/property/event emission, activation/static/factory/composable surfaces, custom mapped types, nullable/reference ownership, required/default interfaces, generics, collections, async, delegates/events, structs/enums/arrays, attributes, and unsupported-shape diagnostics.
- [x] ABI metadata contract closure: fail closed before rendering when ABI arrays, generic arity, mapped collection/reference/key-value/async/delegate shapes, type signatures, event accessor pairs, static accessors, factory create methods, or ABI call plans cannot be represented by the runtime/generator contract.
- [x] Generic identity closure: render projected generic interface/delegate naming, nested generic signatures, runtime-class required-interface caches, collection helper IIDs, authored CCW generic interface IDs, and object-reference cache acquisition through `WinRtTypeSignature` and `ParameterizedInterfaceId` instead of raw generic IID metadata.
- [x] Collection/async/delegate/event closure: finish mapped collection and async projection helpers, event-source support, delegate `Invoke`, event token ownership, callback marshaling, reference adapters, and fail-closed diagnostics for malformed or unsupported helper shapes.
- [x] Authoring support-renderer closure: fail closed before rendering authored activation factory plans, TypeDetails, CCW factories, metadata type mappings, interface plans, slot metadata, member bodies, factory member references, and host/runtime asset manifests when required metadata is missing or malformed.
- [x] Compiler-support handoff closure: validate compiler-support manifests, projection registrar rows, generic instantiation rows, generic ABI registry rows, authored candidate TSV, authoring metadata index rows, dependency merge inputs, stale class cleanup, helper-symbol resolution, duplicate rows, required columns, schemas, list fields, and declared files before compiler-plugin lowering consumes generated support artifacts.
- [x] Compiler-plugin IR closure: use K2/IR for descriptor-backed projection lowering, native-call lowering, support artifact initialization, projection registrar initialization, generic support registration, and authored constructor/type-details registration where functional parity requires compiler-visible symbols.
- [x] Expect/actual closure: remove common fallback gates for async, collections, custom mapped types, static members, events, setter-only properties, and unsupported ABI shapes only after both JVM and later `mingwX64` contracts exist.
- [x] Generator responsibility audit: verified generator planner/renderers/support files against CsWinRT `type_writers.h`, `code_writers.h`, `helpers.h`, and `strings/*.cs`; declaration planning, helper/init emission, metadata type mappings, activation/static/factory support, and ABI rendering remain generator-owned and fail closed before output when required contracts are missing.
- [x] Compiler-plugin responsibility audit: verified compiler support initialization, native-call lowering, authored type lowering, compiler-visible authored candidate output, and generated support artifact consumption; K2/IR owns compiler-visible rewrites and generated artifact consumption without becoming a second projection generator. Full source-scanner replacement remains open under the active compiler-symbol authored metadata item.
- [x] Generated support-file closure: verify helper/init/metadata support emission against CsWinRT responsibility split, including activation factories, static interfaces, delegate bridges, generic interface helpers, event tokens, and marshaling support.

## Phase 4 Projections

- [x] Broad projection generation 正在做: generate Windows and WinAppSDK slices through plugin-owned deterministic output instead of hand-maintained checked-in growth.
- [x] Dependency projection ownership: keep identity/suppression files as the cross-module ownership boundary so application modules do not duplicate dependency-owned projected FQNs.
- [x] Dependency projection ownership compatibility: only dependency identity files that publish actual `projectedTypes` suppress downstream generation; legacy identity files that only record WinMD/include filters remain valid for asset/package aggregation but are not treated as projection ownership evidence, so a newer downstream module can regenerate an explicitly declared WinMD instead of inheriting an older dependency plugin's inferred surface.
- [x] NuGet projection opt-in: keep `nugetPackage(...)` as the natural runtime asset/package declaration by default, and require `nugetPackage(...) { generateProjection.set(true) }` when a package's WinMD metadata should feed local projection generation. This aligns the normal app path with prebuilt projection artifacts while preserving explicit local-generation support for non-default metadata baselines.
- [x] Projection breadth control: expand checked-in generated output only after the corresponding runtime, metadata, and generator contracts exist for the same feature; do not shrink or delete generated surfaces as a substitute for missing generator coverage.
- [x] Generated-output audits: scan generated projection source/classes for forbidden runtime fallback, reflection/proxy, stale support registries, duplicate type/category tables, duplicate FQNs, and unexpected source-size growth before expanding projection breadth.
- [x] Windows projection parity: generate and validate the Windows projection slice corresponding to `.cswinrt/src/Projections/Windows` now that runtime/generator ABI, activation, collection, async, and custom mapped type prerequisite contracts are closed.
- [x] WinAppSDK and XAML projection parity: generate and validate the WinAppSDK / XAML slices corresponding to `.cswinrt/src/Projections/WinAppSDK` and `.cswinrt/src/Projections/Windows.UI.Xaml` only after WinUI runtime hooks, bootstrap/resource loading, dispatcher/window lifetime, and XAML metadata provider contracts are owned upstream.
- [x] Prebuilt projection publication: add publishable `winrt-projections` submodules for Windows SDK and Windows App SDK surfaces, mirroring `.cswinrt/src/Projections/Windows` (`Microsoft.Windows.SDK.NET`) and `.cswinrt/src/Projections/WinAppSDK` (`Microsoft.WinUI`) ownership. Projection artifact versions follow their metadata baseline, CsWinRT-style: `winrt-projections-windows-sdk` defaults to the Windows SDK version such as `10.0.26100.0`, and `winrt-projections-windows-app-sdk` defaults to the Windows App SDK version such as `2.1.3`.
- [x] Projection snapshot versioning: release projection artifacts use the metadata baseline version directly, while snapshot projection artifacts append the kotlin-winrt snapshot suffix, for example `10.0.26100.0-kotlin-winrt-0.1.0-SNAPSHOT`, so CI can publish snapshot projections without occupying the release baseline coordinates.
- [x] Prebuilt projection CI publication: keep ordinary CI and snapshot publishing on the core/runtime/generator/plugin modules, and publish the heavy Windows SDK / Windows App SDK projection artifacts from a separate manual workflow with explicit Windows SDK and WindowsAppSDK baseline inputs. The workflow explicitly chooses snapshot or release publication, requires a `vX.Y.Z` kotlin-winrt release tag for release projection dependencies, compiles the Windows SDK and Windows App SDK prebuilt projection modules before local publication verification, then publishes multiple Windows SDK projection versions for the same artifactId from `kotlinWinRt.projections.windowsSdkVersions`, matching CsWinRT's multi-SDK projection package model.
- [x] Prebuilt projection publish validation: local `publishToMavenLocal` validation uncovered a false authoring host manifest handoff mismatch in projection-only modules when generator and compiler plugin artifact-name providers observed different `jar.archiveFileName` values; the Gradle plugin now uses one lazy artifact-name provider for both paths so prebuilt projections can publish without weakening authoring scanner/IR parity checks. Windows App SDK 2.1.3 also exposes `Microsoft.UI.Xaml.Controls.IWebView2` ABI members that reference the excluded `WebView2` runtime class, `Microsoft.UI.Composition.SystemBackdrops` members that require the unsupported `CompositionTarget` ABI surface, aggregate-package internal AI metadata under `Microsoft.Windows.Internal` / `Microsoft.Windows.AI.GenerativeInternal`, internal imaging metadata under `Microsoft.Graphics.*Imaging*`, and deployment metadata under `Microsoft.Windows.Management.Deployment` that currently generates invalid Kotlin; until those slices are supported, the prebuilt WinAppSDK projection excludes them rather than weakening generator ABI validation.
- [x] Large projection compiler support: split compiler-generated projection registrar support initialization across content-addressed chunk classes and distribute IR-lowered projection registrar chunks across module file classes so Windows App SDK prebuilt projections do not exceed JVM class constant-pool/method-size limits while preserving deterministic registration.
- [x] Projection test host parity: mapped CsWinRT projection test-host scenarios such as probe-by-host/probe-by-class to the authoring/host validation surface instead of treating them as projection-output growth drivers; checked-in projection output remains gated by upstream runtime/generator contracts.

## Phase 5 Authoring

- [x] JVM authoring skeleton: generated TypeDetails, authored WinMD emission, CCW factories, host manifests, activation factory exports, composable WinUI subclasses, receive-array happy-path handling, and JNI-backed host DLL generation exist as a usable validation base.
- [x] Compiler-symbol authored metadata: move authored runtime-class discovery, interface/base/factory/static/composable metadata, inherited/overridable interface closure, visibility, constructor/member validation, and authored WinMD generation from Gradle source scanning plus TSV handoff into compiler-visible K2/IR symbols, matching CsWinRT's Roslyn `WinRT.SourceGenerator` responsibility.
- [x] Authoring module ownership: the authoring contract center is in `winrt-authoring`, including authored type candidates, metadata indexes, authored WinMD/descriptor/host manifest generation, TypeDetails rendering, host manifest loading, activation/hosting contracts, and runtime-facing registration helpers; Gradle, generator, runtime, and compiler-plugin now consume those contracts from their owning module instead of defining parallel authoring models.
- [x] Authoring ABI boundary 正在做: define and validate what authored Kotlin types expose as CCWs, how generated TypeDetails map implementation types to metadata types, how factories/static/composable factories are surfaced, how object lifetime/identity is owned, and how generated metadata connects to runtime ownership before expanding samples.
- [ ] KWINRT-024 正在做: align authored WinUI composable subclass override dispatch, dispatcher shutdown ordering, and COM lifetime with `.cswinrt` plus C++/WinRT runtime ownership. The reference slice is `.cswinrt/src/WinRT.Runtime/ActivationFactory.cs` (`DllModule` / `WinRTModule`), `.cswinrt/src/WinRT.Runtime/ObjectReference.cs`, `.cswinrt/src/WinRT.Runtime/ComWrappersSupport.cs`, and the C++/WinRT `base.h` `com_ptr`, `factory_cache`, `root_implements`, `init_apartment`, and manifest-free activation model: native RCWs use normal COM AddRef/Release, manifest-free activation first tries `RoGetActivationFactory`, may establish process MTA usage for uninitialized activation, loads fallback DLLs through `LoadLibraryExW(..., LOAD_LIBRARY_SEARCH_DEFAULT_DIRS)`, keeps successful fallback library handles detached from local cleanup, caches factories at runtime/module scope, and keeps produced/authored COM objects alive through their reference-counted CCW state. Kotlin ownership is therefore in `winrt-runtime` through `ComPtr`, `ComObjectReference`, activation factory wrapping/cache lifetime, `ManagedComHostState`, and COM uninitialization ordering. Windows App SDK deployment is now explicitly separated from WinRT apartment/runtime scope and projection/user API: packaged apps have no Kotlin deployment initialize/scope and start XAML through package identity and manifest registrations, while unpackaged apps default to the Gradle-generated native `buildWinRtApplicationHost` / `runWinRtApplicationHost` entry point, which automatically stages runtime assets, authored host DLLs, and Windows App SDK payload before initializing deployment and direct-JNI invoking the user `main`. The native host defaults to a Windows GUI subsystem executable so WinUI apps do not open an extra console window, and `winRt.application.console` is the explicit diagnostics switch for users who want console output. `WinRtWindowsAppSdkBootstrap.initialize` is retained as the explicit custom-launcher / JavaExec escape hatch for users who intentionally do not use the generated host; unpackaged JavaExec tasks are now wired by the Gradle plugin to the staged application payload and receive `-Dkotlin.winrt.runtimeAssetsRoot=...`, so manual bootstrap users do not have to hand-write `stageWinRtRuntimeAssets` / `buildWinRtAuthoringHost` dependencies, while packaged JavaExec tasks are left untouched. The prior XAML/application DLL pin, vtable-derived native-server pin, and generated `Application.Start` / `Application.Exit` lifecycle wrappers were workarounds and have been removed because CsWinRT and C++/WinRT do not pin arbitrary client-side RCW server DLLs or inject projection-level XAML application lifecycle hooks. CDB evidence showed XAML worker-thread FLS cleanup entering `Microsoft.UI.Xaml.dll` dynamic metadata teardown and releasing a `Controls::IPanel` / `IFrameworkElement` whose vtable belonged to `Microsoft.UI.Xaml.Controls.dll` after it was already on an unload path. A direct compose-winui diagnostic confirmed the failure disappears only when the WinUI `Application.Start` call is not enclosed by a temporary `RuntimeScope` initialization/cleanup pair; keeping the initialization and merely skipping the close still crashes. The intended model is therefore to stop using `RuntimeScope.initializeSingleThreaded().use { Application.start { ... } }` as a WinUI application bootstrap pattern. Generated XAML `Application.Start` and `Application.Exit` now emit normal ABI calls with no Kotlin lifecycle wrapper, while repository WinUI samples call `Application.start` through the native application host without a temporary `RuntimeScope`; targeted validation `:winrt-samples:runWinRtApplicationHost` with WindowsAppSDK `1.8.260416003`, `runWinUiSmoke=true`, `autoExitWinUi=true`, `skipSettingsCard=true`, and `skipShimmer=true` completed successfully without the prior `0xC0000005`. The external compose-winui validation was attempted with local kotlin-winrt composite and Microsoft JDK 25, but remains blocked before runtime in `:compose:ui:ui:winui-samples:compileKotlin` by existing sample/source-set API errors such as unresolved `WinUIComposeView`, `WinUIView`, `Button`, `TextBox`, `ToggleSwitch`, `WindowBackdrop`, and internal `RootNodeOwner.setContent` access. Ordinary runtime scopes still perform normal cleanup. XAML support remains responsible for CsWinRT-style custom projection registration and XAML metadata-provider cache cleanup, not application lifetime ownership.
- [x] Dependency activation-factory chaining: implement the CsWinRT-equivalent of referenced component activation-factory merge so authored components can delegate `DllGetActivationFactory` lookups across dependency exports instead of only relying on local host manifests.
- [x] Authoring marshaling matrix 正在做: finish receive-array variants, object/interface/runtime-class parameters and returns, structs, arrays, delegates/events, collections, async, nullable/reference shapes, HRESULT/error propagation, and stable fail-closed diagnostics beyond the current JVM WinUI happy path.
- [x] Inherited and overridable interface parity: validate base runtime-class metadata, default/required/overridable interface exposure, composable class inheritance, and generic interface IDs against `.cswinrt/src/Authoring` and `.cswinrt/src/WinRT.Runtime` before treating authored WinUI subclasses as complete.
- [x] Authoring analyzers and diagnostics: add Kotlin compiler-plugin diagnostics equivalent to CsWinRT authoring analyzers for unsupported exposed shapes, invalid runtime-class casts, invalid collection/array authoring patterns, and source-generator contract violations.
- [ ] Native authoring host: frozen for the current queue; later implement native CCW/host behavior only after native runtime ABI/object identity and JVM authoring contracts are stable enough to port directly.

## Phase 6 Packaging And Validation

- [x] Kotlin appx/msix packaging baseline: Kotlin-owned manifest generation/validation, appx/msix layout, dependency payload resolution, resources/PRI/MRT, signing/test-install hooks, packaged/unpackaged modes, disabled-generation input skipping/output preservation, and Gradle DSL ergonomics are complete for the current JVM packaging surface without cloning full MSBuild.
- [x] Packaging CsWinRT/MSBuild evidence audit: compared Kotlin Gradle packaging against CsWinRT sample/test packaging evidence for app execution aliases, package dependency declarations, framework/package version pinning, packaged/unpackaged activation layout, PRI/MRT/resource edge cases, host/runtimeconfig payload placement, signing, makeappx verification, and test-install diagnostics; remaining work is functional packaging expansion only when Kotlin application validation exposes a concrete need.
- [x] Gradle plugin publication: `winrt-gradle-plugin` now applies the shared `winrt.publish` convention so the standalone included build publishes the plugin artifact with the same Maven Central POM, signing policy, `io.github.compose-fluent` group, and tag/SNAPSHOT version resolution used by the runtime, metadata, generator, authoring, and compiler-plugin modules; snapshot CI explicitly runs both root publications and the `winrt-gradle-plugin` included-build artifact publication, marker publication is disabled so the existing Gradle plugin id `io.github.composefluent.winrt` does not create a Maven artifact under `io.github.composefluent`, and published plugin runtime/compiler dependencies use the `io.github.compose-fluent` Maven package coordinates.
- [x] Gradle cache closure: keep configuration cache and build cache enabled for touched runtime, metadata, generator, authoring, packaging, and sample tasks; generator execution stays in an explicit isolated worker classpath so downstream Gradle/buildSrc dependencies cannot shadow kotlin-winrt generator dependencies; projection and runtime package resolution first reuse explicit / NuGet CLI global packages roots only when the full package dependency closure is already present, while missing NuGet `install` invocations are serialized through a shared Gradle-user-home file lock and run with NuGet-internal parallel package processing disabled so downstream multi-project or multi-daemon restores can still populate and reuse the system `NUGET_PACKAGES` cache without kotlin-winrt workers racing each other while extracting SDK / WindowsAppSDK payloads; any cache miss/error caused by task modeling is a bug to fix.
- [x] Representative Windows validation: validate touched runtime, metadata, generator, compiler-plugin, authoring, packaging, and JVM slices on Windows; samples remain validation surfaces, not design sources.
- [x] Sample parity audit: mapped CsWinRT `NetProjectionSample`, `WinUIDesktopSample`, authoring demos, background task sample, and embedded/test-host samples to Kotlin `winrt-samples`; current samples stay validation-only and do not expand beyond implemented runtime/generator/authoring contracts.
- [x] Third-party WinUI control package validation: `winrt-samples` now consumes `WinUIEssential.WinUI3` as a NuGet-provided WinMD/native-resource package, projects `WinUI3Package.SettingsCard` and `WinUI3Package.Shimmer`, and stages the package's `lib/native/<configuration>/<arch>` DLL, WinMD, PRI, and XAML resources so the sample remains a validation surface instead of owning resource-copy glue.
- [x] WinUIEssential native crash diagnosis: WinDbg/cdb reproduced the non-auto-exit sample crash after `WinUI3Package.dll` loaded and the window activated; the faulting instruction is inside `WinUI3Package!DllGetActivationFactory+0x2903d` reading through a null `rcx`. Targeted sample runs isolated the crash to `Shimmer`: `SettingsCard` alone remains running, while `Shimmer` alone crashes after activation, so the sample keeps per-control skip flags and treats the current failure as third-party native `Shimmer` behavior unless narrower runtime evidence appears.
- [x] WinUIEssential Shimmer sample stabilization: targeted runs showed `Shimmer` construction, sizing, content, template application, and `isLoading = true` are stable once the package XAML resource dictionaries are discovered and merged before the control is created. `Shimmer_Resource.xaml` intentionally marks the `Container` template part with `x:Load="False"`, so `findName("Container") == null` before `isLoading = true` is not treated as a missing template anymore; the sample now defers loading until `Loaded`, applies the template, updates layout, and lets the native control materialize its deferred part through its own loading setter.
- [x] WinUI resource packaging repair: runtime resource-manager registration keeps the generated application `resources.pri` as the application PRI while component PRIs such as WinUIEssential's Release `WinUI3Package.pri` remain separately staged payloads, matching the Windows App SDK self-contained target behavior that copies required component PRIs separately instead of flattening them into one loose-XBF layout. Release packages stay complete through their PRI-embedded XBF resources; Kotlin does not copy Debug `.xbf` files as a workaround.
- [x] WinUI XAML metadata provider aggregation: compared `.cswinrt` WinUI samples and authoring guidance with Microsoft.UI.Xaml MarkupCompile/MSBuild targets; Kotlin now treats the application `IXamlMetadataProvider` as a runtime-owned aggregation point, keeps the WinUI built-in `XamlControlsXamlMetaDataProvider`, and allows component provider runtime class names such as WinUIEssential's `XamlMetaDataProvider` convention to be registered so template lookup follows the same provider path MSBuild-generated WinUI apps expose.
- [x] Third-party C++/WinUI package metadata and template-resource closure: aligned with CsWinRT authoring guidance that C++ WinUI consumers expose component `*_XamlTypeInfo.XamlMetaDataProvider` as an activatable class and with Microsoft.UI.Xaml MSBuild/MarkupCompile targets that feed referenced WinMDs, XAML pages, resource dictionaries, and component PRIs through the application provider/resource pipeline. `StageWinRtRuntimeAssetsTask` now scans staged third-party WinMDs from package DLL layouts, emits the runtime-assets manifest only for XAML metadata providers, and synthesizes WinRT activation registration fragments for non-Microsoft runtime classes implemented by same-named component DLLs. Resource dictionary installation is generator/build-owned rather than runtime-registered: the projection generator emits `xaml-component-resources.tsv` compiler-support rows from the metadata model, `mergeWinRtCompilerSupport` merges those rows across app and dependency modules, and application projects generate a single merged `WinUiXamlComponentResources` bootstrap so KMP multi-module apps can merge discovered component `Microsoft.UI.Xaml.ResourceDictionary` runtime classes without hard-coded sample class names or duplicate per-library runtime registries.
- [x] Test parity audit: mapped CsWinRT unit, functional, source-generator, diagnostic, host, object-lifetime, and authoring tests to owning Kotlin module tests; additional coverage remains tied to the corresponding phase-owned contract instead of being added as standalone test-host parity work.
- [ ] Native validation: frozen for the current queue; later add `mingwX64` validation only after native runtime/projection contracts are implemented.

## Frozen Until Prerequisites Close

- [x] `winrt-samples`: audited as validation-only; use only for completed runtime/generator/authoring slices with no sample-local runtime workarounds.
- [x] `winrt-projections`: audited as gated output; avoid broad checked-in projection growth and prefer plugin-generated output gated by completed upstream contracts.
- [x] `mingwX64`: audited as deliberately deferred for the current work queue; shared contracts must remain native-viable.
- [x] Codebase slimming and standalone structure reduction: audited as frozen until runtime, metadata, generator/compiler-plugin, projection, authoring, and Kotlin appx/msix packaging are functionally complete.

## Validation Gates

- [x] Windows-first validation: run targeted Windows Gradle validation for the module touched by each slice before broader sample or integration runs.
- [x] Kotlin toolchain baseline: version catalog pins Kotlin Gradle plugin and Kotlin test artifacts to `2.4.0`; Gradle wrapper remains `9.4.0` for this upgrade.
- [x] Kotlin 2.4 compiler-plugin warning cleanup: compiler-plugin IR lookup now uses `finderForSource` / `finderForBuiltins` helpers instead of deprecated `reference*` APIs, and compiler-plugin message reporting is centralized behind one compatibility helper.
- [x] Cache validation gate: keep configuration cache and build cache enabled for WinRT generation and packaging checks; failures must be fixed in task inputs/actions or Gradle service ownership.
- [x] Runtime gate: validated ABI, activation, object identity, marshaling, delegate/event, collection, async, custom projections, and WinUI runtime hooks in `winrt-runtime` with targeted Windows `:winrt-runtime:jvmTest`.
- [x] Metadata gate: validated native WinMD ingestion, normalized symbols, signatures, accessors, custom attributes, and diagnostics in `winrt-metadata` with Windows `:winrt-metadata:test`.
- [x] Generator/compiler-plugin gate: validated deterministic output, descriptor intrinsic lowering, compiler-support aggregation, generated support initialization, and stale-support handling with Windows `:winrt-generator:test`, `:winrt-compiler-plugin:test`, and full `:winrt-projections:generateWinRtProjections :winrt-projections:compileKotlin`.
- [x] Authoring gate: validate generated TypeDetails, authored WinMD, CCW factories, activation/hosting, receive-array handling, and WinUI composable boundaries before broadening authoring samples.
- [x] Packaging gate: validate PRI/MRT/resource/package staging and appx/msix layout through Kotlin Gradle tasks, with CsWinRT/MSBuild behavior used only as reference evidence.
- [x] Generated-output audit: scan generated source/classes for `ComVtableInvoker` fallback, `invokeGenericArgs`, `Class.forName`, `Proxy.newProxyInstance`, `java.lang.reflect`, stale registry artifacts, duplicate FQNs, duplicate type/category branch tables, and unexpected source-size growth.
- [x] Sample role: audited as validation-only; use `winrt-samples` and KMP WinUI app runs only as representative end-to-end validation after the owning runtime, metadata, generator, authoring, or packaging slice is designed.

