# Plan

## Notes

- [x] Establish the root implementation plan file and integrate it into the repository workflow
- [x] Rewrite AGENTS.md to make `.cswinrt` the primary reference and to define JVM-first execution, dual-target parity, commit discipline, and plan maintenance rules
- [x] Standardize the canonical plan file name to `PLAN.md`
- [x] Clarify in AGENTS.md that `.cswinrt` alignment is responsibility-by-responsibility, with an explicit mapping table rather than a misleading literal name match
- [x] Tighten AGENTS.md to require strict one-to-one module correspondence with `.cswinrt/src`, including matching top-level module names
- [x] Adjust AGENTS.md to use Kotlin-style top-level module names `winrt-runtime`, `winrt-metadata`, `winrt-authoring`, `winrt-projections`, and `winrt-samples`, with tests kept inside each module
- [x] Exclude legacy `sample-jvm-winui3` from the active refactor scope unless explicitly requested
- [x] Clarify in AGENTS.md that implementation is `.cswinrt`-reference-first and design-first rather than test-driven reverse engineering
- [x] Write a hard repository-level constraint that `.cswinrt` responsibility convergence must be preserved: repeated Kotlin enumeration of the same runtime type/category/shape decisions is not acceptable when `.cswinrt` already centralizes that logic
- [x] Collapse the duplicated JVM runtime primitive/object/string/guid classifiers into one shared `.cswinrt`-aligned owner so `GuidGenerator`, `TypeNameSupport`, and `Projections` stop carrying parallel branch tables
- [x] Collapse the duplicated collection/bindable projection marshaler container and borrowed-reference helpers into one shared runtime owner so those helper layers stop maintaining parallel wrappers for the same projection-hosting behavior
- [x] Collapse the duplicated generator-side mapped WinRT type and collection-shape tables into one `.cswinrt/src/cswinrt/helpers.h`-aligned owner so projected type mapping, collection binding discovery, and mapped ABI kind classification stop drifting across `SPECIAL_TYPE_MAPPINGS` and separate collection branch tables
- [x] Collapse the duplicated generator-side integral ABI layout/conversion/readback tables into one shared descriptor owner so enum codegen, ABI call planning, and literal emission stop maintaining parallel `WinRtIntegralType` switch ladders

## Direction

- [x] Define the repository-wide execution rule: inspect the matching `.cswinrt` implementation first, then establish the Kotlin module boundary and runtime/generator contract, then implement, and only then use tests for validation
- [ ] Drive current work in this order: `winrt-runtime` ABI/runtime foundation -> `winrt-metadata` WinMD loading and model construction -> generator pipeline inside the `.cswinrt/src/cswinrt` responsibility split -> deterministic checked-in output in `winrt-projections` -> `winrt-samples` validation surfaces -> `winrt-authoring` hosting/authoring support after generator/projection/sample confidence is established -> targeted tests that confirm parity instead of defining behavior
- [ ] For each active slice, keep the corresponding `.cswinrt` source area explicit in the task text or WIP note before expanding tests
- [ ] Reject test-driven backfilling when `.cswinrt` parity has not been established yet; if a test passes but the matching `.cswinrt` contract is still unknown, keep the slice open
- [ ] Reject duplicated Kotlin branch tables for the same runtime or generator semantic classification; if `.cswinrt` converges a decision into one helper/registry/marshaler/type-name layer, the Kotlin implementation must converge it too instead of repeating the enumeration in parallel files

## Current Focus Queue

- [x] Focus 1: complete the remaining `.cswinrt/src/WinRT.Runtime` object-model parity in `winrt-runtime`, especially full `IWinRTObject` behavior, type-scoped QueryInterface caching, additional type-data caches, and `GetObjectReferenceForType`/`IsInterfaceImplemented` support before resuming any new generator slice. This is now closed by Runtime 1.10 plus Runtime 1.13: the JVM runtime has the formal `IWinRTObject` contract, typed/object-specific wrapper state, typed RCW factory registration, helper-type indirection, runtime-class-name-based wrapper lookup, unwrap helpers, CCW creation, and the Kotlin equivalent of `SingleInterfaceOptimizedObject`.
- [x] Runtime KMP split for `winrt-runtime` is complete. Shared runtime contracts and platform-agnostic logic live in `commonMain`; `jvmMain` and `mingwX64Main` are reserved for platform/native/FFI and target-specific wrappers.
- [x] Focus 2: complete the remaining `.cswinrt/src/WinRT.Runtime/Projections` generic collection runtime parity in `winrt-runtime`, especially `IEnumerable` / `IList` / `IReadOnlyList` / `IDictionary` / `IReadOnlyDictionary` RCW+CCW helpers, `CreateMarshaler` / `FromManaged` / `FromAbi` paths, and runtime-side `ToAbiHelper` / `FromAbiHelper` ownership. This is now closed by Runtime 1.11; the generic collection helper layer is in place, and the next active runtime focus has moved on to the later interop/value-projection/event/XAML queue in Runtime 1.16 through Runtime 1.19.
- [x] Focus 3: complete the remaining `.cswinrt/src/WinRT.Runtime/Projections/Bindable*` runtime parity in `winrt-runtime`, especially `IBindableIterable` / `IBindableVector` / `IBindableVectorView`, non-generic object marshalling, and bindable adapter behavior before restarting collection-parameter or bindable generator work. This is now closed by Runtime 1.12 plus Runtime 1.14: Bindable* helpers are present and the shared marshaler core has landed, and the projection/type-name registry slice from Runtime 1.15 is now also closed.
- [x] Focus 4: after Focus 1 through Focus 3, continue through the remaining runtime-first parity queue `Runtime 1.18` through `Runtime 1.19` plus the residual interop/error-info inventory, especially event infrastructure and XAML/system projection helpers before reopening other module growth. This queue is now closed: `Runtime 1.17` landed the boxed-value/system owner set, `Runtime 1.18` landed the shared event owner set in `EventRegistrationToken.kt`, `EventRegistrationTokenTable.kt`, `EventSourceCache.kt`, `EventSourceState.kt`, `EventSource.kt`, `StandardDelegates.kt`, `GenericDelegateHelper.kt`, and `EventHandlerEventSource.kt`, `Runtime 1.19` landed the actual `ICommand` / `INotify*` / collection-change / data-error / service-provider / `IStringable` projection surfaces in `WinRtXamlSystemModels.kt` and `XamlSystemProjectionRuntime.kt`, `Inventory H` landed the shared `IUnknown`/`IInspectable`-aware host layout in `WinRtInspectableComObject.kt`, the free-threaded-marshaler-backed `IMarshal` owner in `MarshalInterfaceReference.kt`, and the default CCW interop augmentation in `InteropRuntimeHooks.kt`, `Inventory M` landed the shared configuration/message owner set in `FeatureSwitches.kt`, `AttributeMessages.kt`, `WinRtRuntimeErrorStrings.kt`, `XamlProjectionConfiguration.kt`, and `WinRtProjectionAnnotations.kt`, and `Inventory I` now closes the remaining writable restricted-error-info path through the `runtime/exception` owner set (`ManagedErrorInfoComObject.kt`, `ManagedRestrictedErrorInfoComObject.kt`, `ManagedExceptionInterop.kt`, `LanguageExceptionInterop.kt`) plus the `SetRestrictedErrorInfo` platform seam. The explicit Kotlin-specific narrow deviations that remain visible after this queue closes are the CLR/Mono language-exception CCW path now isolated under `runtime/exception`, the `ICustomPropertyProvider` reflection fallback now narrowed to an explicit provider/property contract that future XAML mapping sugar can layer over with Kotlin delegates or `inline reified` helpers, the managed-to-ABI event-args path using synthetic runtime-class CCWs instead of WinUI activation-factory construction, the runtime configuration/resource surface narrowing `.cswinrt`'s `AppContext + localized .resx` model to JVM system-property switches plus English default messages with optional resource-key mode, and the internal `IMarshal` implementation detail narrowing `.cswinrt`'s thread-static CLR helper object to a process-wide cached free-threaded-marshaler proxy while preserving the same ABI-facing forwarding behavior.
- [x] Focus 5: keep the full `Runtime Remaining Parity Inventory` (`Inventory A` through `Inventory M`) as the runtime completion checklist; do not treat Focus 1 through Focus 4 as complete until every remaining `.cswinrt/src/WinRT.Runtime` responsibility is either implemented or explicitly narrowed as a residual Kotlin-specific exception. The runtime inventory is now closed at that level: remaining `.cswinrt` differences are documented Kotlin-specific narrowings rather than open blockers.
- [x] Focus 6: re-converge the `winrt-runtime` COM core on the `.cswinrt/src/WinRT.Runtime/ObjectReference.cs`, `Interop/IUnknownVftbl.cs`, `IInspectable.cs`, `Interop/IActivationFactory.cs`, and `cswinrt/code_writers.h` owner split before deeper generator growth resumes. This is now closed: `ComPtr` owns pointer/lifetime/QI/identity, RCW wrappers call slot constants through fixed `ComVtableInvoker.invoke` / `invokeArgs` overloads, `ComMethodContract` is removed from runtime APIs, no `TVftbl`/`TypedComView` hierarchy or one-vtable-singleton-per-WinMD-interface model was introduced, the `ComObjectReference.invokeXxxMethod*` helper family and lower-level `invokeIntMethod*` bridge methods have been removed from the active runtime surface, handwritten collection/bindable/XAML/system/StandardDelegates/WinUI metadata-provider wrappers call owned slot constants through fixed invoker paths, and `ComPtr.invokeGeneric` / `ComVtableInvoker.invokeGeneric` is narrowed to dynamic delegate invocation, explicit raw-word migration, or rare unsupported shapes.
- [x] Focus 6 validation: `./.agent_scripts/run_windows_gradle.sh :winrt-runtime:compileKotlinJvm :winrt-runtime:compileKotlinMingwX64 :winrt-runtime:compileTestKotlinJvm :winrt-runtime:jvmTest` succeeds with the Windows Gradle script after the `RawComPtr`/`RawAddress` boundary cleanup, `ComMethodContract` API removal, `ComObjectReference.invokeXxxMethod*` removal, and fixed-invoker StandardDelegates cleanup. The remaining compiler output is the pre-existing unchecked cast warning in `ValueBoxingMetadata.kt`.
- [x] Freeze now: do not expand `winrt-metadata`, generator breadth, `winrt-projections`, `winrt-authoring`, or `winrt-samples` beyond maintenance needed to support Focus 1 through Focus 5. This freeze is now lifted for `winrt-metadata`, which becomes the active post-runtime slice while generator, projections, authoring, and samples remain behind the updated metadata/generator queue.
- [x] Freeze now: do not resume broader generator emission growth beyond maintenance needed to support Focus 6. This gate is now satisfied: `Generator 3.5` may target slot constants plus fixed `ComVtableInvoker` overloads rather than the removed `ComObjectReference` helper surface.
- [x] Freeze now: do not resume broader projection output growth or WinUI sample expansion until the async/custom generator/runtime supplement queue is split and the representative generated API slice can be regenerated without handwritten projection growth. This async/custom gate is now closed by Queue 9.5; projection output growth still remains constrained by Queue 10's representative generated API proof.
- [x] Active now: resume the `.cswinrt/src/cswinrt` generator special-case queue at `Generator 3.5`, using the Focus 6 slot-plus-fixed-invoker COM core target owner set and the closed metadata descriptor/index owner set as the source of truth for collection/bindable/interface-proxy ABI expansion. This is now closed for the generator-owned ABI emission slice: generated inline ABI calls target `PlatformAbi.confinedScope()` plus fixed `ComVtableInvoker.invoke` / `invokeArgs` overloads, the generator-owned ABI call planner no longer emits or carries `Arena` / `FunctionDescriptor` / `ValueLayout` / `MemorySegment` call-shape details into generated projection code, mapped collection parameters can marshal through runtime projection helpers for projected interface/runtime-object element families, bindable `Microsoft.UI.Xaml.Interop` and `Windows.UI.Xaml.Interop` collection parameters/returns route through runtime `WinRtBindable*Projection` helpers, and non-generic projected interface returns can be wrapped through generated `Metadata.wrap` proxies when their ABI surface is supported.
- [x] Active now: split the remaining `.cswinrt` async/custom mapping work into the next generator/runtime supplement slice instead of hiding it under Generator 3.5. This is now closed by Runtime 1.20 plus Generator 3.6: the runtime-facing `AsyncInfo` / `TaskToAsync*Adapter` baseline from `.cswinrt/src/cswinrt/strings/additions/Windows.Foundation` is represented by `winrt-runtime`'s coroutine-backed `AsyncInfo` adapter for completed/canceled/error/running actions and operations, including progress-enabled action/operation surfaces, and generator async/custom mapping now emits runtime async reference wrappers instead of treating type-name substitution as full async ABI parity.
- [x] Exit condition: only after Focus 1 through Focus 5 are reduced to bounded residual gaps may the active queue move back to metadata/generator expansion. Runtime is now at bounded residual gaps, so the active queue has returned to metadata fidelity.
- [x] Active now: complete the CsWinRT metadata full-parity gate before further generator/projection/sample breadth. Closed by Metadata Full-Parity 4.20 through 4.26: exclusive/default interface semantics, component/filter-sensitive CCW behavior, private implementation matching, custom QI inventories, fixed-point generic instantiation handoff, auxiliary raw-table boundaries, and a CsWinRT metadata-entry audit are now metadata-owned instead of being rediscovered inside generator emitters.
- [ ] Convergence gate: after the runtime queue closes, do not accept new metadata or generator growth that re-enumerates already-known primitive kinds, projection mappings, collection/bindable shapes, ABI categories, or type-family classifications in another file; first collapse that decision into the Kotlin abstraction that corresponds to the `.cswinrt` ownership point.

## Ordered Implementation Stages

- [ ] Stage 1: `winrt-runtime` first. Mirror the active `.cswinrt/src/WinRT.Runtime` slice, define the ABI/runtime contract, and do not move the primary effort to metadata or projections until the minimum contract for the current slice is in place.
- [ ] Stage 2: `winrt-metadata` second. Implement the WinMD loading and symbol/model layer required by the active projection slice only after the needed runtime contracts are known.
- [ ] Stage 3: generator pipeline third. Implement the matching `.cswinrt/src/cswinrt` generation rules only after Stage 1 and Stage 2 expose the runtime and metadata contracts those rules depend on.
- [ ] Stage 4: `winrt-projections` fourth. Expand deterministic checked-in projection output only after the corresponding generator rule exists; do not use handwritten projection growth as a substitute for missing generator work.
- [ ] Stage 5: `winrt-samples` fifth. Use samples to validate the completed runtime/metadata/generator/projection path, not to define missing behavior.
- [ ] Stage 6: `winrt-authoring` last. Start the authoring/hosting parity slice only after generator, deterministic projections, and the smoke sample path are coherent enough that authoring is not forced to compensate for upstream gaps.
- [ ] Stage gate: do not let `winrt-projections` become the starting point for a new slice. If projections seem blocked, return to the missing runtime, metadata, or generator prerequisite instead of expanding projection code directly.
- [ ] Stage gate: do not expand WinUI samples or broader test matrices until the corresponding runtime/generator slice has a documented `.cswinrt` mapping and an implemented contract.

## Current Ordered Slice Queue

- [x] Queue 1: `winrt-runtime` ABI foundation from `.cswinrt/src/WinRT.Runtime` (GUID/IID/HRESULT primitives, HSTRING ownership, memory ownership boundaries, reusable vtable-call helpers, and initialization scope) is now closed by Runtime 1.1 through Runtime 1.2, so higher-level work no longer waits on ABI-foundation gaps.
- [x] Queue 2: `winrt-runtime` object model and activation foundation from `.cswinrt/src/WinRT.Runtime` (object identity, `IUnknown`/`IInspectable` lifetime, activation factory lookup/caching, runtime-class activation, interface-cast helpers) is now closed by Runtime 1.3 through Runtime 1.4 plus the later object-model closure in Runtime 1.10 and Runtime 1.13.
- [x] Queue 3: `winrt-runtime` projected object-model parity from `.cswinrt/src/WinRT.Runtime` (`IWinRTObject`, type-scoped objref caches, typed RCW factory registration, additional type data, and interface lookup fallback behavior) must be completed before collection/bindable generator growth resumes. This is now closed by Runtime 1.10 plus Runtime 1.13; the shared marshaler core from Runtime 1.14 and the projection/type-name registry from Runtime 1.15 are also in place, so the remaining frozen blockers for generator growth now move to the later value-projection, event, and XAML queue in Runtime 1.16 through Runtime 1.19.
- [x] Queue 4: `winrt-runtime` projection-helper parity from `.cswinrt/src/WinRT.Runtime/Projections` (generic collection RCW/CCW helpers, bindable adapters, object marshalling, and `ToAbiHelper`/`FromAbiHelper` paths) is now closed by Runtime 1.11, Runtime 1.12, Runtime 1.14, and Runtime 1.17, so the frozen generator queue no longer waits on runtime-side helper ownership.
- [x] Queue 5: `winrt-metadata` real WinMD loading from the `.cswinrt/src/cswinrt` metadata responsibilities is now closed by Metadata 2.1 plus the deterministic-ingestion cleanup in Metadata 2.2.
- [x] Queue 6: `winrt-metadata` baseline normalized symbol/model construction from `.cswinrt/src/cswinrt` is now closed through Metadata 2.9. This is enough for the current `Windows.Data.Json` proof, but it is not full CsWinRT metadata parity: the remaining gaps are tracked explicitly in Metadata 2.10 through Metadata 2.18 before broader projection, authoring, or WinUI growth resumes.
- [x] Queue 7: generator planning from `.cswinrt/src/cswinrt` is now closed at the declaration-planning level by Generator 3.2 plus Generator 3.3; remaining work has moved to metadata-dependent emission breadth rather than declaration discovery.
- [x] Queue 8: `winrt-runtime` COM core convergence from `.cswinrt/src/WinRT.Runtime/ObjectReference.cs`, `Interop/IUnknownVftbl.cs`, `IInspectable.cs`, `Interop/IActivationFactory.cs`, and `cswinrt/code_writers.h` has made slot constants plus fixed invoker overloads the primary ABI/runtime target before broader generator emission resumes. This slice owns `ComPtr` pointer/lifetime/QI/identity, foundational handwritten views for `IUnknown`/`IInspectable`/`IActivationFactory`, and the fixed-shape `ComVtableInvoker` seam; it replaces further expansion of the old `ComObjectReference`/`invokeXxxMethod*` helper pattern without introducing a generated `TVftbl` singleton model.
- [x] Queue 9: generator emission from `.cswinrt/src/cswinrt` is now unblocked by Queue 8 and has landed Generator 3.5 for slot-based ABI emission cleanup, mapped collection parameter support for projected reference families, bindable collection ABI categories, and the first non-generic projected-interface proxy/wrap path. These slices target slot constants plus fixed `ComVtableInvoker` overloads instead of ad hoc reference helpers.
- [x] Queue 9.5: finish async/custom mapping ABI parity as a separate generator/runtime supplement slice, starting from `.cswinrt/src/cswinrt/strings/additions/Windows.Foundation/AsyncInfo.cs` and `TaskToAsync*Adapter.cs` rather than treating `CompletableFuture` type-name substitution as complete async projection behavior. Closed by adding runtime `AsyncInfo` / `WinRtTaskToAsyncInfoAdapter` support for non-progress and progress-enabled action/operation CCWs, parameterized async/progress/completed handler IID helpers, progress handler registration/reporting, generated `WinRtAsyncAction*Reference` / `WinRtAsyncOperation*Reference` ABI returns, and generator regression coverage for scalar/string async result readback.
- [x] Queue 10: `winrt-generator` must prove that a representative standard WinRT API slice such as `Windows.Data.Json` can be regenerated through the generator path before any broader checked-in `winrt-projections` surface is added. Closed by adding a generator CLI that loads the installed Windows SDK `Windows.winmd`, generator parity tests mapped directly to `.cswinrt/src/Tests/FunctionalTests/JsonValueFunctionCalls/Program.cs` and the `Windows.Data.Json` block in `.cswinrt/src/Tests/UnitTest/ApiCompatTests.cs`, metadata loader support for real WinMD GUID/custom-attribute blobs needed by that slice, checked-in generated `Windows.Data.Json` plus `Windows.Foundation.IStringable` output in `winrt-projections`, and targeted compile validation for that output.
- [x] Queue 10.5: close the current `winrt-metadata` baseline discovered by comparing the model to `.cswinrt/src/cswinrt/helpers.h`, `cmd_reader.h`, `type_writers.h`, and `code_writers.h`. Metadata 2.10 through Metadata 2.18 are a useful baseline for representative `Windows.Data.Json` generation, but they are no longer treated as full CsWinRT metadata parity.
- [x] Queue 10.6: complete the reopened CsWinRT metadata full-parity gate before any new generator/projection/sample breadth. Closed by Metadata Full-Parity 3.x and 4.1 through 4.26: cache/source, table tolerance, type semantics, attributes, closure, layout/blittability, mapped types, diagnostics, real fixtures, exclusive/default interface semantics, component/filter-sensitive CCW behavior, private implementation matching, custom QI inventory, fixed-point generic instantiation handoff, raw-table boundary documentation, and metadata-entry audit coverage are now available before the final helper-output/member-merge audit in Queue 10.7.
- [x] Queue 10.7: close the remaining metadata-owned CsWinRT traversal/writer handoff gaps discovered from `.cswinrt/src/cswinrt/main.cpp` and `code_writers.h` before Queue 11 resumes. Closed by Metadata Full-Parity 4.27 through 4.32: helper-file emission inventory, NetStandard ABI delegate initializer conditions, generic-instantiation helper conditions, base string helper inclusion, manually generated interface classification, projection-context flag semantics, and class/interface member merge descriptors are now metadata-owned handoff inputs.
- [x] Queue 10.8: close the remaining CsWinRT metadata writer-shape exactness gaps discovered after 4.32. Closed by Metadata Full-Parity 4.33 through 4.38: object/class equality helper descriptors, exact generic ABI delegate operation entries, generic-instantiation writer descriptors, CsWinRT type-name namespace/name-kind descriptors, event-helper subclass descriptors, and platform-guard/member-platform descriptors are now metadata-owned before Queue 11 resumes generator breadth.
- [ ] Queue 10.9: close the newly audited metadata-owned CsWinRT writer handoff gaps from `.cswinrt/src/cswinrt/code_writers.h` before Queue 11 resumes. This gate owns exact projected/ABI signature rendering descriptors, event invoke/event-source descriptors, ABI marshaler plan descriptors, static/factory/composable writer descriptors, custom mapped member output descriptors, property-interface lookup/interface-member-signature descriptors, GUID/IID signature writer descriptors, and vtable delegate/function-pointer descriptors so generator breadth does not rediscover these branch tables locally.
- [ ] Queue 11: resume generator breadth from `.cswinrt/src/cswinrt` only after Queue 10.9 is closed; consume the metadata-owned classifier/diagnostics/closure/member-shape inputs for the next projection-producing generator slice instead of reintroducing local branch tables.
- [ ] Queue 12: add a first-class `kotlin-winrt` Gradle plugin layer that consumes the generator instead of relying on checked-in projection updates or ad hoc CLIs. The plugin must resolve Windows SDK metadata sources and NuGet package metadata sources, feed their WinMD closure into `winrt-metadata` / `winrt-generator`, and produce deterministic Kotlin sources for the owning Gradle source set.
- [ ] Queue 13: model two plugin roles explicitly: `kotlin-winrt-library` packages generated projection source/output plus NuGet identity/metadata dependency information into the produced artifact or klib without integrating runtime resources, while `kotlin-winrt-application` consumes the transitive metadata/resource graph and performs final NuGet runtime/resource integration into the runnable app package.
- [ ] Queue 14: expand `winrt-projections` only with deterministic generator-produced output for the completed generator/plugin slice; checked-in projection growth must be reproducible from the generator CLI or plugin task and validated by targeted compile tests.
- [ ] Queue 15: `winrt-samples` must validate the active completed generated-projection slice with the smallest possible sample first, then expand to NuGet-backed WinUI bootstrap, resource, and message-loop validation only after the corresponding runtime/generator/projection/plugin support exists.
- [ ] Queue 16: targeted tests must be added in the same dependency order: generator regression tests -> plugin source-generation/resource-graph tests -> projection compile/integration checks -> sample smoke validation; runtime/metadata tests remain prerequisites only when the active generator/projection/plugin slice exposes a new upstream gap.
- [ ] Queue 17: `winrt-authoring` stays frozen until Queue 11 through Queue 16 are coherent. When it resumes, it must follow the `.cswinrt/src/Authoring` hosting/lifetime/activation boundary requirements before broader source-generator or convenience work is expanded.

## Metadata And Generator Subslice Order

- [x] Metadata 2.1: replace placeholder modeling in `winrt-metadata` with real WinMD ingestion aligned with the metadata-reading responsibilities inside `.cswinrt/src/cswinrt`.
- [x] Metadata 2.2: stabilize deterministic namespace/type normalization in `winrt-metadata` so the same metadata input always produces the same ordered model before generator breadth expands.
- [x] Metadata 2.3: add core shape fidelity next: kind classification, GUID/default-interface data, base/implemented interface relationships, generic parameter shapes, and runtime-class activation/static/factory metadata.
- [x] Metadata 2.4: add member/signature fidelity next: method/property/event descriptions, parameter direction/pass-by semantics, and the normalized signature inputs needed by the generator.
- [x] Metadata 2.5: add structured type-reference fidelity next: metadata now exposes canonical `WinRtTypeRef` shapes for named types, generic instantiations, arrays, and generic type parameters while preserving the existing string fields for generator compatibility. `WinRtMetadataLoader` now strips generic-arity suffixes from external type refs, canonicalizes generic/array signature names, and the metadata tests cover nested generic arguments plus array and generic-parameter signatures from real CLI metadata.
- [x] Metadata 2.6: add generator-facing ABI semantics next from the `.cswinrt/src/cswinrt` metadata-to-generator handoff. This is now closed by the metadata-owned ABI descriptor layer in `WinRtAbiDescriptors.kt`, which exposes canonical parameter/return descriptors for value/reference/interface/delegate/runtime-class/generic-parameter/array categories, preserves `byref` plus explicit `In`/`Out` flags on `WinRtParameterDefinition` / `WinRtMethodDefinition`, and materializes `.cswinrt`-aligned array parameter categories (`PassArray`, `FillArray`, `ReceiveArray`) so later generator slices stop re-deriving ABI behavior from raw strings plus ad hoc flags.
- [x] Metadata 2.7: add interface/runtime-class closure descriptors next from the `.cswinrt/src/cswinrt` type-analysis responsibilities. This is now closed by the metadata-owned closure layer in `WinRtTypeClosureDescriptors.kt`, which materializes inherited-interface closure with generic type-argument substitution, default-interface precedence plus runtime-class consumable interface surfaces, and structured activatable/static/factory/composable descriptors so later generator slices can stop rescanning `implementedInterfaces` and raw activation strings to reconstruct the same surface.
- [x] Metadata 2.8: add special WinRT shape descriptors next from the `.cswinrt/src/cswinrt` helper/type-classification responsibilities. This is now closed by the metadata-owned special-type layer in `WinRtSpecialTypeDescriptors.kt`, which classifies collection, bindable collection, async, `IReference` / `IReferenceArray`, and event-handler delegate families from canonical metadata type refs so later generator slices can stop carrying their own repeated name tables and generic-argument decoding logic for those WinRT shapes.
- [x] Metadata 2.9: add deterministic lookup/index helpers next from the `.cswinrt/src/cswinrt` symbol-resolution responsibilities. This is now closed by the metadata-owned lookup layer in `WinRtMetadataLookupIndex.kt`, which provides stable namespace/type lookup by full name, canonical type-reference resolution, member lookup by deterministic signature key, overload grouping by name, and implemented/default interface queries through cached type lookups so later generator slices stop rescanning the full metadata model to answer those questions.
- [x] Metadata 2.10: replace the current simple path walker with a CsWinRT-style metadata cache/input model aligned with `.cswinrt/src/cswinrt/cmd_reader.h` and `main.cpp`. Closed by adding `WinRtMetadataSource`, `WinRtMetadataSourceResolver`, and `WinRtMetadataCache` as the metadata-owned input/cache layer; supporting path, `local`, `sdk`, `sdk+`, and explicit SDK-version source specs; resolving Windows SDK roots from `KOTLIN_WINRT_WINDOWS_SDK_ROOT` or the standard Windows Kits location; expanding `Platform.xml` and extension `SDKManifest.xml` `ApiContract` entries into deterministic referenced WinMD files; surfacing missing SDK/contract WinMDs as metadata-source diagnostics; keeping existing `load(Path...)` compatibility through the same cache path; and moving the generator CLI default away from direct `UnionMetadata/Windows.winmd` lookup to the metadata-owned SDK contract source.
- [x] Metadata 2.11: close raw metadata table and signature fidelity gaps from `.cswinrt/src/cswinrt/helpers.h` `get_type_semantics(...)`. Closed by decoding TypeSpec rows as first-class `WinRtTypeRef` inputs for TypeDefOrRef references, preserving generic interface instantiations such as `IIterable<IKeyValuePair<String, IJsonValue>>`, exposing `GenericParam` names/flags and `GenericParamConstraint` type names through `WinRtGenericParameterDefinition`, fixing GenericParam/MethodSpec/GenericParamConstraint table-size handling, widening signature readers to resolve TypeSpec tokens, and adding metadata coverage for generic-parameter names, constraints, and TypeSpec-backed interface implementations.
- [x] Metadata 2.12: replace ad hoc custom-attribute string scanning with a typed attribute decoder equivalent to CsWinRT's `get_attribute(...)` / `get_attribute_value(...)` usage. Closed by exposing `WinRtCustomAttributeDefinition` / typed fixed and named argument values on type definitions, decoding constructor signatures from `MemberRef`, supporting strings, `System.Type`, enum, primitive, array, boolean, and numeric WinRT `GuidAttribute` blobs, and widening custom-attribute owner coverage for the metadata tables needed by later contract/member-shape work.
- [x] Metadata 2.13: expose WinRT contract/version/platform/deprecation metadata as structured generator input, matching `.cswinrt/src/cswinrt/helpers.h` `get_contract_version`, `get_version`, `get_contract_platform`, and `code_writers.h` platform/deprecation attribute emission. Closed by adding `WinRtAvailabilityMetadata` on type definitions with structured `ContractVersion`, `Version`, `PreviousContractVersion`, `Deprecated`, `Threading`, `MarshalingBehavior`, `MuseAttribute`, and `WebHostHidden` data, including CsWinRT-aligned contract-to-supported-platform mappings for the known Windows SDK contracts.
- [x] Metadata 2.14: refine runtime-class and interface closure to match CsWinRT's generator ordering rules. Closed by carrying availability onto resolved interface descriptors, adding runtime-class `classHierarchyIndex`, `isFastAbi`, `gcPressureAmount`, and `fastAbiInterfaces`, sorting FastAbi exclusive interfaces by previous-contract count, contract version, version, and type name, and decoding `FastAbiAttribute` / sealed-class `GCPressureAttribute` into metadata-owned inputs instead of leaving those decisions for generator rediscovery.
- [x] Metadata 2.15: add member-shape fidelity needed by `.cswinrt/src/cswinrt/code_writers.h`. Closed by decoding `OverloadAttribute`, `DefaultOverloadAttribute`, `NoExceptionAttribute`, method visibility/static/special-name flags, parameter default constants, return-parameter attributes, property/event accessor validity, remove-overload detection, and object/class `Equals` / `GetHashCode` recognition into metadata-owned method/property/event fields while keeping existing ABI array categories owned by `WinRtAbiDescriptors`.
- [x] Metadata 2.16: add struct/field/layout fidelity beyond enum constants. Closed by exposing ordered `WinRtFieldDefinition` entries with field flags, static/literal/init-only state, constants, explicit field offsets, per-field ABI size/alignment/blittability, and `WinRtTypeLayout` descriptors from `ClassLayout` / `FieldLayout`; struct metadata now carries conservative ABI size/alignment/blittability inputs so generator/runtime decisions do not infer layout from Kotlin type names.
- [x] Metadata 2.17: converge mapped-type and category classification with `.cswinrt/src/cswinrt/helpers.h` `get_mapped_type`, `get_category`, and adjacent helper ownership. Closed by adding metadata-owned `WinRtMetadataTypeClassifier` / `WinRtTypeClassificationDescriptor` inputs for CsWinRT mapped system/XAML/WUX/MUX types, mapped custom-member and marshaling flags, projection categories, ABI category handoff, special collection/bindable/reference descriptors, projection-internal filtering, API-contract and attribute categories, and by making `WinRtMetadataAbiResolver` consume the shared classifier instead of carrying a second type-category table.
- [x] Metadata 2.18: add metadata-owned validation and diagnostics for unsupported or incomplete CsWinRT parity cases. Closed by adding `WinRtMetadataValidator`, structured diagnostic codes/reporting, and `WinRtMetadataDiagnosticException` in `winrt-metadata` for unresolved type references, missing interface IID, missing runtime-class default/static/factory metadata, unsupported signature and method-generic shapes, invalid property/event accessor semantics, and unknown custom-attribute blob values. Generator planning now delegates its pre-emission checks to the metadata validator, using a filtered-slice option profile so generated API subsets are not rejected for dependencies intentionally outside the selected projection surface.
- [x] Metadata gate baseline: Metadata 2.10 through Metadata 2.18 are closed only as the current upstream metadata baseline. They are not full CsWinRT metadata parity, and they do not unblock broader generator/projection/sample growth by themselves.
- [x] Metadata Full-Parity 3.1: mirror the CsWinRT metadata entry contract from `.cswinrt/src/cswinrt/settings.h`, `cmd_reader.h`, and `main.cpp`, not just the previous source resolver. Closed by adding the metadata-owned `WinRtMetadataProjectionContext`, `WinRtMetadataFilter`, and `WinRtMetadataTarget` owner set for `settings.input`, `include`, `exclude`, `addition_exclude`, `filter`, `addition_filter`, `target`, `component`, `internal`, `embedded`, `public_enums`, `public_exclusiveto`, `idic_exclusiveto`, `partial_factory`, and `verbose`; response-file source expansion; and cache resolution/loading through the typed context.
- [x] Metadata Full-Parity 3.2: mirror the `winmd::reader::cache` tolerance boundary used by `.cswinrt/src/cswinrt/main.cpp` before any generator-driven table discovery. Closed at the ECMA-335 table row-size/cache-tolerance layer by covering all table IDs through `GenericParamConstraint`, including `FieldMarshal`, `DeclSecurity`, `ImplMap`, `FieldRVA`, `ENCLog`, `ENCMap`, and the auxiliary table family from 3.2a, with coded-index sizing for each referenced owner family. Later slices may decode additional table contents, but unsupported table row-size failures are no longer allowed to be generator-discovered.
- [x] Metadata Full-Parity 3.2a: add the first CsWinRT-cache-tolerance table expansion in `WinRtMetadataLoader` for `File`, `ExportedType`, `ManifestResource`, assembly processor/OS, assembly-ref processor/OS, and `NestedClass`, including `Implementation` coded-index sizing and a real managed metadata fixture with forwarded types, embedded resources, and nested classes. This closes the immediate auxiliary-table regression while the broader 3.2 table sweep remains open.
- [x] Metadata Full-Parity 3.3: mirror the exact `helpers.h` semantic kernel. Closed by adding metadata-owned `WinRtTypeSemanticsResolver` plus `WinRtTypeSemantics` / `WinRtFundamentalType`, representing CsWinRT's `fundamental_type`, `object_type`, `guid_type`, `type_type`, `type_definition`, `generic_type_instance`, `generic_type_index`, and `generic_type_param`; resolving `System.Guid`, `System.Object`, `System.Type`, named definitions, local namespace definitions, and generic instantiations; and rejecting method-generic and unsupported array/unknown semantic shapes before generator consumption.
- [x] Metadata Full-Parity 3.4: mirror the `method_signature` and parameter category kernel from `helpers.h`. Closed by adding metadata-owned `WinRtMethodSignatureDescriptor`, `WinRtMethodSignatureParameter`, and `WinRtMetadataParameterCategory`; centralizing `get_param_category` equivalent behavior for byref/in/out/array categories in `metadataParameterCategoryFor`; and making ABI parameter descriptors consume that owner instead of duplicating the parameter category ladder.
- [x] Metadata Full-Parity 3.5: mirror the CsWinRT type/category helper set from `helpers.h`. Closed by adding `WinRtMetadataSemanticHelpers` as the helper facade for `is_remove_overload`, `is_noexcept`, `is_exclusive_to`, `is_overridable`, `is_projection_internal`, `is_flags_enum`, `is_api_contract_type`, `is_attribute_type`, `is_static(TypeDef)`, `has_default_constructor`, `is_special`, `is_static(MethodDef)`, and `get_delegate_invoke`, backed by model fields decoded by the loader and existing classification descriptors.
- [x] Metadata Full-Parity 3.6: mirror property/event semantic helpers from `helpers.h`. Closed by adding metadata-owned `WinRtPropertyAccessorDescriptor` / `WinRtEventAccessorDescriptor` plus `getPropertyMethods`, `getEventMethods`, and `getDefaultInterface` helper entry points over the normalized model; existing loader fields continue to carry object/class `Equals` and `GetHashCode` recognition, accessor validity, and default-interface metadata for generator consumption.
- [x] Metadata Full-Parity 3.7: mirror CsWinRT attribute decoding as used, not generically. Closed by adding `WinRtMetadataSemanticHelpers` entry points for `getAttribute`, `hasAttribute`, `getNumberOfAttributes`, `getAttributeValue`, `getContractVersion`, `getVersion`, and `getContractPlatform`, backed by the typed attribute and availability metadata already decoded by the loader for activation, availability, FastAbi, GC pressure, overloads, defaults, and metadata attributes.
- [x] Metadata Full-Parity 3.8: mirror the activation/factory/static/composable surface from `code_writers.h` `get_attributed_types(...)` and the component pre-scan in `main.cpp`. Closed by adding `WinRtAttributedFactoryDescriptor` and `getAttributedTypes(...)` so activatable, static, and composable factory interfaces are resolved and merged in metadata before generator consumption.
- [x] Metadata Full-Parity 3.9: mirror FastAbi and exclusive-interface helper state from `helpers.h`. Closed by adding metadata helper entry points for `getDefaultAndExclusiveInterfaces`, `getFastAbiClassForClass`, `getFastAbiClassForInterface`, `getGcPressureAmount`, and the `WinRtFastAbiClassDescriptor` / `WinRtFastAbiPropertySlot` owner set over the existing closure resolver's FastAbi ordering and exclusive-interface data.
- [x] Metadata Full-Parity 3.10: mirror mapped projection type ownership from `helpers.h` `get_mapped_types_in_namespace` / `get_mapped_type`, including the complete sorted table, helper-only entries with no projected mapping, WUX/MUX duplicates, `requires_marshaling`, `has_custom_members_output`, and the documented consistency requirement with runtime `Projections.cs` and source-generator type mapping. Closed by the metadata semantic-helper mapped-type lookup surface, which now delegates to the single metadata classifier table instead of requiring generator/projection-local mapped-type ownership.
- [x] Metadata Full-Parity 3.11: mirror struct/enum/value semantics consumed by `code_writers.h`: `is_type_blittable`, `is_value_type`, enum flags/underlying constants, struct field signatures, field layout, mapped-value exceptions, array blittability, and the rule that non-blittable structs require ABI companion shape while blittable structs can project directly. Closed by `WinRtValueTypeDescriptor`, field descriptors, explicit `isValueType` / `isTypeBlittable` helpers, mapped value-type lookup, enum flag/underlying/member exposure, and the non-blittable ABI-companion marker.
- [x] Metadata Full-Parity 3.12: mirror generic ABI inventory ownership from `helpers.h` and `code_writers.h`: `generic_abi_delegate`, `generic_type_instantiation`, `does_abi_interface_implement_ccw_interface`, `add_if_generic_type_reference`, `add_generic_type_references_in_interface_type`, `add_generic_type_references_in_type`, and the generic delegate/type-instantiation sets that CsWinRT builds while walking cached namespaces. Closed by metadata-owned generic ABI inventory collection across delegate/interface method, property, event, struct-field, implemented-interface, nested-generic, and array shapes plus the exclusive-interface CCW predicate.
- [x] Metadata Full-Parity 3.13: mirror namespace cache traversal from `main.cpp`: `cache.namespaces()` grouped classes/interfaces/enums/structs/delegates/types, filter checks before both projection and ABI passes, mapped-type/API-contract/attribute skipping, additions filtering via `addition_filter`, base-type mapping inventory, event-source inventory, authored metadata type mapping inventory, and projection-file-written decisions. Closed by `WinRtMetadataProjectionInventory`, which exposes namespace groupings, filtered/mapped/API-contract/attribute skips, addition inclusion, ABI requirement decisions, base-type mappings, event-source mappings, component authored metadata mappings, generic ABI inventory, and projection-file-written state for generator consumption.
- [x] Metadata Full-Parity 3.14: mirror diagnostics at the same ownership boundary as CsWinRT: invalid command specs fail in source resolution, missing referenced metadata fails in cache/type resolution, unsupported signatures fail in semantic decoding, invalid property/event/delegate/default-interface shapes fail in metadata validation, and every intentional Kotlin-specific gap is recorded before generator emission. Closed by context-level projection input diagnostics, explicit source/missing-reference/command diagnostic codes, semantic-kernel failure diagnostics, and warning diagnostics for documented Kotlin-specific gaps.
- [x] Metadata Full-Parity 3.15: run metadata-only fixture sweeps before reopening generator breadth: Windows SDK `sdk` and `sdk+`, explicit SDK versions, `local`, response-file inputs, extension SDK manifests, NuGet/Windows App SDK WinMDs once package resolution exists, and managed fixtures for exported types/resources/nested classes/generic signatures/FastAbi/activation. Queue 11 stays blocked until this sweep is green or every remaining difference is explicitly narrowed. Closed for the current metadata-only gate by `WinRtMetadataFixtureSweep`, with tests covering response-file inputs, explicit Windows SDK source resolution, SDK extension manifests, and managed fixtures; NuGet/Windows App SDK package resource resolution remains tracked outside this metadata-only sweep until the Gradle plugin package-resolution slice exists.
- [x] Metadata Full-Parity 4.1: add the first NuGet/Windows App SDK package source model before generator/plugin consumption. Closed by `WinRtMetadataSource.NuGetPackage`, package-directory and `.nupkg` resolution, package asset inventory for WinMD/resource/native/other assets, and cache exposure of package assets alongside discovered WinMD files. Full Gradle dependency resolution and transitive package graph handling remain plugin-owned follow-up work.
- [x] Metadata Full-Parity 4.2: upgrade auxiliary ECMA table support from row-size tolerance to metadata-owned inventory. Closed by `WinRtMetadataAuxiliaryTableInventory` / per-file table descriptors for `FieldMarshal`, `DeclSecurity`, `StandAloneSig`, `MethodImpl`, `ModuleRef`, `ImplMap`, `FieldRVA`, `File`, `ExportedType`, `ManifestResource`, `NestedClass`, and `MethodSpec`, including row counts, row sizes, and modeled-state flags.
- [x] Metadata Full-Parity 4.3: preserve signature fidelity needed by CsWinRT-style TypeSpec and semantic decoding instead of reducing signatures to display strings. Closed by extending `WinRtTypeRef` with raw-signature and required/optional modifier carriers, preserving those fields through normalization/substitution, and threading parsed signature refs through fields, parameters, method returns, properties, and events.
- [x] Metadata Full-Parity 4.4: mirror CsWinRT custom/platform attribute projection from `.cswinrt/src/cswinrt/code_writers.h` `write_custom_attributes`. Closed by adding metadata-owned projected attribute descriptors that skip runtime-only metadata attributes, preserve the selected `Windows.Foundation.Metadata` attributes used by projection output, synthesize `SupportedOSPlatform` from contract-version metadata when target rules allow it, and fold `AllowMultipleAttribute` into projected `AttributeUsage` named arguments.
- [x] Metadata Full-Parity 4.5: tighten traversal inventory names against CsWinRT writer conventions before generator consumption. Closed by exposing projected attributes on projection inventory entries, switching authored metadata type mappings to the `ABI.<type>` owner naming used by generated metadata entries, and matching CsWinRT `_EventSource_` event-source class-name escaping.
- [x] Metadata Full-Parity 4.6: enrich metadata diagnostics with source/row location before generator emission. Closed by adding source-name and ECMA row-id fields to diagnostics, formatting those locations in reports, and threading method/property/event/field row ids through unsupported signature, semantic, unresolved-reference, and invalid accessor diagnostics.
- [x] Metadata Full-Parity 4.7: extend metadata-only fixture sweeps for real and optional package inputs. Closed by adding optional sweep-case handling that reports missing dependencies as warnings, carrying NuGet package asset inventory through sweep results, and validating response-file, Windows SDK, extension SDK, NuGet package, and optional missing package cases before generator breadth resumes.
- [x] Metadata Full-Parity 4.8: close the remaining CsWinRT command/source ingestion differences from `.cswinrt/src/cswinrt/cmd_reader.h` and `main.cpp`. Closed by adding per-file `WinRtResolvedMetadataFile` provenance in `WinRtMetadataCache`, preserving source kind/description for path/local/sdk/NuGet inputs, and sorting extension-SDK manifests deterministically before API-contract expansion. Windows SDK registry/module-version probing remains narrowed to the existing explicit root/environment/default-root lookup until plugin/Windows-host integration owns process-level SDK discovery.
- [x] Metadata Full-Parity 4.9: promote `MethodImpl` and explicit/member implementation metadata from auxiliary inventory into the normalized model. Closed by parsing MethodImpl rows into `WinRtMethodImplementationDefinition`, modeling method-def/member-ref bodies and declarations with row/name/owner data, attaching explicit implementation descriptors to `WinRtTypeDefinition`, marking MethodImpl as modeled auxiliary metadata, and exposing `explicitImplementations(...)` through semantic helpers for later ABI writer decisions.
- [x] Metadata Full-Parity 4.10: mirror the writer generic-scope and generic-instantiation metadata inputs from `.cswinrt/src/cswinrt/type_writers.h` and the generic-instantiation blocks in `code_writers.h`. Closed by adding metadata-owned generic scope descriptors for projected/ABI generic parameter names plus generic signature-usage descriptors over nested type refs, while keeping the existing generic ABI/type-instantiation inventory as the worklist source for later generator consumption.
- [x] Metadata Full-Parity 4.11: finish CsWinRT custom-attribute argument/output fidelity from `write_custom_attribute_args` and `write_custom_attributes`. Closed by adding projection-ready rendered attribute arguments for strings, `System.Type`, booleans, integral/floating values, arrays, enum values, `AttributeTargets` flag expansion, named arguments, and the CsWinRT rule that projected `AttributeUsage` always receives an explicit `AllowMultiple = true/false` argument.
- [x] Metadata Full-Parity 4.12: expand real metadata fixture validation from managed fixtures to installed Windows SDK and Windows App SDK/NuGet surfaces before unfreezing broader generator/projection/sample work. Closed by adding real-machine metadata-only fixture sweeps for installed Windows SDK Foundation/UniversalApi contract WinMDs and Windows App SDK NuGet package inputs, validating `Windows.Foundation`, `Windows.Foundation.Collections`, `Microsoft.UI.Xaml.Data`, `Microsoft.UI.Xaml.Interop`, and package WinMD/resource/native asset discovery while keeping optional-missing-package behavior as a warning path. Full transitive SDK/NuGet dependency graph resolution remains plugin-owned rather than a substitute for this metadata source validation.
- [x] Metadata Full-Parity 4.13: close the remaining `.cswinrt/src/cswinrt/cmd_reader.h` command/source ingestion differences before generator breadth. Closed by switching response-file expansion to CsWinRT-style per-line Windows command-line tokenization with comment-line handling, backslash/quote escaping, and `@` misuse diagnostics for directories/WinMDs; and by modeling Windows SDK discovery mode explicitly so environment/default-root lookup stays separate from the Windows-host registry/module-version probing boundary that the later plugin must own.
- [x] Metadata Full-Parity 4.14: make broad real Windows SDK and Windows App SDK package validation projection-strict, not just metadata-load-strict. Closed by resolving CLR/System base-type category references (`System.Enum`, `System.ValueType`, and `System.MulticastDelegate`) in the shared type classifier so real SDK/AppSDK metadata can normalize those cache-style references before generator projection validation; full transitive package dependency graph resolution remains the later kotlin-winrt plugin boundary.
- [x] Metadata Full-Parity 4.15: mirror `code_writers.h` `get_attributed_types(...)` exactly for activation/static/composable metadata. Closed by modeling per-attribute factory shapes on `WinRtActivationShape`, preserving Activatable/Static/Composable kind and composable public visibility, merging repeated factory interfaces into metadata-owned `WinRtAttributedFactoryDescriptor` values, and exposing component-mode activatable-class pre-scan descriptors before factory generation consumes them.
- [x] Metadata Full-Parity 4.16: tighten value-type and blittability semantics to the `code_writers.h` helper contract instead of relying only on precomputed layout flags. Closed by adding `WinRtSemanticValueDescriptor`, metadata-owned `isTypeBlittable(type, forArray)` and `isValueType(type)` helpers, mapped-type `requiresMarshaling` handling, enum array non-blittability, and recursive struct field checks that generator/runtime code can consume without duplicating the branch table.
- [x] Metadata Full-Parity 4.17: finish FastAbi helper exactness from `helpers.h` `fast_abi_class`. Closed by extending `WinRtFastAbiClassDescriptor` with property getter/setter caches, class-hierarchy-adjusted vtable segment starts, getter/setter slot indexes, and `containsOtherInterface` / `containsGetter` / `containsSetter` helper semantics over the existing FastAbi default/exclusive interface ordering.
- [x] Metadata Full-Parity 4.18: close the remaining generic-interface metadata worklist from `code_writers.h`. Closed by adding metadata-owned `hasDerivedGenericInterface`, carrying derived generic interface names in `WinRtGenericAbiInventory`, continuing generic type instance discovery from nested generic references, and exposing concrete generic substitution through the same `WinRtTypeRef.substituteTypeParameters(...)` path used by interface closure resolution.
- [x] Metadata Full-Parity 4.19: expose method/vtable identity helpers at the metadata boundary. Closed by adding `WinRtMethodVtableDescriptor` plus `methodVtableDescriptor(...)` / `methodVtableDescriptors(...)` helpers that derive CsWinRT-style vmethod indexes, vmethod names, and inspectable-offset slot indexes from method row ordering.
- [x] Metadata Full-Parity 4.20: close the remaining helper-owned exclusive/default interface semantics from `.cswinrt/src/cswinrt/helpers.h`. Closed by adding metadata-owned `getExclusiveToType(...)`, `isPType(...)`, and `getDefaultInterfaceSemantics(...)`, resolving `ExclusiveToAttribute` `System.Type` payloads or runtime-class implementers, and exposing default-interface `WinRtTypeSemantics` for generator class/ABI writers.
- [x] Metadata Full-Parity 4.21: make component-mode activation and CCW interface decisions match `.cswinrt/src/cswinrt/main.cpp` and `helpers.h`. Closed by restricting component activatable pre-scan descriptors to Activatable/Static factory shapes and making `doesAbiInterfaceImplementCcwInterface(...)` context-aware with `component`, projection filter inclusion, and exclusive-interface checks; projection inventory now passes the context into generic ABI inventory.
- [x] Metadata Full-Parity 4.22: mirror CsWinRT private implementation detection from `code_writers.h` `method_signature_equal`, `is_implemented_as_private_method`, and `is_implemented_as_private_mapped_interface`. Closed by adding projected method-signature descriptors, signature equality, private explicit method matching, and mapped-interface private implementation descriptors over method/property/event probe members.
- [x] Metadata Full-Parity 4.23: expose the custom query-interface inventory used by `code_writers.h` `write_custom_query_interface_impl`. Closed by adding `WinRtCustomQueryInterfaceDescriptor` with overridable interface names, base-class participation, sealed/base visibility, override/virtual modifier shape, and base delegation state.
- [x] Metadata Full-Parity 4.24: make the generic type-instantiation worklist genuinely fixed-point like `code_writers.h` `generic_type_instances` / `write_generic_type_instantiations(...)`. Closed by adding `WinRtGenericInstantiationWorklistDescriptor` with pending/written state transitions, enqueue deduplication, and context-aware generic inventory handoff for write-time growth.
- [x] Metadata Full-Parity 4.25: close remaining raw-table semantic gaps that are currently only row-size tolerant. Closed by adding auxiliary table semantic boundary descriptors that aggregate tolerated auxiliary tables, flag modeled rows, and mark projection-affecting tables (`FieldMarshal`, `ImplMap`, `FieldRVA`, `DeclSecurity`, `ModuleRef`) as explicit decode-before-consume boundaries instead of silent generator inputs.
- [x] Metadata Full-Parity 4.26: add a final CsWinRT parity audit test that enumerates the helper/code-writer metadata entry points from `.cswinrt/src/cswinrt/helpers.h`, `main.cpp`, `type_writers.h`, and `code_writers.h` against `winrt-metadata` APIs. Closed by adding metadata parity audit entries for the remaining helper/code-writer entry points and tests that fail if any entry remains open.
- [x] Metadata Full-Parity 4.27: mirror the `main.cpp` helper-output inventory as metadata-owned writer input. Closed by adding `WinRtProjectionHelperOutputInventory` on `WinRtMetadataProjectionInventory`, including `WinRTEventHelpers.cs`, `WinRTBaseTypeMappingHelper.cs`, `WinRTAbiDelegateInitializer.cs`, `WinRTGenericTypeInstantiations.cs`, `AuthoringMetadataTypeMappingHelper.cs`, and base helper inclusion state derived from traversal results.
- [x] Metadata Full-Parity 4.28: mirror the NetStandard ABI delegate initializer conditions from `main.cpp`. Closed by reporting `WinRTAbiDelegateInitializer.cs` only when generic ABI delegate entries exist and `NetStandard20` is active, plus the special `Windows.Foundation.AsyncStatus` delegate registration/declaration condition tied to the projection filter.
- [x] Metadata Full-Parity 4.29: mirror the generic-instantiation and base string helper conditions from `main.cpp`. Closed by distinguishing the non-NetStandard `WinRTGenericTypeInstantiations.cs` path from the NetStandard ABI delegate initializer path, and by reporting base helper inclusion from `projectionFileWritten`, including the `ComInteropHelpers` skip unless the filter includes `Windows`.
- [x] Metadata Full-Parity 4.30: mirror `code_writers.h` `is_manually_generated_iface` as a metadata-owned descriptor. Closed by adding `WinRtManualInterfaceDescriptor` / `isManuallyGeneratedInterface(...)` for `Microsoft.UI.Xaml.Interop.IBindableIterable` and `Microsoft.UI.Xaml.Interop.IBindableVector`.
- [x] Metadata Full-Parity 4.31: turn the stored projection context flags into semantic descriptors before generator consumption. Closed by adding `WinRtProjectionContextSemanticsDescriptor` and `WinRtTypeProjectionContextDescriptor` for `internal`, `embedded`, `public_enums`, `public_exclusiveto`, `idic_exclusiveto`, and `partial_factory` accessibility, IDIC, vtable-pointer, and activation-factory fallback decisions.
- [x] Metadata Full-Parity 4.32: mirror the `code_writers.h` class/interface member merge handoff used by `write_class_members`. Closed by adding `WinRtClassMemberMergeDescriptor`, interface member descriptors, and merged property descriptors for default/exclusive/interface getter/setter targets, platform metadata, overridable/public/private state, and static/factory call targets.
- [x] Metadata Full-Parity 4.33: expose the object/class equality and hash-code helper family from `helpers.h` as metadata-owned descriptors. Closed by adding `WinRtObjectMethodMatchDescriptor`, `WinRtClassObjectMethodDescriptor`, `isObjectEqualsMethod`, `isClassEqualsMethod`, `isObjectHashCodeMethod`, `hasObjectEqualsMethod`, `hasClassEqualsMethod`, `hasObjectHashCodeMethod`, and `classObjectMethodDescriptor` with return-type match state.
- [x] Metadata Full-Parity 4.34: make generic ABI delegate inventory exact, not merely type-key based. Closed by expanding `WinRtGenericAbiDelegateDescriptor` with operation name, ABI parameter shape, declaration, and type-array shape, and by collecting distinct operation entries for CsWinRT generic ABI delegates instead of collapsing them by ABI type.
- [x] Metadata Full-Parity 4.35: expose generic type-instantiation writer descriptors from `write_generic_type_instantiation(...)` / `write_generic_type_instantiations(...)`. Closed by adding `WinRtGenericInstantiationWriterDescriptor` plus helpers that describe delegate-vs-interface instantiations, RCW/vtable/property function names, special-method skips, generic-signature gates, and initialization dependencies from implemented interfaces and event types.
- [x] Metadata Full-Parity 4.36: mirror CsWinRT type-name writer context from `type_writers.h` / `code_writers.h` instead of leaving name-kind rewriting to generator-local rules. Closed by adding `WinRtProjectedNameKind`, `WinRtTypeNameWriterContext`, and `WinRtTypeNameDescriptor` for Projected/ABI/CCW/NonProjected/StaticAbiClass name kinds, ABI namespace and ABI.Impl component context, exclusive-to projected-to-CCW rewriting, namespace-prefix decisions, and generic arity stripping.
- [x] Metadata Full-Parity 4.37: model event-helper subclass writer inputs exactly. Closed by adding `WinRtEventHelperSubclassDescriptor` and `eventHelperSubclassDescriptors(...)` with projected event type, ABI event type, owner type, generic parameters, source class name, and duplicate suppression.
- [x] Metadata Full-Parity 4.38: expose platform-guard and member-platform descriptors used by `write_platform_guard` / `write_platform_attribute_temp`. Closed by adding `WinRtPlatformGuardDescriptor` and `platformGuardDescriptor(...)` for platform-check state, per-member platform attributes, and duplicate platform suppression.
- [ ] Metadata Full-Parity 4.39: expose exact projected/ABI signature writer descriptors from `code_writers.h` `is_keyword`, `write_escaped_identifier`, `write_parameter_name(_with_modifier)`, `write_projection_parameter_type`, `write_projection_return_type`, `write_abi_type`, `write_abi_parameter*`, `write_abi_return*`, `write_abi_signature`, and generic-signature detection. Metadata must carry escaped identifier state, Kotlin/CsWinRT keyword collisions, projection parameter modifier/category text, ABI parameter and return shapes for NetStandard-vs-pointer modes, array receive/fill/pass expansion, and generic ABI type-parameter substitutions before generator method/property/event emission consumes them.
- [ ] Metadata Full-Parity 4.40: model event invoke and event-source writer inputs from `write_event_source_type_name`, `get_event_invoke_method(_signature)`, `write_event_invoke_params`, `write_event_invoke_return(_default)`, `write_event_out_defaults`, `write_event_invoke_args`, `write_event_param_types`, `write_event_source_ctor`, `write_event_sources`, and `write_event_source_table`. Metadata must describe delegate invoke parameter/return/default/out-reset behavior, projected event generic argument text, source-table method indexes, add/remove token flow shape, and static-vs-instance event-source target state instead of leaving those choices to generator templates.
- [ ] Metadata Full-Parity 4.41: expose CsWinRT ABI marshaler plan descriptors from `abi_marshaler`, `set_abi_marshaler`, `get_abi_marshalers`, and `write_abi_method_call_marshalers`. Metadata must classify each parameter/return as object-in/ref/out/array/pinnable/generic, record marshaler type/local type/interface IID/init-RCW-helper/generic-instantiation dependencies, and expose assignment/invoke/from-ABI/dispose requirements for method calls, static ABI calls, generic instantiation classes, and raw-return factory methods.
- [ ] Metadata Full-Parity 4.42: model static, activation-factory, factory, and composable writer descriptors from `write_class_static_cache_definition`, `write_activation_factory_objref_definition`, `write_static_objref_definition`, `write_factory_constructors`, `write_composable_constructors`, `write_static_members`, and `write_attributed_types`. Existing activation metadata identifies factories, but metadata still needs writer-ready cache-object names, default-interface names, base-factory `As<I>` hiding state, merged static property getter/setter targets, constructor visibility, GC-pressure handoff, and platform attributes per factory interface.
- [ ] Metadata Full-Parity 4.43: expose custom mapped member output descriptors from `write_custom_mapped_type_members` and its collection/XAML/system helpers. Metadata must turn mapped `IIterable`, `IIterator`, `IMap`, `IMapView`, `IVector`, `IVectorView`, bindable `IEnumerable`/`IList`, `IDisposable`, and `INotifyDataErrorInfo` into deterministic member-plan entries with IDIC-vs-static-ABI call mode, explicit/private emission, enumerable inclusion, indexer-name attributes, and target/objref naming.
- [ ] Metadata Full-Parity 4.44: close property-interface lookup and interface member signature exactness from `find_property_interface` and `write_interface_member_signatures`. Metadata must expose base-vs-peer-exclusive getter lookup, static factory interface search, `new` property keyword decisions for setter-only overrides, projected interface method/property/event signature descriptors, and explicit-interface property/event names without making generator recursively search interfaces during emission.
- [ ] Metadata Full-Parity 4.45: expose GUID/IID signature writer descriptors from `write_guid_signature`, `write_guid`, `write_guid_attribute`, `write_guid_bytes`, and parameterized generic signature construction. Metadata already stores GUID values, but it still needs writer-ready lowercase/uppercase/text/byte forms, namespace-qualified GuidAttribute selection, enum/struct/delegate/interface/runtime-class signature fragments, default-interface class signatures, generic instantiation signature fragments, and blittable field-signature recursion for later parameterized IID/generator parity.
- [ ] Metadata Full-Parity 4.46: model vtable delegate/function-pointer writer descriptors from `write_vtable`, `write_static_abi_class_members`, `write_generic_method_delegate_variable`, `get_vmethod_delegate_type`, and the generic ABI type-array paths used by generic interfaces. Metadata must expose vtable field kind, delegate cache indexes, function-pointer-vs-Delegate fallback, NetStandard/generic workarounds, marshal-to-ABI/projection initialization snippets as structured operations, and generic method/type-array requirements before ABI interface generation resumes.
- [x] Generator 3.1: keep the Kotlin generator behind completed Metadata 2.1 through Metadata 2.4 prerequisites and derive its behavior from `.cswinrt/src/cswinrt` responsibilities rather than from existing handwritten projection files.
- [x] Generator 3.2: implement declaration planning first: determine the Kotlin declaration set for enums, structs, delegates, interfaces, runtime classes, and metadata companions before detailed member emission grows.
- [x] Generator 3.3: implement deterministic declaration-shell emission next: namespace layout, type declarations, interface/runtime-class scaffolding, companion metadata surfaces, and stable file/text ordering.
- [x] Generator 3.4: implement member emission next: methods, properties, events, interface inheritance handling, default-interface/runtime-class composition, and activation/static/factory surfaces.
- [x] Generator 3.5: now that Metadata 2.9 is coherent, resume the previously frozen `.cswinrt/src/cswinrt` special-case rule queue for the generator-owned ABI emission layer. Closed by slot-based ABI emission cleanup, common `RawAddress` / `PlatformAbi` allocation-readback helpers, fixed `ComVtableInvoker` calls instead of the old JVM FFM descriptor surface, mapped collection parameter marshaling through runtime helpers, bindable collection ABI categories for MUX/WUX, and non-generic projected-interface proxy wrapping for supported ABI surfaces.
- [x] Generator 3.6: implement the remaining async/custom mapping ABI supplement after the runtime-facing async projection contract is explicit. Closed by replacing the old `CompletableFuture` type-name substitution with generated `WinRtAsyncActionReference` / `WinRtAsyncActionWithProgressReference<TProgress>` / `WinRtAsyncOperationReference<T>` / `WinRtAsyncOperationWithProgressReference<TResult, TProgress>` ABI return wrapping backed by runtime async references, parameterized async/progress/completed handler IID construction from `WinRtTypeSignature`, and direct `GetResults` slot readback for the supported scalar/string result families.
- [x] Generator 3.7: split the `.cswinrt/src/cswinrt` generator pipeline into `winrt-generator`, leaving `winrt-projections` as the deterministic generated-output module corresponding to `.cswinrt/src/Projections`. Closed by moving the generator into `winrt-generator`, adding a Windows SDK WinMD-driven generation CLI, and using it to produce the first checked-in representative projection slice.
- [ ] Plugin 4.1: create the `kotlin-winrt` Gradle plugin module and DSL. It must expose metadata inputs for Windows SDK (`sdk`, `sdk+`, explicit SDK version/root) and NuGet packages, projection namespace/type filters, generated-source output wiring, and a stable task model that can be used by both library and application projects.
- [ ] Plugin 4.2: implement NuGet package resolution for metadata: restore or locate packages, parse `.nuspec` dependency closure, discover package WinMD files deterministically, and feed them through `WinRtMetadataSource` without mixing runtime resource staging into metadata loading.
- [ ] Plugin 4.3: implement the library model (`kotlin-winrt-library`): generated Kotlin sources become part of the library artifact, and the artifact/klib carries enough NuGet package identity plus WinMD metadata dependency information for a downstream application to reproduce the runtime/resource graph.
- [ ] Plugin 4.4: implement the application model (`kotlin-winrt-application`): consume transitive library metadata/resource descriptors, resolve the final NuGet runtime closure, and stage app resources/native assets/MSIX fragments/PRI files into the application distribution instead of forcing each library to copy runtime assets.
- [ ] Plugin 4.5: migrate the useful NuGet runtime/resource staging logic currently trapped in legacy `sample-jvm-winui3/build.gradle.kts` into the plugin application model, then validate it from `winrt-samples`.
- [ ] Generator gate: the current `KotlinProjectionGenerator` skeleton and `Windows.Data.Json` regression fixture are validation scaffolding only; do not treat them as permission to add handwritten standard WinRT projection files before the metadata and generator prerequisites are closed.

## Authoring And Sample Subslice Order

- [ ] Sample 6.1: keep `winrt-samples` on the smallest smoke scenario for the currently completed generator/projection slice first; do not let samples depend on handwritten standard WinRT projection files.
- [ ] Sample 6.2: use samples to validate completed upstream work in dependency order: generated projection smoke first, plugin-driven Windows SDK/NuGet source generation next, then broader WinRT API smoke, then WinUI bootstrap/resource/message-loop scenarios after the corresponding projection and application-resource support exists.
- [ ] Sample 6.3: only after runtime, metadata, generator, and projection prerequisites are documented and implemented, expand to WinUI bootstrap, resources, window/message-loop, and broader desktop validation.
- [ ] Sample gate: if a sample requires new handwritten projection logic, authoring glue, or runtime workarounds, stop and move that missing behavior back into `winrt-runtime`, `winrt-metadata`, `winrt-projections`, or `winrt-authoring` instead of embedding it in `winrt-samples`.
- [ ] Authoring 5.1: authoring is intentionally last for now. Do not start the `.cswinrt/src/Authoring` hosting boundary until generator/projections/sample work has a coherent generated-projection validation path.
- [ ] Authoring 5.2: when authoring resumes, define the minimum `.cswinrt/src/Authoring`-aligned hosting boundary first: authored runtime object lifetime, ABI-facing identity/ownership, and activation/factory expectations.
- [ ] Authoring 5.3: define how authoring depends on the existing runtime and metadata/generator contracts next, including what generated metadata or helper code must exist before authored components can be surfaced safely.
- [ ] Authoring gate: the current `winrt-authoring` module is still a placeholder and explicitly frozen; do not let sample, projection, or test pressure invent its contract before the `.cswinrt/src/Authoring` boundary is intentionally reopened.

## Runtime Subslice Order

- [x] Runtime 1.1: close `.cswinrt/src/WinRT.Runtime` ABI primitive gaps first: `Guid`, `HRESULT`, low-level memory layout helpers, raw vtable-call helpers, and WinRT string ownership/reference mechanics.
- [x] Runtime 1.2: close platform-boundary gaps next: COM/WinRT initialization lifetime, Windows API bindings, module loading, proc lookup, and HRESULT-to-exception/error translation.
- [x] Runtime 1.3: close object-reference gaps next: `IUnknown`/`IInspectable` wrappers, ownership/dispose semantics, interface query/cast helpers, runtime class name access, and object identity behavior.
- [x] Runtime 1.4: close activation gaps next: `RoGetActivationFactory`, manifest-free activation fallback, activation factory caching, and runtime-class activation helpers.
- [x] Runtime 1.5: close generic-signature prerequisites next: type-signature rendering and parameterized IID hashing needed by later collection/delegate/generic projection slices.
- [x] Runtime 1.6: only after Runtime 1.1 through Runtime 1.5 are coherent, expand delegate and callback marshaling support.
- [x] Runtime 1.7: only after Runtime 1.1 through Runtime 1.6 are coherent, expand collection adapters and projected collection interop breadth.
- [x] Runtime 1.8: only after Runtime 1.1 through Runtime 1.7 are coherent, expand async bridges and completion callback semantics.
- [x] Runtime 1.9: only after Runtime 1.1 through Runtime 1.8 are coherent, expand WinUI-specific runtime hooks, Xaml-specific mappings, and reference-tracking behavior.
- [x] Runtime 1.10: complete `.cswinrt/src/WinRT.Runtime/IWinRTObject*` parity next: query-interface caches, additional type-data caches, `GetObjectReferenceForType`, `IsInterfaceImplemented`, and the typed RCW lookup behavior that later runtime projections depend on. The JVM path now has a formal `IWinRTObject` contract, per-wrapper QI/additional-data state, object-reference lookup, and primary-interface self-lookup; later dedicated optimized-object/RCW-registration work remains in Runtime 1.13.
- [x] Runtime 1.11: complete `.cswinrt/src/WinRT.Runtime/Projections/IEnumerable*`, `IList*`, `IReadOnlyList*`, `IDictionary*`, and `IReadOnlyDictionary*` runtime parity next: RCW/CCW helpers, `CreateMarshaler` / `FromManaged` / `FromAbi`, and runtime-side `ToAbiHelper` / `FromAbiHelper` paths. The JVM runtime now has explicit iterable/list/map/view projection helpers and managed-hosted CCW stubs for these generic collection shapes; exact typed RCW registration and precise CCW backing allocation/reclamation are closed in Runtime 1.13 / Inventory B, while the next remaining blockers now move to the projection/type-name registry and later value-projection queue in Runtime 1.15 and Runtime 1.17.
- [x] Runtime 1.12: complete `.cswinrt/src/WinRT.Runtime/Projections/Bindable*` runtime parity next: `IBindableIterable` / `IBindableVector` / `IBindableVectorView`, non-generic object marshalling, and bindable adapter behavior before restarting bindable generator work. The JVM runtime now has explicit IBindable object-reference wrappers, non-generic iterable/vector/vector-view RCW+CCW helpers, bindable `CreateMarshaler` / `FromManaged` / `FromAbi` paths, bounded inspectable/object round-tripping for bindable collections, the shared marshaler core from Runtime 1.14 behind them, and the shared boxed-value projection owner from Runtime 1.17 feeding the non-generic object path. Remaining work now shifts to the later event/XAML/system-service slices rather than back to another round of `IPropertyValue` glue.
- [x] Runtime 1.13: complete `.cswinrt/src/WinRT.Runtime/ComWrappersSupport*`, `ObjectReference.cs`, `IInspectable*`, `SingleInterfaceOptimizedObject.net5.cs`, `CastExtensions.cs`, and `DerivedComposed.cs` parity next: typed RCW creation, CCW creation, runtime-class-name based wrapper lookup, unwrap helpers, and single-interface optimized object behavior. The JVM runtime now has `ComWrappersSupport`, helper-type and typed-RCW registration, runtime-class-name wrapper lookup, managed CCW factory registration, unwrap/cast helpers, `DerivedComposed`, `SingleInterfaceOptimizedObject`, and inspectable-info lookup for managed CCW hosts; the shared marshaler model is now also closed by Runtime 1.14, and the ongoing KMP split has now moved the `ComWrappersSupport` policy core itself into `commonMain` so JVM only keeps the Java `Class` compatibility overloads and the `PlatformInspectableInterop` actual that still bridges built-in inspectable/value projections. The next runtime blocker stays on the remaining marshaler/system-projection/value-boxing owners rather than more object-wrapper glue.
- [x] Runtime 1.14: complete `.cswinrt/src/WinRT.Runtime/Marshalers.cs`, `FundamentalMarshalers.cs`, and projection marshaller parity next: generic `Marshaler<T>` behavior, `MarshalInterface`, `MarshalInspectable`, `MarshalBlittable`, object/value boxing rules, and array marshalling support instead of per-slice ad hoc generator logic. The JVM runtime now has a shared `Marshalers.kt` layer covering generic `Marshaler<T>`, blittable/scalar marshalling, HSTRING marshalling, interface/inspectable/object marshalling, delegate marshalling entry points, and typed array marshalling support.
- [x] Runtime 1.14 KMP native-value seam follow-up: keep `.cswinrt/src/WinRT.Runtime/FundamentalMarshalers.cs` and the adjacent ABI layout helpers from remaining as JVM-owned production owners while the remaining marshaler/value-boxing layers are drained. `BooleanMarshaller`, `CharMarshaller`, and `GuidMarshaller` now live in `commonMain`, `StringMarshaller` is reduced to a thin JVM bridge over the shared `NativeStringMarshaller`, and the old `AbiLayouts.kt` owner is gone in favor of internal `NativeLayoutsJvmCompat` / `RawVtableCallJvmCompat` helpers that are no longer treated as runtime policy owners.
- [x] Runtime 1.15: complete `.cswinrt/src/WinRT.Runtime/TypeNameSupport.cs`, `TypeExtensions.cs`, `GuidGenerator.cs`, and `Projections.cs` parity next: runtime-class-name lookup, projection assembly registration, custom type mapping registry, non-WinRT runtime-class lookup hooks, and helper-type discovery. The JVM runtime now has explicit `Projections.kt`, `TypeNameSupport.kt`, `TypeExtensions.kt`, `GuidGenerator.kt`, projection annotations for runtime GUID/helper/default-interface metadata, `ComWrappersSupport` registration hooks for projection assemblies and non-WinRT runtime-class-name lookups, targeted registry tests, and the ongoing KMP split now keeps this owner set on explicit `KClass`/descriptor registration instead of `Class.forName`, runtime annotation scanning, or any typed-array reflection seam. The next remaining runtime blocker moves to the interop/error-info/platform slice in Runtime 1.16.
- [x] Runtime 1.16: complete `.cswinrt/src/WinRT.Runtime/Interop/*`, `AgileReference.cs`, `WeakReference.netstandard2.0.cs`, `Context.cs`, `MonoSupport.cs`, `ExceptionHelpers*`, and `Module.cs` / `Platform.cs` parity next: agile/weak/context/marshal interop, activation-factory interop, IID catalog coverage, richer exception/error-info flow, Mono-specific runtime hooks, and remaining platform helper surfaces. The JVM runtime now has the shared IID catalog converged on `.cswinrt/src/WinRT.Runtime/Interop/IID.tt`/`IID.g.cs`, `WinRtModule`-owned MTA usage for activation, dynamic WinRT error-entry loading plus system-message/BSTR/platform helpers, `ExceptionHelpers`-owned HRESULT classification and managed `IErrorInfo` fallback propagation, `AgileReference` with `RoGetAgileReference`/GIT fallback, `WeakReferenceSource`/`IWeakReference` consumer wrappers, `Context` token/callback helpers, and an explicit shared `MonoSupport` no-op owner. During the KMP split, this platform/interop owner set is also converging physically under `jvmMain`: `WinRtPlatformApi` now carries the former `WindowsRuntimePlatform` FFM entrypoints directly instead of routing them through an extra abstraction layer, and the adjacent `AgileReference`, `Context`, `WeakReference`, and HSTRING owner `WindowsRuntimeStrings` have also been moved out of the legacy shared source tree. The follow-up quick migration now also lifts the raw `IWeakReferenceSource` / `IWeakReference` wrappers, the thin `WinRtExceptionTranslator` facade, and the `ExceptionHelpers` HRESULT/restricted-error-info core into `commonMain`, leaving target-specific throwable-class mapping plus managed `SetErrorInfo` hosting behind `platformHResultFromThrowable` / `platformSetErrorInfo` actuals instead of another JVM-owned policy owner. This keeps the eventual common runtime seam close to cswinrt's `Platform.cs` / interop responsibility split without adding wrapper overhead on the JVM hot path. Narrow residual deviations are explicit rather than implicit drift: Kotlin/JVM does not host CLR/Mono exception CCWs or automatic COM aggregation, so `.cswinrt`'s language-exception propagation path and free-threaded-marshaler-backed `IMarshal` aggregation remain isolated as later platform-adaptation residuals instead of being re-spread across runtime call sites.
- [x] Runtime 1.17: complete `.cswinrt/src/WinRT.Runtime/Projections/Nullable.cs`, `IReferenceArray*`, `IPropertyValue*`, `IDisposable.net5.cs`, `IDisposable.netstandard2.0.cs`, `Type.cs`, `Type.ArrayMarshalling.cs`, `SystemTypes.cs`, `Uri.cs`, `Numerics.cs`, and `Geometry.cs` parity next: nullable/value boxing, `IReference<T>` / `IReferenceArray<T>`, property-value projections, `IClosable`/`IDisposable` projection behavior, WinRT type projections, and system/numerics/geometry custom projections. The JVM runtime now has `.cswinrt`-aligned boxed-value ownership in `ValueBoxing.kt`, including boxed runtime-class-name generation, enum/type/HResult nullable support, descriptor-backed enum ABI value registration instead of reflective `abiValue` field lookup, and shared RCW unboxing through `ComWrappersSupport` plus bindable object marshalling. The projection-facing `IReference<T>` / `IReferenceArray<T>` / `IPropertyValue` entry owners from `.cswinrt/src/WinRT.Runtime/Projections/Nullable.cs` now live in shared `commonMain` code under `ValueProjectionInterop.kt`, while `PlatformValueProjectionInterop` keeps the remaining platform boxing/reference seams target-specific for JVM and future `mingwX64` parity. `SystemProjections.kt` owns the matching `DateTime` / `TimeSpan` / `Type` / `Uri` / `IClosable` / numerics / geometry projections, but the Kotlin public/common contract now narrows the former JVM-only `java.net.URI` mapping to a runtime-owned `WinRtUri` value type so this slice stays viable for `mingwX64`. The JVM registry bridge now also treats `@WindowsRuntimeType` / `@WinRtGuid` / helper/default-interface annotations as a real metadata source, so the built-in numerics/geometry struct registration no longer duplicates WinRT type-name and signature literals in `SystemProjections.kt`. The primitive scalar and primitive-array classifier/boxing table now also converges under `WinRtTypeClassifier`, so `TypeNameSupport.kt` and `ValueBoxing.kt` no longer maintain parallel byte/char/int/unsigned array lookup tables, and `TypeProjection.fromManaged` no longer keeps a second `Any`/`String`/`Guid`/`Class` metadata-special-case branch outside `Projections.isTypeWindowsRuntimeType(...)`. Boxed `IReference<T>` / `IReferenceArray<T>` runtime-name construction and parsing now also converge under shared `WinRtReferenceTypeNames`, so `TypeNameSupport.kt`, `ValueBoxing.kt`, and `SystemProjections.kt` no longer hand-maintain parallel boxed-name string builders. `TypeNameSupport.kt` resolves boxed runtime names such as `Windows.Foundation.IReference`1<T>` and `Windows.Foundation.IReferenceArray`1<T>` back to the projected Kotlin type. The remaining delegate/event-handler-specific `Nullable.cs` branches are not left behind as drift; they stay explicitly queued with Runtime 1.18's event/delegate owner set.
- [x] Runtime 1.18: complete `.cswinrt/src/WinRT.Runtime/Projections/EventHandler.cs`, `GenericDelegateHelper.net5.cs`, `DelegateExtensions.cs`, `EventRegistrationToken*`, `Interop/StandardDelegates.cs`, and `Interop/EventSource*` parity next: event source caches, registration-token tables, helper delegates, standard delegate ABI helpers, and projected event plumbing beyond the current minimal delegate bridge. The shared Kotlin owner set is now in place across `EventRegistrationToken.kt`, `EventRegistrationTokenTable.kt`, `EventSourceCache.kt`, `EventSourceState.kt`, `EventSource.kt`, `StandardDelegates.kt`, `GenericDelegateHelper.kt`, and `EventHandlerEventSource.kt`. The `.cswinrt` `System.EventHandler` / `DelegateExtensions` responsibilities are narrowed explicitly to Kotlin function-type adapters plus direct vtable invocation instead of a CLR multicast-delegate compatibility layer, while preserving the same event source / token / weak-cache ownership split.
- [x] Runtime 1.19: complete `.cswinrt/src/WinRT.Runtime/Projections/ICommand*`, `ICustomPropertyProvider*`, `INotifyPropertyChanged*`, `INotifyCollectionChanged*`, `INotifyDataErrorInfo*`, `PropertyChangedEventHandler.cs`, `PropertyChangedEventArgs.cs`, `NotifyCollectionChangedEventHandler.cs`, `NotifyCollectionChangedEventArgs.cs`, `NotifyCollectionChangedAction.cs`, `DataErrorsChangedEventArgs.cs`, `IServiceProvider*`, and `IStringable.cs` parity next: XAML binding/runtime projections, non-generic event projections, property/data-error change event args, and remaining system-service projection helpers. The JVM runtime now owns this slice in `WinRtXamlSystemModels.kt` and `XamlSystemProjectionRuntime.kt`, wiring `.cswinrt`-aligned RCW/CCW definitions, WUX/MUX alias handling, runtime-class-name registration for the XAML event-args types, service-provider and stringable projections. The explicit Kotlin-specific narrowing is kept narrow and documented rather than diffused across call sites: `ICustomPropertyProvider` retains an explicit provider/property contract instead of `.cswinrt`'s CLR reflection fallback, so any later XAML mapping sugar should live above the runtime via delegates / `inline reified` helpers, and the managed-to-ABI event-args path uses synthetic runtime-class CCWs rather than calling WinUI activation factories from the runtime core.
- [x] Runtime 1.20: add the `.cswinrt/src/cswinrt/strings/additions/Windows.Foundation/AsyncInfo.cs` and `TaskToAsync*Adapter.cs` runtime supplement for async authoring/projection output. `AsyncInfo.kt` now owns coroutine-backed `IAsyncAction` and `IAsyncActionWithProgress<TProgress>` CCWs for completed, canceled, failed, and running actions plus `IAsyncOperation<T>` and `IAsyncOperationWithProgress<TResult, TProgress>` CCWs for completed, canceled, and failed operation results through an explicit `WinRtAsyncResultWriter<T>` ABI writeback seam. The runtime also owns progress handler registration/reporting and parameterized async/progress/completed handler IID helpers for the generator to consume.
- [ ] Runtime gate: the existing delegate, collection, bindable, value-boxing, XAML/system, and async authoring/projection helpers already present in `winrt-runtime` are now the completed staged runtime queue through Runtime 1.20; do not use that as justification to skip any future broader parity inventory item.
- [ ] Runtime gate: while closing the remaining runtime inventory after Runtime 1.19, do not introduce fresh duplicated primitive/category/shape classifiers in `Marshalers`, `GuidGenerator`, `TypeNameSupport`, `Projections`, collection helpers, bindable helpers, or generator call planners. If a new slice needs one of those decisions, refactor it into the shared `.cswinrt`-aligned owner first.

## Runtime Remaining Parity Inventory

- [x] Inventory A: close the remaining object-wrapper contract from `.cswinrt/src/WinRT.Runtime/IInspectable.cs`, `IInspectable.net5.cs`, `IWinRTObject.net5.cs`, and `SingleInterfaceOptimizedObject.net5.cs` by adding the missing dynamic interface-cast behavior, query-interface caching, additional type data, and wrapper-identity rules to `winrt-runtime`. The JVM runtime now has the formal `IWinRTObject` state model from Runtime 1.10 plus the `SingleInterfaceOptimizedObject` / cast / wrapper-identity layer from Runtime 1.13.
- [x] Inventory B: close the remaining RCW/CCW infrastructure from `.cswinrt/src/WinRT.Runtime/ComWrappersSupport.cs`, `ComWrappersSupport.net5.cs`, `ComWrappersSupport.netstandard2.0.cs`, `ObjectReference.cs`, `CastExtensions.cs`, and `DerivedComposed.cs` by implementing typed RCW registration, CCW creation, unwrap support, runtime-class-name-based wrapper lookup, and helper-type registration instead of leaving those responsibilities scattered across generated code. This is now closed by the JVM-side `ComWrappersSupport`, helper-type and typed-RCW factory registries, managed CCW creation, unwrap helpers, cast helpers, and `DerivedComposed` marker landed in Runtime 1.13.
- [x] Inventory C: close the remaining marshaler infrastructure from `.cswinrt/src/WinRT.Runtime/Marshalers.cs` and `FundamentalMarshalers.cs` by providing a single runtime marshaling model for interfaces, inspectable/object values, blittable/value types, arrays, and generic projections rather than relying on generator-specific inline special cases. This is now closed by `Marshalers.kt`; the remaining `GuidGenerator.cs` / `TypeExtensions.cs` responsibilities are tracked with the type-name and projection-registry slice in Runtime 1.15 / Inventory D rather than with the universal marshaler core itself.
- [x] Inventory D: close the remaining projection registry and type-mapping infrastructure from `.cswinrt/src/WinRT.Runtime/Projections.cs`, `Projections.CustomTypeMappings.g.cs`, `Projections.CustomTypeMappings.EventHandler.cs`, and `TypeNameSupport.cs`, including projection assembly registration, runtime-class-name lookup, custom mapped ABI/helper types, and non-WinRT runtime class name lookup hooks. This is now closed by the JVM-side `Projections.kt`, `TypeNameSupport.kt`, `TypeExtensions.kt`, `GuidGenerator.kt`, and runtime annotation metadata; later system/value/event/XAML projections stay queued under Inventory G through Inventory K instead of this registry slice.
- [x] Inventory E: close the remaining generic collection projection layer from `.cswinrt/src/WinRT.Runtime/Projections/IEnumerable*`, `IList*`, `IReadOnlyList*`, `IDictionary*`, `IReadOnlyDictionary*`, `CollectionHybrid.net5.cs`, and `KeyValuePair.cs`, including RCW/CCW helper registration, managed-to-ABI collection adapters, and collection helper factories that generator slices can depend on without inventing new runtime contracts. The collection helper layer itself is now present in `winrt-runtime`; any remaining typed registration / wrapper-factory gaps are tracked under Inventory B, and any remaining universal value/interface marshaling gaps are tracked under Inventory C.
- [x] Inventory F: close the remaining bindable/non-generic collection projection layer from `.cswinrt/src/WinRT.Runtime/Projections/Bindable*`, including `IBindableIterator`, `IBindableIterable`, `IBindableVector`, `IBindableVectorView`, object marshalling, non-generic enumerable/list adapters, and bindable helper factories. The remaining higher-level object/value projection work is now explicitly the broader `Marshalers.cs` / `IPropertyValue` queue in Inventory C and Inventory G, not Bindable* helper ownership.
- [x] Inventory G: close the remaining value-boxing and system projection layer from `.cswinrt/src/WinRT.Runtime/Projections/Nullable.cs`, `IReferenceArray*`, `IPropertyValue*`, `Type.cs`, `Type.ArrayMarshalling.cs`, `SystemTypes.cs`, `Uri.cs`, `Numerics.cs`, and `Geometry.cs`, including `IReference<T>`, `IReferenceArray<T>`, property-value semantics, WinRT type projections, and system/numerics custom projections. This is now closed by the shared JVM owner split across `ValueBoxing.kt`, `SystemProjections.kt`, `ComWrappersSupport.kt`, `WinRtBindableProjections.kt`, and `TypeNameSupport.kt`.
- [x] Inventory H: close the remaining interop surface from `.cswinrt/src/WinRT.Runtime/Interop/*`, `AgileReference.cs`, `WeakReference.netstandard2.0.cs`, `Context.cs`, `MonoSupport.cs`, and `Module.cs` / `Platform.cs`, including agile references, weak references, context callbacks, marshal interfaces, activation-factory interop, IID coverage, standard delegate ABI helpers, and remaining platform helpers. Runtime 1.16 closed the JVM baseline owner convergence for IID coverage, activation-factory MTA usage, agile/GIT fallback, weak-reference consumers, context callbacks, Mono no-op ownership, and the low-level platform entry points, Runtime 1.18 closed the standard delegate ABI helper subset through `StandardDelegates.kt` plus the event weak-cache consumers, and the remaining COM-aggregation/`IMarshal` gap is now closed by the shared `IUnknown`-vs-`IInspectable` host layout in `WinRtInspectableComObject.kt`, the free-threaded-marshaler forwarding owner in `MarshalInterfaceReference.kt`, and `InteropRuntimeHooks.kt`, which now injects `.cswinrt`-aligned `IWeakReferenceSource`, `IMarshal`, and `IAgileObject` support into managed CCWs. The explicit Kotlin-specific narrowing stays implementation-local rather than ABI-visible: instead of relying on CLR `ComWrappers` entry synthesis and a thread-static helper field, the runtime uses the shared CCW host plus a process-wide cached free-threaded-marshaler proxy while preserving the same WinRT-facing interop contract.
- [x] Inventory I: close the remaining exception and restricted-error-info surface from `.cswinrt/src/WinRT.Runtime/ExceptionHelpers*`, `Interop/ExceptionErrorInfo*`, and related plumbing so WinRT-originated exceptions, managed exception wrapping, and restricted error info follow the same ownership/reporting model as `.cswinrt`. Runtime 1.16 closed the JVM baseline for HRESULT classification, restricted-error-info reads, managed `IErrorInfo` fallback hosts, and CCW stub `SetErrorInfo` propagation; the follow-up `runtime/exception` owner set now adds writable `IRestrictedErrorInfo` COM hosts plus the `SetRestrictedErrorInfo` platform seam so managed `WinRtRuntimeException` instances can replay restricted error details back to WinRT. The remaining CLR-specific language-exception / propagation-context path is now an explicit narrow owner in `runtime/exception/LanguageExceptionInterop.kt` rather than an open runtime blocker.
- [x] Inventory J: close the remaining delegate/event runtime infrastructure from `.cswinrt/src/WinRT.Runtime/DelegateExtensions.cs`, `EventRegistrationToken.cs`, `EventRegistrationTokenTable{T}.cs`, `Interop/EventSource*`, `Interop/StandardDelegates.cs`, and `Projections/EventHandler.cs`, including token tables, event-source caches, helper delegates, standard delegate ABI helpers, and event-source state management beyond the current minimal delegate ABI bridge. Kotlin runtime now has the matching owner convergence in `EventRegistrationToken.kt`, `EventRegistrationTokenTable.kt`, `EventSourceCache.kt`, `EventSourceState.kt`, `EventSource.kt`, `StandardDelegates.kt`, `GenericDelegateHelper.kt`, and `EventHandlerEventSource.kt`, with the explicit Kotlin-specific narrowing that `.cswinrt`'s CLR multicast-delegate adapters become function-type-based event helpers rather than a separate delegate-compat shim.
- [x] Inventory K: close the remaining XAML/system projection layer from `.cswinrt/src/WinRT.Runtime/Projections/ICommand*`, `ICustomPropertyProvider*`, `INotifyPropertyChanged*`, `INotifyCollectionChanged*`, `INotifyDataErrorInfo*`, `PropertyChangedEventHandler.cs`, `PropertyChangedEventArgs.cs`, `NotifyCollectionChangedEventHandler.cs`, `NotifyCollectionChangedEventArgs.cs`, `NotifyCollectionChangedAction.cs`, `DataErrorsChangedEventArgs.cs`, `IServiceProvider*`, and `IStringable.cs`. This is now closed by the shared runtime owner in `WinRtXamlSystemModels.kt` plus `XamlSystemProjectionRuntime.kt`, which centralizes the mapped type metadata, RCW/CCW factories, non-generic event sources and native handler adapters, XAML event-args runtime-class projections, and the default `IStringable` augmentation for inspectable CCWs. The explicit Kotlin-specific narrow deviations are documented rather than hidden: `ICustomPropertyProvider` does not reuse `.cswinrt`'s CLR reflection fallback and instead keeps an explicit provider/property contract intended for later Kotlin-side XAML mapping sugar, and managed event-args values marshal back to ABI through synthetic runtime-class CCWs instead of runtime activation-factory construction.
- [x] Inventory L: close the remaining disposable/system projection layer from `.cswinrt/src/WinRT.Runtime/Projections/IDisposable.net5.cs`, `IDisposable.netstandard2.0.cs`, and related `IClosable`/`IDisposable` adapter behavior so system-disposable projection is owned by `winrt-runtime` rather than by ad hoc generator rules. This is now closed by the `AutoCloseable` <-> `Windows.Foundation.IClosable` custom mapping and the shared `WinRtClosableObject` / `ComWrappersSupport` / `SystemProjections` ownership in Runtime 1.17.
- [x] Inventory M: close the remaining runtime attributes, feature switches, configuration, and runtime-message surfaces from `.cswinrt/src/WinRT.Runtime/Attributes.cs`, `AttributeMessages.net5.cs`, `Configuration/FeatureSwitches.cs`, `Interop/WuxMuxProjectedTypeAttribute.cs`, `WinRTRuntimeErrorStrings.Designer.cs`, `WinRTRuntimeErrorStrings.resx`, and the localized `ResX/*` resources so later runtime/generator/authoring slices inherit the same metadata/configuration/error-message hooks as `.cswinrt`. This is now closed by the shared Kotlin owner set in `FeatureSwitches.kt`, `AttributeMessages.kt`, `WinRtRuntimeErrorStrings.kt`, `XamlProjectionConfiguration.kt`, and `WinRtProjectionAnnotations.kt`, plus the activation and XAML projection wiring in `ActivationFactory.kt`, `ActivationFactoryLookup.kt`, `TypeProjection.kt`, `BuiltInInspectableProjectionHooks.kt`, and `XamlSystemProjectionRuntime.kt`. The explicit Kotlin-specific narrowing is kept narrow and documented: runtime switches use JVM system properties with the same switch keys instead of `.cswinrt`'s `AppContext` API, localized `.resx` bundles are currently narrowed to English defaults plus resource-key mode through `FeatureSwitches.useExceptionResourceKeys`, and the attribute surface is represented as runtime-visible Kotlin metadata annotations rather than CLR-only attribute behaviors or resource-manager plumbing.

## JVM Primary Path

- [x] Establish the JVM-first Gradle multi-module structure with the Kotlin top-level module names `winrt-runtime`, `winrt-metadata`, `winrt-authoring`, `winrt-projections`, and `winrt-samples`, and wire it into settings.gradle.kts
- [x] Split the Stage 3 generator owner into `winrt-generator` while keeping `winrt-projections` reserved for Stage 4 generated output. The generator implementation/tests now live in `winrt-generator`, and `winrt-projections` contains generated `Windows.Data.Json` / `Windows.Foundation.IStringable` output plus the projection marker.
- [ ] Migrate the existing Kotlin package layout to `io.github.kitectlab.winrt` (正在做: new modules use the canonical package root; legacy `sample-jvm-winui3` is out of current scope unless explicitly requested)
- [x] Create the `winrt-runtime` module and land the JVM runtime foundation corresponding directly to `.cswinrt/src/WinRT.Runtime`. Runtime 1.1 through Runtime 1.19 plus Inventory H, Inventory I, and Inventory M now cover the baseline ABI/object/activation path, the formal JVM `IWinRTObject` contract, generic and bindable collection projection helpers, the `.cswinrt`-equivalent RCW/CCW registration/object-wrapper layer, the shared marshaler core, the projection/type-name registry slice, the interop/error-info/platform owner convergence slice, the shared value-boxing/system-projection owner set, the shared event/delegate infrastructure owner set, the XAML/system projection owner set, and the runtime configuration/feature-switch/message owner set. Remaining JVM/Kotlin differences are documented narrowings rather than open blockers: CLR exception CCWs, the lack of CLR reflection fallback in `ICustomPropertyProvider`, synthetic runtime-class CCWs for managed XAML event-args values, and the narrowing of `.cswinrt`'s localized resource/AppContext plumbing to JVM system-property switches plus English default messages.
- [x] Re-converge the JVM `winrt-runtime` COM core on slot-based RCW invocation aligned with `.cswinrt/src/WinRT.Runtime/ObjectReference.cs`, `Interop/IUnknownVftbl.cs`, `IInspectable.cs`, `Interop/IActivationFactory.cs`, and generated ABI wrapper behavior in `cswinrt/code_writers.h`. The expanding `ComObjectReference` helper family has been removed in favor of explicit `ComPtr` ownership plus fixed `ComVtableInvoker` overloads so generator-produced WinMD interfaces can call slots directly on JVM and later `mingwX64`; JVM fixed overloads stay direct FFM downcalls and keep interpreted `invokeGeneric` out of hot RCW calls.
- [x] Freeze new `ComObjectReference` helper-family growth while the COM-core convergence slice is open. This freeze is closed by removing the helper family from the active runtime surface and documenting that generator output must target fixed `ComVtableInvoker` calls.
- [x] Finish the earliest `winrt-runtime` prerequisite slices in queue order before taking on broader metadata or projection work (Runtime 1.1 through Runtime 1.19 plus Inventory H, Inventory I, and Inventory M are now closed as the staged runtime/configuration queue. Remaining `.cswinrt/src/WinRT.Runtime` differences are bounded Kotlin-specific narrowings rather than active blockers.)
- [x] Freeze expansion of later runtime-adjacent slices until the remaining runtime prerequisites are explicitly closed. This gate is now satisfied: runtime prerequisites are closed, and the active upstream work has moved to metadata while generator, projections, authoring, and samples remain behind the metadata/generator queue.
- [x] Create the `winrt-metadata` module and land the JVM metadata foundation aligned with the metadata-loading responsibilities inside `.cswinrt/src/cswinrt`. Metadata 2.1 through Metadata 2.18 are closed as the current baseline: JVM PE/CLI ingestion, canonical path deduplication, deterministic normalization, core type/member/signature modeling, structured `WinRtTypeRef`, ABI descriptors, closure descriptors, special WinRT shape descriptors, lookup/index helpers, CsWinRT-style SDK/API-contract source expansion, TypeSpec/generic-parameter fidelity, typed custom-attribute decoding, structured availability metadata, runtime-class/interface ordering descriptors, member-shape semantics, struct/field/layout descriptors, centralized mapped-type/category classification, and metadata-owned validation diagnostics. Full CsWinRT metadata parity is reopened under Metadata Full-Parity 3.x before further generator breadth.
- [x] Keep `winrt-metadata` work behind the active runtime prerequisite queue and focus it on real WinMD ingestion before richer projection-shape expansion. This gate is now satisfied: runtime is closed enough that metadata can advance, and ingestion is already in place.
- [ ] Keep `winrt-metadata` in ingestion-first then fidelity-second order. The Metadata 2.18 baseline is not enough to move on: the active 3.x gate must now close CsWinRT cache/source parity, full CLI table tolerance, type semantics, category caches, attributes, interface/runtime-class closure, member semantics, layout/blittability, mapped projection tables, diagnostics, and real Windows SDK/NuGet fixture sweeps before subsequent generator work resumes.
- [x] Create the generator surface aligned with `.cswinrt/src/cswinrt` while keeping Kotlin tests inside the owning modules. The metadata -> projection-plan -> KotlinPoet generator is now owned by `winrt-generator`, includes the WinMD-driven CLI, and has real `Windows.Data.Json` parity coverage against the matching CsWinRT functional/API-compat tests.
- [ ] Keep generator work in declaration-first then member-emission order rather than expanding whichever projection file currently exists in `winrt-projections` (正在做: generator code now belongs in `winrt-generator`; `winrt-projections` should only receive deterministic output or narrow projection glue once the generator rule exists.)
- [ ] Freeze handwritten projection growth until the existing generator skeleton can reproduce a representative standard WinRT API slice such as `Windows.Data.Json` through the planned metadata/generator path
- [ ] Create the `winrt-authoring` module and land the JVM authoring and hosting foundation corresponding directly to `.cswinrt/src/Authoring` (冻结: module exists, host/source-generator parity still missing, and authoring is deferred until generator/projections/sample work has advanced)
- [ ] Keep `winrt-authoring` on boundary-first design (冻结: the module is still only a placeholder; when reopened, map hosting/lifetime/activation boundaries from `.cswinrt/src/Authoring` before any convenience API or sample glue appears)
- [x] Create the `winrt-projections` module and wire deterministic generated output corresponding directly to `.cswinrt/src/Projections`. The module now compiles generated `Windows.Data.Json` plus `Windows.Foundation.IStringable` output produced by `winrt-generator` from the installed Windows SDK `Windows.winmd`.
- [ ] Limit `winrt-projections` work to generator-produced output for already-implemented slices; do not grow handwritten projection breadth
- [x] Delete handwritten standard WinRT API projection files such as `Windows.Data.Json` when they drift into `winrt-projections`; only generator output or the narrow `.cswinrt/src/Projections`-style handwritten glue slices may remain there
- [ ] Create the `winrt-samples` module and wire the JVM sample validation surface corresponding directly to `.cswinrt/src/Samples` (正在做: module is wired into the root Gradle build; sample work is intentionally held to bootstrap-level scaffolding until generator-produced standard API projections exist, so the module does not keep handwritten standard WinRT projection-driven smoke code.)
- [ ] Keep `winrt-samples` on smoke-first validation only until the upstream runtime/generator/projection prerequisites are ready (正在做: the current module is intentionally minimal and should not expand into broader WinUI or standard API smoke scenarios until the upstream generated projection contracts exist; authoring is no longer a prerequisite for the near-term sample path)
- [ ] Keep sample work to the smallest validation surface for the currently completed slice before any broader WinUI sample expansion
- [ ] Place tests in the relevant module source sets instead of creating a separate top-level tests module
- [ ] Rename or delete legacy top-level modules that do not match the strict `.cswinrt/src` names (正在做: legacy `sample-jvm-winui3` may remain on disk for reference, but it is not part of the active refactor target)
- [ ] Complete WinUI 3 startup, bootstrap, resource, and message-loop validation through the `winrt-samples` module and per-module test layouts

## .cswinrt Alignment Slices

- [ ] Align COM/WinRT marshaling, HRESULT translation, and lifetime management (正在做: Runtime 1.1 through Runtime 1.3 are closed for ABI primitives, COM/WinRT init lifecycle, Windows loader/proc bindings, semantic HRESULT translation, and baseline object-reference ownership/query/identity behavior; the remaining work in this parity slice now shifts to later activation, generic, delegate, and collection-dependent mechanics.)
- [ ] Align activation factory lookup and runtime-class activation semantics (正在做: Runtime 1.4 is closed for JVM-side `RoGetActivationFactory`, manifest-free fallback probing, activation factory caching, and direct runtime-class activation helpers; the current KMP split now also pushes the manifest-free fallback owner (`DllModule` plus the fallback probing path) and the activation-factory cache/lookup core into `commonMain` against raw loader/proc/`IUnknown` seams so the remaining activation/reference commonization can build on a shared lookup core. Runtime 1.9 also adds WinUI-specific activation consumers for Xaml metadata provider initialization, while broader authoring/reference-tracking parity still remains for later slices.)
- [x] Align the Kotlin COM core owner split with `.cswinrt`'s `ObjectReference` plus generated ABI wrapper responsibilities. The Kotlin owner split is now explicit through `ComPtr` for ownership/QI/identity and `ComVtableInvoker` for fixed ABI call shapes; `.cswinrt` direct slot-call behavior is the primary RCW fast path, while dynamic `invokeGeneric` is narrowed to delegate/raw-word fallback cases rather than becoming a dispatcher-first design.
- [ ] Align `IWinRTObject`, typed RCW/CCW registration, and runtime projection lookup behavior (正在做: Runtime 1.10 plus Runtime 1.13 now cover the JVM-side `IWinRTObject` contract, query-interface caches, additional type data, `GetObjectReferenceForType`, `IsInterfaceImplemented`, typed RCW registration, helper-type indirection, runtime-class-name wrapper lookup, managed CCW creation, unwrap/cast helpers, and the `SingleInterfaceOptimizedObject` model. The current KMP split now also lifts the `IWinRTObject` cache/additional-type-data/finalization support into `commonMain` through a generic owner so later reference-layer commonization no longer depends on a JVM-only state store. The shared marshaler core is closed by Runtime 1.14 and the projection/type-name registry slice is closed by Runtime 1.15; the remaining work in this broader runtime-projection-lookup story now moves to the later value/event/XAML/runtime-system slices rather than to more object-wrapper or registry glue.)
- [ ] Align generic interface projection, parameterized IID, and type-signature behavior (正在做: Runtime 1.5 is closed for primitive/enum/struct/delegate/runtime-class/parameterized signature rendering and parameterized IID hashing with cswinrt parity samples; remaining work now shifts to consuming those signatures in later delegate, collection, and generator slices rather than to the signature primitives themselves.)
- [ ] Align delegate bridges, delegate handles, and callback marshaling (正在做: Runtime 1.6 is closed for JVM-side delegate descriptors, parameter-kind decoding, real `IUnknown + Invoke` ABI stub generation, AddRef/Release/QueryInterface lifetime behavior, HRESULT-safe callback dispatch, explicit delegate return-kind descriptors, and native delegate reference invocation/readback for the currently supported scalar/string/reference return categories; remaining work now shifts to consuming these delegate primitives in later collection, async, and generator slices rather than to the delegate ABI boundary itself.)
- [ ] Align collection projection for iterable/list/map/view adapters (正在做: Runtime 1.11 closed the generic collection helper layer for `IEnumerable` / `IList` / `IReadOnlyList` / `IDictionary` / `IReadOnlyDictionary`, including JVM-side `CreateMarshaler` / `FromManaged` / `FromAbi` plus `ToAbiHelper` / `FromAbiHelper`. The non-generic bindable layer is also closed by Runtime 1.12, the shared marshaler core is closed by Runtime 1.14, and the higher-level boxed value/system projection owner is closed by Runtime 1.17; remaining work in the broader parity story now shifts to later event/XAML slices rather than to more collection or value-boxing helper ownership.)
- [ ] Align bindable collection projection and non-generic object marshalling (正在做: Runtime 1.12 has now landed the Bindable* runtime projection layer in `winrt-runtime`, including IBindable object references, non-generic iterable/list helpers, and bounded inspectable/object round-tripping for bindable collections. The shared universal object/value marshaler core is closed by Runtime 1.14, and the higher-level `IPropertyValue` / value-boxing / system projection owner is now closed by Runtime 1.17; the remaining gaps move to the later event/XAML/system-service slices rather than more Bindable* or boxed-value scaffolding.)
- [x] Align async projection, task bridging, and completion-callback semantics. Runtime 1.8 closed `IAsyncInfo`/`IAsyncAction`/`IAsyncOperation<T>` wrapper coverage, async status/error/cancel/close semantics, completed-handler registration via delegate ABI stubs, parameterized async interface/handler IID helpers, and the coroutine-based `await()` / blocking `join()` surface that replaces the old JVM `CompletableFuture` detour. Runtime 1.20 plus Generator 3.6 now close the `.cswinrt` `AsyncInfo` / `TaskToAsync*Adapter`-style managed-to-WinRT async supplement and generated ABI return consumption for non-progress and progress-enabled action/operation shapes.
- [ ] Align authoring support, projected-object bridges, and hosting support
- [ ] Align WinUI-specific type mappings, Xaml metadata, and bootstrap behavior (正在做: Runtime 1.9 is closed for Windows App SDK bootstrap discovery/initialize/shutdown hooks, `Microsoft.UI.Xaml.XamlTypeInfo.XamlControlsXamlMetaDataProvider` activation/initialize helpers, `IXamlMetadataProvider` full-name lookup access, and baseline `IReferenceTracker` lifecycle integration on `ComObjectReference`; later work can consume these hooks from authoring/generator/sample layers rather than reimplementing them there.)

## Generation And Validation

- [ ] Add real WinMD fixture coverage in `winrt-metadata` tests for the generator-relevant families that later generator and WinUI sample work depend on, starting with `Windows.Foundation`, `Windows.Foundation.Collections`, and `Microsoft.UI.Xaml.Data`; keep this as module-test validation rather than as a metadata feature slice.
- [ ] Establish deterministic generator-output validation and regression checks after the corresponding `.cswinrt`-derived generator contracts are explicit
- [ ] Establish a targeted JVM unit-test and integration-test matrix that validates agreed runtime/generator contracts instead of defining them
- [ ] Establish a Windows-first verification flow that prioritizes affected modules
- [ ] Keep validation breadth aligned with the ordered slice queue so that smoke, unit, generator-regression, authoring, and sample coverage expand only after their owning contracts are explicit

## mingwX64 Parity

- [ ] After the matching JVM slices are complete, define the runtime parity plan for `mingwX64` under the shared contracts
- [ ] Create `sample-mingw-winui3` or an equivalent native validation module
- [ ] Add validation coverage that proves `mingwX64` behavior matches JVM semantics
