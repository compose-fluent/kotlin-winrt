# Plan

## Operating Rules

- [x] `.cswinrt/` is the implementation baseline for observable WinRT behavior, ABI ownership, metadata interpretation, generated surface shape, authoring contracts, packaging evidence, and validation strategy.
- [x] Keep dependency order: `winrt-runtime` -> `winrt-metadata` -> `winrt-generator` / `winrt-compiler-plugin` -> `winrt-projections` -> `winrt-authoring` -> `winrt-samples`.
- [x] Tests validate the reference-first design; do not derive runtime, generator, authoring, or packaging rules from sample failures.
- [x] Keep Gradle configuration cache and build cache enabled; cache failures are task modeling bugs to fix.
- [x] Work on Windows. Prefer `.\gradlew.bat`; when running from WSL, use `./.agent_scripts/run_windows_gradle.sh <gradle-args...>`.
- [x] Keep codebase slimming frozen until functional completeness is closed. Remove duplication only when it is necessary for an active parity slice.

## Reference Boundaries

- [x] CsWinRT is projection-behavior evidence, not a mandate to copy `.NET` target splits, MSBuild infrastructure, release tooling, `Perf`, `Benchmarks`, or `cswinmd`.
- [x] Kotlin reflection is not a substitute for CsWinRT source-generator discovery. Equivalent annotation, metadata, registration, and source-generation behavior belongs in `winrt-generator` and `winrt-compiler-plugin`; runtime discovery stays fail-closed.
- [x] WinMD ingestion targets native WinMD metadata. Do not add `cswinmd` compatibility unless a concrete Kotlin projection requirement appears.
- [x] Packaging targets Kotlin `appx` / `msix` applications, PRI/MRT/resource indexing, manifest processing, dependency payload staging, signing/test-install hooks, and runtime asset layout without cloning full MSBuild.
- [x] Use `io.github.composefluent.winrt` as the base package for runtime, metadata, generator support, generated projections, authoring, and samples unless a narrow external compatibility exception is documented.

## Current Focus Queue

- [x] Default-WASDK-2.2.0: updated the active Windows App SDK projection, sample, README, CI, publication, and current-default test-fixture baseline from `2.1.3` to `2.2.0` while preserving explicit version overrides, the Windows SDK `10.0.26100.0` baseline, the WebView2 `1.0.3719.77` baseline required by `Microsoft.WindowsAppSDK.WinUI` `2.2.1`, and historical validation records. This remains a `.cswinrt/src/Projections/WinAppSDK`-mapped dependency-baseline update owned by `winrt-projections`, Gradle orchestration, and validation surfaces; runtime, metadata, and generator projection behavior are unchanged. Validation: the active-surface stale-version scan is clean; `:winrt-projections:windows-app-sdk:compileCommonMainKotlinMetadata` passed against the restored `Microsoft.WindowsAppSDK` `2.2.0` closure; generated host metadata identifies the `2.2.0` projection artifact; and the two affected `KotlinWinRTPluginTest` cases pass. Full local `compileKotlinJvm` did not complete under the available memory envelope: default 4 GiB and 2 GiB C2 runs ended in JVM native-memory OOM during metadata/Kotlin codegen, while a 2 GiB Tier-1 single-JIT retry avoided the crash but exceeded the one-hour execution window; `mingwX64` was not started after that resource-bound JVM gate. CI projection compilation remains the full-target validation surface. Design: `docs/superpowers/specs/2026-07-16-windows-app-sdk-2.2.0-design.md`. Plan: `docs/superpowers/plans/2026-07-16-windows-app-sdk-2.2.0.md`.
- [x] Review-Fix-01: hardened `winrt-runtime` against `.cswinrt/src/WinRT.Runtime` by correcting GUID ABI alignment, keeping JVM-declared `object[]` as `InspectableArray`, and giving native weak-reference handles deterministic/finalized release on JVM and `mingwX64`. Kotlin/Native erases reference-array component types, so implicit homogeneous arrays use all-element inference while exact generated/authoring object-array paths pass `IID.IReferenceArrayOfObject` explicitly.
- [x] Review-Fix-02: `winrt-metadata` rejects conflicting non-null IIDs for duplicate qualified type definitions before model merge or generator planning, while preserving equal identities and null enrichment.
- [x] Review-Fix-03: repaired generator/authoring ABI ownership by returning authored arrays from `CoTaskMemAlloc`, rejecting normalized and case-folded duplicate generated output paths, and clearing `DllGetActivationFactory` output pointers before fallible work.
- [x] Review-Fix-04: restored prebuilt projection artifact ownership by eliminating cross-artifact duplicate FQNs, including dependency-owned event-helper output that bypassed the existing projected-type suppression, exposing Windows SDK dependencies through public variants, correcting the KMP root POM to publish that dependency with compile scope, evaluating the SDK identity producer before split dependency wiring, wiring generated sources into KMP metadata compilation and `sourcesJar` explicitly, and validating published JVM/`mingwX64` consumer topology with the plugin applied to every fixture module that consumes WinRT types plus an explicit dependency-identity edge so consumers do not regenerate dependency-owned projections. Pure consumers with no local metadata/type/namespace/package projection request do not treat dependency NuGet metadata as a local generation request. WebView2 follows the Windows App SDK package boundary without removing the API: `Microsoft.Web.WebView2.Core` is owned by the separately published `winrt-projections-windows-webview2` artifact, the App SDK projection excludes duplicate Core output and exposes that artifact as an API dependency, and Kotlin emits the WinUI `Microsoft.UI.Xaml.Controls.WebView2` control in the App SDK artifact because, unlike the standalone CsWinRT test projection, it cannot consume the official `Microsoft.WinUI` managed assembly for that class. Legacy `IWebView` peers remain excluded with `.cswinrt`. `Windows.UI.Xaml.Data.BindableAttribute` and `Windows.UI.Xaml.Markup.ContentPropertyAttribute` follow that same `.cswinrt` project boundary and remain WinAppSDK-owned, while generated-output audits compare Windows SDK, WebView2 Core, Windows.UI.Xaml, and Windows App SDK class ownership together. Split KMP publication validation requires the Windows SDK dependency specifically on `metadataApiElements` with API usage and cannot pass from runtime or `kotlin-winrt-identity` variants. The Windows.UI.Xaml slice temporarily excludes `Windows.UI.Xaml.Controls.Maps` because WinRT's `MapControl.Style: MapStyle` collides with inherited `FrameworkElement.Style: Style?`, which Kotlin cannot implement as two same-name properties with incompatible types; resolve this as a dedicated `.cswinrt` parity naming slice.
- [x] Review-Fix-05: split Gradle local-projection generation gating from compiler-plugin authoring discovery/validation. Authoring-capable JVM and `mingwX64` compilations keep scanner/compiler options, candidates, WinMD, host manifests, TypeDetails, and lifecycle validation, while dependency-owned/runtime-only inventories skip projection Kotlin emission and compiler-support merging. Identity generation retains authoring metadata preparation without acquiring target-JAR or validation dependency cycles.
- [x] Review-Fix-06: made the non-WinUI sample mode reachable through `kotlinWinRT.samples.enableWinUI` (default `true`), kept Windows App SDK and WinUIEssential packages out of disabled mode, isolated metadata fixture `dotnet` builds from user-level NuGet configuration, used sandbox-safe canonical path assertions, resolved NuGet dependency minimums and bounded ranges from installed package versions while reusing compatible nearest versions and rejecting incompatible multi-version closure conflicts, gave standalone published consumers an explicit Gradle JVM budget and propagated the configured offline NuGet root through the tracked `gradle/nuget-root.init.gradle` script into every nested validation build, owner-scoped compiler-support manifest class artifacts across split projections, explicitly validated both Windows SDK and App SDK-to-WebView2 API publication edges, and added the final cross-module gate for runtime, metadata, generator, authoring, Gradle plugin, split projection publication, and representative WinUI/non-WinUI samples. `validateProjectReviewRemediation` passed on Windows for JVM and `mingwX64`.
- [x] Review-Followup-01: converged prebuilt projection publication and validation into the repository-only `kotlin-winrt-build-convention` plugin, restored dependency-identity ownership and mutually exclusive Windows.UI.Xaml/Microsoft.UI.Xaml families, kept Windows App SDK independent from a specific Windows SDK artifact, and replaced JVM reference-array reflection with explicit/reified common metadata flow. The published Kotlin/WinRT plugin remains free of repository-specific prebuilt topology. Design: `docs/superpowers/specs/2026-07-13-projection-topology-and-array-type-boundary-design.md`. Plan: `docs/superpowers/plans/2026-07-13-projection-topology-and-array-type-boundary.md`.
- [x] Review-Followup-01 Task 1: `winrt-gradle-plugin` validates that current-shape projected types and source additions each have one deterministic dependency identity owner before dependency-owned projection suppression. Generator type-shape descriptor compiler input now applies that same suppression boundary, so a transitive library cannot claim dependency-owned WinRT contract identities. This Gradle orchestration rule preserves the existing `.cswinrt/src/cswinrt`-aligned projection boundary rather than defining a Kotlin projection shape.
- [x] Review-Followup-01 Task 2: centralized repository-specific prebuilt projection publication/output validation in `kotlin-winrt-build-convention`. The `winrt.prebuilt-projection` convention applies generic build/publish conventions, waits for the explicitly applied public Kotlin/WinRT plugin, mirrors direct `commonMainCompileOnly` project references into its existing identity configuration, derives publication API contracts from `commonMainApi`, and avoids public-plugin build inclusion or `evaluationDependsOn`. Direct task coverage preserves rejection of runtime-scope POM dependencies, missing expected API dependencies, and dependencies exposed outside `metadataApiElements` with API usage.
- [x] Review-Followup-01 Task 3: restored the `.cswinrt/src/Projections/Windows.UI.Xaml` and `.cswinrt/src/Projections/WinAppSDK` peer topology. The four prebuilt artifacts apply the repository convention directly; Windows SDK is a compile-only build reference for WebView2, Windows.UI.Xaml, and App SDK, while App SDK alone publishes WebView2 as API. Convention-owned publication validation keys both owner and dependency contracts by their actual published artifact names, rejects compile-only artifact leakage from all POMs and Gradle metadata, and validates consumers that select SDK plus their target through ordinary dependencies. Convention-owned output validation retains the established 150 MB full-projection class budget so the complete Windows SDK is audited without per-module task configuration. An isolated SDK + Windows.UI.Xaml + App SDK fixture requires the duplicate dependency identity owner diagnostic with a bounded 512 MiB generator worker so the expected-failure check remains runnable beside the publication daemon. KMP identity propagation observes direct dependencies on every eligible source configuration at declaration time; it does not mutate resolution or publication variants from a `projectsEvaluated` scan after Gradle has consumed them. Ordinary external dependencies and project dependencies declared before their producer is evaluated enter the existing resolution identity graph without explicit consumer identity declarations, while published identity variants still filter non-WinRT projects. Windows.UI.Xaml again owns `BindableAttribute` and `ContentPropertyAttribute`; the tracked Maps collision exclusion remains. Isolated consumer Gradle processes inherit the current build JVM so the Java 25 plugin toolchain boundary does not silently fall back to a Java 21 launcher. The README legacy-XAML example selects only Windows SDK plus Windows.UI.Xaml and no longer introduces the mutually exclusive App SDK NuGet family. `validateWinRTSplitProjectionPublication` passed for all four artifact audits/validators, the expected XAML-family conflict, and all three published JVM/`mingwX64` consumers; Task 4 runtime array-boundary work is next.
- [x] Review-Followup-01 Task 4: aligned reference-array metadata with `.cswinrt/src/WinRT.Runtime/Projections/IReferenceArray.net5.cs`, `ComWrappersSupport.cs`, and compiler-symbol discovery in `.cswinrt/src/Authoring/WinRT.SourceGenerator`. `winrt-runtime` removes JVM array-component reflection, classifies dynamic reference arrays conservatively in common code, accepts explicit/reified element `KClass` metadata for known arrays, and keys cached CCW hosts by that metadata so IID, `PropertyType`, and runtime class name cannot leak between explicit and dynamic marshaling of the same array. Type-only callers now use primitive or explicit registration metadata and fail closed for erased Native reference-array classes. `winrt-generator` passes known object-array element types through `Marshaler.inspectableArray<T>()`; empty, all-null, heterogeneous, unknown, and explicitly object-typed arrays remain `Object`/`InspectableArray`. The combined `winrt-runtime` JVM/`mingwX64` and `winrt-generator` gate passed.
- [x] Review-Followup-01 Task 5: added `kotlin-winrt-build-convention:test` to the aggregate Windows gate. The gate exposed and now regression-tests a public-plugin identity filtering bug from Task 3: unresolved KMP project producers may enter dependency identity resolution before producer evaluation, while plain JVM dependencies and published `identityElements` still require WinRT identity metadata. Targeted identity tests, the real three-layer KMP fixture, and the full public-plugin suite pass. The first aggregate run exposed that the nested no-WinUI sample build inherited the root 4 GiB Gradle heap and exhausted native memory while included-build validation remained resident; that child build now uses a verified 768 MiB single-compiler/JIT budget. The final `validateProjectReviewRemediation` gate passed on Windows in 1h 4m 37s across 292 JVM, `mingwX64`, publication, identity-conflict, and sample-validation tasks.
- [x] Completed owner-scoped projection support initializer fix: generator-emitted compiler support manifests now carry an owner column, compiler-plugin support initializer class and lowered init function hashes include the owner identity, merged manifests preserve owner-scoped rows across application and dependency artifacts, and JVM-only app builds skip the Mingw application-entry task when no native main class is present.
- [x] Completed owner-scoped projection support anchor fix: generated projection modules now include an owner-scoped compiler-plugin anchor file, and projection support initializer IR functions are emitted into that owner-specific file facade instead of arbitrary generated support files such as `AuthoringMetadataTypeMappingHelperKt`.
- [x] Completed CI follow-up for owner-scoped compiler support manifests: updated compiler-plugin malformed-row coverage so the extra-column rejection test now treats the new five-column owner manifest as valid and uses a sixth column for fail-closed validation.
- [x] Completed issue #5 native authored XAML Grid override parity: fixed K/N direct-call struct ABI lowering so `Rect`/large structs are passed by pointer, added a `mingwX64` authored `Grid` + `SwapChainPanel` sample with `onCreateAutomationPeer`, `measureOverride`, and `arrangeOverride`, and validated it against `.cswinrt/src/cswinrt` overridable interface CCW generation plus `.cswinrt/src/WinRT.Runtime` composable/CCW lifetime responsibilities.
- [x] Completed issue #5 ABI follow-up audit: rechecked compiler-plugin descriptor lowering, `winrt-runtime` `ComAbiValueKind.Struct` generic vtable/callback paths, delegate struct marshaling, and JVM FFM struct layout descriptors; fixed TestKit generator-worker classpath drift and added explicit `Struct8_4` / `Struct16_4` intrinsic-lowering regression coverage so generator layout tokens stay visible through compiler lowering.
- [x] Completed WinRT acronym casing normalization: public/runtime/compiler/Gradle symbols, task names, DSL/property names, generated support identifiers, docs, scripts, and tracked file names now use `WinRT` / `winRT` / `KotlinWinRT` casing instead of `WinRt` / `winRt` / `KotlinWinRt`, while the lowercase package and Maven/module path root remains `winrt`.
- [x] Completed KWINRT-064 native authoring CAdapter sharedLib regression: root cause was the `2bbf219dc` public WinRT namespace migration moving runtime-backed public projections into original packages while the generator still emitted duplicate current-module declarations for runtime-owned public types such as `Windows.Foundation.EventHandler`, `Windows.Foundation.TypedEventHandler`, `Windows.Foundation.IStringable`, and runtime-backed `Microsoft.UI.Xaml.*` / `Windows.UI.Xaml.*` types. The fix separates runtime-owned ABI/projection support from runtime-owned public declaration ownership, skips those generated declarations while preserving support-only plans needed for CCW/event helpers, resolves `IStringable` interface IDs through runtime `IID.IStringable`, and keeps `.cswinrt/src/Authoring` export-surface parity (`DllGetActivationFactory` / `DllCanUnloadNow`) without changing public/internal visibility or flattening unrelated activation support.
- [x] Completed WASDK interop source-addition parity: `WinRT.Interop` helper additions and `Microsoft.UI.Win32Interop` are owner-scoped generator/projection output following `.cswinrt/src/cswinrt/WinRT.Interop.idl`, `.cswinrt/src/cswinrt/strings/ComInteropHelpers.cs`, and Windows App SDK `Microsoft.UI.Interop.h`; public helper APIs stay out of `winrt-runtime`, runtime only gained a narrow raw native-export ABI invoker used by generated code, `Windows.*` projection ownership triggers `winrt.interop` helper generation, and dependency identity publishes actual generated `sourceAdditions` from `kotlin-winrt-support/source-additions.tsv` so downstream WASDK/Windows projection suppresses duplicate helper FQNs and fails closed on duplicate owners. Targeted metadata/generator/Gradle tests plus JVM and `mingwX64` runtime compiles pass, full `:winrt-metadata:test :winrt-generator:test` passes, actual generated Windows SDK output includes `winrt.interop` helpers, actual generated Windows App SDK output includes `microsoft.ui.Win32Interop`, and `:winrt-projections:windows-app-sdk:compileCommonMainKotlinMetadata`, `:winrt-projections:windows-app-sdk:compileKotlinMingwX64 --max-workers=1`, and `:winrt-projections:windows-app-sdk:compileKotlinJvm --max-workers=1` pass. Full `:winrt-gradle-plugin:test --max-workers=1` currently fails in unrelated `plugin_generates_sources_into_real_gradle_library_artifact` because the test expects `windows/foundation/IStringable.class` in a real Gradle jar while the generated artifact contains only the current Foundation contract/metadata output.
- [x] Completed WASDK interop CI audit follow-up: `WinRT.Interop` source additions now generate `WinRTProjectionIntrinsic` calls for the `.cswinrt/src/cswinrt/strings/ComInteropHelpers.cs` direct IUnknown vtable slots instead of embedding `ComVtableInvoker.invokeArgs`, compiler-plugin scalar lowering supports `RawAddress` return values, and generated-output audits continue rejecting stale generic vtable fallback calls without source-addition exceptions.
- [ ] Windows COM interop helper parity 正在做: mirror the complete 17-helper public surface from `.cswinrt/src/cswinrt/WinRT.Interop.idl` and `.cswinrt/src/cswinrt/strings/ComInteropHelpers.cs`, absorbing the current `InputPaneInterop` work into one metadata-owned adapter catalog and common generator renderer. Windows helpers require an explicitly declared Windows SDK; metadata now retains normalized SDK selections plus the complete `Platform.xml` contract inventory in `WinRTMetadataCache`/`WinRTMetadataModel`, derives the effective `Windows.Foundation.UniversalApiContract` major version from those declared SDK inputs, rejects malformed contract versions, and preserves extension contracts as file inputs without treating them as the platform UAC source. Next: build the single UAC-gated adapter catalog and replace the one-off InputPane namespace addition. Generated helpers remain projection-owned, use existing ABI/marshalling intrinsics for JVM and `mingwX64`, and preserve dependency identity suppression without duplicating vtable logic in Compose consumers. Validation so far: new SDK contract boundary tests and the full `WinRTMetadataLoaderTest` pass on Windows with `--max-workers=1`.
- [x] Closed KMP WinUI generated source-set ownership leak: `winrt-gradle-plugin` now creates a local `winuiMain` source set for JVM and `mingwX64`, writes KMP WinRT projection and authoring generated sources under `src/winuiMain/kotlin`, attaches merged compiler support to `winuiMain`, keeps old `commonMain` generated roots as stale-layout cleanup inputs, and leaves non-WinUI targets such as `linuxX64` outside the WinRT projection source path.
- [x] Completed compose-winui KWINRT-053..058 Gradle baseline: authoring scanner classpath is locked by plugin defaults, JVM application-host runtime classpath is inferred from the selected KMP JVM target (`winuiJvm` preferred), `mingwX64` application staging remains on the native executable/shared-asset path instead of JVM classpath assumptions, `runWinRTApplicationHost` is a typed task with JVM args/env/log properties whose launch configuration and working directory participate in Gradle task state, and project dependency identity/compiler-support consumption is validated through the existing KMP library-to-app TestKit coverage. Validation: targeted 053-057 plugin tests pass; `plugin_validates_multiplatform_winrt_library_consumed_by_multiplatform_winrt_application` passes; `:winrt-samples:tasks :winrt-samples:winui-kmp-app:tasks` passes. Full `:winrt-gradle-plugin:test --max-workers=1` still reports the known unrelated real Gradle library artifact failure, plus one full-suite order-sensitive authoring diagnostic test that passes when rerun targeted.
- [x] Completed compose-winui KWINRT-056/KWINRT-059/KWINRT-060 plugin follow-up: `WinRTApplicationOptions.runTask(...)` and `Project.registerWinRTApplicationHostRunTask(...)` now register additional typed application-host run tasks wired to `buildWinRTApplicationHost`, each with independent args, JVM args, environment, working directory, and output log task state; `RunWinRTApplicationHostTask` stops inheriting outer `KOTLIN_WINRT_JVM_OPTIONS` unless the task declares it through `jvmArgs` or `environmentVariables`; runtime dependency auto-wiring remains owned by the full `io.github.compose-fluent.winrt` plugin. Validation: targeted `:winrt-gradle-plugin:test` coverage for additional run-host task registration and run-host task-state annotations passes with `--max-workers=1 --rerun-tasks`.
- [x] Completed KMP WinUI runtime/authoring dependency scope tightening: the full `io.github.compose-fluent.winrt` plugin no longer injects WinRT runtime dependencies into `commonMainImplementation`; KMP auto-wiring is scoped to `winuiMainImplementation` because the runtime surface currently has JVM and `mingwX64` implementations only. Full WinRT plugin wiring adds both runtime and authoring to `winuiMain`, while JVM-only projects still receive runtime/authoring through ordinary `implementation`. Validation: targeted `:winrt-gradle-plugin:test` coverage for JVM projects, WinUI KMP source-set injection, WinUI authoring injection, and target-module identity fallback passes with `--max-workers=1`.
- [x] Completed compose-winui KWINRT-061 Native/KMP dependency-owned projection identity follow-up: external Kotlin target modules such as `*-mingw` / `*-mingwx64` are now resolved for WinRT identity through both the target-specific module and the root KMP module, so downstream Native consumers can see root-published `kotlin-winrt-identity` metadata even when the resolved klib artifact itself has no identity variant. Validation: targeted `:winrt-gradle-plugin:test` coverage for target-module root fallback and Maven identity consumption through a `producer-mingw` dependency passes with `--max-workers=1`; existing KWINRT-056/KWINRT-059/KWINRT-060 typed run-task tests also pass.
- [x] Closed Compose WinUI KWINRT-062 NuGet restore isolation: preserve each configured global-packages root as one path when aggregating explicit, NuGet CLI, and environment-provided roots, and clean each task's temporary `nuget-install` directory before restoring the selected package closure so stale Windows App SDK WinMDs cannot re-enter projection generation. This follows `.cswinrt/nuget/Microsoft.Windows.CsWinRT.targets`, where projection consumes the resolved metadata inputs rather than an accumulated restore workspace. Validation: targeted `winrt-metadata` NuGet-root tests and `winrt-gradle-plugin` restore-isolation tests pass; local Maven publication plus Compose WinUI `generateWinRTProjections` pass with the configured Maven/NuGet roots, and the reproduced `IPackageVolume` IID conflict is absent after restoring the current Windows App SDK closure.
- [x] Completed Gradle plugin marker publication follow-up: the single public Gradle plugin id now uses the Maven Central-owned namespace (`io.github.compose-fluent.winrt`) so its generated plugin marker publication lands under a publishable `io.github.compose-fluent.winrt` group instead of the unauthorized `io.github.composefluent*` group; README uses marker-based plugin resolution without the previous `pluginManagement.resolutionStrategy` module workaround.
- [x] Completed WinUI sample source-set dependency follow-up: sample entry and projection-using smoke sources now live under `winuiMain` instead of `commonMain`, keeping `commonMain` free of `winrt-runtime` and generated WinRT projection supertypes after runtime/authoring auto-wiring was intentionally scoped to the JVM/`mingwX64` WinUI source set.
- [x] Completed WinUI sample source-set validation follow-up: moved handwritten WinUI sample sources that import generated WinUI/WinRT projection APIs out of `commonMain` and into `winuiMain`, pointed KMP WinUI sample generation scans at `src/winuiMain/kotlin`, kept generated projection ownership under `winuiMain` for multi-target KMP stability, and validated `.\gradlew.bat :winrt-samples:verifyWinRTSampleRun :winrt-samples:winui-kmp-app:verifyWinuiKmpJvmRun --max-workers=1 --console=plain`.
- [x] Completed KMP WinUI source-set accessor follow-up: `winrt-gradle-plugin` now creates `winuiMain` during plugin application so Kotlin DSL builds can use `sourceSets { winuiMain { ... } }`; the plugin wires only local `commonMain -> winuiMain -> JVM/mingwX64 main` source-set edges and does not apply or replace the user's Kotlin target hierarchy template.
- [x] Completed KMP WinUI hierarchy-template DSL follow-up: `winrt-gradle-plugin` now exposes `io.github.composefluent.winrt.gradle.winui`, a Kotlin hierarchy-template extension that expands to `group("winui") { withJvm(); withMingw() }` for users who want to declare the WinUI target group explicitly in their own `applyHierarchyTemplate` block.
- [x] Completed KMP WinUI test source-set accessor follow-up: `winrt-gradle-plugin` now creates `winuiTest` during plugin application so Kotlin DSL builds can use `sourceSets { winuiTest { ... } }`; the plugin wires only local `commonTest -> winuiTest -> JVM/mingwX64 test` source-set edges and keeps non-WinUI test targets such as `linuxX64Test` outside the WinUI test source path.
- [x] Completed WinUI sample accessor alignment: KMP WinUI app and library sample dependencies now use the plugin-provided `sourceSets { winuiMain { ... } }` accessor, while custom target-name source sets such as `winuiJvmMain` / `winuiJvmTest` remain configured through `named(...)`.
- [x] Completed downstream Skiko JVM authored TypeDetails IID handoff: authoring metadata indexes now preserve interface IIDs, projection registrar rows expose interface IIDs, compiler-authored TypeDetails consumes local and merged registrar IID inputs to backfill WinMD interfaces that lack metadata IIDs, and regression coverage validates the `IAutomationPeerOverrides`-style authoring path against `.cswinrt/src/cswinrt` / `.cswinrt/src/Authoring` interface identity requirements.
- [x] Completed downstream Skiko KMP WinUI dependency wiring follow-up: `winrt-gradle-plugin` source-set auto dependencies for `winrt-runtime` / `winrt-authoring` now stay on variant-aware project or module dependencies, `winrt-authoring` is wired to `winuiMain` instead of `commonMain`, and plugin-internal project/file fallbacks remain scoped to compiler-plugin and worker classpaths. CI follow-up: compiler-plugin runtime classpaths now include `winrt-runtime` explicitly so IID-bearing authoring metadata indexes can construct `Guid` while compiling Native projections.
- [x] Completed compose-winui KWINRT-060 library identity forwarding cycle follow-up: library `kotlin-winrt-identity` remains a transitive forwarding variant for intermediate libraries, but `generateWinRTIdentity` no longer gates on authored-candidate validation or implicitly builds JVM target jars through `jar.archiveFile` inputs. Source-scanned authoring metadata remains generated before identity so subclass/authoring discovery is not disabled for forwarding libraries; KMP `winuiMain` generation now publishes both native `.dll` and JVM `.jar` authored host manifests, and identity collects all generated host manifests plus any already-built JVM target artifact record without depending on `jar`. Validation covers forwarding-library identity dependencies, validation lifecycle gating, empty projection input identity generation, KMP JVM authored target jar identity records, TestKit local runtime/authoring artifact injection, and explicit validation-task execution instead of using identity as a validation trigger.
- [x] Completed compose-winui KWINRT-060 local-generation gating follow-up: Gradle task wiring, generated source roots, compiler-plugin options, authored-candidate validation, and identity dependencies now use the combined projection inventory after dependency identity suppression instead of DSL request fields such as `includeTypes` / `metadataInputs` as the final decision. Runtime-only WinUI libraries and libraries whose explicit `type(...)` requests are already dependency-owned keep runtime/authoring dependencies but do not wire local projection generation or compiler support into compile/identity graphs; configuration-time probing avoids loading the Windows SDK for dependency-owned exact type requests and no longer swallows VM `Error`s as fail-open generation.
- [x] Completed Compose KWINRT-060 compiler-support and library-gating follow-up: generic-instantiation entry resolution uses the actual generated `initializeEntry` parameter type; source-less/runtime-only libraries stop before dependency identity resolution; authoring discovery follows supported JVM and `mingwX64` main compilation graphs and their ancestors; and dependency-owned exact-type requests now use current-shape projected-type identity plus the shared namespace-addition registry to decide whether dependency suppression leaves local output, falling back to the full metadata inventory for explicit SDK shape, XAML resource-dictionary expansion, exclusions, local metadata, namespace, or NuGet requests. Validation: fresh `:winrt-compiler-plugin:test` passes; 10 focused Gradle-plugin regressions pass with zero failures/errors; `publishToMavenLocal` publishes 102 tasks through Gradle's dynamic `mavenLocal()` resolution; and Compose `:compose:foundation:foundation:compileKotlinWinuiJvm` passes against the refreshed local publication in 24m 41s without modifying the Compose repository.
- [ ] generated projection size reduction: reduce repeated generated `.class` output in prebuilt Windows SDK and Windows App SDK projection artifacts without changing Maven coordinates, projected API compatibility, runtime hot-path behavior, or multi-module classpath safety.
- [x] Completed `winrt-runtime` responsibility convergence: split the remaining mixed projection, registry, hook, and marshaller responsibilities back into `.cswinrt/src/WinRT.Runtime`-aligned owners before doing broad runtime naming cleanup.
- [x] Completed `Windows.Foundation.Uri` runtime ownership cleanup: removed the runtime-owned Uri model, ABI helper, custom mapping descriptor, and `WinRTSystemProjectionMarshalers.uriFromAbi` facade so `Windows.Foundation.Uri` returns to ordinary metadata-driven projection generation under the `windows.foundation` package instead of being held by `winrt-runtime`.
- [x] Completed Publish Projections CI heap follow-up: split prebuilt projection JVM and `mingwX64` compile tasks into one-target Gradle invocations with `--max-workers=1` and daemon shutdown between large projection compiles, so Windows SDK / Windows App SDK publication builds do not keep multiple large Kotlin compiler heaps alive in the same daemon lifecycle.
- [x] Completed public WinRT projection namespace migration for stable projection types: `Windows.Foundation.EventRegistrationToken`, `Windows.Foundation.EventHandler`, `Microsoft.UI.Xaml.*`, and `Windows.UI.Xaml.*` runtime-backed public projections now live under their original WinRT namespaces (lowercased Kotlin packages / aliases where ABI matches), while `Windows.Foundation.Uri` remains in the same namespace as an ordinary generated projection rather than a runtime-backed type; generator mappings resolve runtime-backed public names, and runtime/tests/samples import the migrated types directly instead of `io.github.composefluent.winrt.runtime.WinRT*` public model names.
- [x] Completed CI follow-up for XAML system projection helper names: generator-emitted `INotifyPropertyChanged` and `INotifyDataErrorInfo` forwarders now reference the renamed runtime helpers `INotifyPropertyChangedProjection` and `INotifyDataErrorInfoProjection`, and runtime-owned XAML handler delegates are skipped instead of regenerated, matching the public WinRT namespace migration and `.cswinrt/src/WinRT.Runtime` custom projection mapping responsibility.
- [x] Closed KWINRT-030 KMP partial dependency checker early-resolution mutation: resolvable identity inputs now come from live source configuration file collections instead of mutating a resolved identity configuration, so partially-resolved dependency checks and sample classpath resolution no longer force late `kotlinWinRTIdentity` mutation.
- [x] Closed KWINRT-031 projection-unsafe authoring TypeDetails casts: generated authoring bridges now route WinRT runtime-class dispatch through `winrtAs(WinRTTypeHandle(...))` using metadata-owned default-interface or dispatch-interface IIDs instead of direct Kotlin runtime-class casts or projection-only `Metadata.TYPE_HANDLE` assumptions, while authored-class dispatch keeps the narrow local cast path.
- [x] Completed KWINRT-031 CI follow-up for common/mingw TypeDetails: authored WinUI base dispatch no longer emits runtime-class `Metadata.TYPE_HANDLE`, so generated common and `mingwX64` sample sources compile against the current projection metadata surface.
- [x] Closed KWINRT-032 composable/runtime-class inheritance shape mismatch: runtime-class shells now open when the WinMD shape supports composable construction instead of waiting for an in-surface derived type, keeping subclassable WinRT projections non-final.
- [x] Closed KWINRT-041 dependency-owned WinRT class-shape collision: dependency identity now suppresses downstream regeneration of dependency-owned runtime-class base chains so one WinRT identity does not get emitted with conflicting class shapes across jars.
- [x] Closed KWINRT-044 non-WinUI authored-candidate validation scope leak: authored-candidate validation now registers only for Windows JVM and `mingwX64` WinUI targets, so non-WinUI compilations do not consume WinUI authored scanner output.
- [x] Closed KWINRT-045 dependency-owned authored Application subclass regeneration: dependency identity now carries authored host-manifest activatable classes into downstream suppression, so upstream-authored runtime classes like `WinUIXamlApplication` are not regenerated by dependents.
- [x] Closed KWINRT-046 dependency-authored Application manifest registration: staged JVM and `mingwX64` authored host manifests now feed the final Win32 application manifest so dependency-owned activatable classes register under the actual host DLL instead of leaving empty `<asmv3:file>` entries.
- [x] Closed KWINRT-047 dependency-authored WinUI Application run-host activation: `runWinRTApplicationHost` now depends on the JVM authoring host build, generated JVM authoring host DLLs load jars from the run-host `lib` layout, and native `.dll` authored targets remain free of JVM runtimeconfig generation.
- [x] Closed KWINRT-048 cross-module runtime-class wrapper inheritance: generated non-sealed runtime-class `_inner` wrapper constructors now use Kotlin `protected` visibility, matching `.cswinrt/src/cswinrt/code_writers.h` `protected internal` object-reference constructors closely enough for dependency-owned bases such as `Panel` to be subclassed by downstream projections such as `Canvas` without exposing sealed wrapper constructors.
- [x] Closed KWINRT-049 cross-module overridable exclusive interface visibility: generated exclusive interfaces remain internal by default, but exclusive interfaces implemented as `OverridableAttribute` surfaces by non-sealed runtime classes, such as `Microsoft.UI.Xaml.IApplicationOverrides`, are now treated as cross-module authoring contracts and emitted public with exposed vtable metadata, matching `.cswinrt/src/cswinrt/code_writers.h` override-interface ABI exposure rules.
- [x] Closed KWINRT-049 async cancellation exception parity: WinRT async await/terminal helpers now map `AsyncStatus.Error` with `ERROR_CANCELLED` and canceled async terminal states to coroutine `CancellationException`, matching CsWinRT's `ERROR_CANCELLED -> OperationCanceledException` / canceled task semantics across `IAsyncAction`, `IAsyncActionWithProgress`, `IAsyncOperation`, and `IAsyncOperationWithProgress`.
- [x] Closed KWINRT-051 open WinRT enum ABI projection: ordinary generated WinRT enums now follow CsWinRT/C++/WinRT open numeric ABI semantics by emitting Kotlin inline value classes with named constants and `fromAbi` preserving unknown `i4` / `u4` values instead of throwing from closed `enum class` lookup; flags enums keep their existing bitmask value-class shape.
- [x] Closed KWINRT-051 JVM authored handoff validation follow-up: JVM Kotlin compile tasks now declare compiler-authored handoff files (`type-index.tsv`, `authored-candidates.tsv`, authored metadata descriptor, authored WinMD, and host manifest) as explicit outputs, so stale or cache-restored class output directories cannot skip compiler plugin regeneration and then fail validation with scanner/compiler support artifact mismatches.
- [x] Closed KWINRT-050 setter-only interface property projection parity: interface properties are now projected when either getter or setter metadata exists, and setter-only NativeProjection getters follow `.cswinrt/src/cswinrt/code_writers.h` `find_property_interface` by searching base interfaces, exclusive-to peer interfaces, and static attributed interfaces before forwarding through `winrtAs`; this restores cross-module WinUI surfaces such as `ICompositionSupportsSystemBackdrop.systemBackdrop`.
- [x] Closed KWINRT-050 ABI follow-up for struct property setters: generator member rendering now routes one-argument `Unit` property setters by their ABI binding across normal bound properties and required-interface-forward runtime-class properties, so struct setters with layout descriptors emit descriptor-backed by-value calls such as `Struct8_4` / `Struct16_4` instead of generic `setStruct` / `Object` paths; regression coverage includes selection and layout-bounds setter shapes while existing struct method argument/result descriptor coverage remains green.
- [x] Closed KWINRT-051 CsWinRT property-interface recursion parity: Kotlin `findNativeProjectionGetterInterface` now matches `.cswinrt/src/cswinrt/code_writers.h` `find_property_interface` by stopping at the first interface that declares the same property name and can project a property accessor, rather than skipping setter-only peer interfaces to reach a native getter in a base interface; regression coverage verifies the chained setter-only forwarding path.
- [x] Closed KWINRT-052 setter-only exclusive interface projection-surface closure: metadata filtering now mirrors `.cswinrt/src/cswinrt/code_writers.h` `find_property_interface` dependency reachability for setter-only properties, so interfaces reached only through overridable signatures, such as `ICompositionSupportsSystemBackdrop` from `SystemBackdrop.OnTargetConnected`, keep their exclusive-to peer runtime class and getter interface surface (`CompositionTarget`/`IBackdropSource`) instead of publishing an empty dependency-owned interface shell.
- [x] Closed KWINRT-053 projection shape ownership rule alignment: generator visibility now honors `.cswinrt/src/cswinrt/code_writers.h` `settings.public_exclusiveto` for exclusive interfaces, metadata surface closure routes setter-only forwarded properties through a shared member-forwarding helper while event metadata remains fail-closed on add/remove accessor parity, and Gradle dependency identity now carries `projectionShapeVersion` so only current-shape `projectedTypes` suppress downstream generation.
- [x] Closed KWINRT-054 KMP generated projection stale-layout cleanup: KMP projection generation now deletes the legacy `src/main/kotlin` projection and authoring output roots in addition to previous JVM and native-host roots before writing `commonMain` output, so old snapshot-era generated files with internal wrapper constructors or internal override interfaces cannot remain on the source path after `generateWinRTProjections`.
- [x] Closed KWINRT-055 WinAppSDK composition dependency-surface parity: aligned Kotlin with `.cswinrt/src/Projections/WinAppSDK/WinAppSDK.csproj` by treating `Windows.UI.Composition` references in Microsoft WinAppSDK metadata as Windows projection/dependency closure surface, not as an automatic projection exclude or `Microsoft.UI.Composition` type rewrite; kept only the narrow WinUI XAML interop redirects that correspond to the cswinrt WinAppSDK filter comments. Validation: `:winrt-generator:test :winrt-gradle-plugin:test` and local `winrt-generator` / `winrt-gradle-plugin` Maven publication pass; Skiko `:skiko-winui:generateWinRTProjections`, `:skiko-winui:compileKotlinWinuiJvm`, and `:skiko-winui:compileKotlinWinuiMingw` pass with the CI WinUI properties. The full Skiko snapshot local publish entry `:skiko-winui:publishSkikoWinuiToMavenLocal` reaches generated WinRT output but is blocked locally by `:skiko:compileCommonMainKotlinMetadata` in the core Skiko included build, not by the WinRT projection surface.
- [x] Closed KWINRT-056 published identity authored-host manifest portability: dependency identity now publishes structured `authoredHostManifestRecords` instead of requiring consumers to read producer build-machine `*.host.json` paths, downstream projection suppression and runtime asset staging consume only those records, and `buildWinRTAuthoringHost` builds JVM host DLLs only from records with jar targets and `hostExportsClass` so native authoring manifests cannot poison JVM host generation. Validation includes `:winrt-gradle-plugin:test` and local Skiko `:skiko-winui:generateWinRTProjections` plus `:skiko-winui:generateWinRTIdentity` with JDK 25, where the generated identity contains records and no `authoredHostManifests`.
- [x] Closed KWINRT-057 published identity authored-asset portability: dependency identity now publishes binary-safe `authoredMetadataRecords`, `authoredTargetArtifactRecords`, and grouped `compilerSupportFileRecords` instead of producer build-machine `authoredMetadata`, `authoredTargetArtifacts`, `authoringMetadataIndexes`, or `compilerSupportManifests` paths; projection generation materializes dependency-authored `.winmd` records for metadata loading, runtime asset staging writes dependency `.winmd` and target-artifact records into the consumer asset root, compiler-support merge materializes grouped support TSV records before merging, and authoring metadata discovery consumes only `authoringMetadataIndexRows`. Targeted validation covers identity generation, dependency runtime asset staging, malformed portable record names, and dependency compiler-support merge.
- [x] Closed KWINRT-058 published identity runtime-asset portability: library identity now publishes configured runtime assets as binary-safe `runtimeAssetRecords` instead of producer-machine `runtimeAssets` paths, application staging materializes dependency runtime asset records from identity files, and the Gradle plugin no longer wires dependency `runtimeAssets` strings into downstream file collections. Full identity audit confirms remaining dependency identity consumers read only portable scalars/specs (`nugetPackages`, projection surface lists, `authoringMetadataIndexRows`) or structured records (`runtimeAssetRecords`, authored metadata, host manifests, target artifacts, compiler support); published library identity also omits local metadata input paths while preserving portable metadata selectors such as `sdk+` and `nuget:<id>@<version>`. The same audit fixed the authored-candidate validation task graph so scanner-generated artifacts from `generateWinRTProjections` are explicit validation inputs, not implicit ordering assumptions. Application-local identity may still record local `runtimeAssets` and `dependencyIdentityFiles` because it is not a published library contract.
- [x] Closed KWINRT-059 authored WinMD external-interface parity: `winrt-metadata` authored WinMD output now keeps authored runtime classes as TypeDefs while referencing existing WinRT base and override interfaces as TypeRefs, matching `.cswinrt/src/Authoring/WinRT.SourceGenerator/WinRTTypeWriter.cs`; metadata source resolution preserves source priority so SDK/NuGet WinMD definitions win over dependency-authored compatibility records; and dependency-authored metadata records feed both scanner and compiler TypeDetails generation so authored interface IID resolution stays identical across paths. Validation includes `:winrt-metadata:test`, `:winrt-gradle-plugin:test`, local `kotlin-winrt` publish, local `skiko-winui` projection generation and publish, Compose `stageWinRTRuntimeAssets`, and the Compose WinUI Skiko sample host run with local artifacts.
- [x] Closed KWINRT-060 XAML `Style.TargetType` metadata self-initialization parity: compiler-generated projection support now owns generated runtime-class metadata registration rather than `winrt-runtime` reflecting over per-type companions or setter paths manually calling initialization; this keeps Kotlin aligned with `.cswinrt/src/cswinrt/strings/InitializeProjection.cs` and `.cswinrt/src/cswinrt/main.cpp` module-level projection initializer responsibilities.
- [x] Closed KWINRT-061 XAML `DependencyProperty` identifier nullability parity: `winrt-metadata` now treats Microsoft and Windows XAML `DependencyProperty` identifier properties as non-null projection values while preserving nullable projection for ordinary runtime-class properties, so generated static DP surfaces match CsWinRT-style direct use such as `Control.backgroundProperty` instead of nullable `Metadata` call-site guards.
- [x] Closed KWINRT-062 XAML collection property nullability parity: `winrt-metadata` now keeps XAML collection runtime-class properties such as `Style.Setters` non-null while preserving explicitly nullable XAML object properties such as `Style.BasedOn`, `Window.SystemBackdrop`, and `UIElement.Clip`; this lets style construction use `style.setters.add(...)` without generator or sample call-site null guards.
- [x] Closed KWINRT-063 WinUI resource container access parity: sample resource setup now uses direct projected access for `Application.resources` and `ResourceDictionary.mergedDictionaries` instead of redundant `checkNotNull` guards, so XAML resource installation follows the same non-null collection surface used by style and controls resource setup.
- [x] Closed KMP application-entry source-set visibility gap: generated `mingwX64` application entry sources now attach to the `mingwX64` main source set instead of `commonMain`, so entry wrappers can import app `main` functions declared in custom intermediate source sets such as `winuiMain`; compiler-plugin support output also tolerates empty JVM compilation units that still receive merged WinRT support manifests during full sample assembly.
- [x] Closed KWINRT-040 target-artifact mismatch for KMP authoring manifests: `winrt-gradle-plugin` now derives JVM authoring target artifacts from the configured Kotlin JVM target `*Jar.archiveFileName`, passes that artifact name to compiler-authored host manifests, and publishes the same target jar through dependency identity records so downstream application staging can resolve KMP-authored WinRT classes.
- [x] Closed KWINRT-052 application manifest and runtime asset staging parity: generated Win32 application manifests now emit concrete processor architectures (`amd64`, `arm64`, or `x86`) instead of wildcard identities, application manifest activatable-class entries are deduplicated across lifted registration files, and NuGet runtime asset staging consumes MSBuild-style `ReferenceCopyLocalPaths` / resource target-path semantics used by unpackaged C++ WinUI applications so WebView2-style `native_uap` DLLs and WinUI resource payloads are staged with their target subdirectories. Validation covers the targeted KWINRT-052 regressions and the `runtime_assets_task_*` staging suite.
- [x] Closed KWINRT-052 MSBuild import follow-up for the published Gradle-plugin gap: NuGet runtime asset staging now resolves relative imports from `MSBuildThisFileDirectory`, expands `Import Project="...\*"` wildcard imports before evaluating copy-local items, and skips unresolved MSBuild property-function expressions in imports, includes, and `Exists(...)` conditions instead of passing them to `Path.of(...)`; this covers the compose-observed `ImportBefore\*` failure while keeping the lightweight evaluator scoped to staged runtime payload discovery rather than a full MSBuild replacement.
- [x] Closed XAML activatable constructor metadata follow-up: `winrt-metadata` now preserves an activatable factory interface when a runtime class has both default `ActivatableAttribute` and factory `ActivatableAttribute`, matching `.cswinrt/src/cswinrt/code_writers.h` constructor factory traversal so classes such as `Microsoft.UI.Xaml.Setter` can keep both the empty constructor and factory-backed convenience constructors like `(DependencyProperty, Any?)`.
- [x] Closed composable factory traversal parity follow-up: `winrt-generator` now plans composable constructors from the full attributed factory set produced by `winrt-metadata`, matching `.cswinrt/src/cswinrt/code_writers.h` `get_attributed_types()` / `write_attributed_types()` traversal instead of treating `ComposableAttribute` as a single scalar factory interface. Each generated composable factory method now acquires the activation factory for its own interface IID without a scalar fallback or primary-factory compatibility surface, and validation covers multiple composable factory interfaces on one runtime class.
- [x] Closed composable traversal CI follow-up: updated the duplicate-path metadata loader stability fixture so the expected namespace type list includes the second composable factory interface introduced for CsWinRT-style multi-factory coverage, keeping the test aligned with the real WinMD fixture instead of masking the extra `ComposableAttribute`.
- [x] Closed KWINRT-038 public setter visibility gap for projected runtime-class forwarders: expect/actual runtime-class generation now preserves writable property shape when the public interface exposes the setter, including projected enum/value-class properties such as `KeyboardAccelerator.modifiers`.
- [x] Closed KWINRT-042 authored interface override wrapping: compiler-generated TypeDetails now wrap interface override parameters with `IUnknownReference` while preserving `IInspectableReference` wrapping for runtime-class parameters.
- [x] Closed KWINRT-043 nullable struct setter boxing: generated `IReference<T>` setter IID expressions now use full struct field signatures, and generated struct `Metadata.register()` emits value-boxing registration from WinMD struct metadata so nullable WinRT struct values expose the expected `IReference<T>` / `IReferenceArray<T>` interfaces without runtime-owned struct enumeration.
- [x] Completed size-reduction baseline: metadata/accessor helper reduction, owner-qualified event/generic support facades, module-local fixed-shape ABI helpers, projected reference helper regrouping by `IInspectableReference` / `IUnknownReference`, and helper frequency thresholding are in place.
- [x] Completed authored CCW reachable ABI parity audit: aligned generator support with `.cswinrt/src/WinRT.Runtime/Projections/KeyValuePair.cs`, `.cswinrt/src/WinRT.Runtime/Projections/Nullable.cs`, `.cswinrt/src/WinRT.Runtime/ComWrappersSupport.cs`, and `.cswinrt/src/cswinrt/code_writers.h` for the generator-visible authored ABI surface and runtime CCW suffix behavior. Overridable authored interfaces now marshal supported `IKeyValuePair<TKey,TValue>`, generic collections, bindable collection parameters and returns, async references, `IPropertyValue`, runtime-owned custom objects, and ordinary generated runtime-class projections such as `Windows.Foundation.Uri` through the appropriate shared ABI helpers, default CCWs append `ICustomPropertyProvider` after `IStringable` when enabled, delegate arguments use the same full delegate CCW surface as retained delegate handles, and unsupported reachable shapes fail before support rendering.
- [x] Completed custom `IMarshal` CCW suffix parity: matched `.cswinrt/src/WinRT.Runtime/ComWrappersSupport.cs` by preserving an authored or generated `IMarshal` interface in its original exposed interface-table position while only suppressing the default free-threaded-marshaler suffix entry. `GetIids` no longer moves a user-provided `IMarshal` behind `IWeakReferenceSource`.
- [x] Completed authored CCW receive-array fail-closed parity: removed the support-render fallback path for unsupported `ReceiveArray` ABI combinations. Single projected receive-array return slots still render, while multiple receive arrays or receive-array parameters that cannot become the projected Kotlin return now fail during generator validation with the owning runtime class, interface, slot, and parameter name before `WinRTAuthoringCcwFactories.kt` is emitted.
- [x] Completed authored CCW support-output fallback cleanup: removed stale authored ABI class array unsupported helpers, required server activation factory bindings to fail before handler rendering when their ABI shape is unsupported, and extended generated-output audits so authored CCW support files cannot contain hidden `Unsupported authored ABI`, `Authored ReceiveArray parameter`, or `unsupportedAuthoringAbiArrayOperation` fallback tokens.
- [x] Completed WinUI sample merge gate: `:winrt-samples:runWinRTApplicationHost` passes with Windows App SDK `2.1.3`, `runWinUiSmoke=true`, `autoExitWinUi=true`, `skipSettingsCard=true`, and `skipShimmer=true`.
- [x] Completed WinUI Controls sample DPI manifest parity: generated Win32 application manifests now align with `.cswinrt/src/Samples/AuthoringDemo/WinUI3CppApp/app.manifest` by declaring `true/PM` and `PerMonitorV2, PerMonitor`, so JVM and `mingwX64` WinUI hosts do not run under system-DPI virtualization.
- [x] Completed XAML application host lifetime fix: `runWinRTApplicationHost` and the `mingwX64` native application entry now create a runtime-owned STA application-host scope before user `main`, generated hosts pass the packaged/unpackaged deployment mode into that scope, custom launchers can use `WinRTWindowsAppSdkBootstrap.initializeApplicationHost(...)`, and sample smoke paths no longer wrap XAML startup in raw `RuntimeScope.initializeSingleThreaded()`.
- [x] Completed CsWinRT-style prebuilt projection package split: mirror `.cswinrt/src/Projections/Windows`, `.cswinrt/src/Projections/Windows.UI.Xaml`, and `.cswinrt/src/Projections/WinAppSDK` as separate Kotlin prebuilt artifacts so Windows SDK, legacy `Windows.UI.Xaml`, and Windows App SDK / WinUI surfaces have explicit module and publication boundaries.
- [x] Completed prebuilt projection opt-in semantics: prebuilt projection usage is driven by explicit `dependencies` entries for the prebuilt artifacts, while `nugetPackage(...)` keeps local projection generation unless `generateProjection = false` is set on a package that is supplied through an explicit prebuilt dependency.
- [x] Completed `asWinRT<T>()` Kotlin-subclass fast path: the cast helper now behaves as a Kotlin `as` superset by returning an object that already satisfies `T` before attempting WinRT interface projection, so direct authored/projected subclasses such as a `ContentControl` root are not rewrapped through the interface cast path.

## Post-Baseline Completion Queue

The remaining completion work runs in this order after the completed baseline: `winrt-runtime` hardening -> `winrt-authoring` parity -> `winrt-projections` size/output pass -> `winrt-samples` expansion -> final `winrt-projections` release pass. This queue is for post-baseline hardening and validation; it must not move runtime behavior into samples or derive generator/runtime contracts from sample failures.

### Runtime Hardening First

- [x] Runtime-01: validate `.cswinrt/src/WinRT.Runtime` COM lifetime parity in `winrt-runtime` for RCW/CCW identity caches, `QueryInterface`, `AddRef`/`Release`, explicit disposal, finalization, repeated casts, and cross-thread release behavior.
- [x] Runtime-02: close weak-reference, agile-reference, and reference-tracker parity gaps against `.cswinrt/src/WinRT.Runtime/Interop/IReferenceTracker.cs`, `IWeakReferenceSource*`, and `IAgileReference*`, tracking WinRT semantics rather than `.NET` target-framework branches.
- [x] Runtime-03: harden restricted-error-info and exception bridging for Kotlin exceptions crossing activation factories, authored CCWs, delegates, callbacks, and async completions, with HRESULT and restricted-error-info round-trip coverage.
- [x] Runtime-04: validate collection, bindable, async, nullable, `IReference<T>`, `IReferenceArray<T>`, `IKeyValuePair<TKey,TValue>`, and `IMapView` composition through shared runtime projection helpers instead of new per-shape branch tables.
- [x] Runtime-05: stabilize WinUI/XAML runtime hooks for application-host scope, Windows App SDK deployment, XAML metadata providers, resource manager wiring, runtime assets, and packaged/unpackaged startup on JVM and `mingwX64`.
- [x] Runtime gate: before leaving this queue slice, run targeted Windows validation with `.\gradlew.bat :winrt-runtime:jvmTest :winrt-runtime:mingwX64Test validateWinRTMingwParity`.

### Runtime Convergence Planned

- [x] Runtime-06: split `ComWrappersSupport` into narrower `.cswinrt/src/WinRT.Runtime`-aligned owners. RCW/CCW identity, object reference wrapping, and interface query mechanics remain in the core support path; projection factory registration, authored activation lookup, runtime-class-name lookup, CCW factory registration, and test reset fan-out now live in focused registry/reset helpers behind the existing public facade.
- [x] Runtime-07: make `WinRTTypeId` / `WinRTTypeRegistry` the single runtime descriptor source for projected type name, runtime class name, helper type, default interface, IID, boxed name, and aliases. `TypeNameSupport` now resolves registered WinRT names through the registry, `Projections` no longer owns duplicate ABI-name/runtime-class/default-interface descriptor maps, and direct registry test resets invalidate projection bootstrap state so mappings rebuild deterministically.
- [x] Runtime-08: isolated stable built-in projection support for `Windows.Foundation.EventRegistrationToken` and `Windows.Foundation.IClosable` from generic runtime hooks, then removed `Windows.Foundation.Uri` from runtime-owned built-ins because Kotlin has no standard Uri type and the Uri runtime class should be emitted as an ordinary generated projection. Foundation-owned hooks now cover only runtime-owned Foundation helpers, and generated `IClosable` forwarding targets the Foundation-owned closable wrapper instead of a public runtime-package helper.
- [x] Runtime-09: shrank `WinRTObjectMarshaller` back toward generic object marshalling and delegate bridging by moving projected object CCW retention for XAML dependency-property marshaling into `XamlSystemProjectionRuntimeHooks`, with XAML-owned cache cleanup and regression coverage for the retained reference root.
- [x] Runtime-10: tightened `WinRTTypeClassifier` to intrinsic WinRT scalar/object and primitive-array classification only by renaming its descriptor/API boundary around intrinsic types and adding regression coverage that Foundation, XAML, and other projection-owned mappings remain registry/projection policy instead of classifier entries.
- [x] Runtime convergence gate: validated each completed item with targeted `.\gradlew.bat :winrt-runtime:jvmTest` coverage first, then ran `.\gradlew.bat :winrt-runtime:jvmTest :winrt-generator:test` before closing the convergence slice.

### Authoring Second

- [x] Authoring-01: continue replacing source-scanner-derived authoring inputs with compiler-symbol or IR-visible authoring metadata in `winrt-compiler-plugin`, while keeping scanner artifacts only as narrow transition inputs.
- [x] Authoring-02: expand `.cswinrt/src/Authoring/WinRT.SourceGenerator` parity for complex runtime-class inheritance, default interfaces, overridable interfaces, composable factories, protected overrides, and WinUI control-derived classes.
- [x] Authoring-03: extend authored CCW marshaling validation for arrays, nullable structs, generic collections, delegates, events, async references, custom runtime objects, `IPropertyValue`, and mixed object/interface/runtime-class parameters and returns.
- [x] Authoring-04: harden native authoring host fixtures for multiple activatable classes, dependency factory chaining, unload behavior, repeated activation, interface query coverage, and native consumer validation.
- [x] Authoring-05: improve generator and compiler-plugin diagnostics for unsupported authoring members, invalid projected types, bad visibility, invalid constructors or factory shapes, and unsafe runtime-class casts; unsupported shapes must fail before handler rendering.
- [x] Authoring gate: before leaving this queue slice, run `.\gradlew.bat :winrt-authoring:native-component-fixture:verifyNativeAuthoringComponentFixture :winrt-authoring:native-consumer-fixture:verifyNativeAuthoringConsumerFixture`.

### Projections Third

- [x] Projections-01: finish the current generated projection size-reduction slice for prebuilt Windows SDK and Windows App SDK artifacts without changing Maven coordinates, projected API compatibility, runtime hot-path behavior, or multi-module classpath safety.
- [x] Projections-02: keep owner-scoped support output deterministic across compiler-support TSVs, generic ABI support, event helpers, projection registrars, TypeDetails, fixed-shape ABI helpers, and module-local support facades.
- [x] Projections-03: strengthen generated-output audits for duplicate FQNs, duplicate helper emission, repeated type/category branch tables, JVM-only leakage into common or native output, stale support registries, forbidden fallback bodies, and unexpected source or class-size growth.
- [x] Projections-04: keep Windows SDK, Windows.UI.Xaml, and Windows App SDK prebuilt projection compile gates fixed for JVM and `mingwX64`, including `:winrt-projections:windows-sdk:*`, `:winrt-projections:windows-ui-xaml:*`, and `:winrt-projections:windows-app-sdk:*` compile tasks.
- [x] Projections-05: validate third-party WinMD and NuGet projection inputs with dependency identity, preprojected dependency suppression, owner-scoped support manifests, and local-generation opt-in behavior.
- [x] Projection gate: before leaving this queue slice, run `.\gradlew.bat validateWinRTProjectionCompile`.

### Samples Fourth

- [x] Samples-01: keep `winrt-samples` validation-only; do not add sample-local runtime, generator, authoring, packaging, or projection workarounds.
- [x] Samples-02: expand JVM WinUI sample coverage for window lifetime, resource loading, XAML metadata provider aggregation, Windows App SDK deployment, common control events, collection binding, and application shutdown.
- [x] Samples-03: expand `mingwX64` WinUI sample coverage for native application entry, authored control override, DPI manifest behavior, packaged and unpackaged modes, resource staging, and runtime asset layout.
- [x] Samples-04: add an authoring end-to-end sample where a Kotlin-authored WinRT component is consumed by another Kotlin app, covering activation factories, interface query, events, collections, async, and exception propagation.
- [x] Samples-05: add a packaging validation sample for appx/msix layout, PRI/MRT resource indexing, dependency payload staging, signing or test-install hooks, and Windows App SDK payload resolution after the owning upstream contracts are stable.
- [x] Sample gate: before leaving this queue slice, run `.\gradlew.bat validateWinRTSampleSmoke validateWinRTQueue16`.

### Projections Final Pass

- [x] Projection-Final-01: review sample expansion fallout for projected API-shape issues and move any required fixes back to metadata, generator, runtime, or authoring instead of sample glue.
- [x] Projection-Final-02: remeasure projection artifact size, class duplication, generated support ownership, binary/source compatibility, and Maven coordinate stability after samples pass.
- [x] Projection-Final-03: run the release-facing projection validation gates again before treating Windows SDK and Windows App SDK projection artifacts as publish-ready.

## Module Alignment

- [x] `.cswinrt/src/WinRT.Runtime` maps to `winrt-runtime`, which owns ABI primitives, HRESULT/error-info, HSTRING ownership, COM/WinRT initialization, platform calls, object identity, activation, marshaling, delegates/events, collections, async, weak/agile/reference tracking, custom projections, and WinUI runtime hooks.
- [x] `.cswinrt/src/cswinrt` maps to `winrt-metadata`, `winrt-generator`, `winrt-compiler-plugin`, and `winrt-gradle-plugin`, split by metadata normalization, declaration/member planning, generated support emission, compiler-visible lowering, and Gradle orchestration.
- [x] `.cswinrt/src/Projections` maps to `winrt-projections`; checked-in projection output stays narrow while broad Windows SDK and Windows App SDK output is generated deterministically by the plugin.
- [x] `.cswinrt/src/Authoring` maps to `winrt-authoring`, which owns authored type candidates, metadata indexes, authored WinMD/descriptor/host manifest generation, TypeDetails rendering, activation/hosting contracts, and runtime-facing registration helpers.
- [x] `.cswinrt/src/Samples` maps to `winrt-samples`; samples are validation surfaces only and must not contain runtime, generator, authoring, or packaging workarounds.
- [x] `.cswinrt/src/Tests` maps to owning Kotlin module tests instead of a separate top-level test module.

## Completed Baseline

- [x] Runtime JVM baseline: ABI primitives, object references, activation, CCW/RCW ownership, delegate/event/collection/object marshaling, async bridges, WinUI bootstrap, vtable calls, restricted error info, weak/agile references, default `ICustomPropertyProvider` CCW suffix, custom `IMarshal` position preservation, full delegate argument CCW parity, and custom projections are implemented and validated for the current surface.
- [x] Native `mingwX64` runtime/compiler/projection baseline: promoted common runtime tests pass through real native actuals; generated projection compilation covers the full Windows SDK root KMP surface; fallback audits reject reflection, JVM-only imports, `ComVtableInvoker`, and `invokeGenericArgs` leakage.
- [x] Metadata baseline: native WinMD ingestion, SDK/file/directory/NuGet input expansion, deterministic normalization, signatures, default/static/factory metadata, implemented interfaces, custom attributes, mapped types, and diagnostics are implemented.
- [x] Generator/compiler-plugin baseline: declaration planning, member/property/event/method emission, activation/static/factory/composable surfaces, ABI fail-closed diagnostics, compiler-support handoff, descriptor lowering, authored candidate artifacts, and large projection registrar chunking are implemented.
- [x] Projection baseline: Windows SDK, Windows.UI.Xaml, and Windows App SDK projections are generated through deterministic plugin output; prebuilt projection publication, versioning, CI workflow, multi-SDK support, and README baseline badges are in place.
- [x] Authoring baseline: generated TypeDetails, authored WinMD/descriptor/host manifests, CCW factories, activation-factory exports, dependency factory chaining, inherited/overridable interfaces, diagnostics, JVM host bridge, and native exported-DLL host validation are implemented.
- [x] Packaging/resource baseline: Kotlin-owned appx/msix staging, manifests, dependency payloads, PRI/MRT/resource processing, signing/test-install hooks, runtime asset layout, Windows App SDK deployment, XAML metadata provider aggregation, and third-party component resource wiring are implemented.
- [x] Validation baseline: representative Windows gates exist for runtime, metadata, generator, compiler-plugin, projections, authoring, packaging, samples, native runtime, native projection, native authoring, and the broader `validateWinRTQueue16` queue.

## Phase 1 Runtime And ABI

- [x] ABI primitives first: `Guid`, `HResult`, HSTRING/string ownership, `PlatformAbi`, `ComVtableInvoker`, runtime initialization, platform API calls, and low-level COM pointer helpers are centralized in `winrt-runtime`.
- [x] Initialization and platform boundary: COM/WinRT initialization, Windows API loading, HRESULT translation, restricted error info, dynamic library loading, and deployment bootstrap are runtime-owned.
- [x] Object reference and identity: `IUnknown`/`IInspectable` wrappers, CCW/RCW identity caches, query/cast support, composable object lifetime, weak/agile/reference-tracker support, and runtime class name lookup are runtime-owned.
- [x] Activation: `RoGetActivationFactory`, manifest-free fallback, activation factory caching, dependency activation-factory chaining, and runtime-class activation helpers are implemented.
- [x] Parameterized IID/signature support: `WinRTTypeSignature`, `ParameterizedInterfaceId`, collection/async/delegate generic IDs, and generated generic interface acquisition are runtime-owned.
- [x] Delegates, events, collections, async, custom projections, WinUI deployment/resource/XAML hooks, and callback-vtable reuse are implemented after the foundational runtime slices.

## Phase 2 Metadata

- [x] WinMD ingestion, namespace/type discovery, SDK/reference/NuGet input expansion, include/exclude filtering, response files, and cache-stable inputs are implemented.
- [x] Symbol fidelity covers generic parameters, default interfaces, implemented interfaces, activatable/static/factory metadata, method/property/event signatures, accessor identity, parameter semantics, and custom attributes.
- [x] Projection-shape inputs are centralized for primitive, inspectable/interface, collection, bindable, async, mapped type, runtime-class-name, and attribute classification to avoid duplicated branch tables.
- [x] Unsupported or malformed metadata fails closed before generator output.

## Phase 3 Generator And Compiler Plugin

- [x] Declaration planning and shells are stable for namespaces, enums, structs, delegates, interfaces, runtime classes, companions, and metadata surfaces.
- [x] Member/property/event/method emission, activation/static/factory surfaces, collections, async, custom mappings, WinUI source additions, namespace/type excludes, and unsupported-shape diagnostics are implemented.
- [x] Generated support files remain owner-qualified and deterministic across projection modules, including compiler-support TSVs, projection registrars, generic ABI support, event helpers, TypeDetails, and module-local ABI helpers.
- [x] Compiler-plugin lowering owns descriptor-backed calls, native direct calls, compiler-visible authored candidate artifacts, support initialization, projection registrar initialization, and Kotlin 2.4-compatible IR lookups.
- [x] Large generated projections avoid JVM class limits through registrar/support chunking without adding runtime reflection, classpath scanning, vararg dispatch, or global registries.
- [x] Authored CCW pre-render contracts cover generator-known inherited/overridable ABI surfaces: supported `IKeyValuePair<TKey,TValue>`, generic collections, bindable collection parameters and returns, async references, `IPropertyValue`, runtime-owned custom objects, ordinary generated runtime-class projections, and `IReference<T>` / `IReferenceArray<T>` decode/write through shared ABI helpers, while unsupported reachable parameter or return shapes fail closed before `WinRTAuthoringCcwFactories` or server activation factory support can emit unreachable handler bodies.

## Phase 4 Projections

- [x] Broad Windows SDK and Windows App SDK projection output is generated only after upstream runtime, metadata, and generator contracts exist.
- [x] Dependency identity files define cross-module projection ownership so application modules do not duplicate dependency-owned projected FQNs.
- [x] NuGet projection defaults distinguish preprojected baselines from third-party packages, with opt-in local generation for Windows SDK / Windows App SDK packages when needed.
- [x] Generated-output audits scan for forbidden fallback paths, JVM-only leakage, stale support registries, duplicate FQNs, duplicate type/category tables, and unexpected source-size growth.
- [x] Prebuilt projection artifacts preserve metadata-baseline versioning, snapshot suffixing, publication workflows, SDK installation reliability, and package-coordinate stability.

## Phase 5 Authoring

- [x] Authoring contracts live in `winrt-authoring`; Gradle, generator, compiler-plugin, and runtime consume those contracts instead of defining parallel models.
- [x] Compiler-symbol authored metadata replaces source-scanner-derived behavior where parity requires K2/IR-visible symbols, while scanner artifacts remain parity inputs until fully retired.
- [x] Authored ABI boundary covers factories, static members, composable factories, TypeDetails, metadata type mapping, object lifetime/identity, CCW/RCW ownership, and host manifests.
- [x] Authoring marshaling covers arrays, structs, delegates/events, object/interface/runtime-class parameters and returns, collections, async, nullable/reference shapes, HRESULT/error propagation, and fail-closed diagnostics.
- [x] Inherited authored interface CCW coverage includes map-view/key-value-pair, generic/bindable collections, async references, `IPropertyValue`, runtime-owned custom objects, ordinary generated runtime-class projections, and nullable/reference parity: `IMapView`-style `GetValueByIndex` returns are marshaled through the runtime key-value-pair collection host, supported collection/async/property-value/custom-object/reference parameters and bindable collection returns are decoded or written before invoking authored Kotlin members, and unreachable unsupported shapes are diagnosed by the generator instead of downstream CCW scanning.
- [x] Native authoring host covers `DllGetActivationFactory`, `DllCanUnloadNow`, export-table validation, activation lookup/query, dependency factory chaining, native component/consumer fixtures, and common-owned KMP generated support.

## Phase 6 Packaging And Validation

- [x] Kotlin appx/msix packaging covers manifest generation/validation, appx/msix layout, dependency payload resolution, resources/PRI/MRT, signing/test-install hooks, packaged/unpackaged modes, and disabled-generation output preservation.
- [x] Gradle identity propagation publishes and consumes `kotlin-winrt-identity` metadata variants for reusable WinRT libraries and final application modules.
- [x] Gradle plugin publication uses the shared Maven Central publishing convention, signing policy, snapshot/release versioning, and root/included-build publication flows.
- [x] Configuration-cache and build-cache compatibility are required for touched generation, packaging, runtime asset, authoring, projection, and sample tasks.
- [x] WinUI sample validation covers generated app hosts, Windows App SDK deployment, XAML metadata providers, WinUIEssential component resources, `SettingsCard`, stabilized `Shimmer` loading, and `MenuFlyout.ShowAt(Point)` lifetime behavior.

## Frozen Until Prerequisites Close

- [x] `winrt-samples`: keep validation-only; expand only for completed runtime/generator/authoring/packaging slices with no sample-local runtime workarounds.
- [x] `winrt-projections`: avoid broad checked-in generated growth; prefer plugin-generated output gated by completed upstream contracts.
- [x] `mingwX64`: runtime/compiler/projection parity is open only for regression fixes unless a new native parity slice is explicitly planned; sample expansion remains gated by owner-module contracts.
- [x] Codebase slimming and standalone source-count reduction: frozen until runtime, metadata, generator/compiler-plugin, projections, authoring, and packaging are functionally complete.

## Validation Gates

- [x] Runtime gate: targeted Windows `:winrt-runtime:jvmTest`, native runtime compile/test gates, and common-test promotion for platform-neutral contracts.
- [x] Metadata gate: Windows `:winrt-metadata:test` for native WinMD ingestion, normalized symbols, signatures, accessors, custom attributes, and diagnostics.
- [x] Generator/compiler-plugin gate: Windows `:winrt-generator:test`, `:winrt-compiler-plugin:test`, generated projection compile, support-file validation, and stale artifact cleanup.
- [x] Projection gate: generated-output audit plus JVM and `mingwX64` compile gates, including lightweight and full Windows SDK root KMP validation.
- [x] Authoring gate: generated TypeDetails, authored WinMD, CCW factories, activation/hosting, native exported-DLL fixtures, receive-array handling, and WinUI composable boundaries.
- [x] Packaging gate: PRI/MRT/resource/package staging, appx/msix layout, runtime assets, signing/test-install hooks, and Windows App SDK deployment validation.
- [x] Sample gate: use `winrt-samples` and KMP WinUI runs only as representative end-to-end validation after owning contracts are implemented.
