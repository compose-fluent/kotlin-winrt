rootProject.name = "windows-xaml-family-conflict-fixture"

pluginManagement {
    includeBuild("../../windows-toolkit-gradle-plugin")
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}
