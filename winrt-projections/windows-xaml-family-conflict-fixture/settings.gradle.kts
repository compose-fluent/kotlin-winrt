rootProject.name = "windows-xaml-family-conflict-fixture"

pluginManagement {
    includeBuild("../../winrt-gradle-plugin")
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}
