rootProject.name = "windows-webview2-published-consumer-fixture"

pluginManagement {
    includeBuild("../../winrt-gradle-plugin")
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}
