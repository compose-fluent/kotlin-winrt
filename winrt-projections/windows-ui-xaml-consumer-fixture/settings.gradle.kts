rootProject.name = "windows-ui-xaml-published-consumer-fixture"

pluginManagement {
    includeBuild("../../winrt-gradle-plugin")
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}
