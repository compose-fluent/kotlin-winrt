rootProject.name = "windows-app-sdk-published-consumer-fixture"

pluginManagement {
    includeBuild("../../winrt-gradle-plugin")
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}
