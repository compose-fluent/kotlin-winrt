allprojects {
    plugins.withId("io.github.compose-fluent.winrt") {
        def root = System.getenv("NUGET_PACKAGES")
        if (root != null && !root.isBlank()) {
            extensions.getByName("winRT").nugetGlobalPackagesRoots.add(root)
        }
    }
}
