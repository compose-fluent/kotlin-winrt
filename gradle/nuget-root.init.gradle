allprojects {
    plugins.withId("io.github.compose-fluent.windows-toolkit") {
        def root = System.getenv("NUGET_PACKAGES")
        if (root != null && !root.isBlank()) {
            extensions.getByName("windows").packageReferences.nugetGlobalPackagesRoots.add(root)
        }
    }
}
